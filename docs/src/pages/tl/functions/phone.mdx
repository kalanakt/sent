{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Functions: `phone`

Import from `sent.tl.functions.phone`.

## Contents

- [PhoneGetCallConfig](#phonegetcallconfig)
- [PhoneRequestCall](#phonerequestcall)
- [PhoneAcceptCall](#phoneacceptcall)
- [PhoneConfirmCall](#phoneconfirmcall)
- [PhoneReceivedCall](#phonereceivedcall)
- [PhoneDiscardCall](#phonediscardcall)
- [PhoneSetCallRating](#phonesetcallrating)
- [PhoneSaveCallDebug](#phonesavecalldebug)
- [PhoneSendSignalingData](#phonesendsignalingdata)
- [PhoneCreateGroupCall](#phonecreategroupcall)
- [PhoneJoinGroupCall](#phonejoingroupcall)
- [PhoneLeaveGroupCall](#phoneleavegroupcall)
- [PhoneInviteToGroupCall](#phoneinvitetogroupcall)
- [PhoneDiscardGroupCall](#phonediscardgroupcall)
- [PhoneToggleGroupCallSettings](#phonetogglegroupcallsettings)
- [PhoneGetGroupCall](#phonegetgroupcall)
- [PhoneGetGroupParticipants](#phonegetgroupparticipants)
- [PhoneCheckGroupCall](#phonecheckgroupcall)
- [PhoneToggleGroupCallRecord](#phonetogglegroupcallrecord)
- [PhoneEditGroupCallParticipant](#phoneeditgroupcallparticipant)
- [PhoneEditGroupCallTitle](#phoneeditgroupcalltitle)
- [PhoneGetGroupCallJoinAs](#phonegetgroupcalljoinas)
- [PhoneExportGroupCallInvite](#phoneexportgroupcallinvite)
- [PhoneToggleGroupCallStartSubscription](#phonetogglegroupcallstartsubscription)
- [PhoneStartScheduledGroupCall](#phonestartscheduledgroupcall)
- [PhoneSaveDefaultGroupCallJoinAs](#phonesavedefaultgroupcalljoinas)
- [PhoneJoinGroupCallPresentation](#phonejoingroupcallpresentation)
- [PhoneLeaveGroupCallPresentation](#phoneleavegroupcallpresentation)
- [PhoneGetGroupCallStreamChannels](#phonegetgroupcallstreamchannels)
- [PhoneGetGroupCallStreamRtmpUrl](#phonegetgroupcallstreamrtmpurl)
- [PhoneSaveCallLog](#phonesavecalllog)
- [PhoneCreateConferenceCall](#phonecreateconferencecall)
- [PhoneDeleteConferenceCallParticipants](#phonedeleteconferencecallparticipants)
- [PhoneSendConferenceCallBroadcast](#phonesendconferencecallbroadcast)
- [PhoneInviteConferenceCallParticipant](#phoneinviteconferencecallparticipant)
- [PhoneDeclineConferenceCallInvite](#phonedeclineconferencecallinvite)
- [PhoneGetGroupCallChainBlocks](#phonegetgroupcallchainblocks)
- [PhoneSendGroupCallMessage](#phonesendgroupcallmessage)
- [PhoneSendGroupCallEncryptedMessage](#phonesendgroupcallencryptedmessage)
- [PhoneDeleteGroupCallMessages](#phonedeletegroupcallmessages)
- [PhoneDeleteGroupCallParticipantMessages](#phonedeletegroupcallparticipantmessages)
- [PhoneGetGroupCallStars](#phonegetgroupcallstars)
- [PhoneSaveDefaultSendAs](#phonesavedefaultsendas)

## PhoneGetCallConfig

**Constructor ID:** `1430593449`

```python
PhoneGetCallConfig.__init__()
```

_No parameters._

```python
from sent.tl.functions.phone import PhoneGetCallConfig
```

```python
result = await client(PhoneGetCallConfig(...))
```

## PhoneRequestCall

**Constructor ID:** `1124046573`

**Fields:** ``video`, `user_id`, `random_id`, `g_a_hash`, `protocol``

```python
PhoneRequestCall.__init__(user_id: 'InputUser', random_id: int, g_a_hash: bytes, protocol: 'PhoneCallProtocol', video: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `user_id` | `'InputUser'` | `—` |
| `random_id` | `int` | `—` |
| `g_a_hash` | `bytes` | `—` |
| `protocol` | `'PhoneCallProtocol'` | `—` |
| `video` | `bool` | `None` |

```python
from sent.tl.functions.phone import PhoneRequestCall
```

```python
result = await client(PhoneRequestCall(...))
```

## PhoneAcceptCall

**Constructor ID:** `1003664544`

**Fields:** ``peer`, `g_b`, `protocol``

```python
PhoneAcceptCall.__init__(peer: 'InputPhoneCall', g_b: bytes, protocol: 'PhoneCallProtocol')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPhoneCall'` | `—` |
| `g_b` | `bytes` | `—` |
| `protocol` | `'PhoneCallProtocol'` | `—` |

```python
from sent.tl.functions.phone import PhoneAcceptCall
```

```python
result = await client(PhoneAcceptCall(...))
```

## PhoneConfirmCall

**Constructor ID:** `788404002`

**Fields:** ``peer`, `g_a`, `key_fingerprint`, `protocol``

```python
PhoneConfirmCall.__init__(peer: 'InputPhoneCall', g_a: bytes, key_fingerprint: int, protocol: 'PhoneCallProtocol')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPhoneCall'` | `—` |
| `g_a` | `bytes` | `—` |
| `key_fingerprint` | `int` | `—` |
| `protocol` | `'PhoneCallProtocol'` | `—` |

```python
from sent.tl.functions.phone import PhoneConfirmCall
```

```python
result = await client(PhoneConfirmCall(...))
```

## PhoneReceivedCall

**Constructor ID:** `399855457`

```python
PhoneReceivedCall.__init__(peer: 'InputPhoneCall')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPhoneCall'` | `—` |

```python
from sent.tl.functions.phone import PhoneReceivedCall
```

```python
result = await client(PhoneReceivedCall(...))
```

## PhoneDiscardCall

**Constructor ID:** `2999697856`

**Fields:** ``video`, `peer`, `duration`, `reason`, `connection_id``

```python
PhoneDiscardCall.__init__(peer: 'InputPhoneCall', duration: int, reason: 'PhoneCallDiscardReason', connection_id: int, video: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPhoneCall'` | `—` |
| `duration` | `int` | `—` |
| `reason` | `'PhoneCallDiscardReason'` | `—` |
| `connection_id` | `int` | `—` |
| `video` | `bool` | `None` |

```python
from sent.tl.functions.phone import PhoneDiscardCall
```

```python
result = await client(PhoneDiscardCall(...))
```

## PhoneSetCallRating

**Constructor ID:** `1508562471`

**Fields:** ``user_initiative`, `peer`, `rating`, `comment``

```python
PhoneSetCallRating.__init__(peer: 'InputPhoneCall', rating: int, comment: str, user_initiative: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPhoneCall'` | `—` |
| `rating` | `int` | `—` |
| `comment` | `str` | `—` |
| `user_initiative` | `bool` | `None` |

```python
from sent.tl.functions.phone import PhoneSetCallRating
```

```python
result = await client(PhoneSetCallRating(...))
```

## PhoneSaveCallDebug

**Constructor ID:** `662363518`

**Fields:** ``peer`, `debug``

```python
PhoneSaveCallDebug.__init__(peer: 'InputPhoneCall', debug: 'DataJSON')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPhoneCall'` | `—` |
| `debug` | `'DataJSON'` | `—` |

```python
from sent.tl.functions.phone import PhoneSaveCallDebug
```

```python
result = await client(PhoneSaveCallDebug(...))
```

## PhoneSendSignalingData

**Constructor ID:** `4286223235`

**Fields:** ``peer`, `data``

```python
PhoneSendSignalingData.__init__(peer: 'InputPhoneCall', data: bytes)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPhoneCall'` | `—` |
| `data` | `bytes` | `—` |

```python
from sent.tl.functions.phone import PhoneSendSignalingData
```

```python
result = await client(PhoneSendSignalingData(...))
```

## PhoneCreateGroupCall

**Constructor ID:** `1221445336`

**Fields:** ``rtmp_stream`, `peer`, `random_id`, `title`, `schedule_date``

```python
PhoneCreateGroupCall.__init__(peer: 'InputPeer', random_id: int, rtmp_stream: bool = None, title: str = None, schedule_date: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `random_id` | `int` | `—` |
| `rtmp_stream` | `bool` | `None` |
| `title` | `str` | `None` |
| `schedule_date` | `int` | `None` |

```python
from sent.tl.functions.phone import PhoneCreateGroupCall
```

```python
result = await client(PhoneCreateGroupCall(...))
```

## PhoneJoinGroupCall

**Constructor ID:** `2411016279`

**Fields:** ``muted`, `video_stopped`, `call`, `join_as`, `invite_hash`, `public_key`, `block`, `params``

```python
PhoneJoinGroupCall.__init__(call: 'InputGroupCall', join_as: 'InputPeer', params: 'DataJSON', muted: bool = None, video_stopped: bool = None, invite_hash: str = None, public_key: bytes = None, block: bytes = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |
| `join_as` | `'InputPeer'` | `—` |
| `params` | `'DataJSON'` | `—` |
| `muted` | `bool` | `None` |
| `video_stopped` | `bool` | `None` |
| `invite_hash` | `str` | `None` |
| `public_key` | `bytes` | `None` |
| `block` | `bytes` | `None` |

```python
from sent.tl.functions.phone import PhoneJoinGroupCall
```

```python
result = await client(PhoneJoinGroupCall(...))
```

## PhoneLeaveGroupCall

**Constructor ID:** `1342404601`

**Fields:** ``call`, `source``

```python
PhoneLeaveGroupCall.__init__(call: 'InputGroupCall', source: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |
| `source` | `int` | `—` |

```python
from sent.tl.functions.phone import PhoneLeaveGroupCall
```

```python
result = await client(PhoneLeaveGroupCall(...))
```

## PhoneInviteToGroupCall

**Constructor ID:** `2067345760`

**Fields:** ``call`, `users``

```python
PhoneInviteToGroupCall.__init__(call: 'InputGroupCall', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.functions.phone import PhoneInviteToGroupCall
```

```python
result = await client(PhoneInviteToGroupCall(...))
```

## PhoneDiscardGroupCall

**Constructor ID:** `2054648117`

```python
PhoneDiscardGroupCall.__init__(call: 'InputGroupCall')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |

```python
from sent.tl.functions.phone import PhoneDiscardGroupCall
```

```python
result = await client(PhoneDiscardGroupCall(...))
```

## PhoneToggleGroupCallSettings

**Constructor ID:** `2537788146`

**Fields:** ``reset_invite_hash`, `call`, `join_muted`, `messages_enabled`, `send_paid_messages_stars``

```python
PhoneToggleGroupCallSettings.__init__(call: 'InputGroupCall', reset_invite_hash: bool = None, join_muted: bool = None, messages_enabled: bool = None, send_paid_messages_stars: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |
| `reset_invite_hash` | `bool` | `None` |
| `join_muted` | `bool` | `None` |
| `messages_enabled` | `bool` | `None` |
| `send_paid_messages_stars` | `int` | `None` |

```python
from sent.tl.functions.phone import PhoneToggleGroupCallSettings
```

```python
result = await client(PhoneToggleGroupCallSettings(...))
```

## PhoneGetGroupCall

**Constructor ID:** `68699611`

**Fields:** ``call`, `limit``

```python
PhoneGetGroupCall.__init__(call: 'InputGroupCall', limit: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |
| `limit` | `int` | `—` |

```python
from sent.tl.functions.phone import PhoneGetGroupCall
```

```python
result = await client(PhoneGetGroupCall(...))
```

## PhoneGetGroupParticipants

**Constructor ID:** `3310934187`

**Fields:** ``call`, `ids`, `sources`, `offset`, `limit``

```python
PhoneGetGroupParticipants.__init__(call: 'InputGroupCall', ids: 'Vector', sources: 'Vector', offset: str, limit: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |
| `ids` | `'Vector'` | `—` |
| `sources` | `'Vector'` | `—` |
| `offset` | `str` | `—` |
| `limit` | `int` | `—` |

```python
from sent.tl.functions.phone import PhoneGetGroupParticipants
```

```python
result = await client(PhoneGetGroupParticipants(...))
```

## PhoneCheckGroupCall

**Constructor ID:** `3046963575`

**Fields:** ``call`, `sources``

```python
PhoneCheckGroupCall.__init__(call: 'InputGroupCall', sources: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |
| `sources` | `'Vector'` | `—` |

```python
from sent.tl.functions.phone import PhoneCheckGroupCall
```

```python
result = await client(PhoneCheckGroupCall(...))
```

## PhoneToggleGroupCallRecord

**Constructor ID:** `4045981448`

**Fields:** ``start`, `video`, `call`, `title`, `video_portrait``

```python
PhoneToggleGroupCallRecord.__init__(call: 'InputGroupCall', start: bool = None, video: bool = None, title: str = None, video_portrait: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |
| `start` | `bool` | `None` |
| `video` | `bool` | `None` |
| `title` | `str` | `None` |
| `video_portrait` | `bool` | `None` |

```python
from sent.tl.functions.phone import PhoneToggleGroupCallRecord
```

```python
result = await client(PhoneToggleGroupCallRecord(...))
```

## PhoneEditGroupCallParticipant

**Constructor ID:** `2770811583`

**Fields:** ``call`, `participant`, `muted`, `volume`, `raise_hand`, `video_stopped`, `video_paused`, `presentation_paused``

```python
PhoneEditGroupCallParticipant.__init__(call: 'InputGroupCall', participant: 'InputPeer', muted: bool = None, volume: int = None, raise_hand: bool = None, video_stopped: bool = None, video_paused: bool = None, presentation_paused: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |
| `participant` | `'InputPeer'` | `—` |
| `muted` | `bool` | `None` |
| `volume` | `int` | `None` |
| `raise_hand` | `bool` | `None` |
| `video_stopped` | `bool` | `None` |
| `video_paused` | `bool` | `None` |
| `presentation_paused` | `bool` | `None` |

```python
from sent.tl.functions.phone import PhoneEditGroupCallParticipant
```

```python
result = await client(PhoneEditGroupCallParticipant(...))
```

## PhoneEditGroupCallTitle

**Constructor ID:** `480685066`

**Fields:** ``call`, `title``

```python
PhoneEditGroupCallTitle.__init__(call: 'InputGroupCall', title: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |
| `title` | `str` | `—` |

```python
from sent.tl.functions.phone import PhoneEditGroupCallTitle
```

```python
result = await client(PhoneEditGroupCallTitle(...))
```

## PhoneGetGroupCallJoinAs

**Constructor ID:** `4017889594`

```python
PhoneGetGroupCallJoinAs.__init__(peer: 'InputPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |

```python
from sent.tl.functions.phone import PhoneGetGroupCallJoinAs
```

```python
result = await client(PhoneGetGroupCallJoinAs(...))
```

## PhoneExportGroupCallInvite

**Constructor ID:** `3869926527`

**Fields:** ``can_self_unmute`, `call``

```python
PhoneExportGroupCallInvite.__init__(call: 'InputGroupCall', can_self_unmute: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |
| `can_self_unmute` | `bool` | `None` |

```python
from sent.tl.functions.phone import PhoneExportGroupCallInvite
```

```python
result = await client(PhoneExportGroupCallInvite(...))
```

## PhoneToggleGroupCallStartSubscription

**Constructor ID:** `563885286`

**Fields:** ``call`, `subscribed``

```python
PhoneToggleGroupCallStartSubscription.__init__(call: 'InputGroupCall', subscribed: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |
| `subscribed` | `bool` | `—` |

```python
from sent.tl.functions.phone import PhoneToggleGroupCallStartSubscription
```

```python
result = await client(PhoneToggleGroupCallStartSubscription(...))
```

## PhoneStartScheduledGroupCall

**Constructor ID:** `1451287362`

```python
PhoneStartScheduledGroupCall.__init__(call: 'InputGroupCall')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |

```python
from sent.tl.functions.phone import PhoneStartScheduledGroupCall
```

```python
result = await client(PhoneStartScheduledGroupCall(...))
```

## PhoneSaveDefaultGroupCallJoinAs

**Constructor ID:** `1465786252`

**Fields:** ``peer`, `join_as``

```python
PhoneSaveDefaultGroupCallJoinAs.__init__(peer: 'InputPeer', join_as: 'InputPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `join_as` | `'InputPeer'` | `—` |

```python
from sent.tl.functions.phone import PhoneSaveDefaultGroupCallJoinAs
```

```python
result = await client(PhoneSaveDefaultGroupCallJoinAs(...))
```

## PhoneJoinGroupCallPresentation

**Constructor ID:** `3421137860`

**Fields:** ``call`, `params``

```python
PhoneJoinGroupCallPresentation.__init__(call: 'InputGroupCall', params: 'DataJSON')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |
| `params` | `'DataJSON'` | `—` |

```python
from sent.tl.functions.phone import PhoneJoinGroupCallPresentation
```

```python
result = await client(PhoneJoinGroupCallPresentation(...))
```

## PhoneLeaveGroupCallPresentation

**Constructor ID:** `475058500`

```python
PhoneLeaveGroupCallPresentation.__init__(call: 'InputGroupCall')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |

```python
from sent.tl.functions.phone import PhoneLeaveGroupCallPresentation
```

```python
result = await client(PhoneLeaveGroupCallPresentation(...))
```

## PhoneGetGroupCallStreamChannels

**Constructor ID:** `447879488`

```python
PhoneGetGroupCallStreamChannels.__init__(call: 'InputGroupCall')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |

```python
from sent.tl.functions.phone import PhoneGetGroupCallStreamChannels
```

```python
result = await client(PhoneGetGroupCallStreamChannels(...))
```

## PhoneGetGroupCallStreamRtmpUrl

**Constructor ID:** `1525991226`

**Fields:** ``live_story`, `peer`, `revoke``

```python
PhoneGetGroupCallStreamRtmpUrl.__init__(peer: 'InputPeer', revoke: bool, live_story: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `revoke` | `bool` | `—` |
| `live_story` | `bool` | `None` |

```python
from sent.tl.functions.phone import PhoneGetGroupCallStreamRtmpUrl
```

```python
result = await client(PhoneGetGroupCallStreamRtmpUrl(...))
```

## PhoneSaveCallLog

**Constructor ID:** `1092913030`

**Fields:** ``peer`, `file``

```python
PhoneSaveCallLog.__init__(peer: 'InputPhoneCall', file: 'InputFile')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPhoneCall'` | `—` |
| `file` | `'InputFile'` | `—` |

```python
from sent.tl.functions.phone import PhoneSaveCallLog
```

```python
result = await client(PhoneSaveCallLog(...))
```

## PhoneCreateConferenceCall

**Constructor ID:** `2097431739`

**Fields:** ``muted`, `video_stopped`, `join`, `random_id`, `public_key`, `block`, `params``

```python
PhoneCreateConferenceCall.__init__(random_id: int, muted: bool = None, video_stopped: bool = None, join: bool = None, public_key: bytes = None, block: bytes = None, params: 'DataJSON' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `random_id` | `int` | `—` |
| `muted` | `bool` | `None` |
| `video_stopped` | `bool` | `None` |
| `join` | `bool` | `None` |
| `public_key` | `bytes` | `None` |
| `block` | `bytes` | `None` |
| `params` | `'DataJSON'` | `None` |

```python
from sent.tl.functions.phone import PhoneCreateConferenceCall
```

```python
result = await client(PhoneCreateConferenceCall(...))
```

## PhoneDeleteConferenceCallParticipants

**Constructor ID:** `2359690533`

**Fields:** ``only_left`, `kick`, `call`, `ids`, `block``

```python
PhoneDeleteConferenceCallParticipants.__init__(call: 'InputGroupCall', ids: 'Vector', block: bytes, only_left: bool = None, kick: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |
| `ids` | `'Vector'` | `—` |
| `block` | `bytes` | `—` |
| `only_left` | `bool` | `None` |
| `kick` | `bool` | `None` |

```python
from sent.tl.functions.phone import PhoneDeleteConferenceCallParticipants
```

```python
result = await client(PhoneDeleteConferenceCallParticipants(...))
```

## PhoneSendConferenceCallBroadcast

**Constructor ID:** `3329235200`

**Fields:** ``call`, `block``

```python
PhoneSendConferenceCallBroadcast.__init__(call: 'InputGroupCall', block: bytes)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |
| `block` | `bytes` | `—` |

```python
from sent.tl.functions.phone import PhoneSendConferenceCallBroadcast
```

```python
result = await client(PhoneSendConferenceCallBroadcast(...))
```

## PhoneInviteConferenceCallParticipant

**Constructor ID:** `3169986181`

**Fields:** ``video`, `call`, `user_id``

```python
PhoneInviteConferenceCallParticipant.__init__(call: 'InputGroupCall', user_id: 'InputUser', video: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |
| `user_id` | `'InputUser'` | `—` |
| `video` | `bool` | `None` |

```python
from sent.tl.functions.phone import PhoneInviteConferenceCallParticipant
```

```python
result = await client(PhoneInviteConferenceCallParticipant(...))
```

## PhoneDeclineConferenceCallInvite

**Constructor ID:** `1011325297`

```python
PhoneDeclineConferenceCallInvite.__init__(msg_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `msg_id` | `int` | `—` |

```python
from sent.tl.functions.phone import PhoneDeclineConferenceCallInvite
```

```python
result = await client(PhoneDeclineConferenceCallInvite(...))
```

## PhoneGetGroupCallChainBlocks

**Constructor ID:** `4003432614`

**Fields:** ``call`, `sub_chain_id`, `offset`, `limit``

```python
PhoneGetGroupCallChainBlocks.__init__(call: 'InputGroupCall', sub_chain_id: int, offset: int, limit: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |
| `sub_chain_id` | `int` | `—` |
| `offset` | `int` | `—` |
| `limit` | `int` | `—` |

```python
from sent.tl.functions.phone import PhoneGetGroupCallChainBlocks
```

```python
result = await client(PhoneGetGroupCallChainBlocks(...))
```

## PhoneSendGroupCallMessage

**Constructor ID:** `2983269392`

**Fields:** ``call`, `random_id`, `message`, `allow_paid_stars`, `send_as``

```python
PhoneSendGroupCallMessage.__init__(call: 'InputGroupCall', random_id: int, message: 'TextWithEntities', allow_paid_stars: int = None, send_as: 'InputPeer' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |
| `random_id` | `int` | `—` |
| `message` | `'TextWithEntities'` | `—` |
| `allow_paid_stars` | `int` | `None` |
| `send_as` | `'InputPeer'` | `None` |

```python
from sent.tl.functions.phone import PhoneSendGroupCallMessage
```

```python
result = await client(PhoneSendGroupCallMessage(...))
```

## PhoneSendGroupCallEncryptedMessage

**Constructor ID:** `3853493613`

**Fields:** ``call`, `encrypted_message``

```python
PhoneSendGroupCallEncryptedMessage.__init__(call: 'InputGroupCall', encrypted_message: bytes)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |
| `encrypted_message` | `bytes` | `—` |

```python
from sent.tl.functions.phone import PhoneSendGroupCallEncryptedMessage
```

```python
result = await client(PhoneSendGroupCallEncryptedMessage(...))
```

## PhoneDeleteGroupCallMessages

**Constructor ID:** `4132394231`

**Fields:** ``report_spam`, `call`, `messages``

```python
PhoneDeleteGroupCallMessages.__init__(call: 'InputGroupCall', messages: 'Vector', report_spam: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |
| `messages` | `'Vector'` | `—` |
| `report_spam` | `bool` | `None` |

```python
from sent.tl.functions.phone import PhoneDeleteGroupCallMessages
```

```python
result = await client(PhoneDeleteGroupCallMessages(...))
```

## PhoneDeleteGroupCallParticipantMessages

**Constructor ID:** `499117216`

**Fields:** ``report_spam`, `call`, `participant``

```python
PhoneDeleteGroupCallParticipantMessages.__init__(call: 'InputGroupCall', participant: 'InputPeer', report_spam: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |
| `participant` | `'InputPeer'` | `—` |
| `report_spam` | `bool` | `None` |

```python
from sent.tl.functions.phone import PhoneDeleteGroupCallParticipantMessages
```

```python
result = await client(PhoneDeleteGroupCallParticipantMessages(...))
```

## PhoneGetGroupCallStars

**Constructor ID:** `1868784386`

```python
PhoneGetGroupCallStars.__init__(call: 'InputGroupCall')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |

```python
from sent.tl.functions.phone import PhoneGetGroupCallStars
```

```python
result = await client(PhoneGetGroupCallStars(...))
```

## PhoneSaveDefaultSendAs

**Constructor ID:** `1097313745`

**Fields:** ``call`, `send_as``

```python
PhoneSaveDefaultSendAs.__init__(call: 'InputGroupCall', send_as: 'InputPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'InputGroupCall'` | `—` |
| `send_as` | `'InputPeer'` | `—` |

```python
from sent.tl.functions.phone import PhoneSaveDefaultSendAs
```

```python
result = await client(PhoneSaveDefaultSendAs(...))
```
