{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Functions: `account`

Import from `sent.tl.functions.account`.

## Contents

- [AccountRegisterDevice](#accountregisterdevice)
- [AccountUnregisterDevice](#accountunregisterdevice)
- [AccountUpdateNotifySettings](#accountupdatenotifysettings)
- [AccountGetNotifySettings](#accountgetnotifysettings)
- [AccountResetNotifySettings](#accountresetnotifysettings)
- [AccountUpdateProfile](#accountupdateprofile)
- [AccountUpdateStatus](#accountupdatestatus)
- [AccountGetWallPapers](#accountgetwallpapers)
- [AccountReportPeer](#accountreportpeer)
- [AccountCheckUsername](#accountcheckusername)
- [AccountUpdateUsername](#accountupdateusername)
- [AccountGetPrivacy](#accountgetprivacy)
- [AccountSetPrivacy](#accountsetprivacy)
- [AccountDeleteAccount](#accountdeleteaccount)
- [AccountGetAccountTTL](#accountgetaccountttl)
- [AccountSetAccountTTL](#accountsetaccountttl)
- [AccountSendChangePhoneCode](#accountsendchangephonecode)
- [AccountChangePhone](#accountchangephone)
- [AccountUpdateDeviceLocked](#accountupdatedevicelocked)
- [AccountGetAuthorizations](#accountgetauthorizations)
- [AccountResetAuthorization](#accountresetauthorization)
- [AccountGetPassword](#accountgetpassword)
- [AccountGetPasswordSettings](#accountgetpasswordsettings)
- [AccountUpdatePasswordSettings](#accountupdatepasswordsettings)
- [AccountSendConfirmPhoneCode](#accountsendconfirmphonecode)
- [AccountConfirmPhone](#accountconfirmphone)
- [AccountGetTmpPassword](#accountgettmppassword)
- [AccountGetWebAuthorizations](#accountgetwebauthorizations)
- [AccountResetWebAuthorization](#accountresetwebauthorization)
- [AccountResetWebAuthorizations](#accountresetwebauthorizations)
- [AccountGetAllSecureValues](#accountgetallsecurevalues)
- [AccountGetSecureValue](#accountgetsecurevalue)
- [AccountSaveSecureValue](#accountsavesecurevalue)
- [AccountDeleteSecureValue](#accountdeletesecurevalue)
- [AccountGetAuthorizationForm](#accountgetauthorizationform)
- [AccountAcceptAuthorization](#accountacceptauthorization)
- [AccountSendVerifyPhoneCode](#accountsendverifyphonecode)
- [AccountVerifyPhone](#accountverifyphone)
- [AccountSendVerifyEmailCode](#accountsendverifyemailcode)
- [AccountVerifyEmail](#accountverifyemail)
- [AccountInitTakeoutSession](#accountinittakeoutsession)
- [AccountFinishTakeoutSession](#accountfinishtakeoutsession)
- [AccountConfirmPasswordEmail](#accountconfirmpasswordemail)
- [AccountResendPasswordEmail](#accountresendpasswordemail)
- [AccountCancelPasswordEmail](#accountcancelpasswordemail)
- [AccountGetContactSignUpNotification](#accountgetcontactsignupnotification)
- [AccountSetContactSignUpNotification](#accountsetcontactsignupnotification)
- [AccountGetNotifyExceptions](#accountgetnotifyexceptions)
- [AccountGetWallPaper](#accountgetwallpaper)
- [AccountUploadWallPaper](#accountuploadwallpaper)
- [AccountSaveWallPaper](#accountsavewallpaper)
- [AccountInstallWallPaper](#accountinstallwallpaper)
- [AccountResetWallPapers](#accountresetwallpapers)
- [AccountGetAutoDownloadSettings](#accountgetautodownloadsettings)
- [AccountSaveAutoDownloadSettings](#accountsaveautodownloadsettings)
- [AccountUploadTheme](#accountuploadtheme)
- [AccountCreateTheme](#accountcreatetheme)
- [AccountUpdateTheme](#accountupdatetheme)
- [AccountSaveTheme](#accountsavetheme)
- [AccountInstallTheme](#accountinstalltheme)
- [AccountGetTheme](#accountgettheme)
- [AccountGetThemes](#accountgetthemes)
- [AccountSetContentSettings](#accountsetcontentsettings)
- [AccountGetContentSettings](#accountgetcontentsettings)
- [AccountGetMultiWallPapers](#accountgetmultiwallpapers)
- [AccountGetGlobalPrivacySettings](#accountgetglobalprivacysettings)
- [AccountSetGlobalPrivacySettings](#accountsetglobalprivacysettings)
- [AccountReportProfilePhoto](#accountreportprofilephoto)
- [AccountResetPassword](#accountresetpassword)
- [AccountDeclinePasswordReset](#accountdeclinepasswordreset)
- [AccountGetChatThemes](#accountgetchatthemes)
- [AccountSetAuthorizationTTL](#accountsetauthorizationttl)
- [AccountChangeAuthorizationSettings](#accountchangeauthorizationsettings)
- [AccountGetSavedRingtones](#accountgetsavedringtones)
- [AccountSaveRingtone](#accountsaveringtone)
- [AccountUploadRingtone](#accountuploadringtone)
- [AccountUpdateEmojiStatus](#accountupdateemojistatus)
- [AccountGetDefaultEmojiStatuses](#accountgetdefaultemojistatuses)
- [AccountGetRecentEmojiStatuses](#accountgetrecentemojistatuses)
- [AccountClearRecentEmojiStatuses](#accountclearrecentemojistatuses)
- [AccountReorderUsernames](#accountreorderusernames)
- [AccountToggleUsername](#accounttoggleusername)
- [AccountGetDefaultProfilePhotoEmojis](#accountgetdefaultprofilephotoemojis)
- [AccountGetDefaultGroupPhotoEmojis](#accountgetdefaultgroupphotoemojis)
- [AccountGetAutoSaveSettings](#accountgetautosavesettings)
- [AccountSaveAutoSaveSettings](#accountsaveautosavesettings)
- [AccountDeleteAutoSaveExceptions](#accountdeleteautosaveexceptions)
- [AccountInvalidateSignInCodes](#accountinvalidatesignincodes)
- [AccountUpdateColor](#accountupdatecolor)
- [AccountGetDefaultBackgroundEmojis](#accountgetdefaultbackgroundemojis)
- [AccountGetChannelDefaultEmojiStatuses](#accountgetchanneldefaultemojistatuses)
- [AccountGetChannelRestrictedStatusEmojis](#accountgetchannelrestrictedstatusemojis)
- [AccountUpdateBusinessWorkHours](#accountupdatebusinessworkhours)
- [AccountUpdateBusinessLocation](#accountupdatebusinesslocation)
- [AccountUpdateBusinessGreetingMessage](#accountupdatebusinessgreetingmessage)
- [AccountUpdateBusinessAwayMessage](#accountupdatebusinessawaymessage)
- [AccountUpdateConnectedBot](#accountupdateconnectedbot)
- [AccountGetConnectedBots](#accountgetconnectedbots)
- [AccountGetBotBusinessConnection](#accountgetbotbusinessconnection)
- [AccountUpdateBusinessIntro](#accountupdatebusinessintro)
- [AccountToggleConnectedBotPaused](#accounttoggleconnectedbotpaused)
- [AccountDisablePeerConnectedBot](#accountdisablepeerconnectedbot)
- [AccountUpdateBirthday](#accountupdatebirthday)
- [AccountCreateBusinessChatLink](#accountcreatebusinesschatlink)
- [AccountEditBusinessChatLink](#accounteditbusinesschatlink)
- [AccountDeleteBusinessChatLink](#accountdeletebusinesschatlink)
- [AccountGetBusinessChatLinks](#accountgetbusinesschatlinks)
- [AccountResolveBusinessChatLink](#accountresolvebusinesschatlink)
- [AccountUpdatePersonalChannel](#accountupdatepersonalchannel)
- [AccountToggleSponsoredMessages](#accounttogglesponsoredmessages)
- [AccountGetReactionsNotifySettings](#accountgetreactionsnotifysettings)
- [AccountSetReactionsNotifySettings](#accountsetreactionsnotifysettings)
- [AccountGetCollectibleEmojiStatuses](#accountgetcollectibleemojistatuses)
- [AccountGetPaidMessagesRevenue](#accountgetpaidmessagesrevenue)
- [AccountToggleNoPaidMessagesException](#accounttogglenopaidmessagesexception)
- [AccountSetMainProfileTab](#accountsetmainprofiletab)
- [AccountSaveMusic](#accountsavemusic)
- [AccountGetSavedMusicIds](#accountgetsavedmusicids)
- [AccountGetUniqueGiftChatThemes](#accountgetuniquegiftchatthemes)
- [AccountInitPasskeyRegistration](#accountinitpasskeyregistration)
- [AccountRegisterPasskey](#accountregisterpasskey)
- [AccountGetPasskeys](#accountgetpasskeys)
- [AccountDeletePasskey](#accountdeletepasskey)

## AccountRegisterDevice

**Constructor ID:** `3968205178`

**Fields:** ``no_muted`, `token_type`, `token`, `app_sandbox`, `secret`, `other_uids``

```python
AccountRegisterDevice.__init__(token_type: int, token: str, app_sandbox: bool, secret: bytes, other_uids: 'Vector', no_muted: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `token_type` | `int` | `—` |
| `token` | `str` | `—` |
| `app_sandbox` | `bool` | `—` |
| `secret` | `bytes` | `—` |
| `other_uids` | `'Vector'` | `—` |
| `no_muted` | `bool` | `None` |

```python
from sent.tl.functions.account import AccountRegisterDevice
```

```python
result = await client(AccountRegisterDevice(...))
```

## AccountUnregisterDevice

**Constructor ID:** `1779249670`

**Fields:** ``token_type`, `token`, `other_uids``

```python
AccountUnregisterDevice.__init__(token_type: int, token: str, other_uids: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `token_type` | `int` | `—` |
| `token` | `str` | `—` |
| `other_uids` | `'Vector'` | `—` |

```python
from sent.tl.functions.account import AccountUnregisterDevice
```

```python
result = await client(AccountUnregisterDevice(...))
```

## AccountUpdateNotifySettings

**Constructor ID:** `2227067795`

**Fields:** ``peer`, `settings``

```python
AccountUpdateNotifySettings.__init__(peer: 'InputNotifyPeer', settings: 'InputPeerNotifySettings')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputNotifyPeer'` | `—` |
| `settings` | `'InputPeerNotifySettings'` | `—` |

```python
from sent.tl.functions.account import AccountUpdateNotifySettings
```

```python
result = await client(AccountUpdateNotifySettings(...))
```

## AccountGetNotifySettings

**Constructor ID:** `313765169`

```python
AccountGetNotifySettings.__init__(peer: 'InputNotifyPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputNotifyPeer'` | `—` |

```python
from sent.tl.functions.account import AccountGetNotifySettings
```

```python
result = await client(AccountGetNotifySettings(...))
```

## AccountResetNotifySettings

**Constructor ID:** `3682473799`

```python
AccountResetNotifySettings.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountResetNotifySettings
```

```python
result = await client(AccountResetNotifySettings(...))
```

## AccountUpdateProfile

**Constructor ID:** `2018596725`

**Fields:** ``first_name`, `last_name`, `about``

```python
AccountUpdateProfile.__init__(first_name: str = None, last_name: str = None, about: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `first_name` | `str` | `None` |
| `last_name` | `str` | `None` |
| `about` | `str` | `None` |

```python
from sent.tl.functions.account import AccountUpdateProfile
```

```python
result = await client(AccountUpdateProfile(...))
```

## AccountUpdateStatus

**Constructor ID:** `1713919532`

```python
AccountUpdateStatus.__init__(offline: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `offline` | `bool` | `—` |

```python
from sent.tl.functions.account import AccountUpdateStatus
```

```python
result = await client(AccountUpdateStatus(...))
```

## AccountGetWallPapers

**Constructor ID:** `127302966`

```python
AccountGetWallPapers.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.account import AccountGetWallPapers
```

```python
result = await client(AccountGetWallPapers(...))
```

## AccountReportPeer

**Constructor ID:** `3317316998`

**Fields:** ``peer`, `reason`, `message``

```python
AccountReportPeer.__init__(peer: 'InputPeer', reason: 'ReportReason', message: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `reason` | `'ReportReason'` | `—` |
| `message` | `str` | `—` |

```python
from sent.tl.functions.account import AccountReportPeer
```

```python
result = await client(AccountReportPeer(...))
```

## AccountCheckUsername

**Constructor ID:** `655677548`

```python
AccountCheckUsername.__init__(username: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `username` | `str` | `—` |

```python
from sent.tl.functions.account import AccountCheckUsername
```

```python
result = await client(AccountCheckUsername(...))
```

## AccountUpdateUsername

**Constructor ID:** `1040964988`

```python
AccountUpdateUsername.__init__(username: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `username` | `str` | `—` |

```python
from sent.tl.functions.account import AccountUpdateUsername
```

```python
result = await client(AccountUpdateUsername(...))
```

## AccountGetPrivacy

**Constructor ID:** `3671837008`

```python
AccountGetPrivacy.__init__(key: 'InputPrivacyKey')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `key` | `'InputPrivacyKey'` | `—` |

```python
from sent.tl.functions.account import AccountGetPrivacy
```

```python
result = await client(AccountGetPrivacy(...))
```

## AccountSetPrivacy

**Constructor ID:** `3388480744`

**Fields:** ``key`, `rules``

```python
AccountSetPrivacy.__init__(key: 'InputPrivacyKey', rules: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `key` | `'InputPrivacyKey'` | `—` |
| `rules` | `'Vector'` | `—` |

```python
from sent.tl.functions.account import AccountSetPrivacy
```

```python
result = await client(AccountSetPrivacy(...))
```

## AccountDeleteAccount

**Constructor ID:** `2730545012`

**Fields:** ``reason`, `password``

```python
AccountDeleteAccount.__init__(reason: str, password: 'InputCheckPasswordSRP' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `reason` | `str` | `—` |
| `password` | `'InputCheckPasswordSRP'` | `None` |

```python
from sent.tl.functions.account import AccountDeleteAccount
```

```python
result = await client(AccountDeleteAccount(...))
```

## AccountGetAccountTTL

**Constructor ID:** `150761757`

```python
AccountGetAccountTTL.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountGetAccountTTL
```

```python
result = await client(AccountGetAccountTTL(...))
```

## AccountSetAccountTTL

**Constructor ID:** `608323678`

```python
AccountSetAccountTTL.__init__(ttl: 'AccountDaysTTL')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `ttl` | `'AccountDaysTTL'` | `—` |

```python
from sent.tl.functions.account import AccountSetAccountTTL
```

```python
result = await client(AccountSetAccountTTL(...))
```

## AccountSendChangePhoneCode

**Constructor ID:** `2186758885`

**Fields:** ``phone_number`, `settings``

```python
AccountSendChangePhoneCode.__init__(phone_number: str, settings: 'CodeSettings')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `phone_number` | `str` | `—` |
| `settings` | `'CodeSettings'` | `—` |

```python
from sent.tl.functions.account import AccountSendChangePhoneCode
```

```python
result = await client(AccountSendChangePhoneCode(...))
```

## AccountChangePhone

**Constructor ID:** `1891839707`

**Fields:** ``phone_number`, `phone_code_hash`, `phone_code``

```python
AccountChangePhone.__init__(phone_number: str, phone_code_hash: str, phone_code: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `phone_number` | `str` | `—` |
| `phone_code_hash` | `str` | `—` |
| `phone_code` | `str` | `—` |

```python
from sent.tl.functions.account import AccountChangePhone
```

```python
result = await client(AccountChangePhone(...))
```

## AccountUpdateDeviceLocked

**Constructor ID:** `954152242`

```python
AccountUpdateDeviceLocked.__init__(period: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `period` | `int` | `—` |

```python
from sent.tl.functions.account import AccountUpdateDeviceLocked
```

```python
result = await client(AccountUpdateDeviceLocked(...))
```

## AccountGetAuthorizations

**Constructor ID:** `3810574680`

```python
AccountGetAuthorizations.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountGetAuthorizations
```

```python
result = await client(AccountGetAuthorizations(...))
```

## AccountResetAuthorization

**Constructor ID:** `3749180348`

```python
AccountResetAuthorization.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.account import AccountResetAuthorization
```

```python
result = await client(AccountResetAuthorization(...))
```

## AccountGetPassword

**Constructor ID:** `1418342645`

```python
AccountGetPassword.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountGetPassword
```

```python
result = await client(AccountGetPassword(...))
```

## AccountGetPasswordSettings

**Constructor ID:** `2631199481`

```python
AccountGetPasswordSettings.__init__(password: 'InputCheckPasswordSRP')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `password` | `'InputCheckPasswordSRP'` | `—` |

```python
from sent.tl.functions.account import AccountGetPasswordSettings
```

```python
result = await client(AccountGetPasswordSettings(...))
```

## AccountUpdatePasswordSettings

**Constructor ID:** `2778402863`

**Fields:** ``password`, `new_settings``

```python
AccountUpdatePasswordSettings.__init__(password: 'InputCheckPasswordSRP', new_settings: 'AccountPasswordInputSettings')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `password` | `'InputCheckPasswordSRP'` | `—` |
| `new_settings` | `'AccountPasswordInputSettings'` | `—` |

```python
from sent.tl.functions.account import AccountUpdatePasswordSettings
```

```python
result = await client(AccountUpdatePasswordSettings(...))
```

## AccountSendConfirmPhoneCode

**Constructor ID:** `457157256`

**Fields:** ``hash`, `settings``

```python
AccountSendConfirmPhoneCode.__init__(hash: str, settings: 'CodeSettings')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `str` | `—` |
| `settings` | `'CodeSettings'` | `—` |

```python
from sent.tl.functions.account import AccountSendConfirmPhoneCode
```

```python
result = await client(AccountSendConfirmPhoneCode(...))
```

## AccountConfirmPhone

**Constructor ID:** `1596029123`

**Fields:** ``phone_code_hash`, `phone_code``

```python
AccountConfirmPhone.__init__(phone_code_hash: str, phone_code: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `phone_code_hash` | `str` | `—` |
| `phone_code` | `str` | `—` |

```python
from sent.tl.functions.account import AccountConfirmPhone
```

```python
result = await client(AccountConfirmPhone(...))
```

## AccountGetTmpPassword

**Constructor ID:** `1151208273`

**Fields:** ``password`, `period``

```python
AccountGetTmpPassword.__init__(password: 'InputCheckPasswordSRP', period: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `password` | `'InputCheckPasswordSRP'` | `—` |
| `period` | `int` | `—` |

```python
from sent.tl.functions.account import AccountGetTmpPassword
```

```python
result = await client(AccountGetTmpPassword(...))
```

## AccountGetWebAuthorizations

**Constructor ID:** `405695855`

```python
AccountGetWebAuthorizations.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountGetWebAuthorizations
```

```python
result = await client(AccountGetWebAuthorizations(...))
```

## AccountResetWebAuthorization

**Constructor ID:** `755087855`

```python
AccountResetWebAuthorization.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.account import AccountResetWebAuthorization
```

```python
result = await client(AccountResetWebAuthorization(...))
```

## AccountResetWebAuthorizations

**Constructor ID:** `1747789204`

```python
AccountResetWebAuthorizations.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountResetWebAuthorizations
```

```python
result = await client(AccountResetWebAuthorizations(...))
```

## AccountGetAllSecureValues

**Constructor ID:** `2995305597`

```python
AccountGetAllSecureValues.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountGetAllSecureValues
```

```python
result = await client(AccountGetAllSecureValues(...))
```

## AccountGetSecureValue

**Constructor ID:** `1936088002`

```python
AccountGetSecureValue.__init__(types: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `types` | `'Vector'` | `—` |

```python
from sent.tl.functions.account import AccountGetSecureValue
```

```python
result = await client(AccountGetSecureValue(...))
```

## AccountSaveSecureValue

**Constructor ID:** `2308956957`

**Fields:** ``value`, `secure_secret_id``

```python
AccountSaveSecureValue.__init__(value: 'InputSecureValue', secure_secret_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `value` | `'InputSecureValue'` | `—` |
| `secure_secret_id` | `int` | `—` |

```python
from sent.tl.functions.account import AccountSaveSecureValue
```

```python
result = await client(AccountSaveSecureValue(...))
```

## AccountDeleteSecureValue

**Constructor ID:** `3095444555`

```python
AccountDeleteSecureValue.__init__(types: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `types` | `'Vector'` | `—` |

```python
from sent.tl.functions.account import AccountDeleteSecureValue
```

```python
result = await client(AccountDeleteSecureValue(...))
```

## AccountGetAuthorizationForm

**Constructor ID:** `2838059386`

**Fields:** ``bot_id`, `scope`, `public_key``

```python
AccountGetAuthorizationForm.__init__(bot_id: int, scope: str, public_key: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot_id` | `int` | `—` |
| `scope` | `str` | `—` |
| `public_key` | `str` | `—` |

```python
from sent.tl.functions.account import AccountGetAuthorizationForm
```

```python
result = await client(AccountGetAuthorizationForm(...))
```

## AccountAcceptAuthorization

**Constructor ID:** `4092415091`

**Fields:** ``bot_id`, `scope`, `public_key`, `value_hashes`, `credentials``

```python
AccountAcceptAuthorization.__init__(bot_id: int, scope: str, public_key: str, value_hashes: 'Vector', credentials: 'SecureCredentialsEncrypted')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot_id` | `int` | `—` |
| `scope` | `str` | `—` |
| `public_key` | `str` | `—` |
| `value_hashes` | `'Vector'` | `—` |
| `credentials` | `'SecureCredentialsEncrypted'` | `—` |

```python
from sent.tl.functions.account import AccountAcceptAuthorization
```

```python
result = await client(AccountAcceptAuthorization(...))
```

## AccountSendVerifyPhoneCode

**Constructor ID:** `2778945273`

**Fields:** ``phone_number`, `settings``

```python
AccountSendVerifyPhoneCode.__init__(phone_number: str, settings: 'CodeSettings')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `phone_number` | `str` | `—` |
| `settings` | `'CodeSettings'` | `—` |

```python
from sent.tl.functions.account import AccountSendVerifyPhoneCode
```

```python
result = await client(AccountSendVerifyPhoneCode(...))
```

## AccountVerifyPhone

**Constructor ID:** `1305716726`

**Fields:** ``phone_number`, `phone_code_hash`, `phone_code``

```python
AccountVerifyPhone.__init__(phone_number: str, phone_code_hash: str, phone_code: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `phone_number` | `str` | `—` |
| `phone_code_hash` | `str` | `—` |
| `phone_code` | `str` | `—` |

```python
from sent.tl.functions.account import AccountVerifyPhone
```

```python
result = await client(AccountVerifyPhone(...))
```

## AccountSendVerifyEmailCode

**Constructor ID:** `2564831163`

**Fields:** ``purpose`, `email``

```python
AccountSendVerifyEmailCode.__init__(purpose: 'EmailVerifyPurpose', email: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `purpose` | `'EmailVerifyPurpose'` | `—` |
| `email` | `str` | `—` |

```python
from sent.tl.functions.account import AccountSendVerifyEmailCode
```

```python
result = await client(AccountSendVerifyEmailCode(...))
```

## AccountVerifyEmail

**Constructor ID:** `53322959`

**Fields:** ``purpose`, `verification``

```python
AccountVerifyEmail.__init__(purpose: 'EmailVerifyPurpose', verification: 'EmailVerification')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `purpose` | `'EmailVerifyPurpose'` | `—` |
| `verification` | `'EmailVerification'` | `—` |

```python
from sent.tl.functions.account import AccountVerifyEmail
```

```python
result = await client(AccountVerifyEmail(...))
```

## AccountInitTakeoutSession

**Constructor ID:** `2398350000`

**Fields:** ``contacts`, `message_users`, `message_chats`, `message_megagroups`, `message_channels`, `files`, `file_max_size``

```python
AccountInitTakeoutSession.__init__(contacts: bool = None, message_users: bool = None, message_chats: bool = None, message_megagroups: bool = None, message_channels: bool = None, files: bool = None, file_max_size: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `contacts` | `bool` | `None` |
| `message_users` | `bool` | `None` |
| `message_chats` | `bool` | `None` |
| `message_megagroups` | `bool` | `None` |
| `message_channels` | `bool` | `None` |
| `files` | `bool` | `None` |
| `file_max_size` | `int` | `None` |

```python
from sent.tl.functions.account import AccountInitTakeoutSession
```

```python
result = await client(AccountInitTakeoutSession(...))
```

## AccountFinishTakeoutSession

**Constructor ID:** `489050862`

```python
AccountFinishTakeoutSession.__init__(success: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `success` | `bool` | `None` |

```python
from sent.tl.functions.account import AccountFinishTakeoutSession
```

```python
result = await client(AccountFinishTakeoutSession(...))
```

## AccountConfirmPasswordEmail

**Constructor ID:** `2413762848`

```python
AccountConfirmPasswordEmail.__init__(code: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `code` | `str` | `—` |

```python
from sent.tl.functions.account import AccountConfirmPasswordEmail
```

```python
result = await client(AccountConfirmPasswordEmail(...))
```

## AccountResendPasswordEmail

**Constructor ID:** `2055154197`

```python
AccountResendPasswordEmail.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountResendPasswordEmail
```

```python
result = await client(AccountResendPasswordEmail(...))
```

## AccountCancelPasswordEmail

**Constructor ID:** `3251361206`

```python
AccountCancelPasswordEmail.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountCancelPasswordEmail
```

```python
result = await client(AccountCancelPasswordEmail(...))
```

## AccountGetContactSignUpNotification

**Constructor ID:** `2668087080`

```python
AccountGetContactSignUpNotification.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountGetContactSignUpNotification
```

```python
result = await client(AccountGetContactSignUpNotification(...))
```

## AccountSetContactSignUpNotification

**Constructor ID:** `3488890721`

```python
AccountSetContactSignUpNotification.__init__(silent: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `silent` | `bool` | `—` |

```python
from sent.tl.functions.account import AccountSetContactSignUpNotification
```

```python
result = await client(AccountSetContactSignUpNotification(...))
```

## AccountGetNotifyExceptions

**Constructor ID:** `1398240377`

**Fields:** ``compare_sound`, `compare_stories`, `peer``

```python
AccountGetNotifyExceptions.__init__(compare_sound: bool = None, compare_stories: bool = None, peer: 'InputNotifyPeer' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `compare_sound` | `bool` | `None` |
| `compare_stories` | `bool` | `None` |
| `peer` | `'InputNotifyPeer'` | `None` |

```python
from sent.tl.functions.account import AccountGetNotifyExceptions
```

```python
result = await client(AccountGetNotifyExceptions(...))
```

## AccountGetWallPaper

**Constructor ID:** `4237155306`

```python
AccountGetWallPaper.__init__(wallpaper: 'InputWallPaper')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `wallpaper` | `'InputWallPaper'` | `—` |

```python
from sent.tl.functions.account import AccountGetWallPaper
```

```python
result = await client(AccountGetWallPaper(...))
```

## AccountUploadWallPaper

**Constructor ID:** `3818557187`

**Fields:** ``for_chat`, `file`, `mime_type`, `settings``

```python
AccountUploadWallPaper.__init__(file: 'InputFile', mime_type: str, settings: 'WallPaperSettings', for_chat: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `file` | `'InputFile'` | `—` |
| `mime_type` | `str` | `—` |
| `settings` | `'WallPaperSettings'` | `—` |
| `for_chat` | `bool` | `None` |

```python
from sent.tl.functions.account import AccountUploadWallPaper
```

```python
result = await client(AccountUploadWallPaper(...))
```

## AccountSaveWallPaper

**Constructor ID:** `1817860919`

**Fields:** ``wallpaper`, `unsave`, `settings``

```python
AccountSaveWallPaper.__init__(wallpaper: 'InputWallPaper', unsave: bool, settings: 'WallPaperSettings')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `wallpaper` | `'InputWallPaper'` | `—` |
| `unsave` | `bool` | `—` |
| `settings` | `'WallPaperSettings'` | `—` |

```python
from sent.tl.functions.account import AccountSaveWallPaper
```

```python
result = await client(AccountSaveWallPaper(...))
```

## AccountInstallWallPaper

**Constructor ID:** `4276967273`

**Fields:** ``wallpaper`, `settings``

```python
AccountInstallWallPaper.__init__(wallpaper: 'InputWallPaper', settings: 'WallPaperSettings')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `wallpaper` | `'InputWallPaper'` | `—` |
| `settings` | `'WallPaperSettings'` | `—` |

```python
from sent.tl.functions.account import AccountInstallWallPaper
```

```python
result = await client(AccountInstallWallPaper(...))
```

## AccountResetWallPapers

**Constructor ID:** `3141244932`

```python
AccountResetWallPapers.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountResetWallPapers
```

```python
result = await client(AccountResetWallPapers(...))
```

## AccountGetAutoDownloadSettings

**Constructor ID:** `1457130303`

```python
AccountGetAutoDownloadSettings.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountGetAutoDownloadSettings
```

```python
result = await client(AccountGetAutoDownloadSettings(...))
```

## AccountSaveAutoDownloadSettings

**Constructor ID:** `1995661875`

**Fields:** ``low`, `high`, `settings``

```python
AccountSaveAutoDownloadSettings.__init__(settings: 'AutoDownloadSettings', low: bool = None, high: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `settings` | `'AutoDownloadSettings'` | `—` |
| `low` | `bool` | `None` |
| `high` | `bool` | `None` |

```python
from sent.tl.functions.account import AccountSaveAutoDownloadSettings
```

```python
result = await client(AccountSaveAutoDownloadSettings(...))
```

## AccountUploadTheme

**Constructor ID:** `473805619`

**Fields:** ``file`, `thumb`, `file_name`, `mime_type``

```python
AccountUploadTheme.__init__(file: 'InputFile', file_name: str, mime_type: str, thumb: 'InputFile' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `file` | `'InputFile'` | `—` |
| `file_name` | `str` | `—` |
| `mime_type` | `str` | `—` |
| `thumb` | `'InputFile'` | `None` |

```python
from sent.tl.functions.account import AccountUploadTheme
```

```python
result = await client(AccountUploadTheme(...))
```

## AccountCreateTheme

**Constructor ID:** `1697530880`

**Fields:** ``slug`, `title`, `document`, `settings``

```python
AccountCreateTheme.__init__(slug: str, title: str, document: 'InputDocument' = None, settings: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `slug` | `str` | `—` |
| `title` | `str` | `—` |
| `document` | `'InputDocument'` | `None` |
| `settings` | `'Vector'` | `None` |

```python
from sent.tl.functions.account import AccountCreateTheme
```

```python
result = await client(AccountCreateTheme(...))
```

## AccountUpdateTheme

**Constructor ID:** `737414348`

**Fields:** ``format`, `theme`, `slug`, `title`, `document`, `settings``

```python
AccountUpdateTheme.__init__(format: str, theme: 'InputTheme', slug: str = None, title: str = None, document: 'InputDocument' = None, settings: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `format` | `str` | `—` |
| `theme` | `'InputTheme'` | `—` |
| `slug` | `str` | `None` |
| `title` | `str` | `None` |
| `document` | `'InputDocument'` | `None` |
| `settings` | `'Vector'` | `None` |

```python
from sent.tl.functions.account import AccountUpdateTheme
```

```python
result = await client(AccountUpdateTheme(...))
```

## AccountSaveTheme

**Constructor ID:** `4065792108`

**Fields:** ``theme`, `unsave``

```python
AccountSaveTheme.__init__(theme: 'InputTheme', unsave: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `theme` | `'InputTheme'` | `—` |
| `unsave` | `bool` | `—` |

```python
from sent.tl.functions.account import AccountSaveTheme
```

```python
result = await client(AccountSaveTheme(...))
```

## AccountInstallTheme

**Constructor ID:** `3341269819`

**Fields:** ``dark`, `theme`, `format`, `base_theme``

```python
AccountInstallTheme.__init__(dark: bool = None, theme: 'InputTheme' = None, format: str = None, base_theme: 'BaseTheme' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `dark` | `bool` | `None` |
| `theme` | `'InputTheme'` | `None` |
| `format` | `str` | `None` |
| `base_theme` | `'BaseTheme'` | `None` |

```python
from sent.tl.functions.account import AccountInstallTheme
```

```python
result = await client(AccountInstallTheme(...))
```

## AccountGetTheme

**Constructor ID:** `978872812`

**Fields:** ``format`, `theme``

```python
AccountGetTheme.__init__(format: str, theme: 'InputTheme')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `format` | `str` | `—` |
| `theme` | `'InputTheme'` | `—` |

```python
from sent.tl.functions.account import AccountGetTheme
```

```python
result = await client(AccountGetTheme(...))
```

## AccountGetThemes

**Constructor ID:** `1913054296`

**Fields:** ``format`, `hash``

```python
AccountGetThemes.__init__(format: str, hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `format` | `str` | `—` |
| `hash` | `int` | `—` |

```python
from sent.tl.functions.account import AccountGetThemes
```

```python
result = await client(AccountGetThemes(...))
```

## AccountSetContentSettings

**Constructor ID:** `3044323691`

```python
AccountSetContentSettings.__init__(sensitive_enabled: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `sensitive_enabled` | `bool` | `None` |

```python
from sent.tl.functions.account import AccountSetContentSettings
```

```python
result = await client(AccountSetContentSettings(...))
```

## AccountGetContentSettings

**Constructor ID:** `2342210990`

```python
AccountGetContentSettings.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountGetContentSettings
```

```python
result = await client(AccountGetContentSettings(...))
```

## AccountGetMultiWallPapers

**Constructor ID:** `1705865692`

```python
AccountGetMultiWallPapers.__init__(wallpapers: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `wallpapers` | `'Vector'` | `—` |

```python
from sent.tl.functions.account import AccountGetMultiWallPapers
```

```python
result = await client(AccountGetMultiWallPapers(...))
```

## AccountGetGlobalPrivacySettings

**Constructor ID:** `3945483510`

```python
AccountGetGlobalPrivacySettings.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountGetGlobalPrivacySettings
```

```python
result = await client(AccountGetGlobalPrivacySettings(...))
```

## AccountSetGlobalPrivacySettings

**Constructor ID:** `517647042`

```python
AccountSetGlobalPrivacySettings.__init__(settings: 'GlobalPrivacySettings')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `settings` | `'GlobalPrivacySettings'` | `—` |

```python
from sent.tl.functions.account import AccountSetGlobalPrivacySettings
```

```python
result = await client(AccountSetGlobalPrivacySettings(...))
```

## AccountReportProfilePhoto

**Constructor ID:** `4203529973`

**Fields:** ``peer`, `photo_id`, `reason`, `message``

```python
AccountReportProfilePhoto.__init__(peer: 'InputPeer', photo_id: 'InputPhoto', reason: 'ReportReason', message: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `photo_id` | `'InputPhoto'` | `—` |
| `reason` | `'ReportReason'` | `—` |
| `message` | `str` | `—` |

```python
from sent.tl.functions.account import AccountReportProfilePhoto
```

```python
result = await client(AccountReportProfilePhoto(...))
```

## AccountResetPassword

**Constructor ID:** `2466827803`

```python
AccountResetPassword.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountResetPassword
```

```python
result = await client(AccountResetPassword(...))
```

## AccountDeclinePasswordReset

**Constructor ID:** `1284770294`

```python
AccountDeclinePasswordReset.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountDeclinePasswordReset
```

```python
result = await client(AccountDeclinePasswordReset(...))
```

## AccountGetChatThemes

**Constructor ID:** `3594051209`

```python
AccountGetChatThemes.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.account import AccountGetChatThemes
```

```python
result = await client(AccountGetChatThemes(...))
```

## AccountSetAuthorizationTTL

**Constructor ID:** `3213466272`

```python
AccountSetAuthorizationTTL.__init__(authorization_ttl_days: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `authorization_ttl_days` | `int` | `—` |

```python
from sent.tl.functions.account import AccountSetAuthorizationTTL
```

```python
result = await client(AccountSetAuthorizationTTL(...))
```

## AccountChangeAuthorizationSettings

**Constructor ID:** `1089766498`

**Fields:** ``confirmed`, `hash`, `encrypted_requests_disabled`, `call_requests_disabled``

```python
AccountChangeAuthorizationSettings.__init__(hash: int, confirmed: bool = None, encrypted_requests_disabled: bool = None, call_requests_disabled: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `confirmed` | `bool` | `None` |
| `encrypted_requests_disabled` | `bool` | `None` |
| `call_requests_disabled` | `bool` | `None` |

```python
from sent.tl.functions.account import AccountChangeAuthorizationSettings
```

```python
result = await client(AccountChangeAuthorizationSettings(...))
```

## AccountGetSavedRingtones

**Constructor ID:** `3784319624`

```python
AccountGetSavedRingtones.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.account import AccountGetSavedRingtones
```

```python
result = await client(AccountGetSavedRingtones(...))
```

## AccountSaveRingtone

**Constructor ID:** `1038768899`

**Fields:** ``id`, `unsave``

```python
AccountSaveRingtone.__init__(id: 'InputDocument', unsave: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'InputDocument'` | `—` |
| `unsave` | `bool` | `—` |

```python
from sent.tl.functions.account import AccountSaveRingtone
```

```python
result = await client(AccountSaveRingtone(...))
```

## AccountUploadRingtone

**Constructor ID:** `2199552930`

**Fields:** ``file`, `file_name`, `mime_type``

```python
AccountUploadRingtone.__init__(file: 'InputFile', file_name: str, mime_type: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `file` | `'InputFile'` | `—` |
| `file_name` | `str` | `—` |
| `mime_type` | `str` | `—` |

```python
from sent.tl.functions.account import AccountUploadRingtone
```

```python
result = await client(AccountUploadRingtone(...))
```

## AccountUpdateEmojiStatus

**Constructor ID:** `4224966251`

```python
AccountUpdateEmojiStatus.__init__(emoji_status: 'EmojiStatus')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `emoji_status` | `'EmojiStatus'` | `—` |

```python
from sent.tl.functions.account import AccountUpdateEmojiStatus
```

```python
result = await client(AccountUpdateEmojiStatus(...))
```

## AccountGetDefaultEmojiStatuses

**Constructor ID:** `3598005126`

```python
AccountGetDefaultEmojiStatuses.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.account import AccountGetDefaultEmojiStatuses
```

```python
result = await client(AccountGetDefaultEmojiStatuses(...))
```

## AccountGetRecentEmojiStatuses

**Constructor ID:** `257392901`

```python
AccountGetRecentEmojiStatuses.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.account import AccountGetRecentEmojiStatuses
```

```python
result = await client(AccountGetRecentEmojiStatuses(...))
```

## AccountClearRecentEmojiStatuses

**Constructor ID:** `404757166`

```python
AccountClearRecentEmojiStatuses.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountClearRecentEmojiStatuses
```

```python
result = await client(AccountClearRecentEmojiStatuses(...))
```

## AccountReorderUsernames

**Constructor ID:** `4015001259`

```python
AccountReorderUsernames.__init__(order: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `order` | `'Vector'` | `—` |

```python
from sent.tl.functions.account import AccountReorderUsernames
```

```python
result = await client(AccountReorderUsernames(...))
```

## AccountToggleUsername

**Constructor ID:** `1490465654`

**Fields:** ``username`, `active``

```python
AccountToggleUsername.__init__(username: str, active: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `username` | `str` | `—` |
| `active` | `bool` | `—` |

```python
from sent.tl.functions.account import AccountToggleUsername
```

```python
result = await client(AccountToggleUsername(...))
```

## AccountGetDefaultProfilePhotoEmojis

**Constructor ID:** `3799319336`

```python
AccountGetDefaultProfilePhotoEmojis.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.account import AccountGetDefaultProfilePhotoEmojis
```

```python
result = await client(AccountGetDefaultProfilePhotoEmojis(...))
```

## AccountGetDefaultGroupPhotoEmojis

**Constructor ID:** `2438488238`

```python
AccountGetDefaultGroupPhotoEmojis.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.account import AccountGetDefaultGroupPhotoEmojis
```

```python
result = await client(AccountGetDefaultGroupPhotoEmojis(...))
```

## AccountGetAutoSaveSettings

**Constructor ID:** `2915810522`

```python
AccountGetAutoSaveSettings.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountGetAutoSaveSettings
```

```python
result = await client(AccountGetAutoSaveSettings(...))
```

## AccountSaveAutoSaveSettings

**Constructor ID:** `3600515937`

**Fields:** ``users`, `chats`, `broadcasts`, `peer`, `settings``

```python
AccountSaveAutoSaveSettings.__init__(settings: 'AutoSaveSettings', users: bool = None, chats: bool = None, broadcasts: bool = None, peer: 'InputPeer' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `settings` | `'AutoSaveSettings'` | `—` |
| `users` | `bool` | `None` |
| `chats` | `bool` | `None` |
| `broadcasts` | `bool` | `None` |
| `peer` | `'InputPeer'` | `None` |

```python
from sent.tl.functions.account import AccountSaveAutoSaveSettings
```

```python
result = await client(AccountSaveAutoSaveSettings(...))
```

## AccountDeleteAutoSaveExceptions

**Constructor ID:** `1404829728`

```python
AccountDeleteAutoSaveExceptions.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountDeleteAutoSaveExceptions
```

```python
result = await client(AccountDeleteAutoSaveExceptions(...))
```

## AccountInvalidateSignInCodes

**Constructor ID:** `3398101178`

```python
AccountInvalidateSignInCodes.__init__(codes: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `codes` | `'Vector'` | `—` |

```python
from sent.tl.functions.account import AccountInvalidateSignInCodes
```

```python
result = await client(AccountInvalidateSignInCodes(...))
```

## AccountUpdateColor

**Constructor ID:** `1749885262`

**Fields:** ``for_profile`, `color``

```python
AccountUpdateColor.__init__(for_profile: bool = None, color: 'PeerColor' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `for_profile` | `bool` | `None` |
| `color` | `'PeerColor'` | `None` |

```python
from sent.tl.functions.account import AccountUpdateColor
```

```python
result = await client(AccountUpdateColor(...))
```

## AccountGetDefaultBackgroundEmojis

**Constructor ID:** `2785720782`

```python
AccountGetDefaultBackgroundEmojis.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.account import AccountGetDefaultBackgroundEmojis
```

```python
result = await client(AccountGetDefaultBackgroundEmojis(...))
```

## AccountGetChannelDefaultEmojiStatuses

**Constructor ID:** `1999087573`

```python
AccountGetChannelDefaultEmojiStatuses.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.account import AccountGetChannelDefaultEmojiStatuses
```

```python
result = await client(AccountGetChannelDefaultEmojiStatuses(...))
```

## AccountGetChannelRestrictedStatusEmojis

**Constructor ID:** `900325589`

```python
AccountGetChannelRestrictedStatusEmojis.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.account import AccountGetChannelRestrictedStatusEmojis
```

```python
result = await client(AccountGetChannelRestrictedStatusEmojis(...))
```

## AccountUpdateBusinessWorkHours

**Constructor ID:** `1258348646`

```python
AccountUpdateBusinessWorkHours.__init__(business_work_hours: 'BusinessWorkHours' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `business_work_hours` | `'BusinessWorkHours'` | `None` |

```python
from sent.tl.functions.account import AccountUpdateBusinessWorkHours
```

```python
result = await client(AccountUpdateBusinessWorkHours(...))
```

## AccountUpdateBusinessLocation

**Constructor ID:** `2657817370`

**Fields:** ``geo_point`, `address``

```python
AccountUpdateBusinessLocation.__init__(geo_point: 'InputGeoPoint' = None, address: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `geo_point` | `'InputGeoPoint'` | `None` |
| `address` | `str` | `None` |

```python
from sent.tl.functions.account import AccountUpdateBusinessLocation
```

```python
result = await client(AccountUpdateBusinessLocation(...))
```

## AccountUpdateBusinessGreetingMessage

**Constructor ID:** `1724755908`

```python
AccountUpdateBusinessGreetingMessage.__init__(message: 'InputBusinessGreetingMessage' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `message` | `'InputBusinessGreetingMessage'` | `None` |

```python
from sent.tl.functions.account import AccountUpdateBusinessGreetingMessage
```

```python
result = await client(AccountUpdateBusinessGreetingMessage(...))
```

## AccountUpdateBusinessAwayMessage

**Constructor ID:** `2724888485`

```python
AccountUpdateBusinessAwayMessage.__init__(message: 'InputBusinessAwayMessage' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `message` | `'InputBusinessAwayMessage'` | `None` |

```python
from sent.tl.functions.account import AccountUpdateBusinessAwayMessage
```

```python
result = await client(AccountUpdateBusinessAwayMessage(...))
```

## AccountUpdateConnectedBot

**Constructor ID:** `1721797758`

**Fields:** ``deleted`, `rights`, `bot`, `recipients``

```python
AccountUpdateConnectedBot.__init__(bot: 'InputUser', recipients: 'InputBusinessBotRecipients', deleted: bool = None, rights: 'BusinessBotRights' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |
| `recipients` | `'InputBusinessBotRecipients'` | `—` |
| `deleted` | `bool` | `None` |
| `rights` | `'BusinessBotRights'` | `None` |

```python
from sent.tl.functions.account import AccountUpdateConnectedBot
```

```python
result = await client(AccountUpdateConnectedBot(...))
```

## AccountGetConnectedBots

**Constructor ID:** `1319421967`

```python
AccountGetConnectedBots.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountGetConnectedBots
```

```python
result = await client(AccountGetConnectedBots(...))
```

## AccountGetBotBusinessConnection

**Constructor ID:** `1990746736`

```python
AccountGetBotBusinessConnection.__init__(connection_id: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `connection_id` | `str` | `—` |

```python
from sent.tl.functions.account import AccountGetBotBusinessConnection
```

```python
result = await client(AccountGetBotBusinessConnection(...))
```

## AccountUpdateBusinessIntro

**Constructor ID:** `2786381876`

```python
AccountUpdateBusinessIntro.__init__(intro: 'InputBusinessIntro' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `intro` | `'InputBusinessIntro'` | `None` |

```python
from sent.tl.functions.account import AccountUpdateBusinessIntro
```

```python
result = await client(AccountUpdateBusinessIntro(...))
```

## AccountToggleConnectedBotPaused

**Constructor ID:** `1684934807`

**Fields:** ``peer`, `paused``

```python
AccountToggleConnectedBotPaused.__init__(peer: 'InputPeer', paused: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `paused` | `bool` | `—` |

```python
from sent.tl.functions.account import AccountToggleConnectedBotPaused
```

```python
result = await client(AccountToggleConnectedBotPaused(...))
```

## AccountDisablePeerConnectedBot

**Constructor ID:** `1581481689`

```python
AccountDisablePeerConnectedBot.__init__(peer: 'InputPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |

```python
from sent.tl.functions.account import AccountDisablePeerConnectedBot
```

```python
result = await client(AccountDisablePeerConnectedBot(...))
```

## AccountUpdateBirthday

**Constructor ID:** `3429764113`

```python
AccountUpdateBirthday.__init__(birthday: 'Birthday' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `birthday` | `'Birthday'` | `None` |

```python
from sent.tl.functions.account import AccountUpdateBirthday
```

```python
result = await client(AccountUpdateBirthday(...))
```

## AccountCreateBusinessChatLink

**Constructor ID:** `2287068814`

```python
AccountCreateBusinessChatLink.__init__(link: 'InputBusinessChatLink')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `link` | `'InputBusinessChatLink'` | `—` |

```python
from sent.tl.functions.account import AccountCreateBusinessChatLink
```

```python
result = await client(AccountCreateBusinessChatLink(...))
```

## AccountEditBusinessChatLink

**Constructor ID:** `2352222383`

**Fields:** ``slug`, `link``

```python
AccountEditBusinessChatLink.__init__(slug: str, link: 'InputBusinessChatLink')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `slug` | `str` | `—` |
| `link` | `'InputBusinessChatLink'` | `—` |

```python
from sent.tl.functions.account import AccountEditBusinessChatLink
```

```python
result = await client(AccountEditBusinessChatLink(...))
```

## AccountDeleteBusinessChatLink

**Constructor ID:** `1611085428`

```python
AccountDeleteBusinessChatLink.__init__(slug: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `slug` | `str` | `—` |

```python
from sent.tl.functions.account import AccountDeleteBusinessChatLink
```

```python
result = await client(AccountDeleteBusinessChatLink(...))
```

## AccountGetBusinessChatLinks

**Constructor ID:** `1869667809`

```python
AccountGetBusinessChatLinks.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountGetBusinessChatLinks
```

```python
result = await client(AccountGetBusinessChatLinks(...))
```

## AccountResolveBusinessChatLink

**Constructor ID:** `1418913262`

```python
AccountResolveBusinessChatLink.__init__(slug: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `slug` | `str` | `—` |

```python
from sent.tl.functions.account import AccountResolveBusinessChatLink
```

```python
result = await client(AccountResolveBusinessChatLink(...))
```

## AccountUpdatePersonalChannel

**Constructor ID:** `3645048288`

```python
AccountUpdatePersonalChannel.__init__(channel: 'InputChannel')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |

```python
from sent.tl.functions.account import AccountUpdatePersonalChannel
```

```python
result = await client(AccountUpdatePersonalChannel(...))
```

## AccountToggleSponsoredMessages

**Constructor ID:** `3118048141`

```python
AccountToggleSponsoredMessages.__init__(enabled: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `enabled` | `bool` | `—` |

```python
from sent.tl.functions.account import AccountToggleSponsoredMessages
```

```python
result = await client(AccountToggleSponsoredMessages(...))
```

## AccountGetReactionsNotifySettings

**Constructor ID:** `115172684`

```python
AccountGetReactionsNotifySettings.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountGetReactionsNotifySettings
```

```python
result = await client(AccountGetReactionsNotifySettings(...))
```

## AccountSetReactionsNotifySettings

**Constructor ID:** `829220168`

```python
AccountSetReactionsNotifySettings.__init__(settings: 'ReactionsNotifySettings')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `settings` | `'ReactionsNotifySettings'` | `—` |

```python
from sent.tl.functions.account import AccountSetReactionsNotifySettings
```

```python
result = await client(AccountSetReactionsNotifySettings(...))
```

## AccountGetCollectibleEmojiStatuses

**Constructor ID:** `779830595`

```python
AccountGetCollectibleEmojiStatuses.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.account import AccountGetCollectibleEmojiStatuses
```

```python
result = await client(AccountGetCollectibleEmojiStatuses(...))
```

## AccountGetPaidMessagesRevenue

**Constructor ID:** `431639143`

**Fields:** ``parent_peer`, `user_id``

```python
AccountGetPaidMessagesRevenue.__init__(user_id: 'InputUser', parent_peer: 'InputPeer' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `user_id` | `'InputUser'` | `—` |
| `parent_peer` | `'InputPeer'` | `None` |

```python
from sent.tl.functions.account import AccountGetPaidMessagesRevenue
```

```python
result = await client(AccountGetPaidMessagesRevenue(...))
```

## AccountToggleNoPaidMessagesException

**Constructor ID:** `4264483446`

**Fields:** ``refund_charged`, `require_payment`, `parent_peer`, `user_id``

```python
AccountToggleNoPaidMessagesException.__init__(user_id: 'InputUser', refund_charged: bool = None, require_payment: bool = None, parent_peer: 'InputPeer' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `user_id` | `'InputUser'` | `—` |
| `refund_charged` | `bool` | `None` |
| `require_payment` | `bool` | `None` |
| `parent_peer` | `'InputPeer'` | `None` |

```python
from sent.tl.functions.account import AccountToggleNoPaidMessagesException
```

```python
result = await client(AccountToggleNoPaidMessagesException(...))
```

## AccountSetMainProfileTab

**Constructor ID:** `1575909552`

```python
AccountSetMainProfileTab.__init__(tab: 'ProfileTab')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `tab` | `'ProfileTab'` | `—` |

```python
from sent.tl.functions.account import AccountSetMainProfileTab
```

```python
result = await client(AccountSetMainProfileTab(...))
```

## AccountSaveMusic

**Constructor ID:** `2993107625`

**Fields:** ``unsave`, `id`, `after_id``

```python
AccountSaveMusic.__init__(id: 'InputDocument', unsave: bool = None, after_id: 'InputDocument' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'InputDocument'` | `—` |
| `unsave` | `bool` | `None` |
| `after_id` | `'InputDocument'` | `None` |

```python
from sent.tl.functions.account import AccountSaveMusic
```

```python
result = await client(AccountSaveMusic(...))
```

## AccountGetSavedMusicIds

**Constructor ID:** `3768410031`

```python
AccountGetSavedMusicIds.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.account import AccountGetSavedMusicIds
```

```python
result = await client(AccountGetSavedMusicIds(...))
```

## AccountGetUniqueGiftChatThemes

**Constructor ID:** `3828148681`

**Fields:** ``offset`, `limit`, `hash``

```python
AccountGetUniqueGiftChatThemes.__init__(offset: str, limit: int, hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `offset` | `str` | `—` |
| `limit` | `int` | `—` |
| `hash` | `int` | `—` |

```python
from sent.tl.functions.account import AccountGetUniqueGiftChatThemes
```

```python
result = await client(AccountGetUniqueGiftChatThemes(...))
```

## AccountInitPasskeyRegistration

**Constructor ID:** `1117079528`

```python
AccountInitPasskeyRegistration.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountInitPasskeyRegistration
```

```python
result = await client(AccountInitPasskeyRegistration(...))
```

## AccountRegisterPasskey

**Constructor ID:** `1437867990`

```python
AccountRegisterPasskey.__init__(credential: 'InputPasskeyCredential')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `credential` | `'InputPasskeyCredential'` | `—` |

```python
from sent.tl.functions.account import AccountRegisterPasskey
```

```python
result = await client(AccountRegisterPasskey(...))
```

## AccountGetPasskeys

**Constructor ID:** `3927903314`

```python
AccountGetPasskeys.__init__()
```

_No parameters._

```python
from sent.tl.functions.account import AccountGetPasskeys
```

```python
result = await client(AccountGetPasskeys(...))
```

## AccountDeletePasskey

**Constructor ID:** `4122302015`

```python
AccountDeletePasskey.__init__(id: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `str` | `—` |

```python
from sent.tl.functions.account import AccountDeletePasskey
```

```python
result = await client(AccountDeletePasskey(...))
```
