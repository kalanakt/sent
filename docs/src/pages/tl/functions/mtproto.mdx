{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Functions: `all`

Import from `sent.tl.functions.all`.

## Contents

- [ReqPq](#reqpq)
- [ReqPqMulti](#reqpqmulti)
- [ReqDHParams](#reqdhparams)
- [SetClientDHParams](#setclientdhparams)
- [DestroyAuthKey](#destroyauthkey)
- [RpcDropAnswer](#rpcdropanswer)
- [GetFutureSalts](#getfuturesalts)
- [Ping](#ping)
- [PingDelayDisconnect](#pingdelaydisconnect)
- [DestroySession](#destroysession)
- [InvokeAfterMsg](#invokeaftermsg)
- [InvokeAfterMsgs](#invokeaftermsgs)
- [InitConnection](#initconnection)
- [InvokeWithLayer](#invokewithlayer)
- [InvokeWithoutUpdates](#invokewithoutupdates)
- [InvokeWithMessagesRange](#invokewithmessagesrange)
- [InvokeWithTakeout](#invokewithtakeout)
- [InvokeWithBusinessConnection](#invokewithbusinessconnection)
- [InvokeWithGooglePlayIntegrity](#invokewithgoogleplayintegrity)
- [InvokeWithApnsSecret](#invokewithapnssecret)
- [InvokeWithReCaptcha](#invokewithrecaptcha)

## ReqPq

**Constructor ID:** `1615239032`

```python
ReqPq.__init__(nonce: bytes)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `nonce` | `bytes` | `—` |

```python
from sent.tl.functions.all import ReqPq
```

```python
result = await client(ReqPq(...))
```

## ReqPqMulti

**Constructor ID:** `3195965169`

```python
ReqPqMulti.__init__(nonce: bytes)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `nonce` | `bytes` | `—` |

```python
from sent.tl.functions.all import ReqPqMulti
```

```python
result = await client(ReqPqMulti(...))
```

## ReqDHParams

**Constructor ID:** `3608339646`

**Fields:** ``nonce`, `server_nonce`, `p`, `q`, `public_key_fingerprint`, `encrypted_data``

```python
ReqDHParams.__init__(nonce: bytes, server_nonce: bytes, p: str, q: str, public_key_fingerprint: int, encrypted_data: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `nonce` | `bytes` | `—` |
| `server_nonce` | `bytes` | `—` |
| `p` | `str` | `—` |
| `q` | `str` | `—` |
| `public_key_fingerprint` | `int` | `—` |
| `encrypted_data` | `str` | `—` |

```python
from sent.tl.functions.all import ReqDHParams
```

```python
result = await client(ReqDHParams(...))
```

## SetClientDHParams

**Constructor ID:** `4110704415`

**Fields:** ``nonce`, `server_nonce`, `encrypted_data``

```python
SetClientDHParams.__init__(nonce: bytes, server_nonce: bytes, encrypted_data: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `nonce` | `bytes` | `—` |
| `server_nonce` | `bytes` | `—` |
| `encrypted_data` | `str` | `—` |

```python
from sent.tl.functions.all import SetClientDHParams
```

```python
result = await client(SetClientDHParams(...))
```

## DestroyAuthKey

**Constructor ID:** `3510849888`

```python
DestroyAuthKey.__init__()
```

_No parameters._

```python
from sent.tl.functions.all import DestroyAuthKey
```

```python
result = await client(DestroyAuthKey(...))
```

## RpcDropAnswer

**Constructor ID:** `1491380032`

```python
RpcDropAnswer.__init__(req_msg_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `req_msg_id` | `int` | `—` |

```python
from sent.tl.functions.all import RpcDropAnswer
```

```python
result = await client(RpcDropAnswer(...))
```

## GetFutureSalts

**Constructor ID:** `3105996036`

```python
GetFutureSalts.__init__(num: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `num` | `int` | `—` |

```python
from sent.tl.functions.all import GetFutureSalts
```

```python
result = await client(GetFutureSalts(...))
```

## Ping

**Constructor ID:** `2059302892`

```python
Ping.__init__(ping_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `ping_id` | `int` | `—` |

```python
from sent.tl.functions.all import Ping
```

```python
result = await client(Ping(...))
```

## PingDelayDisconnect

**Constructor ID:** `4081220492`

**Fields:** ``ping_id`, `disconnect_delay``

```python
PingDelayDisconnect.__init__(ping_id: int, disconnect_delay: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `ping_id` | `int` | `—` |
| `disconnect_delay` | `int` | `—` |

```python
from sent.tl.functions.all import PingDelayDisconnect
```

```python
result = await client(PingDelayDisconnect(...))
```

## DestroySession

**Constructor ID:** `3880853798`

```python
DestroySession.__init__(session_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `session_id` | `int` | `—` |

```python
from sent.tl.functions.all import DestroySession
```

```python
result = await client(DestroySession(...))
```

## InvokeAfterMsg

**Constructor ID:** `3416209197`

**Fields:** ``msg_id`, `query``

```python
InvokeAfterMsg.__init__(msg_id: int, query: TLObject)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `msg_id` | `int` | `—` |
| `query` | `TLObject` | `—` |

```python
from sent.tl.functions.all import InvokeAfterMsg
```

```python
result = await client(InvokeAfterMsg(...))
```

## InvokeAfterMsgs

**Constructor ID:** `1036301552`

**Fields:** ``msg_ids`, `query``

```python
InvokeAfterMsgs.__init__(msg_ids: 'Vector', query: TLObject)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `msg_ids` | `'Vector'` | `—` |
| `query` | `TLObject` | `—` |

```python
from sent.tl.functions.all import InvokeAfterMsgs
```

```python
result = await client(InvokeAfterMsgs(...))
```

## InitConnection

**Constructor ID:** `3251461801`

**Fields:** ``api_id`, `device_model`, `system_version`, `app_version`, `system_lang_code`, `lang_pack`, `lang_code`, `proxy`, `params`, `query``

```python
InitConnection.__init__(api_id: int, device_model: str, system_version: str, app_version: str, system_lang_code: str, lang_pack: str, lang_code: str, query: TLObject, proxy: 'InputClientProxy' = None, params: 'JSONValue' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `api_id` | `int` | `—` |
| `device_model` | `str` | `—` |
| `system_version` | `str` | `—` |
| `app_version` | `str` | `—` |
| `system_lang_code` | `str` | `—` |
| `lang_pack` | `str` | `—` |
| `lang_code` | `str` | `—` |
| `query` | `TLObject` | `—` |
| `proxy` | `'InputClientProxy'` | `None` |
| `params` | `'JSONValue'` | `None` |

```python
from sent.tl.functions.all import InitConnection
```

```python
result = await client(InitConnection(...))
```

## InvokeWithLayer

**Constructor ID:** `3667594509`

**Fields:** ``layer`, `query``

```python
InvokeWithLayer.__init__(layer: int, query: TLObject)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `layer` | `int` | `—` |
| `query` | `TLObject` | `—` |

```python
from sent.tl.functions.all import InvokeWithLayer
```

```python
result = await client(InvokeWithLayer(...))
```

## InvokeWithoutUpdates

**Constructor ID:** `3214170551`

```python
InvokeWithoutUpdates.__init__(query: TLObject)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `query` | `TLObject` | `—` |

```python
from sent.tl.functions.all import InvokeWithoutUpdates
```

```python
result = await client(InvokeWithoutUpdates(...))
```

## InvokeWithMessagesRange

**Constructor ID:** `911373810`

**Fields:** ``range`, `query``

```python
InvokeWithMessagesRange.__init__(range: 'MessageRange', query: TLObject)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `range` | `'MessageRange'` | `—` |
| `query` | `TLObject` | `—` |

```python
from sent.tl.functions.all import InvokeWithMessagesRange
```

```python
result = await client(InvokeWithMessagesRange(...))
```

## InvokeWithTakeout

**Constructor ID:** `2896821550`

**Fields:** ``takeout_id`, `query``

```python
InvokeWithTakeout.__init__(takeout_id: int, query: TLObject)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `takeout_id` | `int` | `—` |
| `query` | `TLObject` | `—` |

```python
from sent.tl.functions.all import InvokeWithTakeout
```

```python
result = await client(InvokeWithTakeout(...))
```

## InvokeWithBusinessConnection

**Constructor ID:** `3710427022`

**Fields:** ``connection_id`, `query``

```python
InvokeWithBusinessConnection.__init__(connection_id: str, query: TLObject)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `connection_id` | `str` | `—` |
| `query` | `TLObject` | `—` |

```python
from sent.tl.functions.all import InvokeWithBusinessConnection
```

```python
result = await client(InvokeWithBusinessConnection(...))
```

## InvokeWithGooglePlayIntegrity

**Constructor ID:** `502868356`

**Fields:** ``nonce`, `token`, `query``

```python
InvokeWithGooglePlayIntegrity.__init__(nonce: str, token: str, query: TLObject)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `nonce` | `str` | `—` |
| `token` | `str` | `—` |
| `query` | `TLObject` | `—` |

```python
from sent.tl.functions.all import InvokeWithGooglePlayIntegrity
```

```python
result = await client(InvokeWithGooglePlayIntegrity(...))
```

## InvokeWithApnsSecret

**Constructor ID:** `229528824`

**Fields:** ``nonce`, `secret`, `query``

```python
InvokeWithApnsSecret.__init__(nonce: str, secret: str, query: TLObject)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `nonce` | `str` | `—` |
| `secret` | `str` | `—` |
| `query` | `TLObject` | `—` |

```python
from sent.tl.functions.all import InvokeWithApnsSecret
```

```python
result = await client(InvokeWithApnsSecret(...))
```

## InvokeWithReCaptcha

**Constructor ID:** `2914717588`

**Fields:** ``token`, `query``

```python
InvokeWithReCaptcha.__init__(token: str, query: TLObject)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `token` | `str` | `—` |
| `query` | `TLObject` | `—` |

```python
from sent.tl.functions.all import InvokeWithReCaptcha
```

```python
result = await client(InvokeWithReCaptcha(...))
```
