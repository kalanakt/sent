{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Functions: `chatlists`

Import from `sent.tl.functions.chatlists`.

## Contents

- [ChatlistsExportChatlistInvite](#chatlistsexportchatlistinvite)
- [ChatlistsDeleteExportedInvite](#chatlistsdeleteexportedinvite)
- [ChatlistsEditExportedInvite](#chatlistseditexportedinvite)
- [ChatlistsGetExportedInvites](#chatlistsgetexportedinvites)
- [ChatlistsCheckChatlistInvite](#chatlistscheckchatlistinvite)
- [ChatlistsJoinChatlistInvite](#chatlistsjoinchatlistinvite)
- [ChatlistsGetChatlistUpdates](#chatlistsgetchatlistupdates)
- [ChatlistsJoinChatlistUpdates](#chatlistsjoinchatlistupdates)
- [ChatlistsHideChatlistUpdates](#chatlistshidechatlistupdates)
- [ChatlistsGetLeaveChatlistSuggestions](#chatlistsgetleavechatlistsuggestions)
- [ChatlistsLeaveChatlist](#chatlistsleavechatlist)

## ChatlistsExportChatlistInvite

**Constructor ID:** `2222081934`

**Fields:** ``chatlist`, `title`, `peers``

```python
ChatlistsExportChatlistInvite.__init__(chatlist: 'InputChatlist', title: str, peers: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `chatlist` | `'InputChatlist'` | `—` |
| `title` | `str` | `—` |
| `peers` | `'Vector'` | `—` |

```python
from sent.tl.functions.chatlists import ChatlistsExportChatlistInvite
```

```python
result = await client(ChatlistsExportChatlistInvite(...))
```

## ChatlistsDeleteExportedInvite

**Constructor ID:** `1906072670`

**Fields:** ``chatlist`, `slug``

```python
ChatlistsDeleteExportedInvite.__init__(chatlist: 'InputChatlist', slug: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `chatlist` | `'InputChatlist'` | `—` |
| `slug` | `str` | `—` |

```python
from sent.tl.functions.chatlists import ChatlistsDeleteExportedInvite
```

```python
result = await client(ChatlistsDeleteExportedInvite(...))
```

## ChatlistsEditExportedInvite

**Constructor ID:** `1698543165`

**Fields:** ``chatlist`, `slug`, `title`, `peers``

```python
ChatlistsEditExportedInvite.__init__(chatlist: 'InputChatlist', slug: str, title: str = None, peers: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `chatlist` | `'InputChatlist'` | `—` |
| `slug` | `str` | `—` |
| `title` | `str` | `None` |
| `peers` | `'Vector'` | `None` |

```python
from sent.tl.functions.chatlists import ChatlistsEditExportedInvite
```

```python
result = await client(ChatlistsEditExportedInvite(...))
```

## ChatlistsGetExportedInvites

**Constructor ID:** `3456359043`

```python
ChatlistsGetExportedInvites.__init__(chatlist: 'InputChatlist')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `chatlist` | `'InputChatlist'` | `—` |

```python
from sent.tl.functions.chatlists import ChatlistsGetExportedInvites
```

```python
result = await client(ChatlistsGetExportedInvites(...))
```

## ChatlistsCheckChatlistInvite

**Constructor ID:** `1103171583`

```python
ChatlistsCheckChatlistInvite.__init__(slug: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `slug` | `str` | `—` |

```python
from sent.tl.functions.chatlists import ChatlistsCheckChatlistInvite
```

```python
result = await client(ChatlistsCheckChatlistInvite(...))
```

## ChatlistsJoinChatlistInvite

**Constructor ID:** `2796675994`

**Fields:** ``slug`, `peers``

```python
ChatlistsJoinChatlistInvite.__init__(slug: str, peers: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `slug` | `str` | `—` |
| `peers` | `'Vector'` | `—` |

```python
from sent.tl.functions.chatlists import ChatlistsJoinChatlistInvite
```

```python
result = await client(ChatlistsJoinChatlistInvite(...))
```

## ChatlistsGetChatlistUpdates

**Constructor ID:** `2302776609`

```python
ChatlistsGetChatlistUpdates.__init__(chatlist: 'InputChatlist')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `chatlist` | `'InputChatlist'` | `—` |

```python
from sent.tl.functions.chatlists import ChatlistsGetChatlistUpdates
```

```python
result = await client(ChatlistsGetChatlistUpdates(...))
```

## ChatlistsJoinChatlistUpdates

**Constructor ID:** `3767138549`

**Fields:** ``chatlist`, `peers``

```python
ChatlistsJoinChatlistUpdates.__init__(chatlist: 'InputChatlist', peers: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `chatlist` | `'InputChatlist'` | `—` |
| `peers` | `'Vector'` | `—` |

```python
from sent.tl.functions.chatlists import ChatlistsJoinChatlistUpdates
```

```python
result = await client(ChatlistsJoinChatlistUpdates(...))
```

## ChatlistsHideChatlistUpdates

**Constructor ID:** `1726252795`

```python
ChatlistsHideChatlistUpdates.__init__(chatlist: 'InputChatlist')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `chatlist` | `'InputChatlist'` | `—` |

```python
from sent.tl.functions.chatlists import ChatlistsHideChatlistUpdates
```

```python
result = await client(ChatlistsHideChatlistUpdates(...))
```

## ChatlistsGetLeaveChatlistSuggestions

**Constructor ID:** `4257011476`

```python
ChatlistsGetLeaveChatlistSuggestions.__init__(chatlist: 'InputChatlist')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `chatlist` | `'InputChatlist'` | `—` |

```python
from sent.tl.functions.chatlists import ChatlistsGetLeaveChatlistSuggestions
```

```python
result = await client(ChatlistsGetLeaveChatlistSuggestions(...))
```

## ChatlistsLeaveChatlist

**Constructor ID:** `1962598714`

**Fields:** ``chatlist`, `peers``

```python
ChatlistsLeaveChatlist.__init__(chatlist: 'InputChatlist', peers: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `chatlist` | `'InputChatlist'` | `—` |
| `peers` | `'Vector'` | `—` |

```python
from sent.tl.functions.chatlists import ChatlistsLeaveChatlist
```

```python
result = await client(ChatlistsLeaveChatlist(...))
```
