{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Functions: `messages`

Import from `sent.tl.functions.messages`.

## Contents

- [MessagesGetMessages](#messagesgetmessages)
- [MessagesGetDialogs](#messagesgetdialogs)
- [MessagesGetHistory](#messagesgethistory)
- [MessagesSearch](#messagessearch)
- [MessagesReadHistory](#messagesreadhistory)
- [MessagesDeleteHistory](#messagesdeletehistory)
- [MessagesDeleteMessages](#messagesdeletemessages)
- [MessagesReceivedMessages](#messagesreceivedmessages)
- [MessagesSetTyping](#messagessettyping)
- [MessagesSendMessage](#messagessendmessage)
- [MessagesSendMedia](#messagessendmedia)
- [MessagesForwardMessages](#messagesforwardmessages)
- [MessagesReportSpam](#messagesreportspam)
- [MessagesGetPeerSettings](#messagesgetpeersettings)
- [MessagesReport](#messagesreport)
- [MessagesGetChats](#messagesgetchats)
- [MessagesGetFullChat](#messagesgetfullchat)
- [MessagesEditChatTitle](#messageseditchattitle)
- [MessagesEditChatPhoto](#messageseditchatphoto)
- [MessagesAddChatUser](#messagesaddchatuser)
- [MessagesDeleteChatUser](#messagesdeletechatuser)
- [MessagesCreateChat](#messagescreatechat)
- [MessagesGetDhConfig](#messagesgetdhconfig)
- [MessagesRequestEncryption](#messagesrequestencryption)
- [MessagesAcceptEncryption](#messagesacceptencryption)
- [MessagesDiscardEncryption](#messagesdiscardencryption)
- [MessagesSetEncryptedTyping](#messagessetencryptedtyping)
- [MessagesReadEncryptedHistory](#messagesreadencryptedhistory)
- [MessagesSendEncrypted](#messagessendencrypted)
- [MessagesSendEncryptedFile](#messagessendencryptedfile)
- [MessagesSendEncryptedService](#messagessendencryptedservice)
- [MessagesReceivedQueue](#messagesreceivedqueue)
- [MessagesReportEncryptedSpam](#messagesreportencryptedspam)
- [MessagesReadMessageContents](#messagesreadmessagecontents)
- [MessagesGetStickers](#messagesgetstickers)
- [MessagesGetAllStickers](#messagesgetallstickers)
- [MessagesGetWebPagePreview](#messagesgetwebpagepreview)
- [MessagesExportChatInvite](#messagesexportchatinvite)
- [MessagesCheckChatInvite](#messagescheckchatinvite)
- [MessagesImportChatInvite](#messagesimportchatinvite)
- [MessagesGetStickerSet](#messagesgetstickerset)
- [MessagesInstallStickerSet](#messagesinstallstickerset)
- [MessagesUninstallStickerSet](#messagesuninstallstickerset)
- [MessagesStartBot](#messagesstartbot)
- [MessagesGetMessagesViews](#messagesgetmessagesviews)
- [MessagesEditChatAdmin](#messageseditchatadmin)
- [MessagesMigrateChat](#messagesmigratechat)
- [MessagesSearchGlobal](#messagessearchglobal)
- [MessagesReorderStickerSets](#messagesreorderstickersets)
- [MessagesGetDocumentByHash](#messagesgetdocumentbyhash)
- [MessagesGetSavedGifs](#messagesgetsavedgifs)
- [MessagesSaveGif](#messagessavegif)
- [MessagesGetInlineBotResults](#messagesgetinlinebotresults)
- [MessagesSetInlineBotResults](#messagessetinlinebotresults)
- [MessagesSendInlineBotResult](#messagessendinlinebotresult)
- [MessagesGetMessageEditData](#messagesgetmessageeditdata)
- [MessagesEditMessage](#messageseditmessage)
- [MessagesEditInlineBotMessage](#messageseditinlinebotmessage)
- [MessagesGetBotCallbackAnswer](#messagesgetbotcallbackanswer)
- [MessagesSetBotCallbackAnswer](#messagessetbotcallbackanswer)
- [MessagesGetPeerDialogs](#messagesgetpeerdialogs)
- [MessagesSaveDraft](#messagessavedraft)
- [MessagesGetAllDrafts](#messagesgetalldrafts)
- [MessagesGetFeaturedStickers](#messagesgetfeaturedstickers)
- [MessagesReadFeaturedStickers](#messagesreadfeaturedstickers)
- [MessagesGetRecentStickers](#messagesgetrecentstickers)
- [MessagesSaveRecentSticker](#messagessaverecentsticker)
- [MessagesClearRecentStickers](#messagesclearrecentstickers)
- [MessagesGetArchivedStickers](#messagesgetarchivedstickers)
- [MessagesGetMaskStickers](#messagesgetmaskstickers)
- [MessagesGetAttachedStickers](#messagesgetattachedstickers)
- [MessagesSetGameScore](#messagessetgamescore)
- [MessagesSetInlineGameScore](#messagessetinlinegamescore)
- [MessagesGetGameHighScores](#messagesgetgamehighscores)
- [MessagesGetInlineGameHighScores](#messagesgetinlinegamehighscores)
- [MessagesGetCommonChats](#messagesgetcommonchats)
- [MessagesGetWebPage](#messagesgetwebpage)
- [MessagesToggleDialogPin](#messagestoggledialogpin)
- [MessagesReorderPinnedDialogs](#messagesreorderpinneddialogs)
- [MessagesGetPinnedDialogs](#messagesgetpinneddialogs)
- [MessagesSetBotShippingResults](#messagessetbotshippingresults)
- [MessagesSetBotPrecheckoutResults](#messagessetbotprecheckoutresults)
- [MessagesUploadMedia](#messagesuploadmedia)
- [MessagesSendScreenshotNotification](#messagessendscreenshotnotification)
- [MessagesGetFavedStickers](#messagesgetfavedstickers)
- [MessagesFaveSticker](#messagesfavesticker)
- [MessagesGetUnreadMentions](#messagesgetunreadmentions)
- [MessagesReadMentions](#messagesreadmentions)
- [MessagesGetRecentLocations](#messagesgetrecentlocations)
- [MessagesSendMultiMedia](#messagessendmultimedia)
- [MessagesUploadEncryptedFile](#messagesuploadencryptedfile)
- [MessagesSearchStickerSets](#messagessearchstickersets)
- [MessagesGetSplitRanges](#messagesgetsplitranges)
- [MessagesMarkDialogUnread](#messagesmarkdialogunread)
- [MessagesGetDialogUnreadMarks](#messagesgetdialogunreadmarks)
- [MessagesClearAllDrafts](#messagesclearalldrafts)
- [MessagesUpdatePinnedMessage](#messagesupdatepinnedmessage)
- [MessagesSendVote](#messagessendvote)
- [MessagesGetPollResults](#messagesgetpollresults)
- [MessagesGetOnlines](#messagesgetonlines)
- [MessagesEditChatAbout](#messageseditchatabout)
- [MessagesEditChatDefaultBannedRights](#messageseditchatdefaultbannedrights)
- [MessagesGetEmojiKeywords](#messagesgetemojikeywords)
- [MessagesGetEmojiKeywordsDifference](#messagesgetemojikeywordsdifference)
- [MessagesGetEmojiKeywordsLanguages](#messagesgetemojikeywordslanguages)
- [MessagesGetEmojiURL](#messagesgetemojiurl)
- [MessagesGetSearchCounters](#messagesgetsearchcounters)
- [MessagesRequestUrlAuth](#messagesrequesturlauth)
- [MessagesAcceptUrlAuth](#messagesaccepturlauth)
- [MessagesHidePeerSettingsBar](#messageshidepeersettingsbar)
- [MessagesGetScheduledHistory](#messagesgetscheduledhistory)
- [MessagesGetScheduledMessages](#messagesgetscheduledmessages)
- [MessagesSendScheduledMessages](#messagessendscheduledmessages)
- [MessagesDeleteScheduledMessages](#messagesdeletescheduledmessages)
- [MessagesGetPollVotes](#messagesgetpollvotes)
- [MessagesToggleStickerSets](#messagestogglestickersets)
- [MessagesGetDialogFilters](#messagesgetdialogfilters)
- [MessagesGetSuggestedDialogFilters](#messagesgetsuggesteddialogfilters)
- [MessagesUpdateDialogFilter](#messagesupdatedialogfilter)
- [MessagesUpdateDialogFiltersOrder](#messagesupdatedialogfiltersorder)
- [MessagesGetOldFeaturedStickers](#messagesgetoldfeaturedstickers)
- [MessagesGetReplies](#messagesgetreplies)
- [MessagesGetDiscussionMessage](#messagesgetdiscussionmessage)
- [MessagesReadDiscussion](#messagesreaddiscussion)
- [MessagesUnpinAllMessages](#messagesunpinallmessages)
- [MessagesDeleteChat](#messagesdeletechat)
- [MessagesDeletePhoneCallHistory](#messagesdeletephonecallhistory)
- [MessagesCheckHistoryImport](#messagescheckhistoryimport)
- [MessagesInitHistoryImport](#messagesinithistoryimport)
- [MessagesUploadImportedMedia](#messagesuploadimportedmedia)
- [MessagesStartHistoryImport](#messagesstarthistoryimport)
- [MessagesGetExportedChatInvites](#messagesgetexportedchatinvites)
- [MessagesGetExportedChatInvite](#messagesgetexportedchatinvite)
- [MessagesEditExportedChatInvite](#messageseditexportedchatinvite)
- [MessagesDeleteRevokedExportedChatInvites](#messagesdeleterevokedexportedchatinvites)
- [MessagesDeleteExportedChatInvite](#messagesdeleteexportedchatinvite)
- [MessagesGetAdminsWithInvites](#messagesgetadminswithinvites)
- [MessagesGetChatInviteImporters](#messagesgetchatinviteimporters)
- [MessagesSetHistoryTTL](#messagessethistoryttl)
- [MessagesCheckHistoryImportPeer](#messagescheckhistoryimportpeer)
- [MessagesSetChatTheme](#messagessetchattheme)
- [MessagesGetMessageReadParticipants](#messagesgetmessagereadparticipants)
- [MessagesGetSearchResultsCalendar](#messagesgetsearchresultscalendar)
- [MessagesGetSearchResultsPositions](#messagesgetsearchresultspositions)
- [MessagesHideChatJoinRequest](#messageshidechatjoinrequest)
- [MessagesHideAllChatJoinRequests](#messageshideallchatjoinrequests)
- [MessagesToggleNoForwards](#messagestogglenoforwards)
- [MessagesSaveDefaultSendAs](#messagessavedefaultsendas)
- [MessagesSendReaction](#messagessendreaction)
- [MessagesGetMessagesReactions](#messagesgetmessagesreactions)
- [MessagesGetMessageReactionsList](#messagesgetmessagereactionslist)
- [MessagesSetChatAvailableReactions](#messagessetchatavailablereactions)
- [MessagesGetAvailableReactions](#messagesgetavailablereactions)
- [MessagesSetDefaultReaction](#messagessetdefaultreaction)
- [MessagesTranslateText](#messagestranslatetext)
- [MessagesGetUnreadReactions](#messagesgetunreadreactions)
- [MessagesReadReactions](#messagesreadreactions)
- [MessagesSearchSentMedia](#messagessearchsentmedia)
- [MessagesGetAttachMenuBots](#messagesgetattachmenubots)
- [MessagesGetAttachMenuBot](#messagesgetattachmenubot)
- [MessagesToggleBotInAttachMenu](#messagestogglebotinattachmenu)
- [MessagesRequestWebView](#messagesrequestwebview)
- [MessagesProlongWebView](#messagesprolongwebview)
- [MessagesRequestSimpleWebView](#messagesrequestsimplewebview)
- [MessagesSendWebViewResultMessage](#messagessendwebviewresultmessage)
- [MessagesSendWebViewData](#messagessendwebviewdata)
- [MessagesTranscribeAudio](#messagestranscribeaudio)
- [MessagesRateTranscribedAudio](#messagesratetranscribedaudio)
- [MessagesGetCustomEmojiDocuments](#messagesgetcustomemojidocuments)
- [MessagesGetEmojiStickers](#messagesgetemojistickers)
- [MessagesGetFeaturedEmojiStickers](#messagesgetfeaturedemojistickers)
- [MessagesReportReaction](#messagesreportreaction)
- [MessagesGetTopReactions](#messagesgettopreactions)
- [MessagesGetRecentReactions](#messagesgetrecentreactions)
- [MessagesClearRecentReactions](#messagesclearrecentreactions)
- [MessagesGetExtendedMedia](#messagesgetextendedmedia)
- [MessagesSetDefaultHistoryTTL](#messagessetdefaulthistoryttl)
- [MessagesGetDefaultHistoryTTL](#messagesgetdefaulthistoryttl)
- [MessagesSendBotRequestedPeer](#messagessendbotrequestedpeer)
- [MessagesGetEmojiGroups](#messagesgetemojigroups)
- [MessagesGetEmojiStatusGroups](#messagesgetemojistatusgroups)
- [MessagesGetEmojiProfilePhotoGroups](#messagesgetemojiprofilephotogroups)
- [MessagesSearchCustomEmoji](#messagessearchcustomemoji)
- [MessagesTogglePeerTranslations](#messagestogglepeertranslations)
- [MessagesGetBotApp](#messagesgetbotapp)
- [MessagesRequestAppWebView](#messagesrequestappwebview)
- [MessagesSetChatWallPaper](#messagessetchatwallpaper)
- [MessagesSearchEmojiStickerSets](#messagessearchemojistickersets)
- [MessagesGetSavedDialogs](#messagesgetsaveddialogs)
- [MessagesGetSavedHistory](#messagesgetsavedhistory)
- [MessagesDeleteSavedHistory](#messagesdeletesavedhistory)
- [MessagesGetPinnedSavedDialogs](#messagesgetpinnedsaveddialogs)
- [MessagesToggleSavedDialogPin](#messagestogglesaveddialogpin)
- [MessagesReorderPinnedSavedDialogs](#messagesreorderpinnedsaveddialogs)
- [MessagesGetSavedReactionTags](#messagesgetsavedreactiontags)
- [MessagesUpdateSavedReactionTag](#messagesupdatesavedreactiontag)
- [MessagesGetDefaultTagReactions](#messagesgetdefaulttagreactions)
- [MessagesGetOutboxReadDate](#messagesgetoutboxreaddate)
- [MessagesGetQuickReplies](#messagesgetquickreplies)
- [MessagesReorderQuickReplies](#messagesreorderquickreplies)
- [MessagesCheckQuickReplyShortcut](#messagescheckquickreplyshortcut)
- [MessagesEditQuickReplyShortcut](#messageseditquickreplyshortcut)
- [MessagesDeleteQuickReplyShortcut](#messagesdeletequickreplyshortcut)
- [MessagesGetQuickReplyMessages](#messagesgetquickreplymessages)
- [MessagesSendQuickReplyMessages](#messagessendquickreplymessages)
- [MessagesDeleteQuickReplyMessages](#messagesdeletequickreplymessages)
- [MessagesToggleDialogFilterTags](#messagestoggledialogfiltertags)
- [MessagesGetMyStickers](#messagesgetmystickers)
- [MessagesGetEmojiStickerGroups](#messagesgetemojistickergroups)
- [MessagesGetAvailableEffects](#messagesgetavailableeffects)
- [MessagesEditFactCheck](#messageseditfactcheck)
- [MessagesDeleteFactCheck](#messagesdeletefactcheck)
- [MessagesGetFactCheck](#messagesgetfactcheck)
- [MessagesRequestMainWebView](#messagesrequestmainwebview)
- [MessagesSendPaidReaction](#messagessendpaidreaction)
- [MessagesTogglePaidReactionPrivacy](#messagestogglepaidreactionprivacy)
- [MessagesGetPaidReactionPrivacy](#messagesgetpaidreactionprivacy)
- [MessagesViewSponsoredMessage](#messagesviewsponsoredmessage)
- [MessagesClickSponsoredMessage](#messagesclicksponsoredmessage)
- [MessagesReportSponsoredMessage](#messagesreportsponsoredmessage)
- [MessagesGetSponsoredMessages](#messagesgetsponsoredmessages)
- [MessagesSavePreparedInlineMessage](#messagessavepreparedinlinemessage)
- [MessagesGetPreparedInlineMessage](#messagesgetpreparedinlinemessage)
- [MessagesSearchStickers](#messagessearchstickers)
- [MessagesReportMessagesDelivery](#messagesreportmessagesdelivery)
- [MessagesGetSavedDialogsByID](#messagesgetsaveddialogsbyid)
- [MessagesReadSavedHistory](#messagesreadsavedhistory)
- [MessagesToggleTodoCompleted](#messagestoggletodocompleted)
- [MessagesAppendTodoList](#messagesappendtodolist)
- [MessagesToggleSuggestedPostApproval](#messagestogglesuggestedpostapproval)
- [MessagesGetForumTopics](#messagesgetforumtopics)
- [MessagesGetForumTopicsByID](#messagesgetforumtopicsbyid)
- [MessagesEditForumTopic](#messageseditforumtopic)
- [MessagesUpdatePinnedForumTopic](#messagesupdatepinnedforumtopic)
- [MessagesReorderPinnedForumTopics](#messagesreorderpinnedforumtopics)
- [MessagesCreateForumTopic](#messagescreateforumtopic)
- [MessagesDeleteTopicHistory](#messagesdeletetopichistory)
- [MessagesGetEmojiGameInfo](#messagesgetemojigameinfo)
- [MessagesSummarizeText](#messagessummarizetext)
- [MessagesEditChatCreator](#messageseditchatcreator)
- [MessagesGetFutureChatCreatorAfterLeave](#messagesgetfuturechatcreatorafterleave)
- [MessagesEditChatParticipantRank](#messageseditchatparticipantrank)
- [MessagesDeclineUrlAuth](#messagesdeclineurlauth)
- [MessagesCheckUrlAuthMatchCode](#messagescheckurlauthmatchcode)
- [MessagesComposeMessageWithAI](#messagescomposemessagewithai)
- [MessagesReportReadMetrics](#messagesreportreadmetrics)
- [MessagesReportMusicListen](#messagesreportmusiclisten)
- [MessagesAddPollAnswer](#messagesaddpollanswer)
- [MessagesDeletePollAnswer](#messagesdeletepollanswer)
- [MessagesGetUnreadPollVotes](#messagesgetunreadpollvotes)
- [MessagesReadPollVotes](#messagesreadpollvotes)
- [MessagesSetBotGuestChatResult](#messagessetbotguestchatresult)
- [MessagesDeleteParticipantReactions](#messagesdeleteparticipantreactions)
- [MessagesDeleteParticipantReaction](#messagesdeleteparticipantreaction)
- [MessagesGetPersonalChannelHistory](#messagesgetpersonalchannelhistory)

## MessagesGetMessages

**Constructor ID:** `1673946374`

```python
MessagesGetMessages.__init__(id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.messages import MessagesGetMessages
```

```python
result = await client(MessagesGetMessages(...))
```

## MessagesGetDialogs

**Constructor ID:** `2700397391`

**Fields:** ``exclude_pinned`, `folder_id`, `offset_date`, `offset_id`, `offset_peer`, `limit`, `hash``

```python
MessagesGetDialogs.__init__(offset_date: int, offset_id: int, offset_peer: 'InputPeer', limit: int, hash: int, exclude_pinned: bool = None, folder_id: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `offset_date` | `int` | `—` |
| `offset_id` | `int` | `—` |
| `offset_peer` | `'InputPeer'` | `—` |
| `limit` | `int` | `—` |
| `hash` | `int` | `—` |
| `exclude_pinned` | `bool` | `None` |
| `folder_id` | `int` | `None` |

```python
from sent.tl.functions.messages import MessagesGetDialogs
```

```python
result = await client(MessagesGetDialogs(...))
```

## MessagesGetHistory

**Constructor ID:** `1143203525`

**Fields:** ``peer`, `offset_id`, `offset_date`, `add_offset`, `limit`, `max_id`, `min_id`, `hash``

```python
MessagesGetHistory.__init__(peer: 'InputPeer', offset_id: int, offset_date: int, add_offset: int, limit: int, max_id: int, min_id: int, hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `offset_id` | `int` | `—` |
| `offset_date` | `int` | `—` |
| `add_offset` | `int` | `—` |
| `limit` | `int` | `—` |
| `max_id` | `int` | `—` |
| `min_id` | `int` | `—` |
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetHistory
```

```python
result = await client(MessagesGetHistory(...))
```

## MessagesSearch

**Constructor ID:** `703497338`

**Fields:** ``peer`, `q`, `from_id`, `saved_peer_id`, `saved_reaction`, `top_msg_id`, `filter`, `min_date`, `max_date`, `offset_id`, `add_offset`, `limit`, `max_id`, `min_id`, `hash``

```python
MessagesSearch.__init__(peer: 'InputPeer', q: str, filter: 'MessagesFilter', min_date: int, max_date: int, offset_id: int, add_offset: int, limit: int, max_id: int, min_id: int, hash: int, from_id: 'InputPeer' = None, saved_peer_id: 'InputPeer' = None, saved_reaction: 'Vector' = None, top_msg_id: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `q` | `str` | `—` |
| `filter` | `'MessagesFilter'` | `—` |
| `min_date` | `int` | `—` |
| `max_date` | `int` | `—` |
| `offset_id` | `int` | `—` |
| `add_offset` | `int` | `—` |
| `limit` | `int` | `—` |
| `max_id` | `int` | `—` |
| `min_id` | `int` | `—` |
| `hash` | `int` | `—` |
| `from_id` | `'InputPeer'` | `None` |
| `saved_peer_id` | `'InputPeer'` | `None` |
| `saved_reaction` | `'Vector'` | `None` |
| `top_msg_id` | `int` | `None` |

```python
from sent.tl.functions.messages import MessagesSearch
```

```python
result = await client(MessagesSearch(...))
```

## MessagesReadHistory

**Constructor ID:** `238054714`

**Fields:** ``peer`, `max_id``

```python
MessagesReadHistory.__init__(peer: 'InputPeer', max_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `max_id` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesReadHistory
```

```python
result = await client(MessagesReadHistory(...))
```

## MessagesDeleteHistory

**Constructor ID:** `2962199082`

**Fields:** ``just_clear`, `revoke`, `peer`, `max_id`, `min_date`, `max_date``

```python
MessagesDeleteHistory.__init__(peer: 'InputPeer', max_id: int, just_clear: bool = None, revoke: bool = None, min_date: int = None, max_date: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `max_id` | `int` | `—` |
| `just_clear` | `bool` | `None` |
| `revoke` | `bool` | `None` |
| `min_date` | `int` | `None` |
| `max_date` | `int` | `None` |

```python
from sent.tl.functions.messages import MessagesDeleteHistory
```

```python
result = await client(MessagesDeleteHistory(...))
```

## MessagesDeleteMessages

**Constructor ID:** `3851326930`

**Fields:** ``revoke`, `id``

```python
MessagesDeleteMessages.__init__(id: 'Vector', revoke: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'Vector'` | `—` |
| `revoke` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesDeleteMessages
```

```python
result = await client(MessagesDeleteMessages(...))
```

## MessagesReceivedMessages

**Constructor ID:** `94983360`

```python
MessagesReceivedMessages.__init__(max_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `max_id` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesReceivedMessages
```

```python
result = await client(MessagesReceivedMessages(...))
```

## MessagesSetTyping

**Constructor ID:** `1486110434`

**Fields:** ``peer`, `top_msg_id`, `action``

```python
MessagesSetTyping.__init__(peer: 'InputPeer', action: 'SendMessageAction', top_msg_id: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `action` | `'SendMessageAction'` | `—` |
| `top_msg_id` | `int` | `None` |

```python
from sent.tl.functions.messages import MessagesSetTyping
```

```python
result = await client(MessagesSetTyping(...))
```

## MessagesSendMessage

**Constructor ID:** `1415369050`

**Fields:** ``no_webpage`, `silent`, `background`, `clear_draft`, `noforwards`, `update_stickersets_order`, `invert_media`, `allow_paid_floodskip`, `peer`, `reply_to`, `message`, `random_id`, `reply_markup`, `entities`, `schedule_date`, `schedule_repeat_period`, `send_as`, `quick_reply_shortcut`, `effect`, `allow_paid_stars`, `suggested_post``

```python
MessagesSendMessage.__init__(peer: 'InputPeer', message: str, random_id: int, no_webpage: bool = None, silent: bool = None, background: bool = None, clear_draft: bool = None, noforwards: bool = None, update_stickersets_order: bool = None, invert_media: bool = None, allow_paid_floodskip: bool = None, reply_to: 'InputReplyTo' = None, reply_markup: 'ReplyMarkup' = None, entities: 'Vector' = None, schedule_date: int = None, schedule_repeat_period: int = None, send_as: 'InputPeer' = None, quick_reply_shortcut: 'InputQuickReplyShortcut' = None, effect: int = None, allow_paid_stars: int = None, suggested_post: 'SuggestedPost' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `message` | `str` | `—` |
| `random_id` | `int` | `—` |
| `no_webpage` | `bool` | `None` |
| `silent` | `bool` | `None` |
| `background` | `bool` | `None` |
| `clear_draft` | `bool` | `None` |
| `noforwards` | `bool` | `None` |
| `update_stickersets_order` | `bool` | `None` |
| `invert_media` | `bool` | `None` |
| `allow_paid_floodskip` | `bool` | `None` |
| `reply_to` | `'InputReplyTo'` | `None` |
| `reply_markup` | `'ReplyMarkup'` | `None` |
| `entities` | `'Vector'` | `None` |
| `schedule_date` | `int` | `None` |
| `schedule_repeat_period` | `int` | `None` |
| `send_as` | `'InputPeer'` | `None` |
| `quick_reply_shortcut` | `'InputQuickReplyShortcut'` | `None` |
| `effect` | `int` | `None` |
| `allow_paid_stars` | `int` | `None` |
| `suggested_post` | `'SuggestedPost'` | `None` |

```python
from sent.tl.functions.messages import MessagesSendMessage
```

```python
result = await client(MessagesSendMessage(...))
```

## MessagesSendMedia

**Constructor ID:** `53536639`

**Fields:** ``silent`, `background`, `clear_draft`, `noforwards`, `update_stickersets_order`, `invert_media`, `allow_paid_floodskip`, `peer`, `reply_to`, `media`, `message`, `random_id`, `reply_markup`, `entities`, `schedule_date`, `schedule_repeat_period`, `send_as`, `quick_reply_shortcut`, `effect`, `allow_paid_stars`, `suggested_post``

```python
MessagesSendMedia.__init__(peer: 'InputPeer', media: 'InputMedia', message: str, random_id: int, silent: bool = None, background: bool = None, clear_draft: bool = None, noforwards: bool = None, update_stickersets_order: bool = None, invert_media: bool = None, allow_paid_floodskip: bool = None, reply_to: 'InputReplyTo' = None, reply_markup: 'ReplyMarkup' = None, entities: 'Vector' = None, schedule_date: int = None, schedule_repeat_period: int = None, send_as: 'InputPeer' = None, quick_reply_shortcut: 'InputQuickReplyShortcut' = None, effect: int = None, allow_paid_stars: int = None, suggested_post: 'SuggestedPost' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `media` | `'InputMedia'` | `—` |
| `message` | `str` | `—` |
| `random_id` | `int` | `—` |
| `silent` | `bool` | `None` |
| `background` | `bool` | `None` |
| `clear_draft` | `bool` | `None` |
| `noforwards` | `bool` | `None` |
| `update_stickersets_order` | `bool` | `None` |
| `invert_media` | `bool` | `None` |
| `allow_paid_floodskip` | `bool` | `None` |
| `reply_to` | `'InputReplyTo'` | `None` |
| `reply_markup` | `'ReplyMarkup'` | `None` |
| `entities` | `'Vector'` | `None` |
| `schedule_date` | `int` | `None` |
| `schedule_repeat_period` | `int` | `None` |
| `send_as` | `'InputPeer'` | `None` |
| `quick_reply_shortcut` | `'InputQuickReplyShortcut'` | `None` |
| `effect` | `int` | `None` |
| `allow_paid_stars` | `int` | `None` |
| `suggested_post` | `'SuggestedPost'` | `None` |

```python
from sent.tl.functions.messages import MessagesSendMedia
```

```python
result = await client(MessagesSendMedia(...))
```

## MessagesForwardMessages

**Constructor ID:** `326126204`

**Fields:** ``silent`, `background`, `with_my_score`, `drop_author`, `drop_media_captions`, `noforwards`, `allow_paid_floodskip`, `from_peer`, `id`, `random_id`, `to_peer`, `top_msg_id`, `reply_to`, `schedule_date`, `schedule_repeat_period`, `send_as`, `quick_reply_shortcut`, `effect`, `video_timestamp`, `allow_paid_stars`, `suggested_post``

```python
MessagesForwardMessages.__init__(from_peer: 'InputPeer', id: 'Vector', random_id: 'Vector', to_peer: 'InputPeer', silent: bool = None, background: bool = None, with_my_score: bool = None, drop_author: bool = None, drop_media_captions: bool = None, noforwards: bool = None, allow_paid_floodskip: bool = None, top_msg_id: int = None, reply_to: 'InputReplyTo' = None, schedule_date: int = None, schedule_repeat_period: int = None, send_as: 'InputPeer' = None, quick_reply_shortcut: 'InputQuickReplyShortcut' = None, effect: int = None, video_timestamp: int = None, allow_paid_stars: int = None, suggested_post: 'SuggestedPost' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `from_peer` | `'InputPeer'` | `—` |
| `id` | `'Vector'` | `—` |
| `random_id` | `'Vector'` | `—` |
| `to_peer` | `'InputPeer'` | `—` |
| `silent` | `bool` | `None` |
| `background` | `bool` | `None` |
| `with_my_score` | `bool` | `None` |
| `drop_author` | `bool` | `None` |
| `drop_media_captions` | `bool` | `None` |
| `noforwards` | `bool` | `None` |
| `allow_paid_floodskip` | `bool` | `None` |
| `top_msg_id` | `int` | `None` |
| `reply_to` | `'InputReplyTo'` | `None` |
| `schedule_date` | `int` | `None` |
| `schedule_repeat_period` | `int` | `None` |
| `send_as` | `'InputPeer'` | `None` |
| `quick_reply_shortcut` | `'InputQuickReplyShortcut'` | `None` |
| `effect` | `int` | `None` |
| `video_timestamp` | `int` | `None` |
| `allow_paid_stars` | `int` | `None` |
| `suggested_post` | `'SuggestedPost'` | `None` |

```python
from sent.tl.functions.messages import MessagesForwardMessages
```

```python
result = await client(MessagesForwardMessages(...))
```

## MessagesReportSpam

**Constructor ID:** `3474297563`

```python
MessagesReportSpam.__init__(peer: 'InputPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |

```python
from sent.tl.functions.messages import MessagesReportSpam
```

```python
result = await client(MessagesReportSpam(...))
```

## MessagesGetPeerSettings

**Constructor ID:** `4024018594`

```python
MessagesGetPeerSettings.__init__(peer: 'InputPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |

```python
from sent.tl.functions.messages import MessagesGetPeerSettings
```

```python
result = await client(MessagesGetPeerSettings(...))
```

## MessagesReport

**Constructor ID:** `4235767707`

**Fields:** ``peer`, `id`, `option`, `message``

```python
MessagesReport.__init__(peer: 'InputPeer', id: 'Vector', option: bytes, message: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `'Vector'` | `—` |
| `option` | `bytes` | `—` |
| `message` | `str` | `—` |

```python
from sent.tl.functions.messages import MessagesReport
```

```python
result = await client(MessagesReport(...))
```

## MessagesGetChats

**Constructor ID:** `1240027791`

```python
MessagesGetChats.__init__(id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.messages import MessagesGetChats
```

```python
result = await client(MessagesGetChats(...))
```

## MessagesGetFullChat

**Constructor ID:** `2930772788`

```python
MessagesGetFullChat.__init__(chat_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `chat_id` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetFullChat
```

```python
result = await client(MessagesGetFullChat(...))
```

## MessagesEditChatTitle

**Constructor ID:** `1937260541`

**Fields:** ``chat_id`, `title``

```python
MessagesEditChatTitle.__init__(chat_id: int, title: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `chat_id` | `int` | `—` |
| `title` | `str` | `—` |

```python
from sent.tl.functions.messages import MessagesEditChatTitle
```

```python
result = await client(MessagesEditChatTitle(...))
```

## MessagesEditChatPhoto

**Constructor ID:** `903730804`

**Fields:** ``chat_id`, `photo``

```python
MessagesEditChatPhoto.__init__(chat_id: int, photo: 'InputChatPhoto')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `chat_id` | `int` | `—` |
| `photo` | `'InputChatPhoto'` | `—` |

```python
from sent.tl.functions.messages import MessagesEditChatPhoto
```

```python
result = await client(MessagesEditChatPhoto(...))
```

## MessagesAddChatUser

**Constructor ID:** `3418804487`

**Fields:** ``chat_id`, `user_id`, `fwd_limit``

```python
MessagesAddChatUser.__init__(chat_id: int, user_id: 'InputUser', fwd_limit: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `chat_id` | `int` | `—` |
| `user_id` | `'InputUser'` | `—` |
| `fwd_limit` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesAddChatUser
```

```python
result = await client(MessagesAddChatUser(...))
```

## MessagesDeleteChatUser

**Constructor ID:** `2719505579`

**Fields:** ``revoke_history`, `chat_id`, `user_id``

```python
MessagesDeleteChatUser.__init__(chat_id: int, user_id: 'InputUser', revoke_history: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `chat_id` | `int` | `—` |
| `user_id` | `'InputUser'` | `—` |
| `revoke_history` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesDeleteChatUser
```

```python
result = await client(MessagesDeleteChatUser(...))
```

## MessagesCreateChat

**Constructor ID:** `2463030740`

**Fields:** ``users`, `title`, `ttl_period``

```python
MessagesCreateChat.__init__(users: 'Vector', title: str, ttl_period: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `users` | `'Vector'` | `—` |
| `title` | `str` | `—` |
| `ttl_period` | `int` | `None` |

```python
from sent.tl.functions.messages import MessagesCreateChat
```

```python
result = await client(MessagesCreateChat(...))
```

## MessagesGetDhConfig

**Constructor ID:** `651135312`

**Fields:** ``version`, `random_length``

```python
MessagesGetDhConfig.__init__(version: int, random_length: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `version` | `int` | `—` |
| `random_length` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetDhConfig
```

```python
result = await client(MessagesGetDhConfig(...))
```

## MessagesRequestEncryption

**Constructor ID:** `4132286275`

**Fields:** ``user_id`, `random_id`, `g_a``

```python
MessagesRequestEncryption.__init__(user_id: 'InputUser', random_id: int, g_a: bytes)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `user_id` | `'InputUser'` | `—` |
| `random_id` | `int` | `—` |
| `g_a` | `bytes` | `—` |

```python
from sent.tl.functions.messages import MessagesRequestEncryption
```

```python
result = await client(MessagesRequestEncryption(...))
```

## MessagesAcceptEncryption

**Constructor ID:** `1035731989`

**Fields:** ``peer`, `g_b`, `key_fingerprint``

```python
MessagesAcceptEncryption.__init__(peer: 'InputEncryptedChat', g_b: bytes, key_fingerprint: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputEncryptedChat'` | `—` |
| `g_b` | `bytes` | `—` |
| `key_fingerprint` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesAcceptEncryption
```

```python
result = await client(MessagesAcceptEncryption(...))
```

## MessagesDiscardEncryption

**Constructor ID:** `4086541984`

**Fields:** ``delete_history`, `chat_id``

```python
MessagesDiscardEncryption.__init__(chat_id: int, delete_history: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `chat_id` | `int` | `—` |
| `delete_history` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesDiscardEncryption
```

```python
result = await client(MessagesDiscardEncryption(...))
```

## MessagesSetEncryptedTyping

**Constructor ID:** `2031374829`

**Fields:** ``peer`, `typing``

```python
MessagesSetEncryptedTyping.__init__(peer: 'InputEncryptedChat', typing: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputEncryptedChat'` | `—` |
| `typing` | `bool` | `—` |

```python
from sent.tl.functions.messages import MessagesSetEncryptedTyping
```

```python
result = await client(MessagesSetEncryptedTyping(...))
```

## MessagesReadEncryptedHistory

**Constructor ID:** `2135648522`

**Fields:** ``peer`, `max_date``

```python
MessagesReadEncryptedHistory.__init__(peer: 'InputEncryptedChat', max_date: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputEncryptedChat'` | `—` |
| `max_date` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesReadEncryptedHistory
```

```python
result = await client(MessagesReadEncryptedHistory(...))
```

## MessagesSendEncrypted

**Constructor ID:** `1157265941`

**Fields:** ``silent`, `peer`, `random_id`, `data``

```python
MessagesSendEncrypted.__init__(peer: 'InputEncryptedChat', random_id: int, data: bytes, silent: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputEncryptedChat'` | `—` |
| `random_id` | `int` | `—` |
| `data` | `bytes` | `—` |
| `silent` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesSendEncrypted
```

```python
result = await client(MessagesSendEncrypted(...))
```

## MessagesSendEncryptedFile

**Constructor ID:** `1431914525`

**Fields:** ``silent`, `peer`, `random_id`, `data`, `file``

```python
MessagesSendEncryptedFile.__init__(peer: 'InputEncryptedChat', random_id: int, data: bytes, file: 'InputEncryptedFile', silent: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputEncryptedChat'` | `—` |
| `random_id` | `int` | `—` |
| `data` | `bytes` | `—` |
| `file` | `'InputEncryptedFile'` | `—` |
| `silent` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesSendEncryptedFile
```

```python
result = await client(MessagesSendEncryptedFile(...))
```

## MessagesSendEncryptedService

**Constructor ID:** `852769188`

**Fields:** ``peer`, `random_id`, `data``

```python
MessagesSendEncryptedService.__init__(peer: 'InputEncryptedChat', random_id: int, data: bytes)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputEncryptedChat'` | `—` |
| `random_id` | `int` | `—` |
| `data` | `bytes` | `—` |

```python
from sent.tl.functions.messages import MessagesSendEncryptedService
```

```python
result = await client(MessagesSendEncryptedService(...))
```

## MessagesReceivedQueue

**Constructor ID:** `1436924774`

```python
MessagesReceivedQueue.__init__(max_qts: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `max_qts` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesReceivedQueue
```

```python
result = await client(MessagesReceivedQueue(...))
```

## MessagesReportEncryptedSpam

**Constructor ID:** `1259113487`

```python
MessagesReportEncryptedSpam.__init__(peer: 'InputEncryptedChat')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputEncryptedChat'` | `—` |

```python
from sent.tl.functions.messages import MessagesReportEncryptedSpam
```

```python
result = await client(MessagesReportEncryptedSpam(...))
```

## MessagesReadMessageContents

**Constructor ID:** `916930423`

```python
MessagesReadMessageContents.__init__(id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.messages import MessagesReadMessageContents
```

```python
result = await client(MessagesReadMessageContents(...))
```

## MessagesGetStickers

**Constructor ID:** `3584414625`

**Fields:** ``emoticon`, `hash``

```python
MessagesGetStickers.__init__(emoticon: str, hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `emoticon` | `str` | `—` |
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetStickers
```

```python
result = await client(MessagesGetStickers(...))
```

## MessagesGetAllStickers

**Constructor ID:** `3097534888`

```python
MessagesGetAllStickers.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetAllStickers
```

```python
result = await client(MessagesGetAllStickers(...))
```

## MessagesGetWebPagePreview

**Constructor ID:** `1460498287`

**Fields:** ``message`, `entities``

```python
MessagesGetWebPagePreview.__init__(message: str, entities: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `message` | `str` | `—` |
| `entities` | `'Vector'` | `None` |

```python
from sent.tl.functions.messages import MessagesGetWebPagePreview
```

```python
result = await client(MessagesGetWebPagePreview(...))
```

## MessagesExportChatInvite

**Constructor ID:** `2757090960`

**Fields:** ``legacy_revoke_permanent`, `request_needed`, `peer`, `expire_date`, `usage_limit`, `title`, `subscription_pricing``

```python
MessagesExportChatInvite.__init__(peer: 'InputPeer', legacy_revoke_permanent: bool = None, request_needed: bool = None, expire_date: int = None, usage_limit: int = None, title: str = None, subscription_pricing: 'StarsSubscriptionPricing' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `legacy_revoke_permanent` | `bool` | `None` |
| `request_needed` | `bool` | `None` |
| `expire_date` | `int` | `None` |
| `usage_limit` | `int` | `None` |
| `title` | `str` | `None` |
| `subscription_pricing` | `'StarsSubscriptionPricing'` | `None` |

```python
from sent.tl.functions.messages import MessagesExportChatInvite
```

```python
result = await client(MessagesExportChatInvite(...))
```

## MessagesCheckChatInvite

**Constructor ID:** `1051570619`

```python
MessagesCheckChatInvite.__init__(hash: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `str` | `—` |

```python
from sent.tl.functions.messages import MessagesCheckChatInvite
```

```python
result = await client(MessagesCheckChatInvite(...))
```

## MessagesImportChatInvite

**Constructor ID:** `1817183516`

```python
MessagesImportChatInvite.__init__(hash: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `str` | `—` |

```python
from sent.tl.functions.messages import MessagesImportChatInvite
```

```python
result = await client(MessagesImportChatInvite(...))
```

## MessagesGetStickerSet

**Constructor ID:** `3365989492`

**Fields:** ``stickerset`, `hash``

```python
MessagesGetStickerSet.__init__(stickerset: 'InputStickerSet', hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `stickerset` | `'InputStickerSet'` | `—` |
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetStickerSet
```

```python
result = await client(MessagesGetStickerSet(...))
```

## MessagesInstallStickerSet

**Constructor ID:** `3348096096`

**Fields:** ``stickerset`, `archived``

```python
MessagesInstallStickerSet.__init__(stickerset: 'InputStickerSet', archived: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `stickerset` | `'InputStickerSet'` | `—` |
| `archived` | `bool` | `—` |

```python
from sent.tl.functions.messages import MessagesInstallStickerSet
```

```python
result = await client(MessagesInstallStickerSet(...))
```

## MessagesUninstallStickerSet

**Constructor ID:** `4184757726`

```python
MessagesUninstallStickerSet.__init__(stickerset: 'InputStickerSet')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `stickerset` | `'InputStickerSet'` | `—` |

```python
from sent.tl.functions.messages import MessagesUninstallStickerSet
```

```python
result = await client(MessagesUninstallStickerSet(...))
```

## MessagesStartBot

**Constructor ID:** `3873403768`

**Fields:** ``bot`, `peer`, `random_id`, `start_param``

```python
MessagesStartBot.__init__(bot: 'InputUser', peer: 'InputPeer', random_id: int, start_param: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |
| `peer` | `'InputPeer'` | `—` |
| `random_id` | `int` | `—` |
| `start_param` | `str` | `—` |

```python
from sent.tl.functions.messages import MessagesStartBot
```

```python
result = await client(MessagesStartBot(...))
```

## MessagesGetMessagesViews

**Constructor ID:** `1468322785`

**Fields:** ``peer`, `id`, `increment``

```python
MessagesGetMessagesViews.__init__(peer: 'InputPeer', id: 'Vector', increment: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `'Vector'` | `—` |
| `increment` | `bool` | `—` |

```python
from sent.tl.functions.messages import MessagesGetMessagesViews
```

```python
result = await client(MessagesGetMessagesViews(...))
```

## MessagesEditChatAdmin

**Constructor ID:** `2824589762`

**Fields:** ``chat_id`, `user_id`, `is_admin``

```python
MessagesEditChatAdmin.__init__(chat_id: int, user_id: 'InputUser', is_admin: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `chat_id` | `int` | `—` |
| `user_id` | `'InputUser'` | `—` |
| `is_admin` | `bool` | `—` |

```python
from sent.tl.functions.messages import MessagesEditChatAdmin
```

```python
result = await client(MessagesEditChatAdmin(...))
```

## MessagesMigrateChat

**Constructor ID:** `2726777625`

```python
MessagesMigrateChat.__init__(chat_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `chat_id` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesMigrateChat
```

```python
result = await client(MessagesMigrateChat(...))
```

## MessagesSearchGlobal

**Constructor ID:** `1271290010`

**Fields:** ``broadcasts_only`, `groups_only`, `users_only`, `folder_id`, `q`, `filter`, `min_date`, `max_date`, `offset_rate`, `offset_peer`, `offset_id`, `limit``

```python
MessagesSearchGlobal.__init__(q: str, filter: 'MessagesFilter', min_date: int, max_date: int, offset_rate: int, offset_peer: 'InputPeer', offset_id: int, limit: int, broadcasts_only: bool = None, groups_only: bool = None, users_only: bool = None, folder_id: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `q` | `str` | `—` |
| `filter` | `'MessagesFilter'` | `—` |
| `min_date` | `int` | `—` |
| `max_date` | `int` | `—` |
| `offset_rate` | `int` | `—` |
| `offset_peer` | `'InputPeer'` | `—` |
| `offset_id` | `int` | `—` |
| `limit` | `int` | `—` |
| `broadcasts_only` | `bool` | `None` |
| `groups_only` | `bool` | `None` |
| `users_only` | `bool` | `None` |
| `folder_id` | `int` | `None` |

```python
from sent.tl.functions.messages import MessagesSearchGlobal
```

```python
result = await client(MessagesSearchGlobal(...))
```

## MessagesReorderStickerSets

**Constructor ID:** `2016638777`

**Fields:** ``masks`, `emojis`, `order``

```python
MessagesReorderStickerSets.__init__(order: 'Vector', masks: bool = None, emojis: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `order` | `'Vector'` | `—` |
| `masks` | `bool` | `None` |
| `emojis` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesReorderStickerSets
```

```python
result = await client(MessagesReorderStickerSets(...))
```

## MessagesGetDocumentByHash

**Constructor ID:** `2985428511`

**Fields:** ``sha256`, `size`, `mime_type``

```python
MessagesGetDocumentByHash.__init__(sha256: bytes, size: int, mime_type: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `sha256` | `bytes` | `—` |
| `size` | `int` | `—` |
| `mime_type` | `str` | `—` |

```python
from sent.tl.functions.messages import MessagesGetDocumentByHash
```

```python
result = await client(MessagesGetDocumentByHash(...))
```

## MessagesGetSavedGifs

**Constructor ID:** `1559270965`

```python
MessagesGetSavedGifs.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetSavedGifs
```

```python
result = await client(MessagesGetSavedGifs(...))
```

## MessagesSaveGif

**Constructor ID:** `846868683`

**Fields:** ``id`, `unsave``

```python
MessagesSaveGif.__init__(id: 'InputDocument', unsave: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'InputDocument'` | `—` |
| `unsave` | `bool` | `—` |

```python
from sent.tl.functions.messages import MessagesSaveGif
```

```python
result = await client(MessagesSaveGif(...))
```

## MessagesGetInlineBotResults

**Constructor ID:** `1364105629`

**Fields:** ``bot`, `peer`, `geo_point`, `query`, `offset``

```python
MessagesGetInlineBotResults.__init__(bot: 'InputUser', peer: 'InputPeer', query: str, offset: str, geo_point: 'InputGeoPoint' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |
| `peer` | `'InputPeer'` | `—` |
| `query` | `str` | `—` |
| `offset` | `str` | `—` |
| `geo_point` | `'InputGeoPoint'` | `None` |

```python
from sent.tl.functions.messages import MessagesGetInlineBotResults
```

```python
result = await client(MessagesGetInlineBotResults(...))
```

## MessagesSetInlineBotResults

**Constructor ID:** `3138561049`

**Fields:** ``gallery`, `private`, `query_id`, `results`, `cache_time`, `next_offset`, `switch_pm`, `switch_webview``

```python
MessagesSetInlineBotResults.__init__(query_id: int, results: 'Vector', cache_time: int, gallery: bool = None, private: bool = None, next_offset: str = None, switch_pm: 'InlineBotSwitchPM' = None, switch_webview: 'InlineBotWebView' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `query_id` | `int` | `—` |
| `results` | `'Vector'` | `—` |
| `cache_time` | `int` | `—` |
| `gallery` | `bool` | `None` |
| `private` | `bool` | `None` |
| `next_offset` | `str` | `None` |
| `switch_pm` | `'InlineBotSwitchPM'` | `None` |
| `switch_webview` | `'InlineBotWebView'` | `None` |

```python
from sent.tl.functions.messages import MessagesSetInlineBotResults
```

```python
result = await client(MessagesSetInlineBotResults(...))
```

## MessagesSendInlineBotResult

**Constructor ID:** `3234821702`

**Fields:** ``silent`, `background`, `clear_draft`, `hide_via`, `peer`, `reply_to`, `random_id`, `query_id`, `id`, `schedule_date`, `send_as`, `quick_reply_shortcut`, `allow_paid_stars``

```python
MessagesSendInlineBotResult.__init__(peer: 'InputPeer', random_id: int, query_id: int, id: str, silent: bool = None, background: bool = None, clear_draft: bool = None, hide_via: bool = None, reply_to: 'InputReplyTo' = None, schedule_date: int = None, send_as: 'InputPeer' = None, quick_reply_shortcut: 'InputQuickReplyShortcut' = None, allow_paid_stars: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `random_id` | `int` | `—` |
| `query_id` | `int` | `—` |
| `id` | `str` | `—` |
| `silent` | `bool` | `None` |
| `background` | `bool` | `None` |
| `clear_draft` | `bool` | `None` |
| `hide_via` | `bool` | `None` |
| `reply_to` | `'InputReplyTo'` | `None` |
| `schedule_date` | `int` | `None` |
| `send_as` | `'InputPeer'` | `None` |
| `quick_reply_shortcut` | `'InputQuickReplyShortcut'` | `None` |
| `allow_paid_stars` | `int` | `None` |

```python
from sent.tl.functions.messages import MessagesSendInlineBotResult
```

```python
result = await client(MessagesSendInlineBotResult(...))
```

## MessagesGetMessageEditData

**Constructor ID:** `4255550774`

**Fields:** ``peer`, `id``

```python
MessagesGetMessageEditData.__init__(peer: 'InputPeer', id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetMessageEditData
```

```python
result = await client(MessagesGetMessageEditData(...))
```

## MessagesEditMessage

**Constructor ID:** `1374175969`

**Fields:** ``no_webpage`, `invert_media`, `peer`, `id`, `message`, `media`, `reply_markup`, `entities`, `schedule_date`, `schedule_repeat_period`, `quick_reply_shortcut_id``

```python
MessagesEditMessage.__init__(peer: 'InputPeer', id: int, no_webpage: bool = None, invert_media: bool = None, message: str = None, media: 'InputMedia' = None, reply_markup: 'ReplyMarkup' = None, entities: 'Vector' = None, schedule_date: int = None, schedule_repeat_period: int = None, quick_reply_shortcut_id: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `int` | `—` |
| `no_webpage` | `bool` | `None` |
| `invert_media` | `bool` | `None` |
| `message` | `str` | `None` |
| `media` | `'InputMedia'` | `None` |
| `reply_markup` | `'ReplyMarkup'` | `None` |
| `entities` | `'Vector'` | `None` |
| `schedule_date` | `int` | `None` |
| `schedule_repeat_period` | `int` | `None` |
| `quick_reply_shortcut_id` | `int` | `None` |

```python
from sent.tl.functions.messages import MessagesEditMessage
```

```python
result = await client(MessagesEditMessage(...))
```

## MessagesEditInlineBotMessage

**Constructor ID:** `2203418042`

**Fields:** ``no_webpage`, `invert_media`, `id`, `message`, `media`, `reply_markup`, `entities``

```python
MessagesEditInlineBotMessage.__init__(id: 'InputBotInlineMessageID', no_webpage: bool = None, invert_media: bool = None, message: str = None, media: 'InputMedia' = None, reply_markup: 'ReplyMarkup' = None, entities: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'InputBotInlineMessageID'` | `—` |
| `no_webpage` | `bool` | `None` |
| `invert_media` | `bool` | `None` |
| `message` | `str` | `None` |
| `media` | `'InputMedia'` | `None` |
| `reply_markup` | `'ReplyMarkup'` | `None` |
| `entities` | `'Vector'` | `None` |

```python
from sent.tl.functions.messages import MessagesEditInlineBotMessage
```

```python
result = await client(MessagesEditInlineBotMessage(...))
```

## MessagesGetBotCallbackAnswer

**Constructor ID:** `2470627847`

**Fields:** ``game`, `peer`, `msg_id`, `data`, `password``

```python
MessagesGetBotCallbackAnswer.__init__(peer: 'InputPeer', msg_id: int, game: bool = None, data: bytes = None, password: 'InputCheckPasswordSRP' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |
| `game` | `bool` | `None` |
| `data` | `bytes` | `None` |
| `password` | `'InputCheckPasswordSRP'` | `None` |

```python
from sent.tl.functions.messages import MessagesGetBotCallbackAnswer
```

```python
result = await client(MessagesGetBotCallbackAnswer(...))
```

## MessagesSetBotCallbackAnswer

**Constructor ID:** `3582923530`

**Fields:** ``alert`, `query_id`, `message`, `url`, `cache_time``

```python
MessagesSetBotCallbackAnswer.__init__(query_id: int, cache_time: int, alert: bool = None, message: str = None, url: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `query_id` | `int` | `—` |
| `cache_time` | `int` | `—` |
| `alert` | `bool` | `None` |
| `message` | `str` | `None` |
| `url` | `str` | `None` |

```python
from sent.tl.functions.messages import MessagesSetBotCallbackAnswer
```

```python
result = await client(MessagesSetBotCallbackAnswer(...))
```

## MessagesGetPeerDialogs

**Constructor ID:** `3832593661`

```python
MessagesGetPeerDialogs.__init__(peers: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peers` | `'Vector'` | `—` |

```python
from sent.tl.functions.messages import MessagesGetPeerDialogs
```

```python
result = await client(MessagesGetPeerDialogs(...))
```

## MessagesSaveDraft

**Constructor ID:** `1420701838`

**Fields:** ``no_webpage`, `invert_media`, `reply_to`, `peer`, `message`, `entities`, `media`, `effect`, `suggested_post``

```python
MessagesSaveDraft.__init__(peer: 'InputPeer', message: str, no_webpage: bool = None, invert_media: bool = None, reply_to: 'InputReplyTo' = None, entities: 'Vector' = None, media: 'InputMedia' = None, effect: int = None, suggested_post: 'SuggestedPost' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `message` | `str` | `—` |
| `no_webpage` | `bool` | `None` |
| `invert_media` | `bool` | `None` |
| `reply_to` | `'InputReplyTo'` | `None` |
| `entities` | `'Vector'` | `None` |
| `media` | `'InputMedia'` | `None` |
| `effect` | `int` | `None` |
| `suggested_post` | `'SuggestedPost'` | `None` |

```python
from sent.tl.functions.messages import MessagesSaveDraft
```

```python
result = await client(MessagesSaveDraft(...))
```

## MessagesGetAllDrafts

**Constructor ID:** `1782549861`

```python
MessagesGetAllDrafts.__init__()
```

_No parameters._

```python
from sent.tl.functions.messages import MessagesGetAllDrafts
```

```python
result = await client(MessagesGetAllDrafts(...))
```

## MessagesGetFeaturedStickers

**Constructor ID:** `1685588756`

```python
MessagesGetFeaturedStickers.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetFeaturedStickers
```

```python
result = await client(MessagesGetFeaturedStickers(...))
```

## MessagesReadFeaturedStickers

**Constructor ID:** `1527873830`

```python
MessagesReadFeaturedStickers.__init__(id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.messages import MessagesReadFeaturedStickers
```

```python
result = await client(MessagesReadFeaturedStickers(...))
```

## MessagesGetRecentStickers

**Constructor ID:** `2645114939`

**Fields:** ``attached`, `hash``

```python
MessagesGetRecentStickers.__init__(hash: int, attached: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `attached` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesGetRecentStickers
```

```python
result = await client(MessagesGetRecentStickers(...))
```

## MessagesSaveRecentSticker

**Constructor ID:** `958863608`

**Fields:** ``attached`, `id`, `unsave``

```python
MessagesSaveRecentSticker.__init__(id: 'InputDocument', unsave: bool, attached: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'InputDocument'` | `—` |
| `unsave` | `bool` | `—` |
| `attached` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesSaveRecentSticker
```

```python
result = await client(MessagesSaveRecentSticker(...))
```

## MessagesClearRecentStickers

**Constructor ID:** `2308530221`

```python
MessagesClearRecentStickers.__init__(attached: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `attached` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesClearRecentStickers
```

```python
result = await client(MessagesClearRecentStickers(...))
```

## MessagesGetArchivedStickers

**Constructor ID:** `1475442322`

**Fields:** ``masks`, `emojis`, `offset_id`, `limit``

```python
MessagesGetArchivedStickers.__init__(offset_id: int, limit: int, masks: bool = None, emojis: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `offset_id` | `int` | `—` |
| `limit` | `int` | `—` |
| `masks` | `bool` | `None` |
| `emojis` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesGetArchivedStickers
```

```python
result = await client(MessagesGetArchivedStickers(...))
```

## MessagesGetMaskStickers

**Constructor ID:** `1678738104`

```python
MessagesGetMaskStickers.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetMaskStickers
```

```python
result = await client(MessagesGetMaskStickers(...))
```

## MessagesGetAttachedStickers

**Constructor ID:** `3428542412`

```python
MessagesGetAttachedStickers.__init__(media: 'InputStickeredMedia')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `media` | `'InputStickeredMedia'` | `—` |

```python
from sent.tl.functions.messages import MessagesGetAttachedStickers
```

```python
result = await client(MessagesGetAttachedStickers(...))
```

## MessagesSetGameScore

**Constructor ID:** `2398678208`

**Fields:** ``edit_message`, `force`, `peer`, `id`, `user_id`, `score``

```python
MessagesSetGameScore.__init__(peer: 'InputPeer', id: int, user_id: 'InputUser', score: int, edit_message: bool = None, force: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `int` | `—` |
| `user_id` | `'InputUser'` | `—` |
| `score` | `int` | `—` |
| `edit_message` | `bool` | `None` |
| `force` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesSetGameScore
```

```python
result = await client(MessagesSetGameScore(...))
```

## MessagesSetInlineGameScore

**Constructor ID:** `363700068`

**Fields:** ``edit_message`, `force`, `id`, `user_id`, `score``

```python
MessagesSetInlineGameScore.__init__(id: 'InputBotInlineMessageID', user_id: 'InputUser', score: int, edit_message: bool = None, force: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'InputBotInlineMessageID'` | `—` |
| `user_id` | `'InputUser'` | `—` |
| `score` | `int` | `—` |
| `edit_message` | `bool` | `None` |
| `force` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesSetInlineGameScore
```

```python
result = await client(MessagesSetInlineGameScore(...))
```

## MessagesGetGameHighScores

**Constructor ID:** `3894568093`

**Fields:** ``peer`, `id`, `user_id``

```python
MessagesGetGameHighScores.__init__(peer: 'InputPeer', id: int, user_id: 'InputUser')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `int` | `—` |
| `user_id` | `'InputUser'` | `—` |

```python
from sent.tl.functions.messages import MessagesGetGameHighScores
```

```python
result = await client(MessagesGetGameHighScores(...))
```

## MessagesGetInlineGameHighScores

**Constructor ID:** `258170395`

**Fields:** ``id`, `user_id``

```python
MessagesGetInlineGameHighScores.__init__(id: 'InputBotInlineMessageID', user_id: 'InputUser')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'InputBotInlineMessageID'` | `—` |
| `user_id` | `'InputUser'` | `—` |

```python
from sent.tl.functions.messages import MessagesGetInlineGameHighScores
```

```python
result = await client(MessagesGetInlineGameHighScores(...))
```

## MessagesGetCommonChats

**Constructor ID:** `3826032900`

**Fields:** ``user_id`, `max_id`, `limit``

```python
MessagesGetCommonChats.__init__(user_id: 'InputUser', max_id: int, limit: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `user_id` | `'InputUser'` | `—` |
| `max_id` | `int` | `—` |
| `limit` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetCommonChats
```

```python
result = await client(MessagesGetCommonChats(...))
```

## MessagesGetWebPage

**Constructor ID:** `2375455395`

**Fields:** ``url`, `hash``

```python
MessagesGetWebPage.__init__(url: str, hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `url` | `str` | `—` |
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetWebPage
```

```python
result = await client(MessagesGetWebPage(...))
```

## MessagesToggleDialogPin

**Constructor ID:** `2805064279`

**Fields:** ``pinned`, `peer``

```python
MessagesToggleDialogPin.__init__(peer: 'InputDialogPeer', pinned: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputDialogPeer'` | `—` |
| `pinned` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesToggleDialogPin
```

```python
result = await client(MessagesToggleDialogPin(...))
```

## MessagesReorderPinnedDialogs

**Constructor ID:** `991616823`

**Fields:** ``force`, `folder_id`, `order``

```python
MessagesReorderPinnedDialogs.__init__(folder_id: int, order: 'Vector', force: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `folder_id` | `int` | `—` |
| `order` | `'Vector'` | `—` |
| `force` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesReorderPinnedDialogs
```

```python
result = await client(MessagesReorderPinnedDialogs(...))
```

## MessagesGetPinnedDialogs

**Constructor ID:** `3602468338`

```python
MessagesGetPinnedDialogs.__init__(folder_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `folder_id` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetPinnedDialogs
```

```python
result = await client(MessagesGetPinnedDialogs(...))
```

## MessagesSetBotShippingResults

**Constructor ID:** `3858133754`

**Fields:** ``query_id`, `error`, `shipping_options``

```python
MessagesSetBotShippingResults.__init__(query_id: int, error: str = None, shipping_options: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `query_id` | `int` | `—` |
| `error` | `str` | `None` |
| `shipping_options` | `'Vector'` | `None` |

```python
from sent.tl.functions.messages import MessagesSetBotShippingResults
```

```python
result = await client(MessagesSetBotShippingResults(...))
```

## MessagesSetBotPrecheckoutResults

**Constructor ID:** `163765653`

**Fields:** ``success`, `query_id`, `error``

```python
MessagesSetBotPrecheckoutResults.__init__(query_id: int, success: bool = None, error: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `query_id` | `int` | `—` |
| `success` | `bool` | `None` |
| `error` | `str` | `None` |

```python
from sent.tl.functions.messages import MessagesSetBotPrecheckoutResults
```

```python
result = await client(MessagesSetBotPrecheckoutResults(...))
```

## MessagesUploadMedia

**Constructor ID:** `345405816`

**Fields:** ``business_connection_id`, `peer`, `media``

```python
MessagesUploadMedia.__init__(peer: 'InputPeer', media: 'InputMedia', business_connection_id: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `media` | `'InputMedia'` | `—` |
| `business_connection_id` | `str` | `None` |

```python
from sent.tl.functions.messages import MessagesUploadMedia
```

```python
result = await client(MessagesUploadMedia(...))
```

## MessagesSendScreenshotNotification

**Constructor ID:** `2705348631`

**Fields:** ``peer`, `reply_to`, `random_id``

```python
MessagesSendScreenshotNotification.__init__(peer: 'InputPeer', reply_to: 'InputReplyTo', random_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `reply_to` | `'InputReplyTo'` | `—` |
| `random_id` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesSendScreenshotNotification
```

```python
result = await client(MessagesSendScreenshotNotification(...))
```

## MessagesGetFavedStickers

**Constructor ID:** `82946729`

```python
MessagesGetFavedStickers.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetFavedStickers
```

```python
result = await client(MessagesGetFavedStickers(...))
```

## MessagesFaveSticker

**Constructor ID:** `3120547163`

**Fields:** ``id`, `unfave``

```python
MessagesFaveSticker.__init__(id: 'InputDocument', unfave: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'InputDocument'` | `—` |
| `unfave` | `bool` | `—` |

```python
from sent.tl.functions.messages import MessagesFaveSticker
```

```python
result = await client(MessagesFaveSticker(...))
```

## MessagesGetUnreadMentions

**Constructor ID:** `4043827088`

**Fields:** ``peer`, `top_msg_id`, `offset_id`, `add_offset`, `limit`, `max_id`, `min_id``

```python
MessagesGetUnreadMentions.__init__(peer: 'InputPeer', offset_id: int, add_offset: int, limit: int, max_id: int, min_id: int, top_msg_id: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `offset_id` | `int` | `—` |
| `add_offset` | `int` | `—` |
| `limit` | `int` | `—` |
| `max_id` | `int` | `—` |
| `min_id` | `int` | `—` |
| `top_msg_id` | `int` | `None` |

```python
from sent.tl.functions.messages import MessagesGetUnreadMentions
```

```python
result = await client(MessagesGetUnreadMentions(...))
```

## MessagesReadMentions

**Constructor ID:** `921026381`

**Fields:** ``peer`, `top_msg_id``

```python
MessagesReadMentions.__init__(peer: 'InputPeer', top_msg_id: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `top_msg_id` | `int` | `None` |

```python
from sent.tl.functions.messages import MessagesReadMentions
```

```python
result = await client(MessagesReadMentions(...))
```

## MessagesGetRecentLocations

**Constructor ID:** `1881817312`

**Fields:** ``peer`, `limit`, `hash``

```python
MessagesGetRecentLocations.__init__(peer: 'InputPeer', limit: int, hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `limit` | `int` | `—` |
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetRecentLocations
```

```python
result = await client(MessagesGetRecentLocations(...))
```

## MessagesSendMultiMedia

**Constructor ID:** `469278068`

**Fields:** ``silent`, `background`, `clear_draft`, `noforwards`, `update_stickersets_order`, `invert_media`, `allow_paid_floodskip`, `peer`, `reply_to`, `multi_media`, `schedule_date`, `send_as`, `quick_reply_shortcut`, `effect`, `allow_paid_stars``

```python
MessagesSendMultiMedia.__init__(peer: 'InputPeer', multi_media: 'Vector', silent: bool = None, background: bool = None, clear_draft: bool = None, noforwards: bool = None, update_stickersets_order: bool = None, invert_media: bool = None, allow_paid_floodskip: bool = None, reply_to: 'InputReplyTo' = None, schedule_date: int = None, send_as: 'InputPeer' = None, quick_reply_shortcut: 'InputQuickReplyShortcut' = None, effect: int = None, allow_paid_stars: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `multi_media` | `'Vector'` | `—` |
| `silent` | `bool` | `None` |
| `background` | `bool` | `None` |
| `clear_draft` | `bool` | `None` |
| `noforwards` | `bool` | `None` |
| `update_stickersets_order` | `bool` | `None` |
| `invert_media` | `bool` | `None` |
| `allow_paid_floodskip` | `bool` | `None` |
| `reply_to` | `'InputReplyTo'` | `None` |
| `schedule_date` | `int` | `None` |
| `send_as` | `'InputPeer'` | `None` |
| `quick_reply_shortcut` | `'InputQuickReplyShortcut'` | `None` |
| `effect` | `int` | `None` |
| `allow_paid_stars` | `int` | `None` |

```python
from sent.tl.functions.messages import MessagesSendMultiMedia
```

```python
result = await client(MessagesSendMultiMedia(...))
```

## MessagesUploadEncryptedFile

**Constructor ID:** `1347929239`

**Fields:** ``peer`, `file``

```python
MessagesUploadEncryptedFile.__init__(peer: 'InputEncryptedChat', file: 'InputEncryptedFile')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputEncryptedChat'` | `—` |
| `file` | `'InputEncryptedFile'` | `—` |

```python
from sent.tl.functions.messages import MessagesUploadEncryptedFile
```

```python
result = await client(MessagesUploadEncryptedFile(...))
```

## MessagesSearchStickerSets

**Constructor ID:** `896555914`

**Fields:** ``exclude_featured`, `q`, `hash``

```python
MessagesSearchStickerSets.__init__(q: str, hash: int, exclude_featured: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `q` | `str` | `—` |
| `hash` | `int` | `—` |
| `exclude_featured` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesSearchStickerSets
```

```python
result = await client(MessagesSearchStickerSets(...))
```

## MessagesGetSplitRanges

**Constructor ID:** `486505992`

```python
MessagesGetSplitRanges.__init__()
```

_No parameters._

```python
from sent.tl.functions.messages import MessagesGetSplitRanges
```

```python
result = await client(MessagesGetSplitRanges(...))
```

## MessagesMarkDialogUnread

**Constructor ID:** `2354054904`

**Fields:** ``unread`, `parent_peer`, `peer``

```python
MessagesMarkDialogUnread.__init__(peer: 'InputDialogPeer', unread: bool = None, parent_peer: 'InputPeer' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputDialogPeer'` | `—` |
| `unread` | `bool` | `None` |
| `parent_peer` | `'InputPeer'` | `None` |

```python
from sent.tl.functions.messages import MessagesMarkDialogUnread
```

```python
result = await client(MessagesMarkDialogUnread(...))
```

## MessagesGetDialogUnreadMarks

**Constructor ID:** `555754018`

```python
MessagesGetDialogUnreadMarks.__init__(parent_peer: 'InputPeer' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `parent_peer` | `'InputPeer'` | `None` |

```python
from sent.tl.functions.messages import MessagesGetDialogUnreadMarks
```

```python
result = await client(MessagesGetDialogUnreadMarks(...))
```

## MessagesClearAllDrafts

**Constructor ID:** `2119757468`

```python
MessagesClearAllDrafts.__init__()
```

_No parameters._

```python
from sent.tl.functions.messages import MessagesClearAllDrafts
```

```python
result = await client(MessagesClearAllDrafts(...))
```

## MessagesUpdatePinnedMessage

**Constructor ID:** `3534419948`

**Fields:** ``silent`, `unpin`, `pm_oneside`, `peer`, `id``

```python
MessagesUpdatePinnedMessage.__init__(peer: 'InputPeer', id: int, silent: bool = None, unpin: bool = None, pm_oneside: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `int` | `—` |
| `silent` | `bool` | `None` |
| `unpin` | `bool` | `None` |
| `pm_oneside` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesUpdatePinnedMessage
```

```python
result = await client(MessagesUpdatePinnedMessage(...))
```

## MessagesSendVote

**Constructor ID:** `283795844`

**Fields:** ``peer`, `msg_id`, `options``

```python
MessagesSendVote.__init__(peer: 'InputPeer', msg_id: int, options: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |
| `options` | `'Vector'` | `—` |

```python
from sent.tl.functions.messages import MessagesSendVote
```

```python
result = await client(MessagesSendVote(...))
```

## MessagesGetPollResults

**Constructor ID:** `3986940731`

**Fields:** ``peer`, `msg_id`, `poll_hash``

```python
MessagesGetPollResults.__init__(peer: 'InputPeer', msg_id: int, poll_hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |
| `poll_hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetPollResults
```

```python
result = await client(MessagesGetPollResults(...))
```

## MessagesGetOnlines

**Constructor ID:** `1848369232`

```python
MessagesGetOnlines.__init__(peer: 'InputPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |

```python
from sent.tl.functions.messages import MessagesGetOnlines
```

```python
result = await client(MessagesGetOnlines(...))
```

## MessagesEditChatAbout

**Constructor ID:** `3740665751`

**Fields:** ``peer`, `about``

```python
MessagesEditChatAbout.__init__(peer: 'InputPeer', about: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `about` | `str` | `—` |

```python
from sent.tl.functions.messages import MessagesEditChatAbout
```

```python
result = await client(MessagesEditChatAbout(...))
```

## MessagesEditChatDefaultBannedRights

**Constructor ID:** `2777049921`

**Fields:** ``peer`, `banned_rights``

```python
MessagesEditChatDefaultBannedRights.__init__(peer: 'InputPeer', banned_rights: 'ChatBannedRights')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `banned_rights` | `'ChatBannedRights'` | `—` |

```python
from sent.tl.functions.messages import MessagesEditChatDefaultBannedRights
```

```python
result = await client(MessagesEditChatDefaultBannedRights(...))
```

## MessagesGetEmojiKeywords

**Constructor ID:** `899735650`

```python
MessagesGetEmojiKeywords.__init__(lang_code: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `lang_code` | `str` | `—` |

```python
from sent.tl.functions.messages import MessagesGetEmojiKeywords
```

```python
result = await client(MessagesGetEmojiKeywords(...))
```

## MessagesGetEmojiKeywordsDifference

**Constructor ID:** `352892591`

**Fields:** ``lang_code`, `from_version``

```python
MessagesGetEmojiKeywordsDifference.__init__(lang_code: str, from_version: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `lang_code` | `str` | `—` |
| `from_version` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetEmojiKeywordsDifference
```

```python
result = await client(MessagesGetEmojiKeywordsDifference(...))
```

## MessagesGetEmojiKeywordsLanguages

**Constructor ID:** `1318675378`

```python
MessagesGetEmojiKeywordsLanguages.__init__(lang_codes: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `lang_codes` | `'Vector'` | `—` |

```python
from sent.tl.functions.messages import MessagesGetEmojiKeywordsLanguages
```

```python
result = await client(MessagesGetEmojiKeywordsLanguages(...))
```

## MessagesGetEmojiURL

**Constructor ID:** `3585149990`

```python
MessagesGetEmojiURL.__init__(lang_code: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `lang_code` | `str` | `—` |

```python
from sent.tl.functions.messages import MessagesGetEmojiURL
```

```python
result = await client(MessagesGetEmojiURL(...))
```

## MessagesGetSearchCounters

**Constructor ID:** `465367808`

**Fields:** ``peer`, `saved_peer_id`, `top_msg_id`, `filters``

```python
MessagesGetSearchCounters.__init__(peer: 'InputPeer', filters: 'Vector', saved_peer_id: 'InputPeer' = None, top_msg_id: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `filters` | `'Vector'` | `—` |
| `saved_peer_id` | `'InputPeer'` | `None` |
| `top_msg_id` | `int` | `None` |

```python
from sent.tl.functions.messages import MessagesGetSearchCounters
```

```python
result = await client(MessagesGetSearchCounters(...))
```

## MessagesRequestUrlAuth

**Constructor ID:** `2303510940`

**Fields:** ``peer`, `msg_id`, `button_id`, `url`, `in_app_origin``

```python
MessagesRequestUrlAuth.__init__(peer: 'InputPeer' = None, msg_id: int = None, button_id: int = None, url: str = None, in_app_origin: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `None` |
| `msg_id` | `int` | `None` |
| `button_id` | `int` | `None` |
| `url` | `str` | `None` |
| `in_app_origin` | `str` | `None` |

```python
from sent.tl.functions.messages import MessagesRequestUrlAuth
```

```python
result = await client(MessagesRequestUrlAuth(...))
```

## MessagesAcceptUrlAuth

**Constructor ID:** `1738797278`

**Fields:** ``write_allowed`, `share_phone_number`, `peer`, `msg_id`, `button_id`, `url`, `match_code``

```python
MessagesAcceptUrlAuth.__init__(write_allowed: bool = None, share_phone_number: bool = None, peer: 'InputPeer' = None, msg_id: int = None, button_id: int = None, url: str = None, match_code: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `write_allowed` | `bool` | `None` |
| `share_phone_number` | `bool` | `None` |
| `peer` | `'InputPeer'` | `None` |
| `msg_id` | `int` | `None` |
| `button_id` | `int` | `None` |
| `url` | `str` | `None` |
| `match_code` | `str` | `None` |

```python
from sent.tl.functions.messages import MessagesAcceptUrlAuth
```

```python
result = await client(MessagesAcceptUrlAuth(...))
```

## MessagesHidePeerSettingsBar

**Constructor ID:** `1336717624`

```python
MessagesHidePeerSettingsBar.__init__(peer: 'InputPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |

```python
from sent.tl.functions.messages import MessagesHidePeerSettingsBar
```

```python
result = await client(MessagesHidePeerSettingsBar(...))
```

## MessagesGetScheduledHistory

**Constructor ID:** `4111889931`

**Fields:** ``peer`, `hash``

```python
MessagesGetScheduledHistory.__init__(peer: 'InputPeer', hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetScheduledHistory
```

```python
result = await client(MessagesGetScheduledHistory(...))
```

## MessagesGetScheduledMessages

**Constructor ID:** `3183150180`

**Fields:** ``peer`, `id``

```python
MessagesGetScheduledMessages.__init__(peer: 'InputPeer', id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.messages import MessagesGetScheduledMessages
```

```python
result = await client(MessagesGetScheduledMessages(...))
```

## MessagesSendScheduledMessages

**Constructor ID:** `3174597898`

**Fields:** ``peer`, `id``

```python
MessagesSendScheduledMessages.__init__(peer: 'InputPeer', id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.messages import MessagesSendScheduledMessages
```

```python
result = await client(MessagesSendScheduledMessages(...))
```

## MessagesDeleteScheduledMessages

**Constructor ID:** `1504586518`

**Fields:** ``peer`, `id``

```python
MessagesDeleteScheduledMessages.__init__(peer: 'InputPeer', id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.messages import MessagesDeleteScheduledMessages
```

```python
result = await client(MessagesDeleteScheduledMessages(...))
```

## MessagesGetPollVotes

**Constructor ID:** `3094231054`

**Fields:** ``peer`, `id`, `option`, `offset`, `limit``

```python
MessagesGetPollVotes.__init__(peer: 'InputPeer', id: int, limit: int, option: bytes = None, offset: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `int` | `—` |
| `limit` | `int` | `—` |
| `option` | `bytes` | `None` |
| `offset` | `str` | `None` |

```python
from sent.tl.functions.messages import MessagesGetPollVotes
```

```python
result = await client(MessagesGetPollVotes(...))
```

## MessagesToggleStickerSets

**Constructor ID:** `3037016042`

**Fields:** ``uninstall`, `archive`, `unarchive`, `stickersets``

```python
MessagesToggleStickerSets.__init__(stickersets: 'Vector', uninstall: bool = None, archive: bool = None, unarchive: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `stickersets` | `'Vector'` | `—` |
| `uninstall` | `bool` | `None` |
| `archive` | `bool` | `None` |
| `unarchive` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesToggleStickerSets
```

```python
result = await client(MessagesToggleStickerSets(...))
```

## MessagesGetDialogFilters

**Constructor ID:** `4023684233`

```python
MessagesGetDialogFilters.__init__()
```

_No parameters._

```python
from sent.tl.functions.messages import MessagesGetDialogFilters
```

```python
result = await client(MessagesGetDialogFilters(...))
```

## MessagesGetSuggestedDialogFilters

**Constructor ID:** `2728186924`

```python
MessagesGetSuggestedDialogFilters.__init__()
```

_No parameters._

```python
from sent.tl.functions.messages import MessagesGetSuggestedDialogFilters
```

```python
result = await client(MessagesGetSuggestedDialogFilters(...))
```

## MessagesUpdateDialogFilter

**Constructor ID:** `450142282`

**Fields:** ``id`, `filter``

```python
MessagesUpdateDialogFilter.__init__(id: int, filter: 'DialogFilter' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `int` | `—` |
| `filter` | `'DialogFilter'` | `None` |

```python
from sent.tl.functions.messages import MessagesUpdateDialogFilter
```

```python
result = await client(MessagesUpdateDialogFilter(...))
```

## MessagesUpdateDialogFiltersOrder

**Constructor ID:** `3311649252`

```python
MessagesUpdateDialogFiltersOrder.__init__(order: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `order` | `'Vector'` | `—` |

```python
from sent.tl.functions.messages import MessagesUpdateDialogFiltersOrder
```

```python
result = await client(MessagesUpdateDialogFiltersOrder(...))
```

## MessagesGetOldFeaturedStickers

**Constructor ID:** `2127598753`

**Fields:** ``offset`, `limit`, `hash``

```python
MessagesGetOldFeaturedStickers.__init__(offset: int, limit: int, hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `offset` | `int` | `—` |
| `limit` | `int` | `—` |
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetOldFeaturedStickers
```

```python
result = await client(MessagesGetOldFeaturedStickers(...))
```

## MessagesGetReplies

**Constructor ID:** `584962828`

**Fields:** ``peer`, `msg_id`, `offset_id`, `offset_date`, `add_offset`, `limit`, `max_id`, `min_id`, `hash``

```python
MessagesGetReplies.__init__(peer: 'InputPeer', msg_id: int, offset_id: int, offset_date: int, add_offset: int, limit: int, max_id: int, min_id: int, hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |
| `offset_id` | `int` | `—` |
| `offset_date` | `int` | `—` |
| `add_offset` | `int` | `—` |
| `limit` | `int` | `—` |
| `max_id` | `int` | `—` |
| `min_id` | `int` | `—` |
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetReplies
```

```python
result = await client(MessagesGetReplies(...))
```

## MessagesGetDiscussionMessage

**Constructor ID:** `1147761405`

**Fields:** ``peer`, `msg_id``

```python
MessagesGetDiscussionMessage.__init__(peer: 'InputPeer', msg_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetDiscussionMessage
```

```python
result = await client(MessagesGetDiscussionMessage(...))
```

## MessagesReadDiscussion

**Constructor ID:** `4147227124`

**Fields:** ``peer`, `msg_id`, `read_max_id``

```python
MessagesReadDiscussion.__init__(peer: 'InputPeer', msg_id: int, read_max_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |
| `read_max_id` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesReadDiscussion
```

```python
result = await client(MessagesReadDiscussion(...))
```

## MessagesUnpinAllMessages

**Constructor ID:** `103667527`

**Fields:** ``peer`, `top_msg_id`, `saved_peer_id``

```python
MessagesUnpinAllMessages.__init__(peer: 'InputPeer', top_msg_id: int = None, saved_peer_id: 'InputPeer' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `top_msg_id` | `int` | `None` |
| `saved_peer_id` | `'InputPeer'` | `None` |

```python
from sent.tl.functions.messages import MessagesUnpinAllMessages
```

```python
result = await client(MessagesUnpinAllMessages(...))
```

## MessagesDeleteChat

**Constructor ID:** `1540419152`

```python
MessagesDeleteChat.__init__(chat_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `chat_id` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesDeleteChat
```

```python
result = await client(MessagesDeleteChat(...))
```

## MessagesDeletePhoneCallHistory

**Constructor ID:** `4190888969`

```python
MessagesDeletePhoneCallHistory.__init__(revoke: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `revoke` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesDeletePhoneCallHistory
```

```python
result = await client(MessagesDeletePhoneCallHistory(...))
```

## MessagesCheckHistoryImport

**Constructor ID:** `1140726259`

```python
MessagesCheckHistoryImport.__init__(import_head: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `import_head` | `str` | `—` |

```python
from sent.tl.functions.messages import MessagesCheckHistoryImport
```

```python
result = await client(MessagesCheckHistoryImport(...))
```

## MessagesInitHistoryImport

**Constructor ID:** `873008187`

**Fields:** ``peer`, `file`, `media_count``

```python
MessagesInitHistoryImport.__init__(peer: 'InputPeer', file: 'InputFile', media_count: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `file` | `'InputFile'` | `—` |
| `media_count` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesInitHistoryImport
```

```python
result = await client(MessagesInitHistoryImport(...))
```

## MessagesUploadImportedMedia

**Constructor ID:** `713433234`

**Fields:** ``peer`, `import_id`, `file_name`, `media``

```python
MessagesUploadImportedMedia.__init__(peer: 'InputPeer', import_id: int, file_name: str, media: 'InputMedia')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `import_id` | `int` | `—` |
| `file_name` | `str` | `—` |
| `media` | `'InputMedia'` | `—` |

```python
from sent.tl.functions.messages import MessagesUploadImportedMedia
```

```python
result = await client(MessagesUploadImportedMedia(...))
```

## MessagesStartHistoryImport

**Constructor ID:** `3023958852`

**Fields:** ``peer`, `import_id``

```python
MessagesStartHistoryImport.__init__(peer: 'InputPeer', import_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `import_id` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesStartHistoryImport
```

```python
result = await client(MessagesStartHistoryImport(...))
```

## MessagesGetExportedChatInvites

**Constructor ID:** `2729812982`

**Fields:** ``revoked`, `peer`, `admin_id`, `offset_date`, `offset_link`, `limit``

```python
MessagesGetExportedChatInvites.__init__(peer: 'InputPeer', admin_id: 'InputUser', limit: int, revoked: bool = None, offset_date: int = None, offset_link: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `admin_id` | `'InputUser'` | `—` |
| `limit` | `int` | `—` |
| `revoked` | `bool` | `None` |
| `offset_date` | `int` | `None` |
| `offset_link` | `str` | `None` |

```python
from sent.tl.functions.messages import MessagesGetExportedChatInvites
```

```python
result = await client(MessagesGetExportedChatInvites(...))
```

## MessagesGetExportedChatInvite

**Constructor ID:** `1937010524`

**Fields:** ``peer`, `link``

```python
MessagesGetExportedChatInvite.__init__(peer: 'InputPeer', link: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `link` | `str` | `—` |

```python
from sent.tl.functions.messages import MessagesGetExportedChatInvite
```

```python
result = await client(MessagesGetExportedChatInvite(...))
```

## MessagesEditExportedChatInvite

**Constructor ID:** `3184144245`

**Fields:** ``revoked`, `peer`, `link`, `expire_date`, `usage_limit`, `request_needed`, `title``

```python
MessagesEditExportedChatInvite.__init__(peer: 'InputPeer', link: str, revoked: bool = None, expire_date: int = None, usage_limit: int = None, request_needed: bool = None, title: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `link` | `str` | `—` |
| `revoked` | `bool` | `None` |
| `expire_date` | `int` | `None` |
| `usage_limit` | `int` | `None` |
| `request_needed` | `bool` | `None` |
| `title` | `str` | `None` |

```python
from sent.tl.functions.messages import MessagesEditExportedChatInvite
```

```python
result = await client(MessagesEditExportedChatInvite(...))
```

## MessagesDeleteRevokedExportedChatInvites

**Constructor ID:** `1452833749`

**Fields:** ``peer`, `admin_id``

```python
MessagesDeleteRevokedExportedChatInvites.__init__(peer: 'InputPeer', admin_id: 'InputUser')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `admin_id` | `'InputUser'` | `—` |

```python
from sent.tl.functions.messages import MessagesDeleteRevokedExportedChatInvites
```

```python
result = await client(MessagesDeleteRevokedExportedChatInvites(...))
```

## MessagesDeleteExportedChatInvite

**Constructor ID:** `3563365419`

**Fields:** ``peer`, `link``

```python
MessagesDeleteExportedChatInvite.__init__(peer: 'InputPeer', link: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `link` | `str` | `—` |

```python
from sent.tl.functions.messages import MessagesDeleteExportedChatInvite
```

```python
result = await client(MessagesDeleteExportedChatInvite(...))
```

## MessagesGetAdminsWithInvites

**Constructor ID:** `958457583`

```python
MessagesGetAdminsWithInvites.__init__(peer: 'InputPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |

```python
from sent.tl.functions.messages import MessagesGetAdminsWithInvites
```

```python
result = await client(MessagesGetAdminsWithInvites(...))
```

## MessagesGetChatInviteImporters

**Constructor ID:** `3741637966`

**Fields:** ``requested`, `subscription_expired`, `peer`, `link`, `q`, `offset_date`, `offset_user`, `limit``

```python
MessagesGetChatInviteImporters.__init__(peer: 'InputPeer', offset_date: int, offset_user: 'InputUser', limit: int, requested: bool = None, subscription_expired: bool = None, link: str = None, q: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `offset_date` | `int` | `—` |
| `offset_user` | `'InputUser'` | `—` |
| `limit` | `int` | `—` |
| `requested` | `bool` | `None` |
| `subscription_expired` | `bool` | `None` |
| `link` | `str` | `None` |
| `q` | `str` | `None` |

```python
from sent.tl.functions.messages import MessagesGetChatInviteImporters
```

```python
result = await client(MessagesGetChatInviteImporters(...))
```

## MessagesSetHistoryTTL

**Constructor ID:** `3087949796`

**Fields:** ``peer`, `period``

```python
MessagesSetHistoryTTL.__init__(peer: 'InputPeer', period: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `period` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesSetHistoryTTL
```

```python
result = await client(MessagesSetHistoryTTL(...))
```

## MessagesCheckHistoryImportPeer

**Constructor ID:** `1573261059`

```python
MessagesCheckHistoryImportPeer.__init__(peer: 'InputPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |

```python
from sent.tl.functions.messages import MessagesCheckHistoryImportPeer
```

```python
result = await client(MessagesCheckHistoryImportPeer(...))
```

## MessagesSetChatTheme

**Constructor ID:** `135398089`

**Fields:** ``peer`, `theme``

```python
MessagesSetChatTheme.__init__(peer: 'InputPeer', theme: 'InputChatTheme')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `theme` | `'InputChatTheme'` | `—` |

```python
from sent.tl.functions.messages import MessagesSetChatTheme
```

```python
result = await client(MessagesSetChatTheme(...))
```

## MessagesGetMessageReadParticipants

**Constructor ID:** `834782287`

**Fields:** ``peer`, `msg_id``

```python
MessagesGetMessageReadParticipants.__init__(peer: 'InputPeer', msg_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetMessageReadParticipants
```

```python
result = await client(MessagesGetMessageReadParticipants(...))
```

## MessagesGetSearchResultsCalendar

**Constructor ID:** `1789130429`

**Fields:** ``peer`, `saved_peer_id`, `filter`, `offset_id`, `offset_date``

```python
MessagesGetSearchResultsCalendar.__init__(peer: 'InputPeer', filter: 'MessagesFilter', offset_id: int, offset_date: int, saved_peer_id: 'InputPeer' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `filter` | `'MessagesFilter'` | `—` |
| `offset_id` | `int` | `—` |
| `offset_date` | `int` | `—` |
| `saved_peer_id` | `'InputPeer'` | `None` |

```python
from sent.tl.functions.messages import MessagesGetSearchResultsCalendar
```

```python
result = await client(MessagesGetSearchResultsCalendar(...))
```

## MessagesGetSearchResultsPositions

**Constructor ID:** `2625580816`

**Fields:** ``peer`, `saved_peer_id`, `filter`, `offset_id`, `limit``

```python
MessagesGetSearchResultsPositions.__init__(peer: 'InputPeer', filter: 'MessagesFilter', offset_id: int, limit: int, saved_peer_id: 'InputPeer' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `filter` | `'MessagesFilter'` | `—` |
| `offset_id` | `int` | `—` |
| `limit` | `int` | `—` |
| `saved_peer_id` | `'InputPeer'` | `None` |

```python
from sent.tl.functions.messages import MessagesGetSearchResultsPositions
```

```python
result = await client(MessagesGetSearchResultsPositions(...))
```

## MessagesHideChatJoinRequest

**Constructor ID:** `2145904661`

**Fields:** ``approved`, `peer`, `user_id``

```python
MessagesHideChatJoinRequest.__init__(peer: 'InputPeer', user_id: 'InputUser', approved: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `user_id` | `'InputUser'` | `—` |
| `approved` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesHideChatJoinRequest
```

```python
result = await client(MessagesHideChatJoinRequest(...))
```

## MessagesHideAllChatJoinRequests

**Constructor ID:** `3766875370`

**Fields:** ``approved`, `peer`, `link``

```python
MessagesHideAllChatJoinRequests.__init__(peer: 'InputPeer', approved: bool = None, link: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `approved` | `bool` | `None` |
| `link` | `str` | `None` |

```python
from sent.tl.functions.messages import MessagesHideAllChatJoinRequests
```

```python
result = await client(MessagesHideAllChatJoinRequests(...))
```

## MessagesToggleNoForwards

**Constructor ID:** `2986875445`

**Fields:** ``peer`, `enabled`, `request_msg_id``

```python
MessagesToggleNoForwards.__init__(peer: 'InputPeer', enabled: bool, request_msg_id: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `enabled` | `bool` | `—` |
| `request_msg_id` | `int` | `None` |

```python
from sent.tl.functions.messages import MessagesToggleNoForwards
```

```python
result = await client(MessagesToggleNoForwards(...))
```

## MessagesSaveDefaultSendAs

**Constructor ID:** `3439189910`

**Fields:** ``peer`, `send_as``

```python
MessagesSaveDefaultSendAs.__init__(peer: 'InputPeer', send_as: 'InputPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `send_as` | `'InputPeer'` | `—` |

```python
from sent.tl.functions.messages import MessagesSaveDefaultSendAs
```

```python
result = await client(MessagesSaveDefaultSendAs(...))
```

## MessagesSendReaction

**Constructor ID:** `3540875476`

**Fields:** ``big`, `add_to_recent`, `peer`, `msg_id`, `reaction``

```python
MessagesSendReaction.__init__(peer: 'InputPeer', msg_id: int, big: bool = None, add_to_recent: bool = None, reaction: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |
| `big` | `bool` | `None` |
| `add_to_recent` | `bool` | `None` |
| `reaction` | `'Vector'` | `None` |

```python
from sent.tl.functions.messages import MessagesSendReaction
```

```python
result = await client(MessagesSendReaction(...))
```

## MessagesGetMessagesReactions

**Constructor ID:** `2344259814`

**Fields:** ``peer`, `id``

```python
MessagesGetMessagesReactions.__init__(peer: 'InputPeer', id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.messages import MessagesGetMessagesReactions
```

```python
result = await client(MessagesGetMessagesReactions(...))
```

## MessagesGetMessageReactionsList

**Constructor ID:** `1176190792`

**Fields:** ``peer`, `id`, `reaction`, `offset`, `limit``

```python
MessagesGetMessageReactionsList.__init__(peer: 'InputPeer', id: int, limit: int, reaction: 'Reaction' = None, offset: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `int` | `—` |
| `limit` | `int` | `—` |
| `reaction` | `'Reaction'` | `None` |
| `offset` | `str` | `None` |

```python
from sent.tl.functions.messages import MessagesGetMessageReactionsList
```

```python
result = await client(MessagesGetMessageReactionsList(...))
```

## MessagesSetChatAvailableReactions

**Constructor ID:** `2253071745`

**Fields:** ``peer`, `available_reactions`, `reactions_limit`, `paid_enabled``

```python
MessagesSetChatAvailableReactions.__init__(peer: 'InputPeer', available_reactions: 'ChatReactions', reactions_limit: int = None, paid_enabled: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `available_reactions` | `'ChatReactions'` | `—` |
| `reactions_limit` | `int` | `None` |
| `paid_enabled` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesSetChatAvailableReactions
```

```python
result = await client(MessagesSetChatAvailableReactions(...))
```

## MessagesGetAvailableReactions

**Constructor ID:** `417243308`

```python
MessagesGetAvailableReactions.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetAvailableReactions
```

```python
result = await client(MessagesGetAvailableReactions(...))
```

## MessagesSetDefaultReaction

**Constructor ID:** `1330094102`

```python
MessagesSetDefaultReaction.__init__(reaction: 'Reaction')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `reaction` | `'Reaction'` | `—` |

```python
from sent.tl.functions.messages import MessagesSetDefaultReaction
```

```python
result = await client(MessagesSetDefaultReaction(...))
```

## MessagesTranslateText

**Constructor ID:** `2783888197`

**Fields:** ``peer`, `id`, `text`, `to_lang`, `tone``

```python
MessagesTranslateText.__init__(to_lang: str, peer: 'InputPeer' = None, id: 'Vector' = None, text: 'Vector' = None, tone: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `to_lang` | `str` | `—` |
| `peer` | `'InputPeer'` | `None` |
| `id` | `'Vector'` | `None` |
| `text` | `'Vector'` | `None` |
| `tone` | `str` | `None` |

```python
from sent.tl.functions.messages import MessagesTranslateText
```

```python
result = await client(MessagesTranslateText(...))
```

## MessagesGetUnreadReactions

**Constructor ID:** `3179253932`

**Fields:** ``peer`, `top_msg_id`, `saved_peer_id`, `offset_id`, `add_offset`, `limit`, `max_id`, `min_id``

```python
MessagesGetUnreadReactions.__init__(peer: 'InputPeer', offset_id: int, add_offset: int, limit: int, max_id: int, min_id: int, top_msg_id: int = None, saved_peer_id: 'InputPeer' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `offset_id` | `int` | `—` |
| `add_offset` | `int` | `—` |
| `limit` | `int` | `—` |
| `max_id` | `int` | `—` |
| `min_id` | `int` | `—` |
| `top_msg_id` | `int` | `None` |
| `saved_peer_id` | `'InputPeer'` | `None` |

```python
from sent.tl.functions.messages import MessagesGetUnreadReactions
```

```python
result = await client(MessagesGetUnreadReactions(...))
```

## MessagesReadReactions

**Constructor ID:** `2663665555`

**Fields:** ``peer`, `top_msg_id`, `saved_peer_id``

```python
MessagesReadReactions.__init__(peer: 'InputPeer', top_msg_id: int = None, saved_peer_id: 'InputPeer' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `top_msg_id` | `int` | `None` |
| `saved_peer_id` | `'InputPeer'` | `None` |

```python
from sent.tl.functions.messages import MessagesReadReactions
```

```python
result = await client(MessagesReadReactions(...))
```

## MessagesSearchSentMedia

**Constructor ID:** `276705696`

**Fields:** ``q`, `filter`, `limit``

```python
MessagesSearchSentMedia.__init__(q: str, filter: 'MessagesFilter', limit: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `q` | `str` | `—` |
| `filter` | `'MessagesFilter'` | `—` |
| `limit` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesSearchSentMedia
```

```python
result = await client(MessagesSearchSentMedia(...))
```

## MessagesGetAttachMenuBots

**Constructor ID:** `385663691`

```python
MessagesGetAttachMenuBots.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetAttachMenuBots
```

```python
result = await client(MessagesGetAttachMenuBots(...))
```

## MessagesGetAttachMenuBot

**Constructor ID:** `1998676370`

```python
MessagesGetAttachMenuBot.__init__(bot: 'InputUser')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |

```python
from sent.tl.functions.messages import MessagesGetAttachMenuBot
```

```python
result = await client(MessagesGetAttachMenuBot(...))
```

## MessagesToggleBotInAttachMenu

**Constructor ID:** `1777704297`

**Fields:** ``write_allowed`, `bot`, `enabled``

```python
MessagesToggleBotInAttachMenu.__init__(bot: 'InputUser', enabled: bool, write_allowed: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |
| `enabled` | `bool` | `—` |
| `write_allowed` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesToggleBotInAttachMenu
```

```python
result = await client(MessagesToggleBotInAttachMenu(...))
```

## MessagesRequestWebView

**Constructor ID:** `647873217`

**Fields:** ``from_bot_menu`, `silent`, `compact`, `fullscreen`, `peer`, `bot`, `url`, `start_param`, `theme_params`, `platform`, `reply_to`, `send_as``

```python
MessagesRequestWebView.__init__(peer: 'InputPeer', bot: 'InputUser', platform: str, from_bot_menu: bool = None, silent: bool = None, compact: bool = None, fullscreen: bool = None, url: str = None, start_param: str = None, theme_params: 'DataJSON' = None, reply_to: 'InputReplyTo' = None, send_as: 'InputPeer' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `bot` | `'InputUser'` | `—` |
| `platform` | `str` | `—` |
| `from_bot_menu` | `bool` | `None` |
| `silent` | `bool` | `None` |
| `compact` | `bool` | `None` |
| `fullscreen` | `bool` | `None` |
| `url` | `str` | `None` |
| `start_param` | `str` | `None` |
| `theme_params` | `'DataJSON'` | `None` |
| `reply_to` | `'InputReplyTo'` | `None` |
| `send_as` | `'InputPeer'` | `None` |

```python
from sent.tl.functions.messages import MessagesRequestWebView
```

```python
result = await client(MessagesRequestWebView(...))
```

## MessagesProlongWebView

**Constructor ID:** `2966952579`

**Fields:** ``silent`, `peer`, `bot`, `query_id`, `reply_to`, `send_as``

```python
MessagesProlongWebView.__init__(peer: 'InputPeer', bot: 'InputUser', query_id: int, silent: bool = None, reply_to: 'InputReplyTo' = None, send_as: 'InputPeer' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `bot` | `'InputUser'` | `—` |
| `query_id` | `int` | `—` |
| `silent` | `bool` | `None` |
| `reply_to` | `'InputReplyTo'` | `None` |
| `send_as` | `'InputPeer'` | `None` |

```python
from sent.tl.functions.messages import MessagesProlongWebView
```

```python
result = await client(MessagesProlongWebView(...))
```

## MessagesRequestSimpleWebView

**Constructor ID:** `1094336115`

**Fields:** ``from_switch_webview`, `from_side_menu`, `compact`, `fullscreen`, `bot`, `url`, `start_param`, `theme_params`, `platform``

```python
MessagesRequestSimpleWebView.__init__(bot: 'InputUser', platform: str, from_switch_webview: bool = None, from_side_menu: bool = None, compact: bool = None, fullscreen: bool = None, url: str = None, start_param: str = None, theme_params: 'DataJSON' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |
| `platform` | `str` | `—` |
| `from_switch_webview` | `bool` | `None` |
| `from_side_menu` | `bool` | `None` |
| `compact` | `bool` | `None` |
| `fullscreen` | `bool` | `None` |
| `url` | `str` | `None` |
| `start_param` | `str` | `None` |
| `theme_params` | `'DataJSON'` | `None` |

```python
from sent.tl.functions.messages import MessagesRequestSimpleWebView
```

```python
result = await client(MessagesRequestSimpleWebView(...))
```

## MessagesSendWebViewResultMessage

**Constructor ID:** `172168437`

**Fields:** ``bot_query_id`, `result``

```python
MessagesSendWebViewResultMessage.__init__(bot_query_id: str, result: 'InputBotInlineResult')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot_query_id` | `str` | `—` |
| `result` | `'InputBotInlineResult'` | `—` |

```python
from sent.tl.functions.messages import MessagesSendWebViewResultMessage
```

```python
result = await client(MessagesSendWebViewResultMessage(...))
```

## MessagesSendWebViewData

**Constructor ID:** `3691135688`

**Fields:** ``bot`, `random_id`, `button_text`, `data``

```python
MessagesSendWebViewData.__init__(bot: 'InputUser', random_id: int, button_text: str, data: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |
| `random_id` | `int` | `—` |
| `button_text` | `str` | `—` |
| `data` | `str` | `—` |

```python
from sent.tl.functions.messages import MessagesSendWebViewData
```

```python
result = await client(MessagesSendWebViewData(...))
```

## MessagesTranscribeAudio

**Constructor ID:** `647928393`

**Fields:** ``peer`, `msg_id``

```python
MessagesTranscribeAudio.__init__(peer: 'InputPeer', msg_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesTranscribeAudio
```

```python
result = await client(MessagesTranscribeAudio(...))
```

## MessagesRateTranscribedAudio

**Constructor ID:** `2132608815`

**Fields:** ``peer`, `msg_id`, `transcription_id`, `good``

```python
MessagesRateTranscribedAudio.__init__(peer: 'InputPeer', msg_id: int, transcription_id: int, good: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |
| `transcription_id` | `int` | `—` |
| `good` | `bool` | `—` |

```python
from sent.tl.functions.messages import MessagesRateTranscribedAudio
```

```python
result = await client(MessagesRateTranscribedAudio(...))
```

## MessagesGetCustomEmojiDocuments

**Constructor ID:** `3651866452`

```python
MessagesGetCustomEmojiDocuments.__init__(document_id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `document_id` | `'Vector'` | `—` |

```python
from sent.tl.functions.messages import MessagesGetCustomEmojiDocuments
```

```python
result = await client(MessagesGetCustomEmojiDocuments(...))
```

## MessagesGetEmojiStickers

**Constructor ID:** `4227637647`

```python
MessagesGetEmojiStickers.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetEmojiStickers
```

```python
result = await client(MessagesGetEmojiStickers(...))
```

## MessagesGetFeaturedEmojiStickers

**Constructor ID:** `248473398`

```python
MessagesGetFeaturedEmojiStickers.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetFeaturedEmojiStickers
```

```python
result = await client(MessagesGetFeaturedEmojiStickers(...))
```

## MessagesReportReaction

**Constructor ID:** `1063567478`

**Fields:** ``peer`, `id`, `reaction_peer``

```python
MessagesReportReaction.__init__(peer: 'InputPeer', id: int, reaction_peer: 'InputPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `int` | `—` |
| `reaction_peer` | `'InputPeer'` | `—` |

```python
from sent.tl.functions.messages import MessagesReportReaction
```

```python
result = await client(MessagesReportReaction(...))
```

## MessagesGetTopReactions

**Constructor ID:** `3145803194`

**Fields:** ``limit`, `hash``

```python
MessagesGetTopReactions.__init__(limit: int, hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `limit` | `int` | `—` |
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetTopReactions
```

```python
result = await client(MessagesGetTopReactions(...))
```

## MessagesGetRecentReactions

**Constructor ID:** `960896434`

**Fields:** ``limit`, `hash``

```python
MessagesGetRecentReactions.__init__(limit: int, hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `limit` | `int` | `—` |
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetRecentReactions
```

```python
result = await client(MessagesGetRecentReactions(...))
```

## MessagesClearRecentReactions

**Constructor ID:** `2650730420`

```python
MessagesClearRecentReactions.__init__()
```

_No parameters._

```python
from sent.tl.functions.messages import MessagesClearRecentReactions
```

```python
result = await client(MessagesClearRecentReactions(...))
```

## MessagesGetExtendedMedia

**Constructor ID:** `2230847508`

**Fields:** ``peer`, `id``

```python
MessagesGetExtendedMedia.__init__(peer: 'InputPeer', id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.messages import MessagesGetExtendedMedia
```

```python
result = await client(MessagesGetExtendedMedia(...))
```

## MessagesSetDefaultHistoryTTL

**Constructor ID:** `2662667333`

```python
MessagesSetDefaultHistoryTTL.__init__(period: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `period` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesSetDefaultHistoryTTL
```

```python
result = await client(MessagesSetDefaultHistoryTTL(...))
```

## MessagesGetDefaultHistoryTTL

**Constructor ID:** `1703637384`

```python
MessagesGetDefaultHistoryTTL.__init__()
```

_No parameters._

```python
from sent.tl.functions.messages import MessagesGetDefaultHistoryTTL
```

```python
result = await client(MessagesGetDefaultHistoryTTL(...))
```

## MessagesSendBotRequestedPeer

**Constructor ID:** `1818030759`

**Fields:** ``peer`, `msg_id`, `webapp_req_id`, `button_id`, `requested_peers``

```python
MessagesSendBotRequestedPeer.__init__(peer: 'InputPeer', button_id: int, requested_peers: 'Vector', msg_id: int = None, webapp_req_id: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `button_id` | `int` | `—` |
| `requested_peers` | `'Vector'` | `—` |
| `msg_id` | `int` | `None` |
| `webapp_req_id` | `str` | `None` |

```python
from sent.tl.functions.messages import MessagesSendBotRequestedPeer
```

```python
result = await client(MessagesSendBotRequestedPeer(...))
```

## MessagesGetEmojiGroups

**Constructor ID:** `1955122779`

```python
MessagesGetEmojiGroups.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetEmojiGroups
```

```python
result = await client(MessagesGetEmojiGroups(...))
```

## MessagesGetEmojiStatusGroups

**Constructor ID:** `785209037`

```python
MessagesGetEmojiStatusGroups.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetEmojiStatusGroups
```

```python
result = await client(MessagesGetEmojiStatusGroups(...))
```

## MessagesGetEmojiProfilePhotoGroups

**Constructor ID:** `564480243`

```python
MessagesGetEmojiProfilePhotoGroups.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetEmojiProfilePhotoGroups
```

```python
result = await client(MessagesGetEmojiProfilePhotoGroups(...))
```

## MessagesSearchCustomEmoji

**Constructor ID:** `739360983`

**Fields:** ``emoticon`, `hash``

```python
MessagesSearchCustomEmoji.__init__(emoticon: str, hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `emoticon` | `str` | `—` |
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesSearchCustomEmoji
```

```python
result = await client(MessagesSearchCustomEmoji(...))
```

## MessagesTogglePeerTranslations

**Constructor ID:** `3833378169`

**Fields:** ``disabled`, `peer``

```python
MessagesTogglePeerTranslations.__init__(peer: 'InputPeer', disabled: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `disabled` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesTogglePeerTranslations
```

```python
result = await client(MessagesTogglePeerTranslations(...))
```

## MessagesGetBotApp

**Constructor ID:** `889046467`

**Fields:** ``app`, `hash``

```python
MessagesGetBotApp.__init__(app: 'InputBotApp', hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `app` | `'InputBotApp'` | `—` |
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetBotApp
```

```python
result = await client(MessagesGetBotApp(...))
```

## MessagesRequestAppWebView

**Constructor ID:** `1398901710`

**Fields:** ``write_allowed`, `compact`, `fullscreen`, `peer`, `app`, `start_param`, `theme_params`, `platform``

```python
MessagesRequestAppWebView.__init__(peer: 'InputPeer', app: 'InputBotApp', platform: str, write_allowed: bool = None, compact: bool = None, fullscreen: bool = None, start_param: str = None, theme_params: 'DataJSON' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `app` | `'InputBotApp'` | `—` |
| `platform` | `str` | `—` |
| `write_allowed` | `bool` | `None` |
| `compact` | `bool` | `None` |
| `fullscreen` | `bool` | `None` |
| `start_param` | `str` | `None` |
| `theme_params` | `'DataJSON'` | `None` |

```python
from sent.tl.functions.messages import MessagesRequestAppWebView
```

```python
result = await client(MessagesRequestAppWebView(...))
```

## MessagesSetChatWallPaper

**Constructor ID:** `2415577825`

**Fields:** ``for_both`, `revert`, `peer`, `wallpaper`, `settings`, `id``

```python
MessagesSetChatWallPaper.__init__(peer: 'InputPeer', for_both: bool = None, revert: bool = None, wallpaper: 'InputWallPaper' = None, settings: 'WallPaperSettings' = None, id: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `for_both` | `bool` | `None` |
| `revert` | `bool` | `None` |
| `wallpaper` | `'InputWallPaper'` | `None` |
| `settings` | `'WallPaperSettings'` | `None` |
| `id` | `int` | `None` |

```python
from sent.tl.functions.messages import MessagesSetChatWallPaper
```

```python
result = await client(MessagesSetChatWallPaper(...))
```

## MessagesSearchEmojiStickerSets

**Constructor ID:** `2461288780`

**Fields:** ``exclude_featured`, `q`, `hash``

```python
MessagesSearchEmojiStickerSets.__init__(q: str, hash: int, exclude_featured: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `q` | `str` | `—` |
| `hash` | `int` | `—` |
| `exclude_featured` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesSearchEmojiStickerSets
```

```python
result = await client(MessagesSearchEmojiStickerSets(...))
```

## MessagesGetSavedDialogs

**Constructor ID:** `512883865`

**Fields:** ``exclude_pinned`, `parent_peer`, `offset_date`, `offset_id`, `offset_peer`, `limit`, `hash``

```python
MessagesGetSavedDialogs.__init__(offset_date: int, offset_id: int, offset_peer: 'InputPeer', limit: int, hash: int, exclude_pinned: bool = None, parent_peer: 'InputPeer' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `offset_date` | `int` | `—` |
| `offset_id` | `int` | `—` |
| `offset_peer` | `'InputPeer'` | `—` |
| `limit` | `int` | `—` |
| `hash` | `int` | `—` |
| `exclude_pinned` | `bool` | `None` |
| `parent_peer` | `'InputPeer'` | `None` |

```python
from sent.tl.functions.messages import MessagesGetSavedDialogs
```

```python
result = await client(MessagesGetSavedDialogs(...))
```

## MessagesGetSavedHistory

**Constructor ID:** `2576003081`

**Fields:** ``parent_peer`, `peer`, `offset_id`, `offset_date`, `add_offset`, `limit`, `max_id`, `min_id`, `hash``

```python
MessagesGetSavedHistory.__init__(peer: 'InputPeer', offset_id: int, offset_date: int, add_offset: int, limit: int, max_id: int, min_id: int, hash: int, parent_peer: 'InputPeer' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `offset_id` | `int` | `—` |
| `offset_date` | `int` | `—` |
| `add_offset` | `int` | `—` |
| `limit` | `int` | `—` |
| `max_id` | `int` | `—` |
| `min_id` | `int` | `—` |
| `hash` | `int` | `—` |
| `parent_peer` | `'InputPeer'` | `None` |

```python
from sent.tl.functions.messages import MessagesGetSavedHistory
```

```python
result = await client(MessagesGetSavedHistory(...))
```

## MessagesDeleteSavedHistory

**Constructor ID:** `1304758367`

**Fields:** ``parent_peer`, `peer`, `max_id`, `min_date`, `max_date``

```python
MessagesDeleteSavedHistory.__init__(peer: 'InputPeer', max_id: int, parent_peer: 'InputPeer' = None, min_date: int = None, max_date: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `max_id` | `int` | `—` |
| `parent_peer` | `'InputPeer'` | `None` |
| `min_date` | `int` | `None` |
| `max_date` | `int` | `None` |

```python
from sent.tl.functions.messages import MessagesDeleteSavedHistory
```

```python
result = await client(MessagesDeleteSavedHistory(...))
```

## MessagesGetPinnedSavedDialogs

**Constructor ID:** `3594360032`

```python
MessagesGetPinnedSavedDialogs.__init__()
```

_No parameters._

```python
from sent.tl.functions.messages import MessagesGetPinnedSavedDialogs
```

```python
result = await client(MessagesGetPinnedSavedDialogs(...))
```

## MessagesToggleSavedDialogPin

**Constructor ID:** `2894183390`

**Fields:** ``pinned`, `peer``

```python
MessagesToggleSavedDialogPin.__init__(peer: 'InputDialogPeer', pinned: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputDialogPeer'` | `—` |
| `pinned` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesToggleSavedDialogPin
```

```python
result = await client(MessagesToggleSavedDialogPin(...))
```

## MessagesReorderPinnedSavedDialogs

**Constructor ID:** `2339464583`

**Fields:** ``force`, `order``

```python
MessagesReorderPinnedSavedDialogs.__init__(order: 'Vector', force: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `order` | `'Vector'` | `—` |
| `force` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesReorderPinnedSavedDialogs
```

```python
result = await client(MessagesReorderPinnedSavedDialogs(...))
```

## MessagesGetSavedReactionTags

**Constructor ID:** `909631579`

**Fields:** ``peer`, `hash``

```python
MessagesGetSavedReactionTags.__init__(hash: int, peer: 'InputPeer' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `peer` | `'InputPeer'` | `None` |

```python
from sent.tl.functions.messages import MessagesGetSavedReactionTags
```

```python
result = await client(MessagesGetSavedReactionTags(...))
```

## MessagesUpdateSavedReactionTag

**Constructor ID:** `1613331948`

**Fields:** ``reaction`, `title``

```python
MessagesUpdateSavedReactionTag.__init__(reaction: 'Reaction', title: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `reaction` | `'Reaction'` | `—` |
| `title` | `str` | `None` |

```python
from sent.tl.functions.messages import MessagesUpdateSavedReactionTag
```

```python
result = await client(MessagesUpdateSavedReactionTag(...))
```

## MessagesGetDefaultTagReactions

**Constructor ID:** `3187225640`

```python
MessagesGetDefaultTagReactions.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetDefaultTagReactions
```

```python
result = await client(MessagesGetDefaultTagReactions(...))
```

## MessagesGetOutboxReadDate

**Constructor ID:** `2353790557`

**Fields:** ``peer`, `msg_id``

```python
MessagesGetOutboxReadDate.__init__(peer: 'InputPeer', msg_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetOutboxReadDate
```

```python
result = await client(MessagesGetOutboxReadDate(...))
```

## MessagesGetQuickReplies

**Constructor ID:** `3565417128`

```python
MessagesGetQuickReplies.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetQuickReplies
```

```python
result = await client(MessagesGetQuickReplies(...))
```

## MessagesReorderQuickReplies

**Constructor ID:** `1613961479`

```python
MessagesReorderQuickReplies.__init__(order: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `order` | `'Vector'` | `—` |

```python
from sent.tl.functions.messages import MessagesReorderQuickReplies
```

```python
result = await client(MessagesReorderQuickReplies(...))
```

## MessagesCheckQuickReplyShortcut

**Constructor ID:** `4057005011`

```python
MessagesCheckQuickReplyShortcut.__init__(shortcut: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `shortcut` | `str` | `—` |

```python
from sent.tl.functions.messages import MessagesCheckQuickReplyShortcut
```

```python
result = await client(MessagesCheckQuickReplyShortcut(...))
```

## MessagesEditQuickReplyShortcut

**Constructor ID:** `1543519471`

**Fields:** ``shortcut_id`, `shortcut``

```python
MessagesEditQuickReplyShortcut.__init__(shortcut_id: int, shortcut: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `shortcut_id` | `int` | `—` |
| `shortcut` | `str` | `—` |

```python
from sent.tl.functions.messages import MessagesEditQuickReplyShortcut
```

```python
result = await client(MessagesEditQuickReplyShortcut(...))
```

## MessagesDeleteQuickReplyShortcut

**Constructor ID:** `1019234112`

```python
MessagesDeleteQuickReplyShortcut.__init__(shortcut_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `shortcut_id` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesDeleteQuickReplyShortcut
```

```python
result = await client(MessagesDeleteQuickReplyShortcut(...))
```

## MessagesGetQuickReplyMessages

**Constructor ID:** `2493814211`

**Fields:** ``shortcut_id`, `id`, `hash``

```python
MessagesGetQuickReplyMessages.__init__(shortcut_id: int, hash: int, id: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `shortcut_id` | `int` | `—` |
| `hash` | `int` | `—` |
| `id` | `'Vector'` | `None` |

```python
from sent.tl.functions.messages import MessagesGetQuickReplyMessages
```

```python
result = await client(MessagesGetQuickReplyMessages(...))
```

## MessagesSendQuickReplyMessages

**Constructor ID:** `1819610593`

**Fields:** ``peer`, `shortcut_id`, `id`, `random_id``

```python
MessagesSendQuickReplyMessages.__init__(peer: 'InputPeer', shortcut_id: int, id: 'Vector', random_id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `shortcut_id` | `int` | `—` |
| `id` | `'Vector'` | `—` |
| `random_id` | `'Vector'` | `—` |

```python
from sent.tl.functions.messages import MessagesSendQuickReplyMessages
```

```python
result = await client(MessagesSendQuickReplyMessages(...))
```

## MessagesDeleteQuickReplyMessages

**Constructor ID:** `3775260944`

**Fields:** ``shortcut_id`, `id``

```python
MessagesDeleteQuickReplyMessages.__init__(shortcut_id: int, id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `shortcut_id` | `int` | `—` |
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.messages import MessagesDeleteQuickReplyMessages
```

```python
result = await client(MessagesDeleteQuickReplyMessages(...))
```

## MessagesToggleDialogFilterTags

**Constructor ID:** `4247640649`

```python
MessagesToggleDialogFilterTags.__init__(enabled: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `enabled` | `bool` | `—` |

```python
from sent.tl.functions.messages import MessagesToggleDialogFilterTags
```

```python
result = await client(MessagesToggleDialogFilterTags(...))
```

## MessagesGetMyStickers

**Constructor ID:** `3501580796`

**Fields:** ``offset_id`, `limit``

```python
MessagesGetMyStickers.__init__(offset_id: int, limit: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `offset_id` | `int` | `—` |
| `limit` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetMyStickers
```

```python
result = await client(MessagesGetMyStickers(...))
```

## MessagesGetEmojiStickerGroups

**Constructor ID:** `500711669`

```python
MessagesGetEmojiStickerGroups.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetEmojiStickerGroups
```

```python
result = await client(MessagesGetEmojiStickerGroups(...))
```

## MessagesGetAvailableEffects

**Constructor ID:** `3735161401`

```python
MessagesGetAvailableEffects.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetAvailableEffects
```

```python
result = await client(MessagesGetAvailableEffects(...))
```

## MessagesEditFactCheck

**Constructor ID:** `92925557`

**Fields:** ``peer`, `msg_id`, `text``

```python
MessagesEditFactCheck.__init__(peer: 'InputPeer', msg_id: int, text: 'TextWithEntities')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |
| `text` | `'TextWithEntities'` | `—` |

```python
from sent.tl.functions.messages import MessagesEditFactCheck
```

```python
result = await client(MessagesEditFactCheck(...))
```

## MessagesDeleteFactCheck

**Constructor ID:** `3520762892`

**Fields:** ``peer`, `msg_id``

```python
MessagesDeleteFactCheck.__init__(peer: 'InputPeer', msg_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesDeleteFactCheck
```

```python
result = await client(MessagesDeleteFactCheck(...))
```

## MessagesGetFactCheck

**Constructor ID:** `3117270510`

**Fields:** ``peer`, `msg_id``

```python
MessagesGetFactCheck.__init__(peer: 'InputPeer', msg_id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `'Vector'` | `—` |

```python
from sent.tl.functions.messages import MessagesGetFactCheck
```

```python
result = await client(MessagesGetFactCheck(...))
```

## MessagesRequestMainWebView

**Constructor ID:** `3386908283`

**Fields:** ``compact`, `fullscreen`, `peer`, `bot`, `start_param`, `theme_params`, `platform``

```python
MessagesRequestMainWebView.__init__(peer: 'InputPeer', bot: 'InputUser', platform: str, compact: bool = None, fullscreen: bool = None, start_param: str = None, theme_params: 'DataJSON' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `bot` | `'InputUser'` | `—` |
| `platform` | `str` | `—` |
| `compact` | `bool` | `None` |
| `fullscreen` | `bool` | `None` |
| `start_param` | `str` | `None` |
| `theme_params` | `'DataJSON'` | `None` |

```python
from sent.tl.functions.messages import MessagesRequestMainWebView
```

```python
result = await client(MessagesRequestMainWebView(...))
```

## MessagesSendPaidReaction

**Constructor ID:** `1488702288`

**Fields:** ``peer`, `msg_id`, `count`, `random_id`, `private``

```python
MessagesSendPaidReaction.__init__(peer: 'InputPeer', msg_id: int, count: int, random_id: int, private: 'PaidReactionPrivacy' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |
| `count` | `int` | `—` |
| `random_id` | `int` | `—` |
| `private` | `'PaidReactionPrivacy'` | `None` |

```python
from sent.tl.functions.messages import MessagesSendPaidReaction
```

```python
result = await client(MessagesSendPaidReaction(...))
```

## MessagesTogglePaidReactionPrivacy

**Constructor ID:** `1129874869`

**Fields:** ``peer`, `msg_id`, `private``

```python
MessagesTogglePaidReactionPrivacy.__init__(peer: 'InputPeer', msg_id: int, private: 'PaidReactionPrivacy')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |
| `private` | `'PaidReactionPrivacy'` | `—` |

```python
from sent.tl.functions.messages import MessagesTogglePaidReactionPrivacy
```

```python
result = await client(MessagesTogglePaidReactionPrivacy(...))
```

## MessagesGetPaidReactionPrivacy

**Constructor ID:** `1193563562`

```python
MessagesGetPaidReactionPrivacy.__init__()
```

_No parameters._

```python
from sent.tl.functions.messages import MessagesGetPaidReactionPrivacy
```

```python
result = await client(MessagesGetPaidReactionPrivacy(...))
```

## MessagesViewSponsoredMessage

**Constructor ID:** `647902787`

```python
MessagesViewSponsoredMessage.__init__(random_id: bytes)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `random_id` | `bytes` | `—` |

```python
from sent.tl.functions.messages import MessagesViewSponsoredMessage
```

```python
result = await client(MessagesViewSponsoredMessage(...))
```

## MessagesClickSponsoredMessage

**Constructor ID:** `2184512894`

**Fields:** ``media`, `fullscreen`, `random_id``

```python
MessagesClickSponsoredMessage.__init__(random_id: bytes, media: bool = None, fullscreen: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `random_id` | `bytes` | `—` |
| `media` | `bool` | `None` |
| `fullscreen` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesClickSponsoredMessage
```

```python
result = await client(MessagesClickSponsoredMessage(...))
```

## MessagesReportSponsoredMessage

**Constructor ID:** `315355332`

**Fields:** ``random_id`, `option``

```python
MessagesReportSponsoredMessage.__init__(random_id: bytes, option: bytes)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `random_id` | `bytes` | `—` |
| `option` | `bytes` | `—` |

```python
from sent.tl.functions.messages import MessagesReportSponsoredMessage
```

```python
result = await client(MessagesReportSponsoredMessage(...))
```

## MessagesGetSponsoredMessages

**Constructor ID:** `1030547536`

**Fields:** ``peer`, `msg_id``

```python
MessagesGetSponsoredMessages.__init__(peer: 'InputPeer', msg_id: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `None` |

```python
from sent.tl.functions.messages import MessagesGetSponsoredMessages
```

```python
result = await client(MessagesGetSponsoredMessages(...))
```

## MessagesSavePreparedInlineMessage

**Constructor ID:** `4062150447`

**Fields:** ``result`, `user_id`, `peer_types``

```python
MessagesSavePreparedInlineMessage.__init__(result: 'InputBotInlineResult', user_id: 'InputUser', peer_types: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `result` | `'InputBotInlineResult'` | `—` |
| `user_id` | `'InputUser'` | `—` |
| `peer_types` | `'Vector'` | `None` |

```python
from sent.tl.functions.messages import MessagesSavePreparedInlineMessage
```

```python
result = await client(MessagesSavePreparedInlineMessage(...))
```

## MessagesGetPreparedInlineMessage

**Constructor ID:** `2239675832`

**Fields:** ``bot`, `id``

```python
MessagesGetPreparedInlineMessage.__init__(bot: 'InputUser', id: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |
| `id` | `str` | `—` |

```python
from sent.tl.functions.messages import MessagesGetPreparedInlineMessage
```

```python
result = await client(MessagesGetPreparedInlineMessage(...))
```

## MessagesSearchStickers

**Constructor ID:** `699516522`

**Fields:** ``emojis`, `q`, `emoticon`, `lang_code`, `offset`, `limit`, `hash``

```python
MessagesSearchStickers.__init__(q: str, emoticon: str, lang_code: 'Vector', offset: int, limit: int, hash: int, emojis: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `q` | `str` | `—` |
| `emoticon` | `str` | `—` |
| `lang_code` | `'Vector'` | `—` |
| `offset` | `int` | `—` |
| `limit` | `int` | `—` |
| `hash` | `int` | `—` |
| `emojis` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesSearchStickers
```

```python
result = await client(MessagesSearchStickers(...))
```

## MessagesReportMessagesDelivery

**Constructor ID:** `1517122453`

**Fields:** ``push`, `peer`, `id``

```python
MessagesReportMessagesDelivery.__init__(peer: 'InputPeer', id: 'Vector', push: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `'Vector'` | `—` |
| `push` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesReportMessagesDelivery
```

```python
result = await client(MessagesReportMessagesDelivery(...))
```

## MessagesGetSavedDialogsByID

**Constructor ID:** `1869585558`

**Fields:** ``parent_peer`, `ids``

```python
MessagesGetSavedDialogsByID.__init__(ids: 'Vector', parent_peer: 'InputPeer' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `ids` | `'Vector'` | `—` |
| `parent_peer` | `'InputPeer'` | `None` |

```python
from sent.tl.functions.messages import MessagesGetSavedDialogsByID
```

```python
result = await client(MessagesGetSavedDialogsByID(...))
```

## MessagesReadSavedHistory

**Constructor ID:** `3125427035`

**Fields:** ``parent_peer`, `peer`, `max_id``

```python
MessagesReadSavedHistory.__init__(parent_peer: 'InputPeer', peer: 'InputPeer', max_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `parent_peer` | `'InputPeer'` | `—` |
| `peer` | `'InputPeer'` | `—` |
| `max_id` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesReadSavedHistory
```

```python
result = await client(MessagesReadSavedHistory(...))
```

## MessagesToggleTodoCompleted

**Constructor ID:** `3554685220`

**Fields:** ``peer`, `msg_id`, `completed`, `incompleted``

```python
MessagesToggleTodoCompleted.__init__(peer: 'InputPeer', msg_id: int, completed: 'Vector', incompleted: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |
| `completed` | `'Vector'` | `—` |
| `incompleted` | `'Vector'` | `—` |

```python
from sent.tl.functions.messages import MessagesToggleTodoCompleted
```

```python
result = await client(MessagesToggleTodoCompleted(...))
```

## MessagesAppendTodoList

**Constructor ID:** `564531287`

**Fields:** ``peer`, `msg_id`, `list``

```python
MessagesAppendTodoList.__init__(peer: 'InputPeer', msg_id: int, list: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |
| `list` | `'Vector'` | `—` |

```python
from sent.tl.functions.messages import MessagesAppendTodoList
```

```python
result = await client(MessagesAppendTodoList(...))
```

## MessagesToggleSuggestedPostApproval

**Constructor ID:** `2164737372`

**Fields:** ``reject`, `peer`, `msg_id`, `schedule_date`, `reject_comment``

```python
MessagesToggleSuggestedPostApproval.__init__(peer: 'InputPeer', msg_id: int, reject: bool = None, schedule_date: int = None, reject_comment: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |
| `reject` | `bool` | `None` |
| `schedule_date` | `int` | `None` |
| `reject_comment` | `str` | `None` |

```python
from sent.tl.functions.messages import MessagesToggleSuggestedPostApproval
```

```python
result = await client(MessagesToggleSuggestedPostApproval(...))
```

## MessagesGetForumTopics

**Constructor ID:** `1000635391`

**Fields:** ``peer`, `q`, `offset_date`, `offset_id`, `offset_topic`, `limit``

```python
MessagesGetForumTopics.__init__(peer: 'InputPeer', offset_date: int, offset_id: int, offset_topic: int, limit: int, q: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `offset_date` | `int` | `—` |
| `offset_id` | `int` | `—` |
| `offset_topic` | `int` | `—` |
| `limit` | `int` | `—` |
| `q` | `str` | `None` |

```python
from sent.tl.functions.messages import MessagesGetForumTopics
```

```python
result = await client(MessagesGetForumTopics(...))
```

## MessagesGetForumTopicsByID

**Constructor ID:** `2936687112`

**Fields:** ``peer`, `topics``

```python
MessagesGetForumTopicsByID.__init__(peer: 'InputPeer', topics: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `topics` | `'Vector'` | `—` |

```python
from sent.tl.functions.messages import MessagesGetForumTopicsByID
```

```python
result = await client(MessagesGetForumTopicsByID(...))
```

## MessagesEditForumTopic

**Constructor ID:** `3469480244`

**Fields:** ``peer`, `topic_id`, `title`, `icon_emoji_id`, `closed`, `hidden``

```python
MessagesEditForumTopic.__init__(peer: 'InputPeer', topic_id: int, title: str = None, icon_emoji_id: int = None, closed: bool = None, hidden: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `topic_id` | `int` | `—` |
| `title` | `str` | `None` |
| `icon_emoji_id` | `int` | `None` |
| `closed` | `bool` | `None` |
| `hidden` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesEditForumTopic
```

```python
result = await client(MessagesEditForumTopic(...))
```

## MessagesUpdatePinnedForumTopic

**Constructor ID:** `392032849`

**Fields:** ``peer`, `topic_id`, `pinned``

```python
MessagesUpdatePinnedForumTopic.__init__(peer: 'InputPeer', topic_id: int, pinned: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `topic_id` | `int` | `—` |
| `pinned` | `bool` | `—` |

```python
from sent.tl.functions.messages import MessagesUpdatePinnedForumTopic
```

```python
result = await client(MessagesUpdatePinnedForumTopic(...))
```

## MessagesReorderPinnedForumTopics

**Constructor ID:** `242762224`

**Fields:** ``force`, `peer`, `order``

```python
MessagesReorderPinnedForumTopics.__init__(peer: 'InputPeer', order: 'Vector', force: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `order` | `'Vector'` | `—` |
| `force` | `bool` | `None` |

```python
from sent.tl.functions.messages import MessagesReorderPinnedForumTopics
```

```python
result = await client(MessagesReorderPinnedForumTopics(...))
```

## MessagesCreateForumTopic

**Constructor ID:** `798540757`

**Fields:** ``title_missing`, `peer`, `title`, `icon_color`, `icon_emoji_id`, `random_id`, `send_as``

```python
MessagesCreateForumTopic.__init__(peer: 'InputPeer', title: str, random_id: int, title_missing: bool = None, icon_color: int = None, icon_emoji_id: int = None, send_as: 'InputPeer' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `title` | `str` | `—` |
| `random_id` | `int` | `—` |
| `title_missing` | `bool` | `None` |
| `icon_color` | `int` | `None` |
| `icon_emoji_id` | `int` | `None` |
| `send_as` | `'InputPeer'` | `None` |

```python
from sent.tl.functions.messages import MessagesCreateForumTopic
```

```python
result = await client(MessagesCreateForumTopic(...))
```

## MessagesDeleteTopicHistory

**Constructor ID:** `3531697936`

**Fields:** ``peer`, `top_msg_id``

```python
MessagesDeleteTopicHistory.__init__(peer: 'InputPeer', top_msg_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `top_msg_id` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesDeleteTopicHistory
```

```python
result = await client(MessagesDeleteTopicHistory(...))
```

## MessagesGetEmojiGameInfo

**Constructor ID:** `4219374759`

```python
MessagesGetEmojiGameInfo.__init__()
```

_No parameters._

```python
from sent.tl.functions.messages import MessagesGetEmojiGameInfo
```

```python
result = await client(MessagesGetEmojiGameInfo(...))
```

## MessagesSummarizeText

**Constructor ID:** `2881213254`

**Fields:** ``peer`, `id`, `to_lang`, `tone``

```python
MessagesSummarizeText.__init__(peer: 'InputPeer', id: int, to_lang: str = None, tone: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `int` | `—` |
| `to_lang` | `str` | `None` |
| `tone` | `str` | `None` |

```python
from sent.tl.functions.messages import MessagesSummarizeText
```

```python
result = await client(MessagesSummarizeText(...))
```

## MessagesEditChatCreator

**Constructor ID:** `4148410455`

**Fields:** ``peer`, `user_id`, `password``

```python
MessagesEditChatCreator.__init__(peer: 'InputPeer', user_id: 'InputUser', password: 'InputCheckPasswordSRP')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `user_id` | `'InputUser'` | `—` |
| `password` | `'InputCheckPasswordSRP'` | `—` |

```python
from sent.tl.functions.messages import MessagesEditChatCreator
```

```python
result = await client(MessagesEditChatCreator(...))
```

## MessagesGetFutureChatCreatorAfterLeave

**Constructor ID:** `998051494`

```python
MessagesGetFutureChatCreatorAfterLeave.__init__(peer: 'InputPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |

```python
from sent.tl.functions.messages import MessagesGetFutureChatCreatorAfterLeave
```

```python
result = await client(MessagesGetFutureChatCreatorAfterLeave(...))
```

## MessagesEditChatParticipantRank

**Constructor ID:** `2685350576`

**Fields:** ``peer`, `participant`, `rank``

```python
MessagesEditChatParticipantRank.__init__(peer: 'InputPeer', participant: 'InputPeer', rank: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `participant` | `'InputPeer'` | `—` |
| `rank` | `str` | `—` |

```python
from sent.tl.functions.messages import MessagesEditChatParticipantRank
```

```python
result = await client(MessagesEditChatParticipantRank(...))
```

## MessagesDeclineUrlAuth

**Constructor ID:** `893610940`

```python
MessagesDeclineUrlAuth.__init__(url: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `url` | `str` | `—` |

```python
from sent.tl.functions.messages import MessagesDeclineUrlAuth
```

```python
result = await client(MessagesDeclineUrlAuth(...))
```

## MessagesCheckUrlAuthMatchCode

**Constructor ID:** `3382999819`

**Fields:** ``url`, `match_code``

```python
MessagesCheckUrlAuthMatchCode.__init__(url: str, match_code: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `url` | `str` | `—` |
| `match_code` | `str` | `—` |

```python
from sent.tl.functions.messages import MessagesCheckUrlAuthMatchCode
```

```python
result = await client(MessagesCheckUrlAuthMatchCode(...))
```

## MessagesComposeMessageWithAI

**Constructor ID:** `3672950153`

**Fields:** ``proofread`, `emojify`, `text`, `translate_to_lang`, `tone``

```python
MessagesComposeMessageWithAI.__init__(text: 'TextWithEntities', proofread: bool = None, emojify: bool = None, translate_to_lang: str = None, tone: 'InputAiComposeTone' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `text` | `'TextWithEntities'` | `—` |
| `proofread` | `bool` | `None` |
| `emojify` | `bool` | `None` |
| `translate_to_lang` | `str` | `None` |
| `tone` | `'InputAiComposeTone'` | `None` |

```python
from sent.tl.functions.messages import MessagesComposeMessageWithAI
```

```python
result = await client(MessagesComposeMessageWithAI(...))
```

## MessagesReportReadMetrics

**Constructor ID:** `1080542694`

**Fields:** ``peer`, `metrics``

```python
MessagesReportReadMetrics.__init__(peer: 'InputPeer', metrics: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `metrics` | `'Vector'` | `—` |

```python
from sent.tl.functions.messages import MessagesReportReadMetrics
```

```python
result = await client(MessagesReportReadMetrics(...))
```

## MessagesReportMusicListen

**Constructor ID:** `3720140825`

**Fields:** ``id`, `listened_duration``

```python
MessagesReportMusicListen.__init__(id: 'InputDocument', listened_duration: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'InputDocument'` | `—` |
| `listened_duration` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesReportMusicListen
```

```python
result = await client(MessagesReportMusicListen(...))
```

## MessagesAddPollAnswer

**Constructor ID:** `431770477`

**Fields:** ``peer`, `msg_id`, `answer``

```python
MessagesAddPollAnswer.__init__(peer: 'InputPeer', msg_id: int, answer: 'PollAnswer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |
| `answer` | `'PollAnswer'` | `—` |

```python
from sent.tl.functions.messages import MessagesAddPollAnswer
```

```python
result = await client(MessagesAddPollAnswer(...))
```

## MessagesDeletePollAnswer

**Constructor ID:** `2894398885`

**Fields:** ``peer`, `msg_id`, `option``

```python
MessagesDeletePollAnswer.__init__(peer: 'InputPeer', msg_id: int, option: bytes)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |
| `option` | `bytes` | `—` |

```python
from sent.tl.functions.messages import MessagesDeletePollAnswer
```

```python
result = await client(MessagesDeletePollAnswer(...))
```

## MessagesGetUnreadPollVotes

**Constructor ID:** `1126722802`

**Fields:** ``peer`, `top_msg_id`, `offset_id`, `add_offset`, `limit`, `max_id`, `min_id``

```python
MessagesGetUnreadPollVotes.__init__(peer: 'InputPeer', offset_id: int, add_offset: int, limit: int, max_id: int, min_id: int, top_msg_id: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `offset_id` | `int` | `—` |
| `add_offset` | `int` | `—` |
| `limit` | `int` | `—` |
| `max_id` | `int` | `—` |
| `min_id` | `int` | `—` |
| `top_msg_id` | `int` | `None` |

```python
from sent.tl.functions.messages import MessagesGetUnreadPollVotes
```

```python
result = await client(MessagesGetUnreadPollVotes(...))
```

## MessagesReadPollVotes

**Constructor ID:** `388019416`

**Fields:** ``peer`, `top_msg_id``

```python
MessagesReadPollVotes.__init__(peer: 'InputPeer', top_msg_id: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `top_msg_id` | `int` | `None` |

```python
from sent.tl.functions.messages import MessagesReadPollVotes
```

```python
result = await client(MessagesReadPollVotes(...))
```

## MessagesSetBotGuestChatResult

**Constructor ID:** `3102803683`

**Fields:** ``query_id`, `result``

```python
MessagesSetBotGuestChatResult.__init__(query_id: int, result: 'InputBotInlineResult')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `query_id` | `int` | `—` |
| `result` | `'InputBotInlineResult'` | `—` |

```python
from sent.tl.functions.messages import MessagesSetBotGuestChatResult
```

```python
result = await client(MessagesSetBotGuestChatResult(...))
```

## MessagesDeleteParticipantReactions

**Constructor ID:** `2696416504`

**Fields:** ``peer`, `participant``

```python
MessagesDeleteParticipantReactions.__init__(peer: 'InputPeer', participant: 'InputPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `participant` | `'InputPeer'` | `—` |

```python
from sent.tl.functions.messages import MessagesDeleteParticipantReactions
```

```python
result = await client(MessagesDeleteParticipantReactions(...))
```

## MessagesDeleteParticipantReaction

**Constructor ID:** `3820484652`

**Fields:** ``peer`, `msg_id`, `participant``

```python
MessagesDeleteParticipantReaction.__init__(peer: 'InputPeer', msg_id: int, participant: 'InputPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |
| `participant` | `'InputPeer'` | `—` |

```python
from sent.tl.functions.messages import MessagesDeleteParticipantReaction
```

```python
result = await client(MessagesDeleteParticipantReaction(...))
```

## MessagesGetPersonalChannelHistory

**Constructor ID:** `1442515350`

**Fields:** ``user_id`, `limit`, `max_id`, `min_id`, `hash``

```python
MessagesGetPersonalChannelHistory.__init__(user_id: 'InputUser', limit: int, max_id: int, min_id: int, hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `user_id` | `'InputUser'` | `—` |
| `limit` | `int` | `—` |
| `max_id` | `int` | `—` |
| `min_id` | `int` | `—` |
| `hash` | `int` | `—` |

```python
from sent.tl.functions.messages import MessagesGetPersonalChannelHistory
```

```python
result = await client(MessagesGetPersonalChannelHistory(...))
```
