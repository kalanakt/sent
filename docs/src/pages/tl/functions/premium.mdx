{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Functions: `premium`

Import from `sent.tl.functions.premium`.

## Contents

- [PremiumGetBoostsList](#premiumgetboostslist)
- [PremiumGetMyBoosts](#premiumgetmyboosts)
- [PremiumApplyBoost](#premiumapplyboost)
- [PremiumGetBoostsStatus](#premiumgetboostsstatus)
- [PremiumGetUserBoosts](#premiumgetuserboosts)

## PremiumGetBoostsList

**Constructor ID:** `1626764896`

**Fields:** ``gifts`, `peer`, `offset`, `limit``

```python
PremiumGetBoostsList.__init__(peer: 'InputPeer', offset: str, limit: int, gifts: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `offset` | `str` | `—` |
| `limit` | `int` | `—` |
| `gifts` | `bool` | `None` |

```python
from sent.tl.functions.premium import PremiumGetBoostsList
```

```python
result = await client(PremiumGetBoostsList(...))
```

## PremiumGetMyBoosts

**Constructor ID:** `199719754`

```python
PremiumGetMyBoosts.__init__()
```

_No parameters._

```python
from sent.tl.functions.premium import PremiumGetMyBoosts
```

```python
result = await client(PremiumGetMyBoosts(...))
```

## PremiumApplyBoost

**Constructor ID:** `1803396934`

**Fields:** ``slots`, `peer``

```python
PremiumApplyBoost.__init__(peer: 'InputPeer', slots: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `slots` | `'Vector'` | `None` |

```python
from sent.tl.functions.premium import PremiumApplyBoost
```

```python
result = await client(PremiumApplyBoost(...))
```

## PremiumGetBoostsStatus

**Constructor ID:** `70197089`

```python
PremiumGetBoostsStatus.__init__(peer: 'InputPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |

```python
from sent.tl.functions.premium import PremiumGetBoostsStatus
```

```python
result = await client(PremiumGetBoostsStatus(...))
```

## PremiumGetUserBoosts

**Constructor ID:** `965037343`

**Fields:** ``peer`, `user_id``

```python
PremiumGetUserBoosts.__init__(peer: 'InputPeer', user_id: 'InputUser')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `user_id` | `'InputUser'` | `—` |

```python
from sent.tl.functions.premium import PremiumGetUserBoosts
```

```python
result = await client(PremiumGetUserBoosts(...))
```
