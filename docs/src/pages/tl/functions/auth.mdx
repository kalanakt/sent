{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Functions: `auth`

Import from `sent.tl.functions.auth`.

## Contents

- [AuthSendCode](#authsendcode)
- [AuthSignUp](#authsignup)
- [AuthSignIn](#authsignin)
- [AuthLogOut](#authlogout)
- [AuthResetAuthorizations](#authresetauthorizations)
- [AuthExportAuthorization](#authexportauthorization)
- [AuthImportAuthorization](#authimportauthorization)
- [AuthBindTempAuthKey](#authbindtempauthkey)
- [AuthImportBotAuthorization](#authimportbotauthorization)
- [AuthCheckPassword](#authcheckpassword)
- [AuthRequestPasswordRecovery](#authrequestpasswordrecovery)
- [AuthRecoverPassword](#authrecoverpassword)
- [AuthResendCode](#authresendcode)
- [AuthCancelCode](#authcancelcode)
- [AuthDropTempAuthKeys](#authdroptempauthkeys)
- [AuthExportLoginToken](#authexportlogintoken)
- [AuthImportLoginToken](#authimportlogintoken)
- [AuthAcceptLoginToken](#authacceptlogintoken)
- [AuthCheckRecoveryPassword](#authcheckrecoverypassword)
- [AuthImportWebTokenAuthorization](#authimportwebtokenauthorization)
- [AuthRequestFirebaseSms](#authrequestfirebasesms)
- [AuthResetLoginEmail](#authresetloginemail)
- [AuthReportMissingCode](#authreportmissingcode)
- [AuthCheckPaidAuth](#authcheckpaidauth)
- [AuthInitPasskeyLogin](#authinitpasskeylogin)
- [AuthFinishPasskeyLogin](#authfinishpasskeylogin)

## AuthSendCode

**Constructor ID:** `2792825935`

**Fields:** ``phone_number`, `api_id`, `api_hash`, `settings``

```python
AuthSendCode.__init__(phone_number: str, api_id: int, api_hash: str, settings: 'CodeSettings')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `phone_number` | `str` | `—` |
| `api_id` | `int` | `—` |
| `api_hash` | `str` | `—` |
| `settings` | `'CodeSettings'` | `—` |

```python
from sent.tl.functions.auth import AuthSendCode
```

```python
result = await client(AuthSendCode(...))
```

## AuthSignUp

**Constructor ID:** `2865215255`

**Fields:** ``no_joined_notifications`, `phone_number`, `phone_code_hash`, `first_name`, `last_name``

```python
AuthSignUp.__init__(phone_number: str, phone_code_hash: str, first_name: str, last_name: str, no_joined_notifications: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `phone_number` | `str` | `—` |
| `phone_code_hash` | `str` | `—` |
| `first_name` | `str` | `—` |
| `last_name` | `str` | `—` |
| `no_joined_notifications` | `bool` | `None` |

```python
from sent.tl.functions.auth import AuthSignUp
```

```python
result = await client(AuthSignUp(...))
```

## AuthSignIn

**Constructor ID:** `2371004753`

**Fields:** ``phone_number`, `phone_code_hash`, `phone_code`, `email_verification``

```python
AuthSignIn.__init__(phone_number: str, phone_code_hash: str, phone_code: str = None, email_verification: 'EmailVerification' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `phone_number` | `str` | `—` |
| `phone_code_hash` | `str` | `—` |
| `phone_code` | `str` | `None` |
| `email_verification` | `'EmailVerification'` | `None` |

```python
from sent.tl.functions.auth import AuthSignIn
```

```python
result = await client(AuthSignIn(...))
```

## AuthLogOut

**Constructor ID:** `1047706137`

```python
AuthLogOut.__init__()
```

_No parameters._

```python
from sent.tl.functions.auth import AuthLogOut
```

```python
result = await client(AuthLogOut(...))
```

## AuthResetAuthorizations

**Constructor ID:** `2678787354`

```python
AuthResetAuthorizations.__init__()
```

_No parameters._

```python
from sent.tl.functions.auth import AuthResetAuthorizations
```

```python
result = await client(AuthResetAuthorizations(...))
```

## AuthExportAuthorization

**Constructor ID:** `3854565325`

```python
AuthExportAuthorization.__init__(dc_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `dc_id` | `int` | `—` |

```python
from sent.tl.functions.auth import AuthExportAuthorization
```

```python
result = await client(AuthExportAuthorization(...))
```

## AuthImportAuthorization

**Constructor ID:** `2776268205`

**Fields:** ``id`, `bytes``

```python
AuthImportAuthorization.__init__(id: int, bytes: bytes)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `int` | `—` |
| `bytes` | `bytes` | `—` |

```python
from sent.tl.functions.auth import AuthImportAuthorization
```

```python
result = await client(AuthImportAuthorization(...))
```

## AuthBindTempAuthKey

**Constructor ID:** `3453233669`

**Fields:** ``perm_auth_key_id`, `nonce`, `expires_at`, `encrypted_message``

```python
AuthBindTempAuthKey.__init__(perm_auth_key_id: int, nonce: int, expires_at: int, encrypted_message: bytes)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `perm_auth_key_id` | `int` | `—` |
| `nonce` | `int` | `—` |
| `expires_at` | `int` | `—` |
| `encrypted_message` | `bytes` | `—` |

```python
from sent.tl.functions.auth import AuthBindTempAuthKey
```

```python
result = await client(AuthBindTempAuthKey(...))
```

## AuthImportBotAuthorization

**Constructor ID:** `1738800940`

**Fields:** ``flags`, `api_id`, `api_hash`, `bot_auth_token``

```python
AuthImportBotAuthorization.__init__(flags: int, api_id: int, api_hash: str, bot_auth_token: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `flags` | `int` | `—` |
| `api_id` | `int` | `—` |
| `api_hash` | `str` | `—` |
| `bot_auth_token` | `str` | `—` |

```python
from sent.tl.functions.auth import AuthImportBotAuthorization
```

```python
result = await client(AuthImportBotAuthorization(...))
```

## AuthCheckPassword

**Constructor ID:** `3515567382`

```python
AuthCheckPassword.__init__(password: 'InputCheckPasswordSRP')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `password` | `'InputCheckPasswordSRP'` | `—` |

```python
from sent.tl.functions.auth import AuthCheckPassword
```

```python
result = await client(AuthCheckPassword(...))
```

## AuthRequestPasswordRecovery

**Constructor ID:** `3633822822`

```python
AuthRequestPasswordRecovery.__init__()
```

_No parameters._

```python
from sent.tl.functions.auth import AuthRequestPasswordRecovery
```

```python
result = await client(AuthRequestPasswordRecovery(...))
```

## AuthRecoverPassword

**Constructor ID:** `923364464`

**Fields:** ``code`, `new_settings``

```python
AuthRecoverPassword.__init__(code: str, new_settings: 'AccountPasswordInputSettings' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `code` | `str` | `—` |
| `new_settings` | `'AccountPasswordInputSettings'` | `None` |

```python
from sent.tl.functions.auth import AuthRecoverPassword
```

```python
result = await client(AuthRecoverPassword(...))
```

## AuthResendCode

**Constructor ID:** `3403969827`

**Fields:** ``phone_number`, `phone_code_hash`, `reason``

```python
AuthResendCode.__init__(phone_number: str, phone_code_hash: str, reason: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `phone_number` | `str` | `—` |
| `phone_code_hash` | `str` | `—` |
| `reason` | `str` | `None` |

```python
from sent.tl.functions.auth import AuthResendCode
```

```python
result = await client(AuthResendCode(...))
```

## AuthCancelCode

**Constructor ID:** `520357240`

**Fields:** ``phone_number`, `phone_code_hash``

```python
AuthCancelCode.__init__(phone_number: str, phone_code_hash: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `phone_number` | `str` | `—` |
| `phone_code_hash` | `str` | `—` |

```python
from sent.tl.functions.auth import AuthCancelCode
```

```python
result = await client(AuthCancelCode(...))
```

## AuthDropTempAuthKeys

**Constructor ID:** `2387124616`

```python
AuthDropTempAuthKeys.__init__(except_auth_keys: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `except_auth_keys` | `'Vector'` | `—` |

```python
from sent.tl.functions.auth import AuthDropTempAuthKeys
```

```python
result = await client(AuthDropTempAuthKeys(...))
```

## AuthExportLoginToken

**Constructor ID:** `3084944894`

**Fields:** ``api_id`, `api_hash`, `except_ids``

```python
AuthExportLoginToken.__init__(api_id: int, api_hash: str, except_ids: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `api_id` | `int` | `—` |
| `api_hash` | `str` | `—` |
| `except_ids` | `'Vector'` | `—` |

```python
from sent.tl.functions.auth import AuthExportLoginToken
```

```python
result = await client(AuthExportLoginToken(...))
```

## AuthImportLoginToken

**Constructor ID:** `2511101156`

```python
AuthImportLoginToken.__init__(token: bytes)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `token` | `bytes` | `—` |

```python
from sent.tl.functions.auth import AuthImportLoginToken
```

```python
result = await client(AuthImportLoginToken(...))
```

## AuthAcceptLoginToken

**Constructor ID:** `3902057805`

```python
AuthAcceptLoginToken.__init__(token: bytes)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `token` | `bytes` | `—` |

```python
from sent.tl.functions.auth import AuthAcceptLoginToken
```

```python
result = await client(AuthAcceptLoginToken(...))
```

## AuthCheckRecoveryPassword

**Constructor ID:** `221691769`

```python
AuthCheckRecoveryPassword.__init__(code: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `code` | `str` | `—` |

```python
from sent.tl.functions.auth import AuthCheckRecoveryPassword
```

```python
result = await client(AuthCheckRecoveryPassword(...))
```

## AuthImportWebTokenAuthorization

**Constructor ID:** `767062953`

**Fields:** ``api_id`, `api_hash`, `web_auth_token``

```python
AuthImportWebTokenAuthorization.__init__(api_id: int, api_hash: str, web_auth_token: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `api_id` | `int` | `—` |
| `api_hash` | `str` | `—` |
| `web_auth_token` | `str` | `—` |

```python
from sent.tl.functions.auth import AuthImportWebTokenAuthorization
```

```python
result = await client(AuthImportWebTokenAuthorization(...))
```

## AuthRequestFirebaseSms

**Constructor ID:** `2386109982`

**Fields:** ``phone_number`, `phone_code_hash`, `safety_net_token`, `play_integrity_token`, `ios_push_secret``

```python
AuthRequestFirebaseSms.__init__(phone_number: str, phone_code_hash: str, safety_net_token: str = None, play_integrity_token: str = None, ios_push_secret: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `phone_number` | `str` | `—` |
| `phone_code_hash` | `str` | `—` |
| `safety_net_token` | `str` | `None` |
| `play_integrity_token` | `str` | `None` |
| `ios_push_secret` | `str` | `None` |

```python
from sent.tl.functions.auth import AuthRequestFirebaseSms
```

```python
result = await client(AuthRequestFirebaseSms(...))
```

## AuthResetLoginEmail

**Constructor ID:** `2123760019`

**Fields:** ``phone_number`, `phone_code_hash``

```python
AuthResetLoginEmail.__init__(phone_number: str, phone_code_hash: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `phone_number` | `str` | `—` |
| `phone_code_hash` | `str` | `—` |

```python
from sent.tl.functions.auth import AuthResetLoginEmail
```

```python
result = await client(AuthResetLoginEmail(...))
```

## AuthReportMissingCode

**Constructor ID:** `3416125430`

**Fields:** ``phone_number`, `phone_code_hash`, `mnc``

```python
AuthReportMissingCode.__init__(phone_number: str, phone_code_hash: str, mnc: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `phone_number` | `str` | `—` |
| `phone_code_hash` | `str` | `—` |
| `mnc` | `str` | `—` |

```python
from sent.tl.functions.auth import AuthReportMissingCode
```

```python
result = await client(AuthReportMissingCode(...))
```

## AuthCheckPaidAuth

**Constructor ID:** `1457889180`

**Fields:** ``phone_number`, `phone_code_hash`, `form_id``

```python
AuthCheckPaidAuth.__init__(phone_number: str, phone_code_hash: str, form_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `phone_number` | `str` | `—` |
| `phone_code_hash` | `str` | `—` |
| `form_id` | `int` | `—` |

```python
from sent.tl.functions.auth import AuthCheckPaidAuth
```

```python
result = await client(AuthCheckPaidAuth(...))
```

## AuthInitPasskeyLogin

**Constructor ID:** `1368051895`

**Fields:** ``api_id`, `api_hash``

```python
AuthInitPasskeyLogin.__init__(api_id: int, api_hash: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `api_id` | `int` | `—` |
| `api_hash` | `str` | `—` |

```python
from sent.tl.functions.auth import AuthInitPasskeyLogin
```

```python
result = await client(AuthInitPasskeyLogin(...))
```

## AuthFinishPasskeyLogin

**Constructor ID:** `2555882759`

**Fields:** ``credential`, `from_dc_id`, `from_auth_key_id``

```python
AuthFinishPasskeyLogin.__init__(credential: 'InputPasskeyCredential', from_dc_id: int = None, from_auth_key_id: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `credential` | `'InputPasskeyCredential'` | `—` |
| `from_dc_id` | `int` | `None` |
| `from_auth_key_id` | `int` | `None` |

```python
from sent.tl.functions.auth import AuthFinishPasskeyLogin
```

```python
result = await client(AuthFinishPasskeyLogin(...))
```
