{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Functions: `bots`

Import from `sent.tl.functions.bots`.

## Contents

- [BotsSendCustomRequest](#botssendcustomrequest)
- [BotsAnswerWebhookJSONQuery](#botsanswerwebhookjsonquery)
- [BotsSetBotCommands](#botssetbotcommands)
- [BotsResetBotCommands](#botsresetbotcommands)
- [BotsGetBotCommands](#botsgetbotcommands)
- [BotsSetBotMenuButton](#botssetbotmenubutton)
- [BotsGetBotMenuButton](#botsgetbotmenubutton)
- [BotsSetBotBroadcastDefaultAdminRights](#botssetbotbroadcastdefaultadminrights)
- [BotsSetBotGroupDefaultAdminRights](#botssetbotgroupdefaultadminrights)
- [BotsSetBotInfo](#botssetbotinfo)
- [BotsGetBotInfo](#botsgetbotinfo)
- [BotsReorderUsernames](#botsreorderusernames)
- [BotsToggleUsername](#botstoggleusername)
- [BotsCanSendMessage](#botscansendmessage)
- [BotsAllowSendMessage](#botsallowsendmessage)
- [BotsInvokeWebViewCustomMethod](#botsinvokewebviewcustommethod)
- [BotsGetPopularAppBots](#botsgetpopularappbots)
- [BotsAddPreviewMedia](#botsaddpreviewmedia)
- [BotsEditPreviewMedia](#botseditpreviewmedia)
- [BotsDeletePreviewMedia](#botsdeletepreviewmedia)
- [BotsReorderPreviewMedias](#botsreorderpreviewmedias)
- [BotsGetPreviewInfo](#botsgetpreviewinfo)
- [BotsGetPreviewMedias](#botsgetpreviewmedias)
- [BotsUpdateUserEmojiStatus](#botsupdateuseremojistatus)
- [BotsToggleUserEmojiStatusPermission](#botstoggleuseremojistatuspermission)
- [BotsCheckDownloadFileParams](#botscheckdownloadfileparams)
- [BotsGetAdminedBots](#botsgetadminedbots)
- [BotsUpdateStarRefProgram](#botsupdatestarrefprogram)
- [BotsSetCustomVerification](#botssetcustomverification)
- [BotsGetBotRecommendations](#botsgetbotrecommendations)
- [BotsCheckUsername](#botscheckusername)
- [BotsCreateBot](#botscreatebot)
- [BotsExportBotToken](#botsexportbottoken)
- [BotsRequestWebViewButton](#botsrequestwebviewbutton)
- [BotsGetRequestedWebViewButton](#botsgetrequestedwebviewbutton)
- [BotsGetAccessSettings](#botsgetaccesssettings)
- [BotsEditAccessSettings](#botseditaccesssettings)

## BotsSendCustomRequest

**Constructor ID:** `2854709741`

**Fields:** ``custom_method`, `params``

```python
BotsSendCustomRequest.__init__(custom_method: str, params: 'DataJSON')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `custom_method` | `str` | `—` |
| `params` | `'DataJSON'` | `—` |

```python
from sent.tl.functions.bots import BotsSendCustomRequest
```

```python
result = await client(BotsSendCustomRequest(...))
```

## BotsAnswerWebhookJSONQuery

**Constructor ID:** `3860938573`

**Fields:** ``query_id`, `data``

```python
BotsAnswerWebhookJSONQuery.__init__(query_id: int, data: 'DataJSON')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `query_id` | `int` | `—` |
| `data` | `'DataJSON'` | `—` |

```python
from sent.tl.functions.bots import BotsAnswerWebhookJSONQuery
```

```python
result = await client(BotsAnswerWebhookJSONQuery(...))
```

## BotsSetBotCommands

**Constructor ID:** `85399130`

**Fields:** ``scope`, `lang_code`, `commands``

```python
BotsSetBotCommands.__init__(scope: 'BotCommandScope', lang_code: str, commands: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `scope` | `'BotCommandScope'` | `—` |
| `lang_code` | `str` | `—` |
| `commands` | `'Vector'` | `—` |

```python
from sent.tl.functions.bots import BotsSetBotCommands
```

```python
result = await client(BotsSetBotCommands(...))
```

## BotsResetBotCommands

**Constructor ID:** `1032708345`

**Fields:** ``scope`, `lang_code``

```python
BotsResetBotCommands.__init__(scope: 'BotCommandScope', lang_code: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `scope` | `'BotCommandScope'` | `—` |
| `lang_code` | `str` | `—` |

```python
from sent.tl.functions.bots import BotsResetBotCommands
```

```python
result = await client(BotsResetBotCommands(...))
```

## BotsGetBotCommands

**Constructor ID:** `3813412310`

**Fields:** ``scope`, `lang_code``

```python
BotsGetBotCommands.__init__(scope: 'BotCommandScope', lang_code: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `scope` | `'BotCommandScope'` | `—` |
| `lang_code` | `str` | `—` |

```python
from sent.tl.functions.bots import BotsGetBotCommands
```

```python
result = await client(BotsGetBotCommands(...))
```

## BotsSetBotMenuButton

**Constructor ID:** `1157944655`

**Fields:** ``user_id`, `button``

```python
BotsSetBotMenuButton.__init__(user_id: 'InputUser', button: 'BotMenuButton')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `user_id` | `'InputUser'` | `—` |
| `button` | `'BotMenuButton'` | `—` |

```python
from sent.tl.functions.bots import BotsSetBotMenuButton
```

```python
result = await client(BotsSetBotMenuButton(...))
```

## BotsGetBotMenuButton

**Constructor ID:** `2623597352`

```python
BotsGetBotMenuButton.__init__(user_id: 'InputUser')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `user_id` | `'InputUser'` | `—` |

```python
from sent.tl.functions.bots import BotsGetBotMenuButton
```

```python
result = await client(BotsGetBotMenuButton(...))
```

## BotsSetBotBroadcastDefaultAdminRights

**Constructor ID:** `2021942497`

```python
BotsSetBotBroadcastDefaultAdminRights.__init__(admin_rights: 'ChatAdminRights')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `admin_rights` | `'ChatAdminRights'` | `—` |

```python
from sent.tl.functions.bots import BotsSetBotBroadcastDefaultAdminRights
```

```python
result = await client(BotsSetBotBroadcastDefaultAdminRights(...))
```

## BotsSetBotGroupDefaultAdminRights

**Constructor ID:** `2455685610`

```python
BotsSetBotGroupDefaultAdminRights.__init__(admin_rights: 'ChatAdminRights')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `admin_rights` | `'ChatAdminRights'` | `—` |

```python
from sent.tl.functions.bots import BotsSetBotGroupDefaultAdminRights
```

```python
result = await client(BotsSetBotGroupDefaultAdminRights(...))
```

## BotsSetBotInfo

**Constructor ID:** `282013987`

**Fields:** ``bot`, `lang_code`, `name`, `about`, `description``

```python
BotsSetBotInfo.__init__(lang_code: str, bot: 'InputUser' = None, name: str = None, about: str = None, description: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `lang_code` | `str` | `—` |
| `bot` | `'InputUser'` | `None` |
| `name` | `str` | `None` |
| `about` | `str` | `None` |
| `description` | `str` | `None` |

```python
from sent.tl.functions.bots import BotsSetBotInfo
```

```python
result = await client(BotsSetBotInfo(...))
```

## BotsGetBotInfo

**Constructor ID:** `3705214205`

**Fields:** ``bot`, `lang_code``

```python
BotsGetBotInfo.__init__(lang_code: str, bot: 'InputUser' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `lang_code` | `str` | `—` |
| `bot` | `'InputUser'` | `None` |

```python
from sent.tl.functions.bots import BotsGetBotInfo
```

```python
result = await client(BotsGetBotInfo(...))
```

## BotsReorderUsernames

**Constructor ID:** `2533994946`

**Fields:** ``bot`, `order``

```python
BotsReorderUsernames.__init__(bot: 'InputUser', order: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |
| `order` | `'Vector'` | `—` |

```python
from sent.tl.functions.bots import BotsReorderUsernames
```

```python
result = await client(BotsReorderUsernames(...))
```

## BotsToggleUsername

**Constructor ID:** `87861619`

**Fields:** ``bot`, `username`, `active``

```python
BotsToggleUsername.__init__(bot: 'InputUser', username: str, active: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |
| `username` | `str` | `—` |
| `active` | `bool` | `—` |

```python
from sent.tl.functions.bots import BotsToggleUsername
```

```python
result = await client(BotsToggleUsername(...))
```

## BotsCanSendMessage

**Constructor ID:** `324662502`

```python
BotsCanSendMessage.__init__(bot: 'InputUser')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |

```python
from sent.tl.functions.bots import BotsCanSendMessage
```

```python
result = await client(BotsCanSendMessage(...))
```

## BotsAllowSendMessage

**Constructor ID:** `4046644207`

```python
BotsAllowSendMessage.__init__(bot: 'InputUser')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |

```python
from sent.tl.functions.bots import BotsAllowSendMessage
```

```python
result = await client(BotsAllowSendMessage(...))
```

## BotsInvokeWebViewCustomMethod

**Constructor ID:** `142591463`

**Fields:** ``bot`, `custom_method`, `params``

```python
BotsInvokeWebViewCustomMethod.__init__(bot: 'InputUser', custom_method: str, params: 'DataJSON')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |
| `custom_method` | `str` | `—` |
| `params` | `'DataJSON'` | `—` |

```python
from sent.tl.functions.bots import BotsInvokeWebViewCustomMethod
```

```python
result = await client(BotsInvokeWebViewCustomMethod(...))
```

## BotsGetPopularAppBots

**Constructor ID:** `3260088722`

**Fields:** ``offset`, `limit``

```python
BotsGetPopularAppBots.__init__(offset: str, limit: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `offset` | `str` | `—` |
| `limit` | `int` | `—` |

```python
from sent.tl.functions.bots import BotsGetPopularAppBots
```

```python
result = await client(BotsGetPopularAppBots(...))
```

## BotsAddPreviewMedia

**Constructor ID:** `397326170`

**Fields:** ``bot`, `lang_code`, `media``

```python
BotsAddPreviewMedia.__init__(bot: 'InputUser', lang_code: str, media: 'InputMedia')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |
| `lang_code` | `str` | `—` |
| `media` | `'InputMedia'` | `—` |

```python
from sent.tl.functions.bots import BotsAddPreviewMedia
```

```python
result = await client(BotsAddPreviewMedia(...))
```

## BotsEditPreviewMedia

**Constructor ID:** `2233819247`

**Fields:** ``bot`, `lang_code`, `media`, `new_media``

```python
BotsEditPreviewMedia.__init__(bot: 'InputUser', lang_code: str, media: 'InputMedia', new_media: 'InputMedia')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |
| `lang_code` | `str` | `—` |
| `media` | `'InputMedia'` | `—` |
| `new_media` | `'InputMedia'` | `—` |

```python
from sent.tl.functions.bots import BotsEditPreviewMedia
```

```python
result = await client(BotsEditPreviewMedia(...))
```

## BotsDeletePreviewMedia

**Constructor ID:** `755054003`

**Fields:** ``bot`, `lang_code`, `media``

```python
BotsDeletePreviewMedia.__init__(bot: 'InputUser', lang_code: str, media: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |
| `lang_code` | `str` | `—` |
| `media` | `'Vector'` | `—` |

```python
from sent.tl.functions.bots import BotsDeletePreviewMedia
```

```python
result = await client(BotsDeletePreviewMedia(...))
```

## BotsReorderPreviewMedias

**Constructor ID:** `3056071594`

**Fields:** ``bot`, `lang_code`, `order``

```python
BotsReorderPreviewMedias.__init__(bot: 'InputUser', lang_code: str, order: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |
| `lang_code` | `str` | `—` |
| `order` | `'Vector'` | `—` |

```python
from sent.tl.functions.bots import BotsReorderPreviewMedias
```

```python
result = await client(BotsReorderPreviewMedias(...))
```

## BotsGetPreviewInfo

**Constructor ID:** `1111143341`

**Fields:** ``bot`, `lang_code``

```python
BotsGetPreviewInfo.__init__(bot: 'InputUser', lang_code: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |
| `lang_code` | `str` | `—` |

```python
from sent.tl.functions.bots import BotsGetPreviewInfo
```

```python
result = await client(BotsGetPreviewInfo(...))
```

## BotsGetPreviewMedias

**Constructor ID:** `2728745293`

```python
BotsGetPreviewMedias.__init__(bot: 'InputUser')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |

```python
from sent.tl.functions.bots import BotsGetPreviewMedias
```

```python
result = await client(BotsGetPreviewMedias(...))
```

## BotsUpdateUserEmojiStatus

**Constructor ID:** `3986632901`

**Fields:** ``user_id`, `emoji_status``

```python
BotsUpdateUserEmojiStatus.__init__(user_id: 'InputUser', emoji_status: 'EmojiStatus')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `user_id` | `'InputUser'` | `—` |
| `emoji_status` | `'EmojiStatus'` | `—` |

```python
from sent.tl.functions.bots import BotsUpdateUserEmojiStatus
```

```python
result = await client(BotsUpdateUserEmojiStatus(...))
```

## BotsToggleUserEmojiStatusPermission

**Constructor ID:** `115237778`

**Fields:** ``bot`, `enabled``

```python
BotsToggleUserEmojiStatusPermission.__init__(bot: 'InputUser', enabled: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |
| `enabled` | `bool` | `—` |

```python
from sent.tl.functions.bots import BotsToggleUserEmojiStatusPermission
```

```python
result = await client(BotsToggleUserEmojiStatusPermission(...))
```

## BotsCheckDownloadFileParams

**Constructor ID:** `1342666121`

**Fields:** ``bot`, `file_name`, `url``

```python
BotsCheckDownloadFileParams.__init__(bot: 'InputUser', file_name: str, url: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |
| `file_name` | `str` | `—` |
| `url` | `str` | `—` |

```python
from sent.tl.functions.bots import BotsCheckDownloadFileParams
```

```python
result = await client(BotsCheckDownloadFileParams(...))
```

## BotsGetAdminedBots

**Constructor ID:** `2960203139`

```python
BotsGetAdminedBots.__init__()
```

_No parameters._

```python
from sent.tl.functions.bots import BotsGetAdminedBots
```

```python
result = await client(BotsGetAdminedBots(...))
```

## BotsUpdateStarRefProgram

**Constructor ID:** `2005621427`

**Fields:** ``bot`, `commission_permille`, `duration_months``

```python
BotsUpdateStarRefProgram.__init__(bot: 'InputUser', commission_permille: int, duration_months: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |
| `commission_permille` | `int` | `—` |
| `duration_months` | `int` | `None` |

```python
from sent.tl.functions.bots import BotsUpdateStarRefProgram
```

```python
result = await client(BotsUpdateStarRefProgram(...))
```

## BotsSetCustomVerification

**Constructor ID:** `2341068733`

**Fields:** ``enabled`, `bot`, `peer`, `custom_description``

```python
BotsSetCustomVerification.__init__(peer: 'InputPeer', enabled: bool = None, bot: 'InputUser' = None, custom_description: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `enabled` | `bool` | `None` |
| `bot` | `'InputUser'` | `None` |
| `custom_description` | `str` | `None` |

```python
from sent.tl.functions.bots import BotsSetCustomVerification
```

```python
result = await client(BotsSetCustomVerification(...))
```

## BotsGetBotRecommendations

**Constructor ID:** `2713126933`

```python
BotsGetBotRecommendations.__init__(bot: 'InputUser')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |

```python
from sent.tl.functions.bots import BotsGetBotRecommendations
```

```python
result = await client(BotsGetBotRecommendations(...))
```

## BotsCheckUsername

**Constructor ID:** `2280792475`

```python
BotsCheckUsername.__init__(username: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `username` | `str` | `—` |

```python
from sent.tl.functions.bots import BotsCheckUsername
```

```python
result = await client(BotsCheckUsername(...))
```

## BotsCreateBot

**Constructor ID:** `3853614891`

**Fields:** ``via_deeplink`, `name`, `username`, `manager_id``

```python
BotsCreateBot.__init__(name: str, username: str, manager_id: 'InputUser', via_deeplink: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `name` | `str` | `—` |
| `username` | `str` | `—` |
| `manager_id` | `'InputUser'` | `—` |
| `via_deeplink` | `bool` | `None` |

```python
from sent.tl.functions.bots import BotsCreateBot
```

```python
result = await client(BotsCreateBot(...))
```

## BotsExportBotToken

**Constructor ID:** `3171785195`

**Fields:** ``bot`, `revoke``

```python
BotsExportBotToken.__init__(bot: 'InputUser', revoke: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |
| `revoke` | `bool` | `—` |

```python
from sent.tl.functions.bots import BotsExportBotToken
```

```python
result = await client(BotsExportBotToken(...))
```

## BotsRequestWebViewButton

**Constructor ID:** `832742238`

**Fields:** ``user_id`, `button``

```python
BotsRequestWebViewButton.__init__(user_id: 'InputUser', button: 'KeyboardButton')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `user_id` | `'InputUser'` | `—` |
| `button` | `'KeyboardButton'` | `—` |

```python
from sent.tl.functions.bots import BotsRequestWebViewButton
```

```python
result = await client(BotsRequestWebViewButton(...))
```

## BotsGetRequestedWebViewButton

**Constructor ID:** `3206920179`

**Fields:** ``bot`, `webapp_req_id``

```python
BotsGetRequestedWebViewButton.__init__(bot: 'InputUser', webapp_req_id: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |
| `webapp_req_id` | `str` | `—` |

```python
from sent.tl.functions.bots import BotsGetRequestedWebViewButton
```

```python
result = await client(BotsGetRequestedWebViewButton(...))
```

## BotsGetAccessSettings

**Constructor ID:** `557339555`

```python
BotsGetAccessSettings.__init__(bot: 'InputUser')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |

```python
from sent.tl.functions.bots import BotsGetAccessSettings
```

```python
result = await client(BotsGetAccessSettings(...))
```

## BotsEditAccessSettings

**Constructor ID:** `830553304`

**Fields:** ``restricted`, `bot`, `add_users``

```python
BotsEditAccessSettings.__init__(bot: 'InputUser', restricted: bool = None, add_users: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bot` | `'InputUser'` | `—` |
| `restricted` | `bool` | `None` |
| `add_users` | `'Vector'` | `None` |

```python
from sent.tl.functions.bots import BotsEditAccessSettings
```

```python
result = await client(BotsEditAccessSettings(...))
```
