{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Functions: `channels`

Import from `sent.tl.functions.channels`.

## Contents

- [ChannelsReadHistory](#channelsreadhistory)
- [ChannelsDeleteMessages](#channelsdeletemessages)
- [ChannelsReportSpam](#channelsreportspam)
- [ChannelsGetMessages](#channelsgetmessages)
- [ChannelsGetParticipants](#channelsgetparticipants)
- [ChannelsGetParticipant](#channelsgetparticipant)
- [ChannelsGetChannels](#channelsgetchannels)
- [ChannelsGetFullChannel](#channelsgetfullchannel)
- [ChannelsCreateChannel](#channelscreatechannel)
- [ChannelsEditAdmin](#channelseditadmin)
- [ChannelsEditTitle](#channelsedittitle)
- [ChannelsEditPhoto](#channelseditphoto)
- [ChannelsCheckUsername](#channelscheckusername)
- [ChannelsUpdateUsername](#channelsupdateusername)
- [ChannelsJoinChannel](#channelsjoinchannel)
- [ChannelsLeaveChannel](#channelsleavechannel)
- [ChannelsInviteToChannel](#channelsinvitetochannel)
- [ChannelsDeleteChannel](#channelsdeletechannel)
- [ChannelsExportMessageLink](#channelsexportmessagelink)
- [ChannelsToggleSignatures](#channelstogglesignatures)
- [ChannelsGetAdminedPublicChannels](#channelsgetadminedpublicchannels)
- [ChannelsEditBanned](#channelseditbanned)
- [ChannelsGetAdminLog](#channelsgetadminlog)
- [ChannelsSetStickers](#channelssetstickers)
- [ChannelsReadMessageContents](#channelsreadmessagecontents)
- [ChannelsDeleteHistory](#channelsdeletehistory)
- [ChannelsTogglePreHistoryHidden](#channelstoggleprehistoryhidden)
- [ChannelsGetLeftChannels](#channelsgetleftchannels)
- [ChannelsGetGroupsForDiscussion](#channelsgetgroupsfordiscussion)
- [ChannelsSetDiscussionGroup](#channelssetdiscussiongroup)
- [ChannelsEditLocation](#channelseditlocation)
- [ChannelsToggleSlowMode](#channelstoggleslowmode)
- [ChannelsGetInactiveChannels](#channelsgetinactivechannels)
- [ChannelsConvertToGigagroup](#channelsconverttogigagroup)
- [ChannelsGetSendAs](#channelsgetsendas)
- [ChannelsDeleteParticipantHistory](#channelsdeleteparticipanthistory)
- [ChannelsToggleJoinToSend](#channelstogglejointosend)
- [ChannelsToggleJoinRequest](#channelstogglejoinrequest)
- [ChannelsReorderUsernames](#channelsreorderusernames)
- [ChannelsToggleUsername](#channelstoggleusername)
- [ChannelsDeactivateAllUsernames](#channelsdeactivateallusernames)
- [ChannelsToggleForum](#channelstoggleforum)
- [ChannelsToggleAntiSpam](#channelstoggleantispam)
- [ChannelsReportAntiSpamFalsePositive](#channelsreportantispamfalsepositive)
- [ChannelsToggleParticipantsHidden](#channelstoggleparticipantshidden)
- [ChannelsUpdateColor](#channelsupdatecolor)
- [ChannelsToggleViewForumAsMessages](#channelstoggleviewforumasmessages)
- [ChannelsGetChannelRecommendations](#channelsgetchannelrecommendations)
- [ChannelsUpdateEmojiStatus](#channelsupdateemojistatus)
- [ChannelsSetBoostsToUnblockRestrictions](#channelssetbooststounblockrestrictions)
- [ChannelsSetEmojiStickers](#channelssetemojistickers)
- [ChannelsRestrictSponsoredMessages](#channelsrestrictsponsoredmessages)
- [ChannelsSearchPosts](#channelssearchposts)
- [ChannelsUpdatePaidMessagesPrice](#channelsupdatepaidmessagesprice)
- [ChannelsToggleAutotranslation](#channelstoggleautotranslation)
- [ChannelsGetMessageAuthor](#channelsgetmessageauthor)
- [ChannelsCheckSearchPostsFlood](#channelschecksearchpostsflood)
- [ChannelsSetMainProfileTab](#channelssetmainprofiletab)

## ChannelsReadHistory

**Constructor ID:** `3423619383`

**Fields:** ``channel`, `max_id``

```python
ChannelsReadHistory.__init__(channel: 'InputChannel', max_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `max_id` | `int` | `—` |

```python
from sent.tl.functions.channels import ChannelsReadHistory
```

```python
result = await client(ChannelsReadHistory(...))
```

## ChannelsDeleteMessages

**Constructor ID:** `2227305806`

**Fields:** ``channel`, `id``

```python
ChannelsDeleteMessages.__init__(channel: 'InputChannel', id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.channels import ChannelsDeleteMessages
```

```python
result = await client(ChannelsDeleteMessages(...))
```

## ChannelsReportSpam

**Constructor ID:** `4098523925`

**Fields:** ``channel`, `participant`, `id``

```python
ChannelsReportSpam.__init__(channel: 'InputChannel', participant: 'InputPeer', id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `participant` | `'InputPeer'` | `—` |
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.channels import ChannelsReportSpam
```

```python
result = await client(ChannelsReportSpam(...))
```

## ChannelsGetMessages

**Constructor ID:** `2911672867`

**Fields:** ``channel`, `id``

```python
ChannelsGetMessages.__init__(channel: 'InputChannel', id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.channels import ChannelsGetMessages
```

```python
result = await client(ChannelsGetMessages(...))
```

## ChannelsGetParticipants

**Constructor ID:** `2010044880`

**Fields:** ``channel`, `filter`, `offset`, `limit`, `hash``

```python
ChannelsGetParticipants.__init__(channel: 'InputChannel', filter: 'ChannelParticipantsFilter', offset: int, limit: int, hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `filter` | `'ChannelParticipantsFilter'` | `—` |
| `offset` | `int` | `—` |
| `limit` | `int` | `—` |
| `hash` | `int` | `—` |

```python
from sent.tl.functions.channels import ChannelsGetParticipants
```

```python
result = await client(ChannelsGetParticipants(...))
```

## ChannelsGetParticipant

**Constructor ID:** `2695589062`

**Fields:** ``channel`, `participant``

```python
ChannelsGetParticipant.__init__(channel: 'InputChannel', participant: 'InputPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `participant` | `'InputPeer'` | `—` |

```python
from sent.tl.functions.channels import ChannelsGetParticipant
```

```python
result = await client(ChannelsGetParticipant(...))
```

## ChannelsGetChannels

**Constructor ID:** `176122811`

```python
ChannelsGetChannels.__init__(id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.channels import ChannelsGetChannels
```

```python
result = await client(ChannelsGetChannels(...))
```

## ChannelsGetFullChannel

**Constructor ID:** `141781513`

```python
ChannelsGetFullChannel.__init__(channel: 'InputChannel')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |

```python
from sent.tl.functions.channels import ChannelsGetFullChannel
```

```python
result = await client(ChannelsGetFullChannel(...))
```

## ChannelsCreateChannel

**Constructor ID:** `2432722695`

**Fields:** ``broadcast`, `megagroup`, `for_import`, `forum`, `title`, `about`, `geo_point`, `address`, `ttl_period``

```python
ChannelsCreateChannel.__init__(title: str, about: str, broadcast: bool = None, megagroup: bool = None, for_import: bool = None, forum: bool = None, geo_point: 'InputGeoPoint' = None, address: str = None, ttl_period: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `title` | `str` | `—` |
| `about` | `str` | `—` |
| `broadcast` | `bool` | `None` |
| `megagroup` | `bool` | `None` |
| `for_import` | `bool` | `None` |
| `forum` | `bool` | `None` |
| `geo_point` | `'InputGeoPoint'` | `None` |
| `address` | `str` | `None` |
| `ttl_period` | `int` | `None` |

```python
from sent.tl.functions.channels import ChannelsCreateChannel
```

```python
result = await client(ChannelsCreateChannel(...))
```

## ChannelsEditAdmin

**Constructor ID:** `2593697128`

**Fields:** ``channel`, `user_id`, `admin_rights`, `rank``

```python
ChannelsEditAdmin.__init__(channel: 'InputChannel', user_id: 'InputUser', admin_rights: 'ChatAdminRights', rank: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `user_id` | `'InputUser'` | `—` |
| `admin_rights` | `'ChatAdminRights'` | `—` |
| `rank` | `str` | `None` |

```python
from sent.tl.functions.channels import ChannelsEditAdmin
```

```python
result = await client(ChannelsEditAdmin(...))
```

## ChannelsEditTitle

**Constructor ID:** `1450044624`

**Fields:** ``channel`, `title``

```python
ChannelsEditTitle.__init__(channel: 'InputChannel', title: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `title` | `str` | `—` |

```python
from sent.tl.functions.channels import ChannelsEditTitle
```

```python
result = await client(ChannelsEditTitle(...))
```

## ChannelsEditPhoto

**Constructor ID:** `4046346185`

**Fields:** ``channel`, `photo``

```python
ChannelsEditPhoto.__init__(channel: 'InputChannel', photo: 'InputChatPhoto')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `photo` | `'InputChatPhoto'` | `—` |

```python
from sent.tl.functions.channels import ChannelsEditPhoto
```

```python
result = await client(ChannelsEditPhoto(...))
```

## ChannelsCheckUsername

**Constructor ID:** `283557164`

**Fields:** ``channel`, `username``

```python
ChannelsCheckUsername.__init__(channel: 'InputChannel', username: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `username` | `str` | `—` |

```python
from sent.tl.functions.channels import ChannelsCheckUsername
```

```python
result = await client(ChannelsCheckUsername(...))
```

## ChannelsUpdateUsername

**Constructor ID:** `890549214`

**Fields:** ``channel`, `username``

```python
ChannelsUpdateUsername.__init__(channel: 'InputChannel', username: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `username` | `str` | `—` |

```python
from sent.tl.functions.channels import ChannelsUpdateUsername
```

```python
result = await client(ChannelsUpdateUsername(...))
```

## ChannelsJoinChannel

**Constructor ID:** `615851205`

```python
ChannelsJoinChannel.__init__(channel: 'InputChannel')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |

```python
from sent.tl.functions.channels import ChannelsJoinChannel
```

```python
result = await client(ChannelsJoinChannel(...))
```

## ChannelsLeaveChannel

**Constructor ID:** `4164332181`

```python
ChannelsLeaveChannel.__init__(channel: 'InputChannel')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |

```python
from sent.tl.functions.channels import ChannelsLeaveChannel
```

```python
result = await client(ChannelsLeaveChannel(...))
```

## ChannelsInviteToChannel

**Constructor ID:** `3387112788`

**Fields:** ``channel`, `users``

```python
ChannelsInviteToChannel.__init__(channel: 'InputChannel', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.functions.channels import ChannelsInviteToChannel
```

```python
result = await client(ChannelsInviteToChannel(...))
```

## ChannelsDeleteChannel

**Constructor ID:** `3222347747`

```python
ChannelsDeleteChannel.__init__(channel: 'InputChannel')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |

```python
from sent.tl.functions.channels import ChannelsDeleteChannel
```

```python
result = await client(ChannelsDeleteChannel(...))
```

## ChannelsExportMessageLink

**Constructor ID:** `3862932971`

**Fields:** ``grouped`, `thread`, `channel`, `id``

```python
ChannelsExportMessageLink.__init__(channel: 'InputChannel', id: int, grouped: bool = None, thread: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `id` | `int` | `—` |
| `grouped` | `bool` | `None` |
| `thread` | `bool` | `None` |

```python
from sent.tl.functions.channels import ChannelsExportMessageLink
```

```python
result = await client(ChannelsExportMessageLink(...))
```

## ChannelsToggleSignatures

**Constructor ID:** `1099781276`

**Fields:** ``signatures_enabled`, `profiles_enabled`, `channel``

```python
ChannelsToggleSignatures.__init__(channel: 'InputChannel', signatures_enabled: bool = None, profiles_enabled: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `signatures_enabled` | `bool` | `None` |
| `profiles_enabled` | `bool` | `None` |

```python
from sent.tl.functions.channels import ChannelsToggleSignatures
```

```python
result = await client(ChannelsToggleSignatures(...))
```

## ChannelsGetAdminedPublicChannels

**Constructor ID:** `4172297903`

**Fields:** ``by_location`, `check_limit`, `for_personal``

```python
ChannelsGetAdminedPublicChannels.__init__(by_location: bool = None, check_limit: bool = None, for_personal: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `by_location` | `bool` | `None` |
| `check_limit` | `bool` | `None` |
| `for_personal` | `bool` | `None` |

```python
from sent.tl.functions.channels import ChannelsGetAdminedPublicChannels
```

```python
result = await client(ChannelsGetAdminedPublicChannels(...))
```

## ChannelsEditBanned

**Constructor ID:** `2531708289`

**Fields:** ``channel`, `participant`, `banned_rights``

```python
ChannelsEditBanned.__init__(channel: 'InputChannel', participant: 'InputPeer', banned_rights: 'ChatBannedRights')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `participant` | `'InputPeer'` | `—` |
| `banned_rights` | `'ChatBannedRights'` | `—` |

```python
from sent.tl.functions.channels import ChannelsEditBanned
```

```python
result = await client(ChannelsEditBanned(...))
```

## ChannelsGetAdminLog

**Constructor ID:** `870184064`

**Fields:** ``channel`, `q`, `events_filter`, `admins`, `max_id`, `min_id`, `limit``

```python
ChannelsGetAdminLog.__init__(channel: 'InputChannel', q: str, max_id: int, min_id: int, limit: int, events_filter: 'ChannelAdminLogEventsFilter' = None, admins: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `q` | `str` | `—` |
| `max_id` | `int` | `—` |
| `min_id` | `int` | `—` |
| `limit` | `int` | `—` |
| `events_filter` | `'ChannelAdminLogEventsFilter'` | `None` |
| `admins` | `'Vector'` | `None` |

```python
from sent.tl.functions.channels import ChannelsGetAdminLog
```

```python
result = await client(ChannelsGetAdminLog(...))
```

## ChannelsSetStickers

**Constructor ID:** `3935085817`

**Fields:** ``channel`, `stickerset``

```python
ChannelsSetStickers.__init__(channel: 'InputChannel', stickerset: 'InputStickerSet')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `stickerset` | `'InputStickerSet'` | `—` |

```python
from sent.tl.functions.channels import ChannelsSetStickers
```

```python
result = await client(ChannelsSetStickers(...))
```

## ChannelsReadMessageContents

**Constructor ID:** `3937786936`

**Fields:** ``channel`, `id``

```python
ChannelsReadMessageContents.__init__(channel: 'InputChannel', id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.channels import ChannelsReadMessageContents
```

```python
result = await client(ChannelsReadMessageContents(...))
```

## ChannelsDeleteHistory

**Constructor ID:** `2611648071`

**Fields:** ``for_everyone`, `channel`, `max_id``

```python
ChannelsDeleteHistory.__init__(channel: 'InputChannel', max_id: int, for_everyone: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `max_id` | `int` | `—` |
| `for_everyone` | `bool` | `None` |

```python
from sent.tl.functions.channels import ChannelsDeleteHistory
```

```python
result = await client(ChannelsDeleteHistory(...))
```

## ChannelsTogglePreHistoryHidden

**Constructor ID:** `3938171212`

**Fields:** ``channel`, `enabled``

```python
ChannelsTogglePreHistoryHidden.__init__(channel: 'InputChannel', enabled: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `enabled` | `bool` | `—` |

```python
from sent.tl.functions.channels import ChannelsTogglePreHistoryHidden
```

```python
result = await client(ChannelsTogglePreHistoryHidden(...))
```

## ChannelsGetLeftChannels

**Constructor ID:** `2202135744`

```python
ChannelsGetLeftChannels.__init__(offset: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `offset` | `int` | `—` |

```python
from sent.tl.functions.channels import ChannelsGetLeftChannels
```

```python
result = await client(ChannelsGetLeftChannels(...))
```

## ChannelsGetGroupsForDiscussion

**Constructor ID:** `4124758904`

```python
ChannelsGetGroupsForDiscussion.__init__()
```

_No parameters._

```python
from sent.tl.functions.channels import ChannelsGetGroupsForDiscussion
```

```python
result = await client(ChannelsGetGroupsForDiscussion(...))
```

## ChannelsSetDiscussionGroup

**Constructor ID:** `1079520178`

**Fields:** ``broadcast`, `group``

```python
ChannelsSetDiscussionGroup.__init__(broadcast: 'InputChannel', group: 'InputChannel')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `broadcast` | `'InputChannel'` | `—` |
| `group` | `'InputChannel'` | `—` |

```python
from sent.tl.functions.channels import ChannelsSetDiscussionGroup
```

```python
result = await client(ChannelsSetDiscussionGroup(...))
```

## ChannelsEditLocation

**Constructor ID:** `1491484525`

**Fields:** ``channel`, `geo_point`, `address``

```python
ChannelsEditLocation.__init__(channel: 'InputChannel', geo_point: 'InputGeoPoint', address: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `geo_point` | `'InputGeoPoint'` | `—` |
| `address` | `str` | `—` |

```python
from sent.tl.functions.channels import ChannelsEditLocation
```

```python
result = await client(ChannelsEditLocation(...))
```

## ChannelsToggleSlowMode

**Constructor ID:** `3990134512`

**Fields:** ``channel`, `seconds``

```python
ChannelsToggleSlowMode.__init__(channel: 'InputChannel', seconds: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `seconds` | `int` | `—` |

```python
from sent.tl.functions.channels import ChannelsToggleSlowMode
```

```python
result = await client(ChannelsToggleSlowMode(...))
```

## ChannelsGetInactiveChannels

**Constructor ID:** `300429806`

```python
ChannelsGetInactiveChannels.__init__()
```

_No parameters._

```python
from sent.tl.functions.channels import ChannelsGetInactiveChannels
```

```python
result = await client(ChannelsGetInactiveChannels(...))
```

## ChannelsConvertToGigagroup

**Constructor ID:** `187239529`

```python
ChannelsConvertToGigagroup.__init__(channel: 'InputChannel')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |

```python
from sent.tl.functions.channels import ChannelsConvertToGigagroup
```

```python
result = await client(ChannelsConvertToGigagroup(...))
```

## ChannelsGetSendAs

**Constructor ID:** `3884295231`

**Fields:** ``for_paid_reactions`, `for_live_stories`, `peer``

```python
ChannelsGetSendAs.__init__(peer: 'InputPeer', for_paid_reactions: bool = None, for_live_stories: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `for_paid_reactions` | `bool` | `None` |
| `for_live_stories` | `bool` | `None` |

```python
from sent.tl.functions.channels import ChannelsGetSendAs
```

```python
result = await client(ChannelsGetSendAs(...))
```

## ChannelsDeleteParticipantHistory

**Constructor ID:** `913655003`

**Fields:** ``channel`, `participant``

```python
ChannelsDeleteParticipantHistory.__init__(channel: 'InputChannel', participant: 'InputPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `participant` | `'InputPeer'` | `—` |

```python
from sent.tl.functions.channels import ChannelsDeleteParticipantHistory
```

```python
result = await client(ChannelsDeleteParticipantHistory(...))
```

## ChannelsToggleJoinToSend

**Constructor ID:** `3838547328`

**Fields:** ``channel`, `enabled``

```python
ChannelsToggleJoinToSend.__init__(channel: 'InputChannel', enabled: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `enabled` | `bool` | `—` |

```python
from sent.tl.functions.channels import ChannelsToggleJoinToSend
```

```python
result = await client(ChannelsToggleJoinToSend(...))
```

## ChannelsToggleJoinRequest

**Constructor ID:** `1277789622`

**Fields:** ``channel`, `enabled``

```python
ChannelsToggleJoinRequest.__init__(channel: 'InputChannel', enabled: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `enabled` | `bool` | `—` |

```python
from sent.tl.functions.channels import ChannelsToggleJoinRequest
```

```python
result = await client(ChannelsToggleJoinRequest(...))
```

## ChannelsReorderUsernames

**Constructor ID:** `3025988893`

**Fields:** ``channel`, `order``

```python
ChannelsReorderUsernames.__init__(channel: 'InputChannel', order: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `order` | `'Vector'` | `—` |

```python
from sent.tl.functions.channels import ChannelsReorderUsernames
```

```python
result = await client(ChannelsReorderUsernames(...))
```

## ChannelsToggleUsername

**Constructor ID:** `1358053637`

**Fields:** ``channel`, `username`, `active``

```python
ChannelsToggleUsername.__init__(channel: 'InputChannel', username: str, active: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `username` | `str` | `—` |
| `active` | `bool` | `—` |

```python
from sent.tl.functions.channels import ChannelsToggleUsername
```

```python
result = await client(ChannelsToggleUsername(...))
```

## ChannelsDeactivateAllUsernames

**Constructor ID:** `170155475`

```python
ChannelsDeactivateAllUsernames.__init__(channel: 'InputChannel')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |

```python
from sent.tl.functions.channels import ChannelsDeactivateAllUsernames
```

```python
result = await client(ChannelsDeactivateAllUsernames(...))
```

## ChannelsToggleForum

**Constructor ID:** `1073174324`

**Fields:** ``channel`, `enabled`, `tabs``

```python
ChannelsToggleForum.__init__(channel: 'InputChannel', enabled: bool, tabs: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `enabled` | `bool` | `—` |
| `tabs` | `bool` | `—` |

```python
from sent.tl.functions.channels import ChannelsToggleForum
```

```python
result = await client(ChannelsToggleForum(...))
```

## ChannelsToggleAntiSpam

**Constructor ID:** `1760814315`

**Fields:** ``channel`, `enabled``

```python
ChannelsToggleAntiSpam.__init__(channel: 'InputChannel', enabled: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `enabled` | `bool` | `—` |

```python
from sent.tl.functions.channels import ChannelsToggleAntiSpam
```

```python
result = await client(ChannelsToggleAntiSpam(...))
```

## ChannelsReportAntiSpamFalsePositive

**Constructor ID:** `2823857811`

**Fields:** ``channel`, `msg_id``

```python
ChannelsReportAntiSpamFalsePositive.__init__(channel: 'InputChannel', msg_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `msg_id` | `int` | `—` |

```python
from sent.tl.functions.channels import ChannelsReportAntiSpamFalsePositive
```

```python
result = await client(ChannelsReportAntiSpamFalsePositive(...))
```

## ChannelsToggleParticipantsHidden

**Constructor ID:** `1785624660`

**Fields:** ``channel`, `enabled``

```python
ChannelsToggleParticipantsHidden.__init__(channel: 'InputChannel', enabled: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `enabled` | `bool` | `—` |

```python
from sent.tl.functions.channels import ChannelsToggleParticipantsHidden
```

```python
result = await client(ChannelsToggleParticipantsHidden(...))
```

## ChannelsUpdateColor

**Constructor ID:** `3635033713`

**Fields:** ``for_profile`, `channel`, `color`, `background_emoji_id``

```python
ChannelsUpdateColor.__init__(channel: 'InputChannel', for_profile: bool = None, color: int = None, background_emoji_id: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `for_profile` | `bool` | `None` |
| `color` | `int` | `None` |
| `background_emoji_id` | `int` | `None` |

```python
from sent.tl.functions.channels import ChannelsUpdateColor
```

```python
result = await client(ChannelsUpdateColor(...))
```

## ChannelsToggleViewForumAsMessages

**Constructor ID:** `2537077525`

**Fields:** ``channel`, `enabled``

```python
ChannelsToggleViewForumAsMessages.__init__(channel: 'InputChannel', enabled: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `enabled` | `bool` | `—` |

```python
from sent.tl.functions.channels import ChannelsToggleViewForumAsMessages
```

```python
result = await client(ChannelsToggleViewForumAsMessages(...))
```

## ChannelsGetChannelRecommendations

**Constructor ID:** `631707458`

```python
ChannelsGetChannelRecommendations.__init__(channel: 'InputChannel' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `None` |

```python
from sent.tl.functions.channels import ChannelsGetChannelRecommendations
```

```python
result = await client(ChannelsGetChannelRecommendations(...))
```

## ChannelsUpdateEmojiStatus

**Constructor ID:** `4040418984`

**Fields:** ``channel`, `emoji_status``

```python
ChannelsUpdateEmojiStatus.__init__(channel: 'InputChannel', emoji_status: 'EmojiStatus')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `emoji_status` | `'EmojiStatus'` | `—` |

```python
from sent.tl.functions.channels import ChannelsUpdateEmojiStatus
```

```python
result = await client(ChannelsUpdateEmojiStatus(...))
```

## ChannelsSetBoostsToUnblockRestrictions

**Constructor ID:** `2906234094`

**Fields:** ``channel`, `boosts``

```python
ChannelsSetBoostsToUnblockRestrictions.__init__(channel: 'InputChannel', boosts: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `boosts` | `int` | `—` |

```python
from sent.tl.functions.channels import ChannelsSetBoostsToUnblockRestrictions
```

```python
result = await client(ChannelsSetBoostsToUnblockRestrictions(...))
```

## ChannelsSetEmojiStickers

**Constructor ID:** `1020866743`

**Fields:** ``channel`, `stickerset``

```python
ChannelsSetEmojiStickers.__init__(channel: 'InputChannel', stickerset: 'InputStickerSet')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `stickerset` | `'InputStickerSet'` | `—` |

```python
from sent.tl.functions.channels import ChannelsSetEmojiStickers
```

```python
result = await client(ChannelsSetEmojiStickers(...))
```

## ChannelsRestrictSponsoredMessages

**Constructor ID:** `2598966553`

**Fields:** ``channel`, `restricted``

```python
ChannelsRestrictSponsoredMessages.__init__(channel: 'InputChannel', restricted: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `restricted` | `bool` | `—` |

```python
from sent.tl.functions.channels import ChannelsRestrictSponsoredMessages
```

```python
result = await client(ChannelsRestrictSponsoredMessages(...))
```

## ChannelsSearchPosts

**Constructor ID:** `4072993357`

**Fields:** ``hashtag`, `query`, `offset_rate`, `offset_peer`, `offset_id`, `limit`, `allow_paid_stars``

```python
ChannelsSearchPosts.__init__(offset_rate: int, offset_peer: 'InputPeer', offset_id: int, limit: int, hashtag: str = None, query: str = None, allow_paid_stars: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `offset_rate` | `int` | `—` |
| `offset_peer` | `'InputPeer'` | `—` |
| `offset_id` | `int` | `—` |
| `limit` | `int` | `—` |
| `hashtag` | `str` | `None` |
| `query` | `str` | `None` |
| `allow_paid_stars` | `int` | `None` |

```python
from sent.tl.functions.channels import ChannelsSearchPosts
```

```python
result = await client(ChannelsSearchPosts(...))
```

## ChannelsUpdatePaidMessagesPrice

**Constructor ID:** `1259483771`

**Fields:** ``broadcast_messages_allowed`, `channel`, `send_paid_messages_stars``

```python
ChannelsUpdatePaidMessagesPrice.__init__(channel: 'InputChannel', send_paid_messages_stars: int, broadcast_messages_allowed: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `send_paid_messages_stars` | `int` | `—` |
| `broadcast_messages_allowed` | `bool` | `None` |

```python
from sent.tl.functions.channels import ChannelsUpdatePaidMessagesPrice
```

```python
result = await client(ChannelsUpdatePaidMessagesPrice(...))
```

## ChannelsToggleAutotranslation

**Constructor ID:** `377471137`

**Fields:** ``channel`, `enabled``

```python
ChannelsToggleAutotranslation.__init__(channel: 'InputChannel', enabled: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `enabled` | `bool` | `—` |

```python
from sent.tl.functions.channels import ChannelsToggleAutotranslation
```

```python
result = await client(ChannelsToggleAutotranslation(...))
```

## ChannelsGetMessageAuthor

**Constructor ID:** `3974275302`

**Fields:** ``channel`, `id``

```python
ChannelsGetMessageAuthor.__init__(channel: 'InputChannel', id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `id` | `int` | `—` |

```python
from sent.tl.functions.channels import ChannelsGetMessageAuthor
```

```python
result = await client(ChannelsGetMessageAuthor(...))
```

## ChannelsCheckSearchPostsFlood

**Constructor ID:** `576090389`

```python
ChannelsCheckSearchPostsFlood.__init__(query: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `query` | `str` | `None` |

```python
from sent.tl.functions.channels import ChannelsCheckSearchPostsFlood
```

```python
result = await client(ChannelsCheckSearchPostsFlood(...))
```

## ChannelsSetMainProfileTab

**Constructor ID:** `897842353`

**Fields:** ``channel`, `tab``

```python
ChannelsSetMainProfileTab.__init__(channel: 'InputChannel', tab: 'ProfileTab')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `tab` | `'ProfileTab'` | `—` |

```python
from sent.tl.functions.channels import ChannelsSetMainProfileTab
```

```python
result = await client(ChannelsSetMainProfileTab(...))
```
