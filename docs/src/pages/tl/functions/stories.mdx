{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Functions: `stories`

Import from `sent.tl.functions.stories`.

## Contents

- [StoriesCanSendStory](#storiescansendstory)
- [StoriesSendStory](#storiessendstory)
- [StoriesEditStory](#storieseditstory)
- [StoriesDeleteStories](#storiesdeletestories)
- [StoriesTogglePinned](#storiestogglepinned)
- [StoriesGetAllStories](#storiesgetallstories)
- [StoriesGetPinnedStories](#storiesgetpinnedstories)
- [StoriesGetStoriesArchive](#storiesgetstoriesarchive)
- [StoriesGetStoriesByID](#storiesgetstoriesbyid)
- [StoriesToggleAllStoriesHidden](#storiestoggleallstorieshidden)
- [StoriesReadStories](#storiesreadstories)
- [StoriesIncrementStoryViews](#storiesincrementstoryviews)
- [StoriesGetStoryViewsList](#storiesgetstoryviewslist)
- [StoriesGetStoriesViews](#storiesgetstoriesviews)
- [StoriesExportStoryLink](#storiesexportstorylink)
- [StoriesReport](#storiesreport)
- [StoriesActivateStealthMode](#storiesactivatestealthmode)
- [StoriesSendReaction](#storiessendreaction)
- [StoriesGetPeerStories](#storiesgetpeerstories)
- [StoriesGetAllReadPeerStories](#storiesgetallreadpeerstories)
- [StoriesGetPeerMaxIDs](#storiesgetpeermaxids)
- [StoriesGetChatsToSend](#storiesgetchatstosend)
- [StoriesTogglePeerStoriesHidden](#storiestogglepeerstorieshidden)
- [StoriesGetStoryReactionsList](#storiesgetstoryreactionslist)
- [StoriesTogglePinnedToTop](#storiestogglepinnedtotop)
- [StoriesSearchPosts](#storiessearchposts)
- [StoriesCreateAlbum](#storiescreatealbum)
- [StoriesUpdateAlbum](#storiesupdatealbum)
- [StoriesReorderAlbums](#storiesreorderalbums)
- [StoriesDeleteAlbum](#storiesdeletealbum)
- [StoriesGetAlbums](#storiesgetalbums)
- [StoriesGetAlbumStories](#storiesgetalbumstories)
- [StoriesStartLive](#storiesstartlive)

## StoriesCanSendStory

**Constructor ID:** `820732912`

```python
StoriesCanSendStory.__init__(peer: 'InputPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |

```python
from sent.tl.functions.stories import StoriesCanSendStory
```

```python
result = await client(StoriesCanSendStory(...))
```

## StoriesSendStory

**Constructor ID:** `2409523352`

**Fields:** ``pinned`, `noforwards`, `fwd_modified`, `peer`, `media`, `media_areas`, `caption`, `entities`, `privacy_rules`, `random_id`, `period`, `fwd_from_id`, `fwd_from_story`, `albums`, `music``

```python
StoriesSendStory.__init__(peer: 'InputPeer', media: 'InputMedia', privacy_rules: 'Vector', random_id: int, pinned: bool = None, noforwards: bool = None, fwd_modified: bool = None, media_areas: 'Vector' = None, caption: str = None, entities: 'Vector' = None, period: int = None, fwd_from_id: 'InputPeer' = None, fwd_from_story: int = None, albums: 'Vector' = None, music: 'InputDocument' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `media` | `'InputMedia'` | `—` |
| `privacy_rules` | `'Vector'` | `—` |
| `random_id` | `int` | `—` |
| `pinned` | `bool` | `None` |
| `noforwards` | `bool` | `None` |
| `fwd_modified` | `bool` | `None` |
| `media_areas` | `'Vector'` | `None` |
| `caption` | `str` | `None` |
| `entities` | `'Vector'` | `None` |
| `period` | `int` | `None` |
| `fwd_from_id` | `'InputPeer'` | `None` |
| `fwd_from_story` | `int` | `None` |
| `albums` | `'Vector'` | `None` |
| `music` | `'InputDocument'` | `None` |

```python
from sent.tl.functions.stories import StoriesSendStory
```

```python
result = await client(StoriesSendStory(...))
```

## StoriesEditStory

**Constructor ID:** `744728363`

**Fields:** ``peer`, `id`, `media`, `media_areas`, `caption`, `entities`, `privacy_rules`, `music``

```python
StoriesEditStory.__init__(peer: 'InputPeer', id: int, media: 'InputMedia' = None, media_areas: 'Vector' = None, caption: str = None, entities: 'Vector' = None, privacy_rules: 'Vector' = None, music: 'InputDocument' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `int` | `—` |
| `media` | `'InputMedia'` | `None` |
| `media_areas` | `'Vector'` | `None` |
| `caption` | `str` | `None` |
| `entities` | `'Vector'` | `None` |
| `privacy_rules` | `'Vector'` | `None` |
| `music` | `'InputDocument'` | `None` |

```python
from sent.tl.functions.stories import StoriesEditStory
```

```python
result = await client(StoriesEditStory(...))
```

## StoriesDeleteStories

**Constructor ID:** `2925124447`

**Fields:** ``peer`, `id``

```python
StoriesDeleteStories.__init__(peer: 'InputPeer', id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.stories import StoriesDeleteStories
```

```python
result = await client(StoriesDeleteStories(...))
```

## StoriesTogglePinned

**Constructor ID:** `2591400431`

**Fields:** ``peer`, `id`, `pinned``

```python
StoriesTogglePinned.__init__(peer: 'InputPeer', id: 'Vector', pinned: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `'Vector'` | `—` |
| `pinned` | `bool` | `—` |

```python
from sent.tl.functions.stories import StoriesTogglePinned
```

```python
result = await client(StoriesTogglePinned(...))
```

## StoriesGetAllStories

**Constructor ID:** `4004566565`

**Fields:** ``next`, `hidden`, `state``

```python
StoriesGetAllStories.__init__(next: bool = None, hidden: bool = None, state: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `next` | `bool` | `None` |
| `hidden` | `bool` | `None` |
| `state` | `str` | `None` |

```python
from sent.tl.functions.stories import StoriesGetAllStories
```

```python
result = await client(StoriesGetAllStories(...))
```

## StoriesGetPinnedStories

**Constructor ID:** `1478600156`

**Fields:** ``peer`, `offset_id`, `limit``

```python
StoriesGetPinnedStories.__init__(peer: 'InputPeer', offset_id: int, limit: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `offset_id` | `int` | `—` |
| `limit` | `int` | `—` |

```python
from sent.tl.functions.stories import StoriesGetPinnedStories
```

```python
result = await client(StoriesGetPinnedStories(...))
```

## StoriesGetStoriesArchive

**Constructor ID:** `3023380502`

**Fields:** ``peer`, `offset_id`, `limit``

```python
StoriesGetStoriesArchive.__init__(peer: 'InputPeer', offset_id: int, limit: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `offset_id` | `int` | `—` |
| `limit` | `int` | `—` |

```python
from sent.tl.functions.stories import StoriesGetStoriesArchive
```

```python
result = await client(StoriesGetStoriesArchive(...))
```

## StoriesGetStoriesByID

**Constructor ID:** `1467271796`

**Fields:** ``peer`, `id``

```python
StoriesGetStoriesByID.__init__(peer: 'InputPeer', id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.stories import StoriesGetStoriesByID
```

```python
result = await client(StoriesGetStoriesByID(...))
```

## StoriesToggleAllStoriesHidden

**Constructor ID:** `2082822084`

```python
StoriesToggleAllStoriesHidden.__init__(hidden: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hidden` | `bool` | `—` |

```python
from sent.tl.functions.stories import StoriesToggleAllStoriesHidden
```

```python
result = await client(StoriesToggleAllStoriesHidden(...))
```

## StoriesReadStories

**Constructor ID:** `2773932744`

**Fields:** ``peer`, `max_id``

```python
StoriesReadStories.__init__(peer: 'InputPeer', max_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `max_id` | `int` | `—` |

```python
from sent.tl.functions.stories import StoriesReadStories
```

```python
result = await client(StoriesReadStories(...))
```

## StoriesIncrementStoryViews

**Constructor ID:** `2986511099`

**Fields:** ``peer`, `id``

```python
StoriesIncrementStoryViews.__init__(peer: 'InputPeer', id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.stories import StoriesIncrementStoryViews
```

```python
result = await client(StoriesIncrementStoryViews(...))
```

## StoriesGetStoryViewsList

**Constructor ID:** `2127707223`

**Fields:** ``just_contacts`, `reactions_first`, `forwards_first`, `peer`, `q`, `id`, `offset`, `limit``

```python
StoriesGetStoryViewsList.__init__(peer: 'InputPeer', id: int, offset: str, limit: int, just_contacts: bool = None, reactions_first: bool = None, forwards_first: bool = None, q: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `int` | `—` |
| `offset` | `str` | `—` |
| `limit` | `int` | `—` |
| `just_contacts` | `bool` | `None` |
| `reactions_first` | `bool` | `None` |
| `forwards_first` | `bool` | `None` |
| `q` | `str` | `None` |

```python
from sent.tl.functions.stories import StoriesGetStoryViewsList
```

```python
result = await client(StoriesGetStoryViewsList(...))
```

## StoriesGetStoriesViews

**Constructor ID:** `685862088`

**Fields:** ``peer`, `id``

```python
StoriesGetStoriesViews.__init__(peer: 'InputPeer', id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.stories import StoriesGetStoriesViews
```

```python
result = await client(StoriesGetStoriesViews(...))
```

## StoriesExportStoryLink

**Constructor ID:** `2072899360`

**Fields:** ``peer`, `id``

```python
StoriesExportStoryLink.__init__(peer: 'InputPeer', id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `int` | `—` |

```python
from sent.tl.functions.stories import StoriesExportStoryLink
```

```python
result = await client(StoriesExportStoryLink(...))
```

## StoriesReport

**Constructor ID:** `433646405`

**Fields:** ``peer`, `id`, `option`, `message``

```python
StoriesReport.__init__(peer: 'InputPeer', id: 'Vector', option: bytes, message: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `'Vector'` | `—` |
| `option` | `bytes` | `—` |
| `message` | `str` | `—` |

```python
from sent.tl.functions.stories import StoriesReport
```

```python
result = await client(StoriesReport(...))
```

## StoriesActivateStealthMode

**Constructor ID:** `1471926630`

**Fields:** ``past`, `future``

```python
StoriesActivateStealthMode.__init__(past: bool = None, future: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `past` | `bool` | `None` |
| `future` | `bool` | `None` |

```python
from sent.tl.functions.stories import StoriesActivateStealthMode
```

```python
result = await client(StoriesActivateStealthMode(...))
```

## StoriesSendReaction

**Constructor ID:** `2144810674`

**Fields:** ``add_to_recent`, `peer`, `story_id`, `reaction``

```python
StoriesSendReaction.__init__(peer: 'InputPeer', story_id: int, reaction: 'Reaction', add_to_recent: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `story_id` | `int` | `—` |
| `reaction` | `'Reaction'` | `—` |
| `add_to_recent` | `bool` | `None` |

```python
from sent.tl.functions.stories import StoriesSendReaction
```

```python
result = await client(StoriesSendReaction(...))
```

## StoriesGetPeerStories

**Constructor ID:** `743103056`

```python
StoriesGetPeerStories.__init__(peer: 'InputPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |

```python
from sent.tl.functions.stories import StoriesGetPeerStories
```

```python
result = await client(StoriesGetPeerStories(...))
```

## StoriesGetAllReadPeerStories

**Constructor ID:** `2606426105`

```python
StoriesGetAllReadPeerStories.__init__()
```

_No parameters._

```python
from sent.tl.functions.stories import StoriesGetAllReadPeerStories
```

```python
result = await client(StoriesGetAllReadPeerStories(...))
```

## StoriesGetPeerMaxIDs

**Constructor ID:** `2018087280`

```python
StoriesGetPeerMaxIDs.__init__(id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.stories import StoriesGetPeerMaxIDs
```

```python
result = await client(StoriesGetPeerMaxIDs(...))
```

## StoriesGetChatsToSend

**Constructor ID:** `2775223136`

```python
StoriesGetChatsToSend.__init__()
```

_No parameters._

```python
from sent.tl.functions.stories import StoriesGetChatsToSend
```

```python
result = await client(StoriesGetChatsToSend(...))
```

## StoriesTogglePeerStoriesHidden

**Constructor ID:** `3171161540`

**Fields:** ``peer`, `hidden``

```python
StoriesTogglePeerStoriesHidden.__init__(peer: 'InputPeer', hidden: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `hidden` | `bool` | `—` |

```python
from sent.tl.functions.stories import StoriesTogglePeerStoriesHidden
```

```python
result = await client(StoriesTogglePeerStoriesHidden(...))
```

## StoriesGetStoryReactionsList

**Constructor ID:** `3115485215`

**Fields:** ``forwards_first`, `peer`, `id`, `reaction`, `offset`, `limit``

```python
StoriesGetStoryReactionsList.__init__(peer: 'InputPeer', id: int, limit: int, forwards_first: bool = None, reaction: 'Reaction' = None, offset: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `int` | `—` |
| `limit` | `int` | `—` |
| `forwards_first` | `bool` | `None` |
| `reaction` | `'Reaction'` | `None` |
| `offset` | `str` | `None` |

```python
from sent.tl.functions.stories import StoriesGetStoryReactionsList
```

```python
result = await client(StoriesGetStoryReactionsList(...))
```

## StoriesTogglePinnedToTop

**Constructor ID:** `187268763`

**Fields:** ``peer`, `id``

```python
StoriesTogglePinnedToTop.__init__(peer: 'InputPeer', id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.stories import StoriesTogglePinnedToTop
```

```python
result = await client(StoriesTogglePinnedToTop(...))
```

## StoriesSearchPosts

**Constructor ID:** `3514894599`

**Fields:** ``hashtag`, `area`, `peer`, `offset`, `limit``

```python
StoriesSearchPosts.__init__(offset: str, limit: int, hashtag: str = None, area: 'MediaArea' = None, peer: 'InputPeer' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `offset` | `str` | `—` |
| `limit` | `int` | `—` |
| `hashtag` | `str` | `None` |
| `area` | `'MediaArea'` | `None` |
| `peer` | `'InputPeer'` | `None` |

```python
from sent.tl.functions.stories import StoriesSearchPosts
```

```python
result = await client(StoriesSearchPosts(...))
```

## StoriesCreateAlbum

**Constructor ID:** `2741212901`

**Fields:** ``peer`, `title`, `stories``

```python
StoriesCreateAlbum.__init__(peer: 'InputPeer', title: str, stories: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `title` | `str` | `—` |
| `stories` | `'Vector'` | `—` |

```python
from sent.tl.functions.stories import StoriesCreateAlbum
```

```python
result = await client(StoriesCreateAlbum(...))
```

## StoriesUpdateAlbum

**Constructor ID:** `1582455222`

**Fields:** ``peer`, `album_id`, `title`, `delete_stories`, `add_stories`, `order``

```python
StoriesUpdateAlbum.__init__(peer: 'InputPeer', album_id: int, title: str = None, delete_stories: 'Vector' = None, add_stories: 'Vector' = None, order: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `album_id` | `int` | `—` |
| `title` | `str` | `None` |
| `delete_stories` | `'Vector'` | `None` |
| `add_stories` | `'Vector'` | `None` |
| `order` | `'Vector'` | `None` |

```python
from sent.tl.functions.stories import StoriesUpdateAlbum
```

```python
result = await client(StoriesUpdateAlbum(...))
```

## StoriesReorderAlbums

**Constructor ID:** `2234907609`

**Fields:** ``peer`, `order``

```python
StoriesReorderAlbums.__init__(peer: 'InputPeer', order: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `order` | `'Vector'` | `—` |

```python
from sent.tl.functions.stories import StoriesReorderAlbums
```

```python
result = await client(StoriesReorderAlbums(...))
```

## StoriesDeleteAlbum

**Constructor ID:** `2369017552`

**Fields:** ``peer`, `album_id``

```python
StoriesDeleteAlbum.__init__(peer: 'InputPeer', album_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `album_id` | `int` | `—` |

```python
from sent.tl.functions.stories import StoriesDeleteAlbum
```

```python
result = await client(StoriesDeleteAlbum(...))
```

## StoriesGetAlbums

**Constructor ID:** `632548039`

**Fields:** ``peer`, `hash``

```python
StoriesGetAlbums.__init__(peer: 'InputPeer', hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `hash` | `int` | `—` |

```python
from sent.tl.functions.stories import StoriesGetAlbums
```

```python
result = await client(StoriesGetAlbums(...))
```

## StoriesGetAlbumStories

**Constructor ID:** `2894097761`

**Fields:** ``peer`, `album_id`, `offset`, `limit``

```python
StoriesGetAlbumStories.__init__(peer: 'InputPeer', album_id: int, offset: int, limit: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `album_id` | `int` | `—` |
| `offset` | `int` | `—` |
| `limit` | `int` | `—` |

```python
from sent.tl.functions.stories import StoriesGetAlbumStories
```

```python
result = await client(StoriesGetAlbumStories(...))
```

## StoriesStartLive

**Constructor ID:** `3496594654`

**Fields:** ``pinned`, `noforwards`, `rtmp_stream`, `peer`, `caption`, `entities`, `privacy_rules`, `random_id`, `messages_enabled`, `send_paid_messages_stars``

```python
StoriesStartLive.__init__(peer: 'InputPeer', privacy_rules: 'Vector', random_id: int, pinned: bool = None, noforwards: bool = None, rtmp_stream: bool = None, caption: str = None, entities: 'Vector' = None, messages_enabled: bool = None, send_paid_messages_stars: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `privacy_rules` | `'Vector'` | `—` |
| `random_id` | `int` | `—` |
| `pinned` | `bool` | `None` |
| `noforwards` | `bool` | `None` |
| `rtmp_stream` | `bool` | `None` |
| `caption` | `str` | `None` |
| `entities` | `'Vector'` | `None` |
| `messages_enabled` | `bool` | `None` |
| `send_paid_messages_stars` | `int` | `None` |

```python
from sent.tl.functions.stories import StoriesStartLive
```

```python
result = await client(StoriesStartLive(...))
```
