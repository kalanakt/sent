{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Functions: `payments`

Import from `sent.tl.functions.payments`.

## Contents

- [PaymentsGetPaymentForm](#paymentsgetpaymentform)
- [PaymentsGetPaymentReceipt](#paymentsgetpaymentreceipt)
- [PaymentsValidateRequestedInfo](#paymentsvalidaterequestedinfo)
- [PaymentsSendPaymentForm](#paymentssendpaymentform)
- [PaymentsGetSavedInfo](#paymentsgetsavedinfo)
- [PaymentsClearSavedInfo](#paymentsclearsavedinfo)
- [PaymentsGetBankCardData](#paymentsgetbankcarddata)
- [PaymentsExportInvoice](#paymentsexportinvoice)
- [PaymentsAssignAppStoreTransaction](#paymentsassignappstoretransaction)
- [PaymentsAssignPlayMarketTransaction](#paymentsassignplaymarkettransaction)
- [PaymentsGetPremiumGiftCodeOptions](#paymentsgetpremiumgiftcodeoptions)
- [PaymentsCheckGiftCode](#paymentscheckgiftcode)
- [PaymentsApplyGiftCode](#paymentsapplygiftcode)
- [PaymentsGetGiveawayInfo](#paymentsgetgiveawayinfo)
- [PaymentsLaunchPrepaidGiveaway](#paymentslaunchprepaidgiveaway)
- [PaymentsGetStarsTopupOptions](#paymentsgetstarstopupoptions)
- [PaymentsGetStarsStatus](#paymentsgetstarsstatus)
- [PaymentsGetStarsTransactions](#paymentsgetstarstransactions)
- [PaymentsSendStarsForm](#paymentssendstarsform)
- [PaymentsRefundStarsCharge](#paymentsrefundstarscharge)
- [PaymentsGetStarsRevenueStats](#paymentsgetstarsrevenuestats)
- [PaymentsGetStarsRevenueWithdrawalUrl](#paymentsgetstarsrevenuewithdrawalurl)
- [PaymentsGetStarsRevenueAdsAccountUrl](#paymentsgetstarsrevenueadsaccounturl)
- [PaymentsGetStarsTransactionsByID](#paymentsgetstarstransactionsbyid)
- [PaymentsGetStarsGiftOptions](#paymentsgetstarsgiftoptions)
- [PaymentsGetStarsSubscriptions](#paymentsgetstarssubscriptions)
- [PaymentsChangeStarsSubscription](#paymentschangestarssubscription)
- [PaymentsFulfillStarsSubscription](#paymentsfulfillstarssubscription)
- [PaymentsGetStarsGiveawayOptions](#paymentsgetstarsgiveawayoptions)
- [PaymentsGetStarGifts](#paymentsgetstargifts)
- [PaymentsSaveStarGift](#paymentssavestargift)
- [PaymentsConvertStarGift](#paymentsconvertstargift)
- [PaymentsBotCancelStarsSubscription](#paymentsbotcancelstarssubscription)
- [PaymentsGetConnectedStarRefBots](#paymentsgetconnectedstarrefbots)
- [PaymentsGetConnectedStarRefBot](#paymentsgetconnectedstarrefbot)
- [PaymentsGetSuggestedStarRefBots](#paymentsgetsuggestedstarrefbots)
- [PaymentsConnectStarRefBot](#paymentsconnectstarrefbot)
- [PaymentsEditConnectedStarRefBot](#paymentseditconnectedstarrefbot)
- [PaymentsGetStarGiftUpgradePreview](#paymentsgetstargiftupgradepreview)
- [PaymentsUpgradeStarGift](#paymentsupgradestargift)
- [PaymentsTransferStarGift](#paymentstransferstargift)
- [PaymentsGetUniqueStarGift](#paymentsgetuniquestargift)
- [PaymentsGetSavedStarGifts](#paymentsgetsavedstargifts)
- [PaymentsGetSavedStarGift](#paymentsgetsavedstargift)
- [PaymentsGetStarGiftWithdrawalUrl](#paymentsgetstargiftwithdrawalurl)
- [PaymentsToggleChatStarGiftNotifications](#paymentstogglechatstargiftnotifications)
- [PaymentsToggleStarGiftsPinnedToTop](#paymentstogglestargiftspinnedtotop)
- [PaymentsCanPurchaseStore](#paymentscanpurchasestore)
- [PaymentsGetResaleStarGifts](#paymentsgetresalestargifts)
- [PaymentsUpdateStarGiftPrice](#paymentsupdatestargiftprice)
- [PaymentsCreateStarGiftCollection](#paymentscreatestargiftcollection)
- [PaymentsUpdateStarGiftCollection](#paymentsupdatestargiftcollection)
- [PaymentsReorderStarGiftCollections](#paymentsreorderstargiftcollections)
- [PaymentsDeleteStarGiftCollection](#paymentsdeletestargiftcollection)
- [PaymentsGetStarGiftCollections](#paymentsgetstargiftcollections)
- [PaymentsGetUniqueStarGiftValueInfo](#paymentsgetuniquestargiftvalueinfo)
- [PaymentsCheckCanSendGift](#paymentscheckcansendgift)
- [PaymentsGetStarGiftAuctionState](#paymentsgetstargiftauctionstate)
- [PaymentsGetStarGiftAuctionAcquiredGifts](#paymentsgetstargiftauctionacquiredgifts)
- [PaymentsGetStarGiftActiveAuctions](#paymentsgetstargiftactiveauctions)
- [PaymentsResolveStarGiftOffer](#paymentsresolvestargiftoffer)
- [PaymentsSendStarGiftOffer](#paymentssendstargiftoffer)
- [PaymentsGetStarGiftUpgradeAttributes](#paymentsgetstargiftupgradeattributes)
- [PaymentsGetCraftStarGifts](#paymentsgetcraftstargifts)
- [PaymentsCraftStarGift](#paymentscraftstargift)

## PaymentsGetPaymentForm

**Constructor ID:** `924093883`

**Fields:** ``invoice`, `theme_params``

```python
PaymentsGetPaymentForm.__init__(invoice: 'InputInvoice', theme_params: 'DataJSON' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `invoice` | `'InputInvoice'` | `—` |
| `theme_params` | `'DataJSON'` | `None` |

```python
from sent.tl.functions.payments import PaymentsGetPaymentForm
```

```python
result = await client(PaymentsGetPaymentForm(...))
```

## PaymentsGetPaymentReceipt

**Constructor ID:** `611897804`

**Fields:** ``peer`, `msg_id``

```python
PaymentsGetPaymentReceipt.__init__(peer: 'InputPeer', msg_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |

```python
from sent.tl.functions.payments import PaymentsGetPaymentReceipt
```

```python
result = await client(PaymentsGetPaymentReceipt(...))
```

## PaymentsValidateRequestedInfo

**Constructor ID:** `3066622251`

**Fields:** ``save`, `invoice`, `info``

```python
PaymentsValidateRequestedInfo.__init__(invoice: 'InputInvoice', info: 'PaymentRequestedInfo', save: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `invoice` | `'InputInvoice'` | `—` |
| `info` | `'PaymentRequestedInfo'` | `—` |
| `save` | `bool` | `None` |

```python
from sent.tl.functions.payments import PaymentsValidateRequestedInfo
```

```python
result = await client(PaymentsValidateRequestedInfo(...))
```

## PaymentsSendPaymentForm

**Constructor ID:** `755192367`

**Fields:** ``form_id`, `invoice`, `requested_info_id`, `shipping_option_id`, `credentials`, `tip_amount``

```python
PaymentsSendPaymentForm.__init__(form_id: int, invoice: 'InputInvoice', credentials: 'InputPaymentCredentials', requested_info_id: str = None, shipping_option_id: str = None, tip_amount: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `form_id` | `int` | `—` |
| `invoice` | `'InputInvoice'` | `—` |
| `credentials` | `'InputPaymentCredentials'` | `—` |
| `requested_info_id` | `str` | `None` |
| `shipping_option_id` | `str` | `None` |
| `tip_amount` | `int` | `None` |

```python
from sent.tl.functions.payments import PaymentsSendPaymentForm
```

```python
result = await client(PaymentsSendPaymentForm(...))
```

## PaymentsGetSavedInfo

**Constructor ID:** `578650699`

```python
PaymentsGetSavedInfo.__init__()
```

_No parameters._

```python
from sent.tl.functions.payments import PaymentsGetSavedInfo
```

```python
result = await client(PaymentsGetSavedInfo(...))
```

## PaymentsClearSavedInfo

**Constructor ID:** `3627905217`

**Fields:** ``credentials`, `info``

```python
PaymentsClearSavedInfo.__init__(credentials: bool = None, info: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `credentials` | `bool` | `None` |
| `info` | `bool` | `None` |

```python
from sent.tl.functions.payments import PaymentsClearSavedInfo
```

```python
result = await client(PaymentsClearSavedInfo(...))
```

## PaymentsGetBankCardData

**Constructor ID:** `779736953`

```python
PaymentsGetBankCardData.__init__(number: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `number` | `str` | `—` |

```python
from sent.tl.functions.payments import PaymentsGetBankCardData
```

```python
result = await client(PaymentsGetBankCardData(...))
```

## PaymentsExportInvoice

**Constructor ID:** `261206117`

```python
PaymentsExportInvoice.__init__(invoice_media: 'InputMedia')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `invoice_media` | `'InputMedia'` | `—` |

```python
from sent.tl.functions.payments import PaymentsExportInvoice
```

```python
result = await client(PaymentsExportInvoice(...))
```

## PaymentsAssignAppStoreTransaction

**Constructor ID:** `2163045501`

**Fields:** ``receipt`, `purpose``

```python
PaymentsAssignAppStoreTransaction.__init__(receipt: bytes, purpose: 'InputStorePaymentPurpose')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `receipt` | `bytes` | `—` |
| `purpose` | `'InputStorePaymentPurpose'` | `—` |

```python
from sent.tl.functions.payments import PaymentsAssignAppStoreTransaction
```

```python
result = await client(PaymentsAssignAppStoreTransaction(...))
```

## PaymentsAssignPlayMarketTransaction

**Constructor ID:** `3757920467`

**Fields:** ``receipt`, `purpose``

```python
PaymentsAssignPlayMarketTransaction.__init__(receipt: 'DataJSON', purpose: 'InputStorePaymentPurpose')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `receipt` | `'DataJSON'` | `—` |
| `purpose` | `'InputStorePaymentPurpose'` | `—` |

```python
from sent.tl.functions.payments import PaymentsAssignPlayMarketTransaction
```

```python
result = await client(PaymentsAssignPlayMarketTransaction(...))
```

## PaymentsGetPremiumGiftCodeOptions

**Constructor ID:** `660060756`

```python
PaymentsGetPremiumGiftCodeOptions.__init__(boost_peer: 'InputPeer' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `boost_peer` | `'InputPeer'` | `None` |

```python
from sent.tl.functions.payments import PaymentsGetPremiumGiftCodeOptions
```

```python
result = await client(PaymentsGetPremiumGiftCodeOptions(...))
```

## PaymentsCheckGiftCode

**Constructor ID:** `2387719361`

```python
PaymentsCheckGiftCode.__init__(slug: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `slug` | `str` | `—` |

```python
from sent.tl.functions.payments import PaymentsCheckGiftCode
```

```python
result = await client(PaymentsCheckGiftCode(...))
```

## PaymentsApplyGiftCode

**Constructor ID:** `4142032980`

```python
PaymentsApplyGiftCode.__init__(slug: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `slug` | `str` | `—` |

```python
from sent.tl.functions.payments import PaymentsApplyGiftCode
```

```python
result = await client(PaymentsApplyGiftCode(...))
```

## PaymentsGetGiveawayInfo

**Constructor ID:** `4095972389`

**Fields:** ``peer`, `msg_id``

```python
PaymentsGetGiveawayInfo.__init__(peer: 'InputPeer', msg_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |

```python
from sent.tl.functions.payments import PaymentsGetGiveawayInfo
```

```python
result = await client(PaymentsGetGiveawayInfo(...))
```

## PaymentsLaunchPrepaidGiveaway

**Constructor ID:** `1609928480`

**Fields:** ``peer`, `giveaway_id`, `purpose``

```python
PaymentsLaunchPrepaidGiveaway.__init__(peer: 'InputPeer', giveaway_id: int, purpose: 'InputStorePaymentPurpose')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `giveaway_id` | `int` | `—` |
| `purpose` | `'InputStorePaymentPurpose'` | `—` |

```python
from sent.tl.functions.payments import PaymentsLaunchPrepaidGiveaway
```

```python
result = await client(PaymentsLaunchPrepaidGiveaway(...))
```

## PaymentsGetStarsTopupOptions

**Constructor ID:** `3222194131`

```python
PaymentsGetStarsTopupOptions.__init__()
```

_No parameters._

```python
from sent.tl.functions.payments import PaymentsGetStarsTopupOptions
```

```python
result = await client(PaymentsGetStarsTopupOptions(...))
```

## PaymentsGetStarsStatus

**Constructor ID:** `1319744447`

**Fields:** ``ton`, `peer``

```python
PaymentsGetStarsStatus.__init__(peer: 'InputPeer', ton: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `ton` | `bool` | `None` |

```python
from sent.tl.functions.payments import PaymentsGetStarsStatus
```

```python
result = await client(PaymentsGetStarsStatus(...))
```

## PaymentsGetStarsTransactions

**Constructor ID:** `1775912279`

**Fields:** ``inbound`, `outbound`, `ascending`, `ton`, `subscription_id`, `peer`, `offset`, `limit``

```python
PaymentsGetStarsTransactions.__init__(peer: 'InputPeer', offset: str, limit: int, inbound: bool = None, outbound: bool = None, ascending: bool = None, ton: bool = None, subscription_id: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `offset` | `str` | `—` |
| `limit` | `int` | `—` |
| `inbound` | `bool` | `None` |
| `outbound` | `bool` | `None` |
| `ascending` | `bool` | `None` |
| `ton` | `bool` | `None` |
| `subscription_id` | `str` | `None` |

```python
from sent.tl.functions.payments import PaymentsGetStarsTransactions
```

```python
result = await client(PaymentsGetStarsTransactions(...))
```

## PaymentsSendStarsForm

**Constructor ID:** `2040056084`

**Fields:** ``form_id`, `invoice``

```python
PaymentsSendStarsForm.__init__(form_id: int, invoice: 'InputInvoice')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `form_id` | `int` | `—` |
| `invoice` | `'InputInvoice'` | `—` |

```python
from sent.tl.functions.payments import PaymentsSendStarsForm
```

```python
result = await client(PaymentsSendStarsForm(...))
```

## PaymentsRefundStarsCharge

**Constructor ID:** `632196938`

**Fields:** ``user_id`, `charge_id``

```python
PaymentsRefundStarsCharge.__init__(user_id: 'InputUser', charge_id: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `user_id` | `'InputUser'` | `—` |
| `charge_id` | `str` | `—` |

```python
from sent.tl.functions.payments import PaymentsRefundStarsCharge
```

```python
result = await client(PaymentsRefundStarsCharge(...))
```

## PaymentsGetStarsRevenueStats

**Constructor ID:** `3642751702`

**Fields:** ``dark`, `ton`, `peer``

```python
PaymentsGetStarsRevenueStats.__init__(peer: 'InputPeer', dark: bool = None, ton: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `dark` | `bool` | `None` |
| `ton` | `bool` | `None` |

```python
from sent.tl.functions.payments import PaymentsGetStarsRevenueStats
```

```python
result = await client(PaymentsGetStarsRevenueStats(...))
```

## PaymentsGetStarsRevenueWithdrawalUrl

**Constructor ID:** `607378578`

**Fields:** ``ton`, `peer`, `amount`, `password``

```python
PaymentsGetStarsRevenueWithdrawalUrl.__init__(peer: 'InputPeer', password: 'InputCheckPasswordSRP', ton: bool = None, amount: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `password` | `'InputCheckPasswordSRP'` | `—` |
| `ton` | `bool` | `None` |
| `amount` | `int` | `None` |

```python
from sent.tl.functions.payments import PaymentsGetStarsRevenueWithdrawalUrl
```

```python
result = await client(PaymentsGetStarsRevenueWithdrawalUrl(...))
```

## PaymentsGetStarsRevenueAdsAccountUrl

**Constructor ID:** `3520589765`

```python
PaymentsGetStarsRevenueAdsAccountUrl.__init__(peer: 'InputPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |

```python
from sent.tl.functions.payments import PaymentsGetStarsRevenueAdsAccountUrl
```

```python
result = await client(PaymentsGetStarsRevenueAdsAccountUrl(...))
```

## PaymentsGetStarsTransactionsByID

**Constructor ID:** `768218808`

**Fields:** ``ton`, `peer`, `id``

```python
PaymentsGetStarsTransactionsByID.__init__(peer: 'InputPeer', id: 'Vector', ton: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `'Vector'` | `—` |
| `ton` | `bool` | `None` |

```python
from sent.tl.functions.payments import PaymentsGetStarsTransactionsByID
```

```python
result = await client(PaymentsGetStarsTransactionsByID(...))
```

## PaymentsGetStarsGiftOptions

**Constructor ID:** `3553192904`

```python
PaymentsGetStarsGiftOptions.__init__(user_id: 'InputUser' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `user_id` | `'InputUser'` | `None` |

```python
from sent.tl.functions.payments import PaymentsGetStarsGiftOptions
```

```python
result = await client(PaymentsGetStarsGiftOptions(...))
```

## PaymentsGetStarsSubscriptions

**Constructor ID:** `52761285`

**Fields:** ``missing_balance`, `peer`, `offset``

```python
PaymentsGetStarsSubscriptions.__init__(peer: 'InputPeer', offset: str, missing_balance: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `offset` | `str` | `—` |
| `missing_balance` | `bool` | `None` |

```python
from sent.tl.functions.payments import PaymentsGetStarsSubscriptions
```

```python
result = await client(PaymentsGetStarsSubscriptions(...))
```

## PaymentsChangeStarsSubscription

**Constructor ID:** `3346466936`

**Fields:** ``peer`, `subscription_id`, `canceled``

```python
PaymentsChangeStarsSubscription.__init__(peer: 'InputPeer', subscription_id: str, canceled: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `subscription_id` | `str` | `—` |
| `canceled` | `bool` | `None` |

```python
from sent.tl.functions.payments import PaymentsChangeStarsSubscription
```

```python
result = await client(PaymentsChangeStarsSubscription(...))
```

## PaymentsFulfillStarsSubscription

**Constructor ID:** `3428576179`

**Fields:** ``peer`, `subscription_id``

```python
PaymentsFulfillStarsSubscription.__init__(peer: 'InputPeer', subscription_id: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `subscription_id` | `str` | `—` |

```python
from sent.tl.functions.payments import PaymentsFulfillStarsSubscription
```

```python
result = await client(PaymentsFulfillStarsSubscription(...))
```

## PaymentsGetStarsGiveawayOptions

**Constructor ID:** `3172924734`

```python
PaymentsGetStarsGiveawayOptions.__init__()
```

_No parameters._

```python
from sent.tl.functions.payments import PaymentsGetStarsGiveawayOptions
```

```python
result = await client(PaymentsGetStarsGiveawayOptions(...))
```

## PaymentsGetStarGifts

**Constructor ID:** `3293984144`

```python
PaymentsGetStarGifts.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.payments import PaymentsGetStarGifts
```

```python
result = await client(PaymentsGetStarGifts(...))
```

## PaymentsSaveStarGift

**Constructor ID:** `707422588`

**Fields:** ``unsave`, `stargift``

```python
PaymentsSaveStarGift.__init__(stargift: 'InputSavedStarGift', unsave: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `stargift` | `'InputSavedStarGift'` | `—` |
| `unsave` | `bool` | `None` |

```python
from sent.tl.functions.payments import PaymentsSaveStarGift
```

```python
result = await client(PaymentsSaveStarGift(...))
```

## PaymentsConvertStarGift

**Constructor ID:** `1958676331`

```python
PaymentsConvertStarGift.__init__(stargift: 'InputSavedStarGift')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `stargift` | `'InputSavedStarGift'` | `—` |

```python
from sent.tl.functions.payments import PaymentsConvertStarGift
```

```python
result = await client(PaymentsConvertStarGift(...))
```

## PaymentsBotCancelStarsSubscription

**Constructor ID:** `1845102114`

**Fields:** ``restore`, `user_id`, `charge_id``

```python
PaymentsBotCancelStarsSubscription.__init__(user_id: 'InputUser', charge_id: str, restore: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `user_id` | `'InputUser'` | `—` |
| `charge_id` | `str` | `—` |
| `restore` | `bool` | `None` |

```python
from sent.tl.functions.payments import PaymentsBotCancelStarsSubscription
```

```python
result = await client(PaymentsBotCancelStarsSubscription(...))
```

## PaymentsGetConnectedStarRefBots

**Constructor ID:** `1483318611`

**Fields:** ``peer`, `offset_date`, `offset_link`, `limit``

```python
PaymentsGetConnectedStarRefBots.__init__(peer: 'InputPeer', limit: int, offset_date: int = None, offset_link: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `limit` | `int` | `—` |
| `offset_date` | `int` | `None` |
| `offset_link` | `str` | `None` |

```python
from sent.tl.functions.payments import PaymentsGetConnectedStarRefBots
```

```python
result = await client(PaymentsGetConnectedStarRefBots(...))
```

## PaymentsGetConnectedStarRefBot

**Constructor ID:** `3084490992`

**Fields:** ``peer`, `bot``

```python
PaymentsGetConnectedStarRefBot.__init__(peer: 'InputPeer', bot: 'InputUser')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `bot` | `'InputUser'` | `—` |

```python
from sent.tl.functions.payments import PaymentsGetConnectedStarRefBot
```

```python
result = await client(PaymentsGetConnectedStarRefBot(...))
```

## PaymentsGetSuggestedStarRefBots

**Constructor ID:** `225134839`

**Fields:** ``order_by_revenue`, `order_by_date`, `peer`, `offset`, `limit``

```python
PaymentsGetSuggestedStarRefBots.__init__(peer: 'InputPeer', offset: str, limit: int, order_by_revenue: bool = None, order_by_date: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `offset` | `str` | `—` |
| `limit` | `int` | `—` |
| `order_by_revenue` | `bool` | `None` |
| `order_by_date` | `bool` | `None` |

```python
from sent.tl.functions.payments import PaymentsGetSuggestedStarRefBots
```

```python
result = await client(PaymentsGetSuggestedStarRefBots(...))
```

## PaymentsConnectStarRefBot

**Constructor ID:** `2127901834`

**Fields:** ``peer`, `bot``

```python
PaymentsConnectStarRefBot.__init__(peer: 'InputPeer', bot: 'InputUser')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `bot` | `'InputUser'` | `—` |

```python
from sent.tl.functions.payments import PaymentsConnectStarRefBot
```

```python
result = await client(PaymentsConnectStarRefBot(...))
```

## PaymentsEditConnectedStarRefBot

**Constructor ID:** `3841762467`

**Fields:** ``revoked`, `peer`, `link``

```python
PaymentsEditConnectedStarRefBot.__init__(peer: 'InputPeer', link: str, revoked: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `link` | `str` | `—` |
| `revoked` | `bool` | `None` |

```python
from sent.tl.functions.payments import PaymentsEditConnectedStarRefBot
```

```python
result = await client(PaymentsEditConnectedStarRefBot(...))
```

## PaymentsGetStarGiftUpgradePreview

**Constructor ID:** `2627386545`

```python
PaymentsGetStarGiftUpgradePreview.__init__(gift_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `gift_id` | `int` | `—` |

```python
from sent.tl.functions.payments import PaymentsGetStarGiftUpgradePreview
```

```python
result = await client(PaymentsGetStarGiftUpgradePreview(...))
```

## PaymentsUpgradeStarGift

**Constructor ID:** `2933318901`

**Fields:** ``keep_original_details`, `stargift``

```python
PaymentsUpgradeStarGift.__init__(stargift: 'InputSavedStarGift', keep_original_details: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `stargift` | `'InputSavedStarGift'` | `—` |
| `keep_original_details` | `bool` | `None` |

```python
from sent.tl.functions.payments import PaymentsUpgradeStarGift
```

```python
result = await client(PaymentsUpgradeStarGift(...))
```

## PaymentsTransferStarGift

**Constructor ID:** `2132285290`

**Fields:** ``stargift`, `to_id``

```python
PaymentsTransferStarGift.__init__(stargift: 'InputSavedStarGift', to_id: 'InputPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `stargift` | `'InputSavedStarGift'` | `—` |
| `to_id` | `'InputPeer'` | `—` |

```python
from sent.tl.functions.payments import PaymentsTransferStarGift
```

```python
result = await client(PaymentsTransferStarGift(...))
```

## PaymentsGetUniqueStarGift

**Constructor ID:** `2711047538`

```python
PaymentsGetUniqueStarGift.__init__(slug: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `slug` | `str` | `—` |

```python
from sent.tl.functions.payments import PaymentsGetUniqueStarGift
```

```python
result = await client(PaymentsGetUniqueStarGift(...))
```

## PaymentsGetSavedStarGifts

**Constructor ID:** `2736383337`

**Fields:** ``exclude_unsaved`, `exclude_saved`, `exclude_unlimited`, `exclude_unique`, `sort_by_value`, `exclude_upgradable`, `exclude_unupgradable`, `peer_color_available`, `exclude_hosted`, `peer`, `collection_id`, `offset`, `limit``

```python
PaymentsGetSavedStarGifts.__init__(peer: 'InputPeer', offset: str, limit: int, exclude_unsaved: bool = None, exclude_saved: bool = None, exclude_unlimited: bool = None, exclude_unique: bool = None, sort_by_value: bool = None, exclude_upgradable: bool = None, exclude_unupgradable: bool = None, peer_color_available: bool = None, exclude_hosted: bool = None, collection_id: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `offset` | `str` | `—` |
| `limit` | `int` | `—` |
| `exclude_unsaved` | `bool` | `None` |
| `exclude_saved` | `bool` | `None` |
| `exclude_unlimited` | `bool` | `None` |
| `exclude_unique` | `bool` | `None` |
| `sort_by_value` | `bool` | `None` |
| `exclude_upgradable` | `bool` | `None` |
| `exclude_unupgradable` | `bool` | `None` |
| `peer_color_available` | `bool` | `None` |
| `exclude_hosted` | `bool` | `None` |
| `collection_id` | `int` | `None` |

```python
from sent.tl.functions.payments import PaymentsGetSavedStarGifts
```

```python
result = await client(PaymentsGetSavedStarGifts(...))
```

## PaymentsGetSavedStarGift

**Constructor ID:** `3025510662`

```python
PaymentsGetSavedStarGift.__init__(stargift: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `stargift` | `'Vector'` | `—` |

```python
from sent.tl.functions.payments import PaymentsGetSavedStarGift
```

```python
result = await client(PaymentsGetSavedStarGift(...))
```

## PaymentsGetStarGiftWithdrawalUrl

**Constructor ID:** `3496907688`

**Fields:** ``stargift`, `password``

```python
PaymentsGetStarGiftWithdrawalUrl.__init__(stargift: 'InputSavedStarGift', password: 'InputCheckPasswordSRP')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `stargift` | `'InputSavedStarGift'` | `—` |
| `password` | `'InputCheckPasswordSRP'` | `—` |

```python
from sent.tl.functions.payments import PaymentsGetStarGiftWithdrawalUrl
```

```python
result = await client(PaymentsGetStarGiftWithdrawalUrl(...))
```

## PaymentsToggleChatStarGiftNotifications

**Constructor ID:** `1626009505`

**Fields:** ``enabled`, `peer``

```python
PaymentsToggleChatStarGiftNotifications.__init__(peer: 'InputPeer', enabled: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `enabled` | `bool` | `None` |

```python
from sent.tl.functions.payments import PaymentsToggleChatStarGiftNotifications
```

```python
result = await client(PaymentsToggleChatStarGiftNotifications(...))
```

## PaymentsToggleStarGiftsPinnedToTop

**Constructor ID:** `353626032`

**Fields:** ``peer`, `stargift``

```python
PaymentsToggleStarGiftsPinnedToTop.__init__(peer: 'InputPeer', stargift: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `stargift` | `'Vector'` | `—` |

```python
from sent.tl.functions.payments import PaymentsToggleStarGiftsPinnedToTop
```

```python
result = await client(PaymentsToggleStarGiftsPinnedToTop(...))
```

## PaymentsCanPurchaseStore

**Constructor ID:** `1339842215`

```python
PaymentsCanPurchaseStore.__init__(purpose: 'InputStorePaymentPurpose')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `purpose` | `'InputStorePaymentPurpose'` | `—` |

```python
from sent.tl.functions.payments import PaymentsCanPurchaseStore
```

```python
result = await client(PaymentsCanPurchaseStore(...))
```

## PaymentsGetResaleStarGifts

**Constructor ID:** `2053087798`

**Fields:** ``sort_by_price`, `sort_by_num`, `for_craft`, `stars_only`, `attributes_hash`, `gift_id`, `attributes`, `offset`, `limit``

```python
PaymentsGetResaleStarGifts.__init__(gift_id: int, offset: str, limit: int, sort_by_price: bool = None, sort_by_num: bool = None, for_craft: bool = None, stars_only: bool = None, attributes_hash: int = None, attributes: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `gift_id` | `int` | `—` |
| `offset` | `str` | `—` |
| `limit` | `int` | `—` |
| `sort_by_price` | `bool` | `None` |
| `sort_by_num` | `bool` | `None` |
| `for_craft` | `bool` | `None` |
| `stars_only` | `bool` | `None` |
| `attributes_hash` | `int` | `None` |
| `attributes` | `'Vector'` | `None` |

```python
from sent.tl.functions.payments import PaymentsGetResaleStarGifts
```

```python
result = await client(PaymentsGetResaleStarGifts(...))
```

## PaymentsUpdateStarGiftPrice

**Constructor ID:** `3988679883`

**Fields:** ``stargift`, `resell_amount``

```python
PaymentsUpdateStarGiftPrice.__init__(stargift: 'InputSavedStarGift', resell_amount: 'StarsAmount')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `stargift` | `'InputSavedStarGift'` | `—` |
| `resell_amount` | `'StarsAmount'` | `—` |

```python
from sent.tl.functions.payments import PaymentsUpdateStarGiftPrice
```

```python
result = await client(PaymentsUpdateStarGiftPrice(...))
```

## PaymentsCreateStarGiftCollection

**Constructor ID:** `524947079`

**Fields:** ``peer`, `title`, `stargift``

```python
PaymentsCreateStarGiftCollection.__init__(peer: 'InputPeer', title: str, stargift: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `title` | `str` | `—` |
| `stargift` | `'Vector'` | `—` |

```python
from sent.tl.functions.payments import PaymentsCreateStarGiftCollection
```

```python
result = await client(PaymentsCreateStarGiftCollection(...))
```

## PaymentsUpdateStarGiftCollection

**Constructor ID:** `1339932391`

**Fields:** ``peer`, `collection_id`, `title`, `delete_stargift`, `add_stargift`, `order``

```python
PaymentsUpdateStarGiftCollection.__init__(peer: 'InputPeer', collection_id: int, title: str = None, delete_stargift: 'Vector' = None, add_stargift: 'Vector' = None, order: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `collection_id` | `int` | `—` |
| `title` | `str` | `None` |
| `delete_stargift` | `'Vector'` | `None` |
| `add_stargift` | `'Vector'` | `None` |
| `order` | `'Vector'` | `None` |

```python
from sent.tl.functions.payments import PaymentsUpdateStarGiftCollection
```

```python
result = await client(PaymentsUpdateStarGiftCollection(...))
```

## PaymentsReorderStarGiftCollections

**Constructor ID:** `3274372300`

**Fields:** ``peer`, `order``

```python
PaymentsReorderStarGiftCollections.__init__(peer: 'InputPeer', order: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `order` | `'Vector'` | `—` |

```python
from sent.tl.functions.payments import PaymentsReorderStarGiftCollections
```

```python
result = await client(PaymentsReorderStarGiftCollections(...))
```

## PaymentsDeleteStarGiftCollection

**Constructor ID:** `2908113128`

**Fields:** ``peer`, `collection_id``

```python
PaymentsDeleteStarGiftCollection.__init__(peer: 'InputPeer', collection_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `collection_id` | `int` | `—` |

```python
from sent.tl.functions.payments import PaymentsDeleteStarGiftCollection
```

```python
result = await client(PaymentsDeleteStarGiftCollection(...))
```

## PaymentsGetStarGiftCollections

**Constructor ID:** `2551943645`

**Fields:** ``peer`, `hash``

```python
PaymentsGetStarGiftCollections.__init__(peer: 'InputPeer', hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `hash` | `int` | `—` |

```python
from sent.tl.functions.payments import PaymentsGetStarGiftCollections
```

```python
result = await client(PaymentsGetStarGiftCollections(...))
```

## PaymentsGetUniqueStarGiftValueInfo

**Constructor ID:** `1130737515`

```python
PaymentsGetUniqueStarGiftValueInfo.__init__(slug: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `slug` | `str` | `—` |

```python
from sent.tl.functions.payments import PaymentsGetUniqueStarGiftValueInfo
```

```python
result = await client(PaymentsGetUniqueStarGiftValueInfo(...))
```

## PaymentsCheckCanSendGift

**Constructor ID:** `3234131401`

```python
PaymentsCheckCanSendGift.__init__(gift_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `gift_id` | `int` | `—` |

```python
from sent.tl.functions.payments import PaymentsCheckCanSendGift
```

```python
result = await client(PaymentsCheckCanSendGift(...))
```

## PaymentsGetStarGiftAuctionState

**Constructor ID:** `1553986774`

**Fields:** ``auction`, `version``

```python
PaymentsGetStarGiftAuctionState.__init__(auction: 'InputStarGiftAuction', version: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `auction` | `'InputStarGiftAuction'` | `—` |
| `version` | `int` | `—` |

```python
from sent.tl.functions.payments import PaymentsGetStarGiftAuctionState
```

```python
result = await client(PaymentsGetStarGiftAuctionState(...))
```

## PaymentsGetStarGiftAuctionAcquiredGifts

**Constructor ID:** `1805831148`

```python
PaymentsGetStarGiftAuctionAcquiredGifts.__init__(gift_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `gift_id` | `int` | `—` |

```python
from sent.tl.functions.payments import PaymentsGetStarGiftAuctionAcquiredGifts
```

```python
result = await client(PaymentsGetStarGiftAuctionAcquiredGifts(...))
```

## PaymentsGetStarGiftActiveAuctions

**Constructor ID:** `2781892941`

```python
PaymentsGetStarGiftActiveAuctions.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.payments import PaymentsGetStarGiftActiveAuctions
```

```python
result = await client(PaymentsGetStarGiftActiveAuctions(...))
```

## PaymentsResolveStarGiftOffer

**Constructor ID:** `3922622492`

**Fields:** ``decline`, `offer_msg_id``

```python
PaymentsResolveStarGiftOffer.__init__(offer_msg_id: int, decline: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `offer_msg_id` | `int` | `—` |
| `decline` | `bool` | `None` |

```python
from sent.tl.functions.payments import PaymentsResolveStarGiftOffer
```

```python
result = await client(PaymentsResolveStarGiftOffer(...))
```

## PaymentsSendStarGiftOffer

**Constructor ID:** `2411227969`

**Fields:** ``peer`, `slug`, `price`, `duration`, `random_id`, `allow_paid_stars``

```python
PaymentsSendStarGiftOffer.__init__(peer: 'InputPeer', slug: str, price: 'StarsAmount', duration: int, random_id: int, allow_paid_stars: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `slug` | `str` | `—` |
| `price` | `'StarsAmount'` | `—` |
| `duration` | `int` | `—` |
| `random_id` | `int` | `—` |
| `allow_paid_stars` | `int` | `None` |

```python
from sent.tl.functions.payments import PaymentsSendStarGiftOffer
```

```python
result = await client(PaymentsSendStarGiftOffer(...))
```

## PaymentsGetStarGiftUpgradeAttributes

**Constructor ID:** `1828948824`

```python
PaymentsGetStarGiftUpgradeAttributes.__init__(gift_id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `gift_id` | `int` | `—` |

```python
from sent.tl.functions.payments import PaymentsGetStarGiftUpgradeAttributes
```

```python
result = await client(PaymentsGetStarGiftUpgradeAttributes(...))
```

## PaymentsGetCraftStarGifts

**Constructor ID:** `4245019904`

**Fields:** ``gift_id`, `offset`, `limit``

```python
PaymentsGetCraftStarGifts.__init__(gift_id: int, offset: str, limit: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `gift_id` | `int` | `—` |
| `offset` | `str` | `—` |
| `limit` | `int` | `—` |

```python
from sent.tl.functions.payments import PaymentsGetCraftStarGifts
```

```python
result = await client(PaymentsGetCraftStarGifts(...))
```

## PaymentsCraftStarGift

**Constructor ID:** `2969135183`

```python
PaymentsCraftStarGift.__init__(stargift: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `stargift` | `'Vector'` | `—` |

```python
from sent.tl.functions.payments import PaymentsCraftStarGift
```

```python
result = await client(PaymentsCraftStarGift(...))
```
