{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Functions: `photos`

Import from `sent.tl.functions.photos`.

## Contents

- [PhotosUpdateProfilePhoto](#photosupdateprofilephoto)
- [PhotosUploadProfilePhoto](#photosuploadprofilephoto)
- [PhotosDeletePhotos](#photosdeletephotos)
- [PhotosGetUserPhotos](#photosgetuserphotos)
- [PhotosUploadContactProfilePhoto](#photosuploadcontactprofilephoto)

## PhotosUpdateProfilePhoto

**Constructor ID:** `166207545`

**Fields:** ``fallback`, `bot`, `id``

```python
PhotosUpdateProfilePhoto.__init__(id: 'InputPhoto', fallback: bool = None, bot: 'InputUser' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'InputPhoto'` | `—` |
| `fallback` | `bool` | `None` |
| `bot` | `'InputUser'` | `None` |

```python
from sent.tl.functions.photos import PhotosUpdateProfilePhoto
```

```python
result = await client(PhotosUpdateProfilePhoto(...))
```

## PhotosUploadProfilePhoto

**Constructor ID:** `59286453`

**Fields:** ``fallback`, `bot`, `file`, `video`, `video_start_ts`, `video_emoji_markup``

```python
PhotosUploadProfilePhoto.__init__(fallback: bool = None, bot: 'InputUser' = None, file: 'InputFile' = None, video: 'InputFile' = None, video_start_ts: float = None, video_emoji_markup: 'VideoSize' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `fallback` | `bool` | `None` |
| `bot` | `'InputUser'` | `None` |
| `file` | `'InputFile'` | `None` |
| `video` | `'InputFile'` | `None` |
| `video_start_ts` | `float` | `None` |
| `video_emoji_markup` | `'VideoSize'` | `None` |

```python
from sent.tl.functions.photos import PhotosUploadProfilePhoto
```

```python
result = await client(PhotosUploadProfilePhoto(...))
```

## PhotosDeletePhotos

**Constructor ID:** `2278522671`

```python
PhotosDeletePhotos.__init__(id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.photos import PhotosDeletePhotos
```

```python
result = await client(PhotosDeletePhotos(...))
```

## PhotosGetUserPhotos

**Constructor ID:** `2446144168`

**Fields:** ``user_id`, `offset`, `max_id`, `limit``

```python
PhotosGetUserPhotos.__init__(user_id: 'InputUser', offset: int, max_id: int, limit: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `user_id` | `'InputUser'` | `—` |
| `offset` | `int` | `—` |
| `max_id` | `int` | `—` |
| `limit` | `int` | `—` |

```python
from sent.tl.functions.photos import PhotosGetUserPhotos
```

```python
result = await client(PhotosGetUserPhotos(...))
```

## PhotosUploadContactProfilePhoto

**Constructor ID:** `3779873393`

**Fields:** ``suggest`, `save`, `user_id`, `file`, `video`, `video_start_ts`, `video_emoji_markup``

```python
PhotosUploadContactProfilePhoto.__init__(user_id: 'InputUser', suggest: bool = None, save: bool = None, file: 'InputFile' = None, video: 'InputFile' = None, video_start_ts: float = None, video_emoji_markup: 'VideoSize' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `user_id` | `'InputUser'` | `—` |
| `suggest` | `bool` | `None` |
| `save` | `bool` | `None` |
| `file` | `'InputFile'` | `None` |
| `video` | `'InputFile'` | `None` |
| `video_start_ts` | `float` | `None` |
| `video_emoji_markup` | `'VideoSize'` | `None` |

```python
from sent.tl.functions.photos import PhotosUploadContactProfilePhoto
```

```python
result = await client(PhotosUploadContactProfilePhoto(...))
```
