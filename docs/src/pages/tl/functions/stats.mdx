{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Functions: `stats`

Import from `sent.tl.functions.stats`.

## Contents

- [StatsGetBroadcastStats](#statsgetbroadcaststats)
- [StatsLoadAsyncGraph](#statsloadasyncgraph)
- [StatsGetMegagroupStats](#statsgetmegagroupstats)
- [StatsGetMessagePublicForwards](#statsgetmessagepublicforwards)
- [StatsGetMessageStats](#statsgetmessagestats)
- [StatsGetStoryStats](#statsgetstorystats)
- [StatsGetStoryPublicForwards](#statsgetstorypublicforwards)
- [StatsGetPollStats](#statsgetpollstats)

## StatsGetBroadcastStats

**Constructor ID:** `2873246746`

**Fields:** ``dark`, `channel``

```python
StatsGetBroadcastStats.__init__(channel: 'InputChannel', dark: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `dark` | `bool` | `None` |

```python
from sent.tl.functions.stats import StatsGetBroadcastStats
```

```python
result = await client(StatsGetBroadcastStats(...))
```

## StatsLoadAsyncGraph

**Constructor ID:** `1646092192`

**Fields:** ``token`, `x``

```python
StatsLoadAsyncGraph.__init__(token: str, x: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `token` | `str` | `—` |
| `x` | `int` | `None` |

```python
from sent.tl.functions.stats import StatsLoadAsyncGraph
```

```python
result = await client(StatsLoadAsyncGraph(...))
```

## StatsGetMegagroupStats

**Constructor ID:** `3705636359`

**Fields:** ``dark`, `channel``

```python
StatsGetMegagroupStats.__init__(channel: 'InputChannel', dark: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `dark` | `bool` | `None` |

```python
from sent.tl.functions.stats import StatsGetMegagroupStats
```

```python
result = await client(StatsGetMegagroupStats(...))
```

## StatsGetMessagePublicForwards

**Constructor ID:** `1595212100`

**Fields:** ``channel`, `msg_id`, `offset`, `limit``

```python
StatsGetMessagePublicForwards.__init__(channel: 'InputChannel', msg_id: int, offset: str, limit: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `msg_id` | `int` | `—` |
| `offset` | `str` | `—` |
| `limit` | `int` | `—` |

```python
from sent.tl.functions.stats import StatsGetMessagePublicForwards
```

```python
result = await client(StatsGetMessagePublicForwards(...))
```

## StatsGetMessageStats

**Constructor ID:** `3068175349`

**Fields:** ``dark`, `channel`, `msg_id``

```python
StatsGetMessageStats.__init__(channel: 'InputChannel', msg_id: int, dark: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channel` | `'InputChannel'` | `—` |
| `msg_id` | `int` | `—` |
| `dark` | `bool` | `None` |

```python
from sent.tl.functions.stats import StatsGetMessageStats
```

```python
result = await client(StatsGetMessageStats(...))
```

## StatsGetStoryStats

**Constructor ID:** `927985472`

**Fields:** ``dark`, `peer`, `id``

```python
StatsGetStoryStats.__init__(peer: 'InputPeer', id: int, dark: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `int` | `—` |
| `dark` | `bool` | `None` |

```python
from sent.tl.functions.stats import StatsGetStoryStats
```

```python
result = await client(StatsGetStoryStats(...))
```

## StatsGetStoryPublicForwards

**Constructor ID:** `2789441270`

**Fields:** ``peer`, `id`, `offset`, `limit``

```python
StatsGetStoryPublicForwards.__init__(peer: 'InputPeer', id: int, offset: str, limit: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `id` | `int` | `—` |
| `offset` | `str` | `—` |
| `limit` | `int` | `—` |

```python
from sent.tl.functions.stats import StatsGetStoryPublicForwards
```

```python
result = await client(StatsGetStoryPublicForwards(...))
```

## StatsGetPollStats

**Constructor ID:** `3263036008`

**Fields:** ``dark`, `peer`, `msg_id``

```python
StatsGetPollStats.__init__(peer: 'InputPeer', msg_id: int, dark: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'InputPeer'` | `—` |
| `msg_id` | `int` | `—` |
| `dark` | `bool` | `None` |

```python
from sent.tl.functions.stats import StatsGetPollStats
```

```python
result = await client(StatsGetPollStats(...))
```
