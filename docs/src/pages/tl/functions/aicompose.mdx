{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Functions: `aicompose`

Import from `sent.tl.functions.aicompose`.

## Contents

- [AicomposeCreateTone](#aicomposecreatetone)
- [AicomposeUpdateTone](#aicomposeupdatetone)
- [AicomposeSaveTone](#aicomposesavetone)
- [AicomposeDeleteTone](#aicomposedeletetone)
- [AicomposeGetTone](#aicomposegettone)
- [AicomposeGetTones](#aicomposegettones)
- [AicomposeGetToneExample](#aicomposegettoneexample)

## AicomposeCreateTone

**Constructor ID:** `1252538643`

**Fields:** ``display_author`, `emoji_id`, `title`, `prompt``

```python
AicomposeCreateTone.__init__(emoji_id: int, title: str, prompt: str, display_author: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `emoji_id` | `int` | `—` |
| `title` | `str` | `—` |
| `prompt` | `str` | `—` |
| `display_author` | `bool` | `None` |

```python
from sent.tl.functions.aicompose import AicomposeCreateTone
```

```python
result = await client(AicomposeCreateTone(...))
```

## AicomposeUpdateTone

**Constructor ID:** `2419838809`

**Fields:** ``tone`, `display_author`, `emoji_id`, `title`, `prompt``

```python
AicomposeUpdateTone.__init__(tone: 'InputAiComposeTone', display_author: bool = None, emoji_id: int = None, title: str = None, prompt: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `tone` | `'InputAiComposeTone'` | `—` |
| `display_author` | `bool` | `None` |
| `emoji_id` | `int` | `None` |
| `title` | `str` | `None` |
| `prompt` | `str` | `None` |

```python
from sent.tl.functions.aicompose import AicomposeUpdateTone
```

```python
result = await client(AicomposeUpdateTone(...))
```

## AicomposeSaveTone

**Constructor ID:** `394447793`

**Fields:** ``tone`, `unsave``

```python
AicomposeSaveTone.__init__(tone: 'InputAiComposeTone', unsave: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `tone` | `'InputAiComposeTone'` | `—` |
| `unsave` | `bool` | `—` |

```python
from sent.tl.functions.aicompose import AicomposeSaveTone
```

```python
result = await client(AicomposeSaveTone(...))
```

## AicomposeDeleteTone

**Constructor ID:** `3711512938`

```python
AicomposeDeleteTone.__init__(tone: 'InputAiComposeTone')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `tone` | `'InputAiComposeTone'` | `—` |

```python
from sent.tl.functions.aicompose import AicomposeDeleteTone
```

```python
result = await client(AicomposeDeleteTone(...))
```

## AicomposeGetTone

**Constructor ID:** `3001596419`

```python
AicomposeGetTone.__init__(tone: 'InputAiComposeTone')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `tone` | `'InputAiComposeTone'` | `—` |

```python
from sent.tl.functions.aicompose import AicomposeGetTone
```

```python
result = await client(AicomposeGetTone(...))
```

## AicomposeGetTones

**Constructor ID:** `2882900481`

```python
AicomposeGetTones.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.aicompose import AicomposeGetTones
```

```python
result = await client(AicomposeGetTones(...))
```

## AicomposeGetToneExample

**Constructor ID:** `3518278420`

**Fields:** ``tone`, `num``

```python
AicomposeGetToneExample.__init__(tone: 'InputAiComposeTone', num: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `tone` | `'InputAiComposeTone'` | `—` |
| `num` | `int` | `—` |

```python
from sent.tl.functions.aicompose import AicomposeGetToneExample
```

```python
result = await client(AicomposeGetToneExample(...))
```
