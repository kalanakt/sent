{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Functions: `contacts`

Import from `sent.tl.functions.contacts`.

## Contents

- [ContactsGetContactIDs](#contactsgetcontactids)
- [ContactsGetStatuses](#contactsgetstatuses)
- [ContactsGetContacts](#contactsgetcontacts)
- [ContactsImportContacts](#contactsimportcontacts)
- [ContactsDeleteContacts](#contactsdeletecontacts)
- [ContactsDeleteByPhones](#contactsdeletebyphones)
- [ContactsBlock](#contactsblock)
- [ContactsUnblock](#contactsunblock)
- [ContactsGetBlocked](#contactsgetblocked)
- [ContactsSearch](#contactssearch)
- [ContactsResolveUsername](#contactsresolveusername)
- [ContactsGetTopPeers](#contactsgettoppeers)
- [ContactsResetTopPeerRating](#contactsresettoppeerrating)
- [ContactsResetSaved](#contactsresetsaved)
- [ContactsGetSaved](#contactsgetsaved)
- [ContactsToggleTopPeers](#contactstoggletoppeers)
- [ContactsAddContact](#contactsaddcontact)
- [ContactsAcceptContact](#contactsacceptcontact)
- [ContactsGetLocated](#contactsgetlocated)
- [ContactsBlockFromReplies](#contactsblockfromreplies)
- [ContactsResolvePhone](#contactsresolvephone)
- [ContactsExportContactToken](#contactsexportcontacttoken)
- [ContactsImportContactToken](#contactsimportcontacttoken)
- [ContactsEditCloseFriends](#contactseditclosefriends)
- [ContactsSetBlocked](#contactssetblocked)
- [ContactsGetBirthdays](#contactsgetbirthdays)
- [ContactsGetSponsoredPeers](#contactsgetsponsoredpeers)
- [ContactsUpdateContactNote](#contactsupdatecontactnote)

## ContactsGetContactIDs

**Constructor ID:** `2061264541`

```python
ContactsGetContactIDs.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.contacts import ContactsGetContactIDs
```

```python
result = await client(ContactsGetContactIDs(...))
```

## ContactsGetStatuses

**Constructor ID:** `3299038190`

```python
ContactsGetStatuses.__init__()
```

_No parameters._

```python
from sent.tl.functions.contacts import ContactsGetStatuses
```

```python
result = await client(ContactsGetStatuses(...))
```

## ContactsGetContacts

**Constructor ID:** `1574346258`

```python
ContactsGetContacts.__init__(hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |

```python
from sent.tl.functions.contacts import ContactsGetContacts
```

```python
result = await client(ContactsGetContacts(...))
```

## ContactsImportContacts

**Constructor ID:** `746589157`

```python
ContactsImportContacts.__init__(contacts: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `contacts` | `'Vector'` | `—` |

```python
from sent.tl.functions.contacts import ContactsImportContacts
```

```python
result = await client(ContactsImportContacts(...))
```

## ContactsDeleteContacts

**Constructor ID:** `157945344`

```python
ContactsDeleteContacts.__init__(id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.contacts import ContactsDeleteContacts
```

```python
result = await client(ContactsDeleteContacts(...))
```

## ContactsDeleteByPhones

**Constructor ID:** `269745566`

```python
ContactsDeleteByPhones.__init__(phones: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `phones` | `'Vector'` | `—` |

```python
from sent.tl.functions.contacts import ContactsDeleteByPhones
```

```python
result = await client(ContactsDeleteByPhones(...))
```

## ContactsBlock

**Constructor ID:** `774801204`

**Fields:** ``my_stories_from`, `id``

```python
ContactsBlock.__init__(id: 'InputPeer', my_stories_from: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'InputPeer'` | `—` |
| `my_stories_from` | `bool` | `None` |

```python
from sent.tl.functions.contacts import ContactsBlock
```

```python
result = await client(ContactsBlock(...))
```

## ContactsUnblock

**Constructor ID:** `3041973032`

**Fields:** ``my_stories_from`, `id``

```python
ContactsUnblock.__init__(id: 'InputPeer', my_stories_from: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'InputPeer'` | `—` |
| `my_stories_from` | `bool` | `None` |

```python
from sent.tl.functions.contacts import ContactsUnblock
```

```python
result = await client(ContactsUnblock(...))
```

## ContactsGetBlocked

**Constructor ID:** `2592509824`

**Fields:** ``my_stories_from`, `offset`, `limit``

```python
ContactsGetBlocked.__init__(offset: int, limit: int, my_stories_from: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `offset` | `int` | `—` |
| `limit` | `int` | `—` |
| `my_stories_from` | `bool` | `None` |

```python
from sent.tl.functions.contacts import ContactsGetBlocked
```

```python
result = await client(ContactsGetBlocked(...))
```

## ContactsSearch

**Constructor ID:** `301470424`

**Fields:** ``q`, `limit``

```python
ContactsSearch.__init__(q: str, limit: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `q` | `str` | `—` |
| `limit` | `int` | `—` |

```python
from sent.tl.functions.contacts import ContactsSearch
```

```python
result = await client(ContactsSearch(...))
```

## ContactsResolveUsername

**Constructor ID:** `1918565308`

**Fields:** ``username`, `referer``

```python
ContactsResolveUsername.__init__(username: str, referer: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `username` | `str` | `—` |
| `referer` | `str` | `None` |

```python
from sent.tl.functions.contacts import ContactsResolveUsername
```

```python
result = await client(ContactsResolveUsername(...))
```

## ContactsGetTopPeers

**Constructor ID:** `2536798390`

**Fields:** ``correspondents`, `bots_pm`, `bots_inline`, `phone_calls`, `forward_users`, `forward_chats`, `groups`, `channels`, `bots_app`, `bots_guestchat`, `offset`, `limit`, `hash``

```python
ContactsGetTopPeers.__init__(offset: int, limit: int, hash: int, correspondents: bool = None, bots_pm: bool = None, bots_inline: bool = None, phone_calls: bool = None, forward_users: bool = None, forward_chats: bool = None, groups: bool = None, channels: bool = None, bots_app: bool = None, bots_guestchat: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `offset` | `int` | `—` |
| `limit` | `int` | `—` |
| `hash` | `int` | `—` |
| `correspondents` | `bool` | `None` |
| `bots_pm` | `bool` | `None` |
| `bots_inline` | `bool` | `None` |
| `phone_calls` | `bool` | `None` |
| `forward_users` | `bool` | `None` |
| `forward_chats` | `bool` | `None` |
| `groups` | `bool` | `None` |
| `channels` | `bool` | `None` |
| `bots_app` | `bool` | `None` |
| `bots_guestchat` | `bool` | `None` |

```python
from sent.tl.functions.contacts import ContactsGetTopPeers
```

```python
result = await client(ContactsGetTopPeers(...))
```

## ContactsResetTopPeerRating

**Constructor ID:** `451113900`

**Fields:** ``category`, `peer``

```python
ContactsResetTopPeerRating.__init__(category: 'TopPeerCategory', peer: 'InputPeer')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `category` | `'TopPeerCategory'` | `—` |
| `peer` | `'InputPeer'` | `—` |

```python
from sent.tl.functions.contacts import ContactsResetTopPeerRating
```

```python
result = await client(ContactsResetTopPeerRating(...))
```

## ContactsResetSaved

**Constructor ID:** `2274703345`

```python
ContactsResetSaved.__init__()
```

_No parameters._

```python
from sent.tl.functions.contacts import ContactsResetSaved
```

```python
result = await client(ContactsResetSaved(...))
```

## ContactsGetSaved

**Constructor ID:** `2196890527`

```python
ContactsGetSaved.__init__()
```

_No parameters._

```python
from sent.tl.functions.contacts import ContactsGetSaved
```

```python
result = await client(ContactsGetSaved(...))
```

## ContactsToggleTopPeers

**Constructor ID:** `2232729050`

```python
ContactsToggleTopPeers.__init__(enabled: bool)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `enabled` | `bool` | `—` |

```python
from sent.tl.functions.contacts import ContactsToggleTopPeers
```

```python
result = await client(ContactsToggleTopPeers(...))
```

## ContactsAddContact

**Constructor ID:** `3652857428`

**Fields:** ``add_phone_privacy_exception`, `id`, `first_name`, `last_name`, `phone`, `note``

```python
ContactsAddContact.__init__(id: 'InputUser', first_name: str, last_name: str, phone: str, add_phone_privacy_exception: bool = None, note: 'TextWithEntities' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'InputUser'` | `—` |
| `first_name` | `str` | `—` |
| `last_name` | `str` | `—` |
| `phone` | `str` | `—` |
| `add_phone_privacy_exception` | `bool` | `None` |
| `note` | `'TextWithEntities'` | `None` |

```python
from sent.tl.functions.contacts import ContactsAddContact
```

```python
result = await client(ContactsAddContact(...))
```

## ContactsAcceptContact

**Constructor ID:** `4164002319`

```python
ContactsAcceptContact.__init__(id: 'InputUser')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'InputUser'` | `—` |

```python
from sent.tl.functions.contacts import ContactsAcceptContact
```

```python
result = await client(ContactsAcceptContact(...))
```

## ContactsGetLocated

**Constructor ID:** `3544759364`

**Fields:** ``background`, `geo_point`, `self_expires``

```python
ContactsGetLocated.__init__(geo_point: 'InputGeoPoint', background: bool = None, self_expires: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `geo_point` | `'InputGeoPoint'` | `—` |
| `background` | `bool` | `None` |
| `self_expires` | `int` | `None` |

```python
from sent.tl.functions.contacts import ContactsGetLocated
```

```python
result = await client(ContactsGetLocated(...))
```

## ContactsBlockFromReplies

**Constructor ID:** `698914348`

**Fields:** ``delete_message`, `delete_history`, `report_spam`, `msg_id``

```python
ContactsBlockFromReplies.__init__(msg_id: int, delete_message: bool = None, delete_history: bool = None, report_spam: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `msg_id` | `int` | `—` |
| `delete_message` | `bool` | `None` |
| `delete_history` | `bool` | `None` |
| `report_spam` | `bool` | `None` |

```python
from sent.tl.functions.contacts import ContactsBlockFromReplies
```

```python
result = await client(ContactsBlockFromReplies(...))
```

## ContactsResolvePhone

**Constructor ID:** `2331591492`

```python
ContactsResolvePhone.__init__(phone: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `phone` | `str` | `—` |

```python
from sent.tl.functions.contacts import ContactsResolvePhone
```

```python
result = await client(ContactsResolvePhone(...))
```

## ContactsExportContactToken

**Constructor ID:** `4167385127`

```python
ContactsExportContactToken.__init__()
```

_No parameters._

```python
from sent.tl.functions.contacts import ContactsExportContactToken
```

```python
result = await client(ContactsExportContactToken(...))
```

## ContactsImportContactToken

**Constructor ID:** `318789512`

```python
ContactsImportContactToken.__init__(token: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `token` | `str` | `—` |

```python
from sent.tl.functions.contacts import ContactsImportContactToken
```

```python
result = await client(ContactsImportContactToken(...))
```

## ContactsEditCloseFriends

**Constructor ID:** `3127313904`

```python
ContactsEditCloseFriends.__init__(id: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'Vector'` | `—` |

```python
from sent.tl.functions.contacts import ContactsEditCloseFriends
```

```python
result = await client(ContactsEditCloseFriends(...))
```

## ContactsSetBlocked

**Constructor ID:** `2496027766`

**Fields:** ``my_stories_from`, `id`, `limit``

```python
ContactsSetBlocked.__init__(id: 'Vector', limit: int, my_stories_from: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'Vector'` | `—` |
| `limit` | `int` | `—` |
| `my_stories_from` | `bool` | `None` |

```python
from sent.tl.functions.contacts import ContactsSetBlocked
```

```python
result = await client(ContactsSetBlocked(...))
```

## ContactsGetBirthdays

**Constructor ID:** `3673008228`

```python
ContactsGetBirthdays.__init__()
```

_No parameters._

```python
from sent.tl.functions.contacts import ContactsGetBirthdays
```

```python
result = await client(ContactsGetBirthdays(...))
```

## ContactsGetSponsoredPeers

**Constructor ID:** `3066610579`

```python
ContactsGetSponsoredPeers.__init__(q: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `q` | `str` | `—` |

```python
from sent.tl.functions.contacts import ContactsGetSponsoredPeers
```

```python
result = await client(ContactsGetSponsoredPeers(...))
```

## ContactsUpdateContactNote

**Constructor ID:** `329212923`

**Fields:** ``id`, `note``

```python
ContactsUpdateContactNote.__init__(id: 'InputUser', note: 'TextWithEntities')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'InputUser'` | `—` |
| `note` | `'TextWithEntities'` | `—` |

```python
from sent.tl.functions.contacts import ContactsUpdateContactNote
```

```python
result = await client(ContactsUpdateContactNote(...))
```
