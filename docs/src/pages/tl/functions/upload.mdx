{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Functions: `upload`

Import from `sent.tl.functions.upload`.

## Contents

- [UploadSaveFilePart](#uploadsavefilepart)
- [UploadGetFile](#uploadgetfile)
- [UploadSaveBigFilePart](#uploadsavebigfilepart)
- [UploadGetWebFile](#uploadgetwebfile)
- [UploadGetCdnFile](#uploadgetcdnfile)
- [UploadReuploadCdnFile](#uploadreuploadcdnfile)
- [UploadGetCdnFileHashes](#uploadgetcdnfilehashes)
- [UploadGetFileHashes](#uploadgetfilehashes)

## UploadSaveFilePart

**Constructor ID:** `3003426337`

**Fields:** ``file_id`, `file_part`, `bytes``

```python
UploadSaveFilePart.__init__(file_id: int, file_part: int, bytes: bytes)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `file_id` | `int` | `—` |
| `file_part` | `int` | `—` |
| `bytes` | `bytes` | `—` |

```python
from sent.tl.functions.upload import UploadSaveFilePart
```

```python
result = await client(UploadSaveFilePart(...))
```

## UploadGetFile

**Constructor ID:** `3193124286`

**Fields:** ``precise`, `cdn_supported`, `location`, `offset`, `limit``

```python
UploadGetFile.__init__(location: 'InputFileLocation', offset: int, limit: int, precise: bool = None, cdn_supported: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `location` | `'InputFileLocation'` | `—` |
| `offset` | `int` | `—` |
| `limit` | `int` | `—` |
| `precise` | `bool` | `None` |
| `cdn_supported` | `bool` | `None` |

```python
from sent.tl.functions.upload import UploadGetFile
```

```python
result = await client(UploadGetFile(...))
```

## UploadSaveBigFilePart

**Constructor ID:** `3732629309`

**Fields:** ``file_id`, `file_part`, `file_total_parts`, `bytes``

```python
UploadSaveBigFilePart.__init__(file_id: int, file_part: int, file_total_parts: int, bytes: bytes)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `file_id` | `int` | `—` |
| `file_part` | `int` | `—` |
| `file_total_parts` | `int` | `—` |
| `bytes` | `bytes` | `—` |

```python
from sent.tl.functions.upload import UploadSaveBigFilePart
```

```python
result = await client(UploadSaveBigFilePart(...))
```

## UploadGetWebFile

**Constructor ID:** `619086221`

**Fields:** ``location`, `offset`, `limit``

```python
UploadGetWebFile.__init__(location: 'InputWebFileLocation', offset: int, limit: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `location` | `'InputWebFileLocation'` | `—` |
| `offset` | `int` | `—` |
| `limit` | `int` | `—` |

```python
from sent.tl.functions.upload import UploadGetWebFile
```

```python
result = await client(UploadGetWebFile(...))
```

## UploadGetCdnFile

**Constructor ID:** `962554330`

**Fields:** ``file_token`, `offset`, `limit``

```python
UploadGetCdnFile.__init__(file_token: bytes, offset: int, limit: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `file_token` | `bytes` | `—` |
| `offset` | `int` | `—` |
| `limit` | `int` | `—` |

```python
from sent.tl.functions.upload import UploadGetCdnFile
```

```python
result = await client(UploadGetCdnFile(...))
```

## UploadReuploadCdnFile

**Constructor ID:** `2603046056`

**Fields:** ``file_token`, `request_token``

```python
UploadReuploadCdnFile.__init__(file_token: bytes, request_token: bytes)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `file_token` | `bytes` | `—` |
| `request_token` | `bytes` | `—` |

```python
from sent.tl.functions.upload import UploadReuploadCdnFile
```

```python
result = await client(UploadReuploadCdnFile(...))
```

## UploadGetCdnFileHashes

**Constructor ID:** `2447130417`

**Fields:** ``file_token`, `offset``

```python
UploadGetCdnFileHashes.__init__(file_token: bytes, offset: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `file_token` | `bytes` | `—` |
| `offset` | `int` | `—` |

```python
from sent.tl.functions.upload import UploadGetCdnFileHashes
```

```python
result = await client(UploadGetCdnFileHashes(...))
```

## UploadGetFileHashes

**Constructor ID:** `2438371370`

**Fields:** ``location`, `offset``

```python
UploadGetFileHashes.__init__(location: 'InputFileLocation', offset: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `location` | `'InputFileLocation'` | `—` |
| `offset` | `int` | `—` |

```python
from sent.tl.functions.upload import UploadGetFileHashes
```

```python
result = await client(UploadGetFileHashes(...))
```
