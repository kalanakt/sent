{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Functions: `stickers`

Import from `sent.tl.functions.stickers`.

## Contents

- [StickersCreateStickerSet](#stickerscreatestickerset)
- [StickersRemoveStickerFromSet](#stickersremovestickerfromset)
- [StickersChangeStickerPosition](#stickerschangestickerposition)
- [StickersAddStickerToSet](#stickersaddstickertoset)
- [StickersSetStickerSetThumb](#stickerssetstickersetthumb)
- [StickersCheckShortName](#stickerscheckshortname)
- [StickersSuggestShortName](#stickerssuggestshortname)
- [StickersChangeSticker](#stickerschangesticker)
- [StickersRenameStickerSet](#stickersrenamestickerset)
- [StickersDeleteStickerSet](#stickersdeletestickerset)
- [StickersReplaceSticker](#stickersreplacesticker)

## StickersCreateStickerSet

**Constructor ID:** `2418125671`

**Fields:** ``masks`, `emojis`, `text_color`, `user_id`, `title`, `short_name`, `thumb`, `stickers`, `software``

```python
StickersCreateStickerSet.__init__(user_id: 'InputUser', title: str, short_name: str, stickers: 'Vector', masks: bool = None, emojis: bool = None, text_color: bool = None, thumb: 'InputDocument' = None, software: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `user_id` | `'InputUser'` | `—` |
| `title` | `str` | `—` |
| `short_name` | `str` | `—` |
| `stickers` | `'Vector'` | `—` |
| `masks` | `bool` | `None` |
| `emojis` | `bool` | `None` |
| `text_color` | `bool` | `None` |
| `thumb` | `'InputDocument'` | `None` |
| `software` | `str` | `None` |

```python
from sent.tl.functions.stickers import StickersCreateStickerSet
```

```python
result = await client(StickersCreateStickerSet(...))
```

## StickersRemoveStickerFromSet

**Constructor ID:** `4151709521`

```python
StickersRemoveStickerFromSet.__init__(sticker: 'InputDocument')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `sticker` | `'InputDocument'` | `—` |

```python
from sent.tl.functions.stickers import StickersRemoveStickerFromSet
```

```python
result = await client(StickersRemoveStickerFromSet(...))
```

## StickersChangeStickerPosition

**Constructor ID:** `4290172106`

**Fields:** ``sticker`, `position``

```python
StickersChangeStickerPosition.__init__(sticker: 'InputDocument', position: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `sticker` | `'InputDocument'` | `—` |
| `position` | `int` | `—` |

```python
from sent.tl.functions.stickers import StickersChangeStickerPosition
```

```python
result = await client(StickersChangeStickerPosition(...))
```

## StickersAddStickerToSet

**Constructor ID:** `2253651646`

**Fields:** ``stickerset`, `sticker``

```python
StickersAddStickerToSet.__init__(stickerset: 'InputStickerSet', sticker: 'InputStickerSetItem')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `stickerset` | `'InputStickerSet'` | `—` |
| `sticker` | `'InputStickerSetItem'` | `—` |

```python
from sent.tl.functions.stickers import StickersAddStickerToSet
```

```python
result = await client(StickersAddStickerToSet(...))
```

## StickersSetStickerSetThumb

**Constructor ID:** `2808763282`

**Fields:** ``stickerset`, `thumb`, `thumb_document_id``

```python
StickersSetStickerSetThumb.__init__(stickerset: 'InputStickerSet', thumb: 'InputDocument' = None, thumb_document_id: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `stickerset` | `'InputStickerSet'` | `—` |
| `thumb` | `'InputDocument'` | `None` |
| `thumb_document_id` | `int` | `None` |

```python
from sent.tl.functions.stickers import StickersSetStickerSetThumb
```

```python
result = await client(StickersSetStickerSetThumb(...))
```

## StickersCheckShortName

**Constructor ID:** `676017721`

```python
StickersCheckShortName.__init__(short_name: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `short_name` | `str` | `—` |

```python
from sent.tl.functions.stickers import StickersCheckShortName
```

```python
result = await client(StickersCheckShortName(...))
```

## StickersSuggestShortName

**Constructor ID:** `1303364867`

```python
StickersSuggestShortName.__init__(title: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `title` | `str` | `—` |

```python
from sent.tl.functions.stickers import StickersSuggestShortName
```

```python
result = await client(StickersSuggestShortName(...))
```

## StickersChangeSticker

**Constructor ID:** `4115889852`

**Fields:** ``sticker`, `emoji`, `mask_coords`, `keywords``

```python
StickersChangeSticker.__init__(sticker: 'InputDocument', emoji: str = None, mask_coords: 'MaskCoords' = None, keywords: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `sticker` | `'InputDocument'` | `—` |
| `emoji` | `str` | `None` |
| `mask_coords` | `'MaskCoords'` | `None` |
| `keywords` | `str` | `None` |

```python
from sent.tl.functions.stickers import StickersChangeSticker
```

```python
result = await client(StickersChangeSticker(...))
```

## StickersRenameStickerSet

**Constructor ID:** `306912256`

**Fields:** ``stickerset`, `title``

```python
StickersRenameStickerSet.__init__(stickerset: 'InputStickerSet', title: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `stickerset` | `'InputStickerSet'` | `—` |
| `title` | `str` | `—` |

```python
from sent.tl.functions.stickers import StickersRenameStickerSet
```

```python
result = await client(StickersRenameStickerSet(...))
```

## StickersDeleteStickerSet

**Constructor ID:** `2272281492`

```python
StickersDeleteStickerSet.__init__(stickerset: 'InputStickerSet')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `stickerset` | `'InputStickerSet'` | `—` |

```python
from sent.tl.functions.stickers import StickersDeleteStickerSet
```

```python
result = await client(StickersDeleteStickerSet(...))
```

## StickersReplaceSticker

**Constructor ID:** `1184253338`

**Fields:** ``sticker`, `new_sticker``

```python
StickersReplaceSticker.__init__(sticker: 'InputDocument', new_sticker: 'InputStickerSetItem')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `sticker` | `'InputDocument'` | `—` |
| `new_sticker` | `'InputStickerSetItem'` | `—` |

```python
from sent.tl.functions.stickers import StickersReplaceSticker
```

```python
result = await client(StickersReplaceSticker(...))
```
