{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Types: `stories`

Import from `sent.tl.types.stories`.

## Contents

- [StoriesAllStoriesNotModified](#storiesallstoriesnotmodified)
- [StoriesAllStories](#storiesallstories)
- [StoriesStories](#storiesstories)
- [StoriesStoryViewsList](#storiesstoryviewslist)
- [StoriesStoryViews](#storiesstoryviews)
- [StoriesPeerStories](#storiespeerstories)
- [StoriesStoryReactionsList](#storiesstoryreactionslist)
- [StoriesFoundStories](#storiesfoundstories)
- [StoriesCanSendStoryCount](#storiescansendstorycount)
- [StoriesAlbumsNotModified](#storiesalbumsnotmodified)
- [StoriesAlbums](#storiesalbums)

## StoriesAllStoriesNotModified

**Constructor ID:** `291044926`

**Fields:** ``state`, `stealth_mode``

```python
StoriesAllStoriesNotModified.__init__(state: str, stealth_mode: 'StoriesStealthMode')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `state` | `str` | `—` |
| `stealth_mode` | `'StoriesStealthMode'` | `—` |

```python
from sent.tl.types.stories import StoriesAllStoriesNotModified
```

## StoriesAllStories

**Constructor ID:** `1862033025`

**Fields:** ``has_more`, `count`, `state`, `peer_stories`, `chats`, `users`, `stealth_mode``

```python
StoriesAllStories.__init__(count: int, state: str, peer_stories: 'Vector', chats: 'Vector', users: 'Vector', stealth_mode: 'StoriesStealthMode', has_more: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `state` | `str` | `—` |
| `peer_stories` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `stealth_mode` | `'StoriesStealthMode'` | `—` |
| `has_more` | `bool` | `None` |

```python
from sent.tl.types.stories import StoriesAllStories
```

## StoriesStories

**Constructor ID:** `1673780490`

**Fields:** ``count`, `stories`, `pinned_to_top`, `chats`, `users``

```python
StoriesStories.__init__(count: int, stories: 'Vector', chats: 'Vector', users: 'Vector', pinned_to_top: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `stories` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `pinned_to_top` | `'Vector'` | `None` |

```python
from sent.tl.types.stories import StoriesStories
```

## StoriesStoryViewsList

**Constructor ID:** `1507299269`

**Fields:** ``count`, `views_count`, `forwards_count`, `reactions_count`, `views`, `chats`, `users`, `next_offset``

```python
StoriesStoryViewsList.__init__(count: int, views_count: int, forwards_count: int, reactions_count: int, views: 'Vector', chats: 'Vector', users: 'Vector', next_offset: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `views_count` | `int` | `—` |
| `forwards_count` | `int` | `—` |
| `reactions_count` | `int` | `—` |
| `views` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `next_offset` | `str` | `None` |

```python
from sent.tl.types.stories import StoriesStoryViewsList
```

## StoriesStoryViews

**Constructor ID:** `3734957341`

**Fields:** ``views`, `users``

```python
StoriesStoryViews.__init__(views: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `views` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.stories import StoriesStoryViews
```

## StoriesPeerStories

**Constructor ID:** `3404105576`

**Fields:** ``stories`, `chats`, `users``

```python
StoriesPeerStories.__init__(stories: 'PeerStories', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `stories` | `'PeerStories'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.stories import StoriesPeerStories
```

## StoriesStoryReactionsList

**Constructor ID:** `2858383516`

**Fields:** ``count`, `reactions`, `chats`, `users`, `next_offset``

```python
StoriesStoryReactionsList.__init__(count: int, reactions: 'Vector', chats: 'Vector', users: 'Vector', next_offset: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `reactions` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `next_offset` | `str` | `None` |

```python
from sent.tl.types.stories import StoriesStoryReactionsList
```

## StoriesFoundStories

**Constructor ID:** `3806230327`

**Fields:** ``count`, `stories`, `next_offset`, `chats`, `users``

```python
StoriesFoundStories.__init__(count: int, stories: 'Vector', chats: 'Vector', users: 'Vector', next_offset: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `stories` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `next_offset` | `str` | `None` |

```python
from sent.tl.types.stories import StoriesFoundStories
```

## StoriesCanSendStoryCount

**Constructor ID:** `3280453710`

```python
StoriesCanSendStoryCount.__init__(count_remains: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count_remains` | `int` | `—` |

```python
from sent.tl.types.stories import StoriesCanSendStoryCount
```

## StoriesAlbumsNotModified

**Constructor ID:** `1448008427`

```python
StoriesAlbumsNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.stories import StoriesAlbumsNotModified
```

## StoriesAlbums

**Constructor ID:** `3281549882`

**Fields:** ``hash`, `albums``

```python
StoriesAlbums.__init__(hash: int, albums: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `albums` | `'Vector'` | `—` |

```python
from sent.tl.types.stories import StoriesAlbums
```
