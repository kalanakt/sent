{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Types: `account`

Import from `sent.tl.types.account`.

## Contents

- [AccountPrivacyRules](#accountprivacyrules)
- [AccountAuthorizations](#accountauthorizations)
- [AccountPassword](#accountpassword)
- [AccountPasswordSettings](#accountpasswordsettings)
- [AccountPasswordInputSettings](#accountpasswordinputsettings)
- [AccountTmpPassword](#accounttmppassword)
- [AccountWebAuthorizations](#accountwebauthorizations)
- [AccountAuthorizationForm](#accountauthorizationform)
- [AccountSentEmailCode](#accountsentemailcode)
- [AccountTakeout](#accounttakeout)
- [AccountWallPapersNotModified](#accountwallpapersnotmodified)
- [AccountWallPapers](#accountwallpapers)
- [AccountAutoDownloadSettings](#accountautodownloadsettings)
- [AccountThemesNotModified](#accountthemesnotmodified)
- [AccountThemes](#accountthemes)
- [AccountContentSettings](#accountcontentsettings)
- [AccountResetPasswordFailedWait](#accountresetpasswordfailedwait)
- [AccountResetPasswordRequestedWait](#accountresetpasswordrequestedwait)
- [AccountResetPasswordOk](#accountresetpasswordok)
- [AccountChatThemesNotModified](#accountchatthemesnotmodified)
- [AccountChatThemes](#accountchatthemes)
- [AccountSavedRingtonesNotModified](#accountsavedringtonesnotmodified)
- [AccountSavedRingtones](#accountsavedringtones)
- [AccountSavedRingtone](#accountsavedringtone)
- [AccountSavedRingtoneConverted](#accountsavedringtoneconverted)
- [AccountEmojiStatusesNotModified](#accountemojistatusesnotmodified)
- [AccountEmojiStatuses](#accountemojistatuses)
- [AccountEmailVerified](#accountemailverified)
- [AccountEmailVerifiedLogin](#accountemailverifiedlogin)
- [AccountAutoSaveSettings](#accountautosavesettings)
- [AccountConnectedBots](#accountconnectedbots)
- [AccountBusinessChatLinks](#accountbusinesschatlinks)
- [AccountResolvedBusinessChatLinks](#accountresolvedbusinesschatlinks)
- [AccountPaidMessagesRevenue](#accountpaidmessagesrevenue)
- [AccountSavedMusicIdsNotModified](#accountsavedmusicidsnotmodified)
- [AccountSavedMusicIds](#accountsavedmusicids)
- [AccountPasskeys](#accountpasskeys)
- [AccountPasskeyRegistrationOptions](#accountpasskeyregistrationoptions)

## AccountPrivacyRules

**Constructor ID:** `1352683077`

**Fields:** ``rules`, `chats`, `users``

```python
AccountPrivacyRules.__init__(rules: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `rules` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.account import AccountPrivacyRules
```

## AccountAuthorizations

**Constructor ID:** `1275039392`

**Fields:** ``authorization_ttl_days`, `authorizations``

```python
AccountAuthorizations.__init__(authorization_ttl_days: int, authorizations: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `authorization_ttl_days` | `int` | `—` |
| `authorizations` | `'Vector'` | `—` |

```python
from sent.tl.types.account import AccountAuthorizations
```

## AccountPassword

**Constructor ID:** `2507886843`

**Fields:** ``has_recovery`, `has_secure_values`, `has_password`, `current_algo`, `srp_B`, `srp_id`, `hint`, `email_unconfirmed_pattern`, `new_algo`, `new_secure_algo`, `secure_random`, `pending_reset_date`, `login_email_pattern``

```python
AccountPassword.__init__(new_algo: 'PasswordKdfAlgo', new_secure_algo: 'SecurePasswordKdfAlgo', secure_random: bytes, has_recovery: bool = None, has_secure_values: bool = None, has_password: bool = None, current_algo: 'PasswordKdfAlgo' = None, srp_B: bytes = None, srp_id: int = None, hint: str = None, email_unconfirmed_pattern: str = None, pending_reset_date: int = None, login_email_pattern: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `new_algo` | `'PasswordKdfAlgo'` | `—` |
| `new_secure_algo` | `'SecurePasswordKdfAlgo'` | `—` |
| `secure_random` | `bytes` | `—` |
| `has_recovery` | `bool` | `None` |
| `has_secure_values` | `bool` | `None` |
| `has_password` | `bool` | `None` |
| `current_algo` | `'PasswordKdfAlgo'` | `None` |
| `srp_B` | `bytes` | `None` |
| `srp_id` | `int` | `None` |
| `hint` | `str` | `None` |
| `email_unconfirmed_pattern` | `str` | `None` |
| `pending_reset_date` | `int` | `None` |
| `login_email_pattern` | `str` | `None` |

```python
from sent.tl.types.account import AccountPassword
```

## AccountPasswordSettings

**Constructor ID:** `2589733861`

**Fields:** ``email`, `secure_settings``

```python
AccountPasswordSettings.__init__(email: str = None, secure_settings: 'SecureSecretSettings' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `email` | `str` | `None` |
| `secure_settings` | `'SecureSecretSettings'` | `None` |

```python
from sent.tl.types.account import AccountPasswordSettings
```

## AccountPasswordInputSettings

**Constructor ID:** `3258394569`

**Fields:** ``new_algo`, `new_password_hash`, `hint`, `email`, `new_secure_settings``

```python
AccountPasswordInputSettings.__init__(new_algo: 'PasswordKdfAlgo' = None, new_password_hash: bytes = None, hint: str = None, email: str = None, new_secure_settings: 'SecureSecretSettings' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `new_algo` | `'PasswordKdfAlgo'` | `None` |
| `new_password_hash` | `bytes` | `None` |
| `hint` | `str` | `None` |
| `email` | `str` | `None` |
| `new_secure_settings` | `'SecureSecretSettings'` | `None` |

```python
from sent.tl.types.account import AccountPasswordInputSettings
```

## AccountTmpPassword

**Constructor ID:** `3680828724`

**Fields:** ``tmp_password`, `valid_until``

```python
AccountTmpPassword.__init__(tmp_password: bytes, valid_until: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `tmp_password` | `bytes` | `—` |
| `valid_until` | `int` | `—` |

```python
from sent.tl.types.account import AccountTmpPassword
```

## AccountWebAuthorizations

**Constructor ID:** `3981887996`

**Fields:** ``authorizations`, `users``

```python
AccountWebAuthorizations.__init__(authorizations: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `authorizations` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.account import AccountWebAuthorizations
```

## AccountAuthorizationForm

**Constructor ID:** `2905480408`

**Fields:** ``required_types`, `values`, `errors`, `users`, `privacy_policy_url``

```python
AccountAuthorizationForm.__init__(required_types: 'Vector', values: 'Vector', errors: 'Vector', users: 'Vector', privacy_policy_url: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `required_types` | `'Vector'` | `—` |
| `values` | `'Vector'` | `—` |
| `errors` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `privacy_policy_url` | `str` | `None` |

```python
from sent.tl.types.account import AccountAuthorizationForm
```

## AccountSentEmailCode

**Constructor ID:** `2166326607`

**Fields:** ``email_pattern`, `length``

```python
AccountSentEmailCode.__init__(email_pattern: str, length: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `email_pattern` | `str` | `—` |
| `length` | `int` | `—` |

```python
from sent.tl.types.account import AccountSentEmailCode
```

## AccountTakeout

**Constructor ID:** `1304052993`

```python
AccountTakeout.__init__(id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `int` | `—` |

```python
from sent.tl.types.account import AccountTakeout
```

## AccountWallPapersNotModified

**Constructor ID:** `471437699`

```python
AccountWallPapersNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.account import AccountWallPapersNotModified
```

## AccountWallPapers

**Constructor ID:** `3452142988`

**Fields:** ``hash`, `wallpapers``

```python
AccountWallPapers.__init__(hash: int, wallpapers: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `wallpapers` | `'Vector'` | `—` |

```python
from sent.tl.types.account import AccountWallPapers
```

## AccountAutoDownloadSettings

**Constructor ID:** `1674235686`

**Fields:** ``low`, `medium`, `high``

```python
AccountAutoDownloadSettings.__init__(low: 'AutoDownloadSettings', medium: 'AutoDownloadSettings', high: 'AutoDownloadSettings')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `low` | `'AutoDownloadSettings'` | `—` |
| `medium` | `'AutoDownloadSettings'` | `—` |
| `high` | `'AutoDownloadSettings'` | `—` |

```python
from sent.tl.types.account import AccountAutoDownloadSettings
```

## AccountThemesNotModified

**Constructor ID:** `4095653410`

```python
AccountThemesNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.account import AccountThemesNotModified
```

## AccountThemes

**Constructor ID:** `2587724909`

**Fields:** ``hash`, `themes``

```python
AccountThemes.__init__(hash: int, themes: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `themes` | `'Vector'` | `—` |

```python
from sent.tl.types.account import AccountThemes
```

## AccountContentSettings

**Constructor ID:** `1474462241`

**Fields:** ``sensitive_enabled`, `sensitive_can_change``

```python
AccountContentSettings.__init__(sensitive_enabled: bool = None, sensitive_can_change: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `sensitive_enabled` | `bool` | `None` |
| `sensitive_can_change` | `bool` | `None` |

```python
from sent.tl.types.account import AccountContentSettings
```

## AccountResetPasswordFailedWait

**Constructor ID:** `3816265825`

```python
AccountResetPasswordFailedWait.__init__(retry_date: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `retry_date` | `int` | `—` |

```python
from sent.tl.types.account import AccountResetPasswordFailedWait
```

## AccountResetPasswordRequestedWait

**Constructor ID:** `3924819069`

```python
AccountResetPasswordRequestedWait.__init__(until_date: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `until_date` | `int` | `—` |

```python
from sent.tl.types.account import AccountResetPasswordRequestedWait
```

## AccountResetPasswordOk

**Constructor ID:** `3911636542`

```python
AccountResetPasswordOk.__init__()
```

_No parameters._

```python
from sent.tl.types.account import AccountResetPasswordOk
```

## AccountChatThemesNotModified

**Constructor ID:** `3759268292`

```python
AccountChatThemesNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.account import AccountChatThemesNotModified
```

## AccountChatThemes

**Constructor ID:** `3188294003`

**Fields:** ``hash`, `themes`, `chats`, `users`, `next_offset``

```python
AccountChatThemes.__init__(hash: int, themes: 'Vector', chats: 'Vector', users: 'Vector', next_offset: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `themes` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `next_offset` | `str` | `None` |

```python
from sent.tl.types.account import AccountChatThemes
```

## AccountSavedRingtonesNotModified

**Constructor ID:** `4227262641`

```python
AccountSavedRingtonesNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.account import AccountSavedRingtonesNotModified
```

## AccountSavedRingtones

**Constructor ID:** `3253284037`

**Fields:** ``hash`, `ringtones``

```python
AccountSavedRingtones.__init__(hash: int, ringtones: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `ringtones` | `'Vector'` | `—` |

```python
from sent.tl.types.account import AccountSavedRingtones
```

## AccountSavedRingtone

**Constructor ID:** `3072737133`

```python
AccountSavedRingtone.__init__()
```

_No parameters._

```python
from sent.tl.types.account import AccountSavedRingtone
```

## AccountSavedRingtoneConverted

**Constructor ID:** `523271863`

```python
AccountSavedRingtoneConverted.__init__(document: 'Document')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `document` | `'Document'` | `—` |

```python
from sent.tl.types.account import AccountSavedRingtoneConverted
```

## AccountEmojiStatusesNotModified

**Constructor ID:** `3498894917`

```python
AccountEmojiStatusesNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.account import AccountEmojiStatusesNotModified
```

## AccountEmojiStatuses

**Constructor ID:** `2428790737`

**Fields:** ``hash`, `statuses``

```python
AccountEmojiStatuses.__init__(hash: int, statuses: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `statuses` | `'Vector'` | `—` |

```python
from sent.tl.types.account import AccountEmojiStatuses
```

## AccountEmailVerified

**Constructor ID:** `731303195`

```python
AccountEmailVerified.__init__(email: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `email` | `str` | `—` |

```python
from sent.tl.types.account import AccountEmailVerified
```

## AccountEmailVerifiedLogin

**Constructor ID:** `3787132257`

**Fields:** ``email`, `sent_code``

```python
AccountEmailVerifiedLogin.__init__(email: str, sent_code: 'AuthSentCode')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `email` | `str` | `—` |
| `sent_code` | `'AuthSentCode'` | `—` |

```python
from sent.tl.types.account import AccountEmailVerifiedLogin
```

## AccountAutoSaveSettings

**Constructor ID:** `1279133341`

**Fields:** ``users_settings`, `chats_settings`, `broadcasts_settings`, `exceptions`, `chats`, `users``

```python
AccountAutoSaveSettings.__init__(users_settings: 'AutoSaveSettings', chats_settings: 'AutoSaveSettings', broadcasts_settings: 'AutoSaveSettings', exceptions: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `users_settings` | `'AutoSaveSettings'` | `—` |
| `chats_settings` | `'AutoSaveSettings'` | `—` |
| `broadcasts_settings` | `'AutoSaveSettings'` | `—` |
| `exceptions` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.account import AccountAutoSaveSettings
```

## AccountConnectedBots

**Constructor ID:** `400029819`

**Fields:** ``connected_bots`, `users``

```python
AccountConnectedBots.__init__(connected_bots: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `connected_bots` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.account import AccountConnectedBots
```

## AccountBusinessChatLinks

**Constructor ID:** `3963855569`

**Fields:** ``links`, `chats`, `users``

```python
AccountBusinessChatLinks.__init__(links: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `links` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.account import AccountBusinessChatLinks
```

## AccountResolvedBusinessChatLinks

**Constructor ID:** `2586029857`

**Fields:** ``peer`, `message`, `entities`, `chats`, `users``

```python
AccountResolvedBusinessChatLinks.__init__(peer: 'Peer', message: str, chats: 'Vector', users: 'Vector', entities: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'Peer'` | `—` |
| `message` | `str` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `entities` | `'Vector'` | `None` |

```python
from sent.tl.types.account import AccountResolvedBusinessChatLinks
```

## AccountPaidMessagesRevenue

**Constructor ID:** `504403720`

```python
AccountPaidMessagesRevenue.__init__(stars_amount: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `stars_amount` | `int` | `—` |

```python
from sent.tl.types.account import AccountPaidMessagesRevenue
```

## AccountSavedMusicIdsNotModified

**Constructor ID:** `1338514798`

```python
AccountSavedMusicIdsNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.account import AccountSavedMusicIdsNotModified
```

## AccountSavedMusicIds

**Constructor ID:** `2576180790`

```python
AccountSavedMusicIds.__init__(ids: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `ids` | `'Vector'` | `—` |

```python
from sent.tl.types.account import AccountSavedMusicIds
```

## AccountPasskeys

**Constructor ID:** `4175473180`

```python
AccountPasskeys.__init__(passkeys: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `passkeys` | `'Vector'` | `—` |

```python
from sent.tl.types.account import AccountPasskeys
```

## AccountPasskeyRegistrationOptions

**Constructor ID:** `3781909729`

```python
AccountPasskeyRegistrationOptions.__init__(options: 'DataJSON')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `options` | `'DataJSON'` | `—` |

```python
from sent.tl.types.account import AccountPasskeyRegistrationOptions
```
