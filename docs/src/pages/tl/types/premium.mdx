{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Types: `premium`

Import from `sent.tl.types.premium`.

## Contents

- [PremiumBoostsList](#premiumboostslist)
- [PremiumMyBoosts](#premiummyboosts)
- [PremiumBoostsStatus](#premiumboostsstatus)

## PremiumBoostsList

**Constructor ID:** `2264424764`

**Fields:** ``count`, `boosts`, `next_offset`, `users``

```python
PremiumBoostsList.__init__(count: int, boosts: 'Vector', users: 'Vector', next_offset: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `boosts` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `next_offset` | `str` | `None` |

```python
from sent.tl.types.premium import PremiumBoostsList
```

## PremiumMyBoosts

**Constructor ID:** `2598512866`

**Fields:** ``my_boosts`, `chats`, `users``

```python
PremiumMyBoosts.__init__(my_boosts: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `my_boosts` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.premium import PremiumMyBoosts
```

## PremiumBoostsStatus

**Constructor ID:** `1230586490`

**Fields:** ``my_boost`, `level`, `current_level_boosts`, `boosts`, `gift_boosts`, `next_level_boosts`, `premium_audience`, `boost_url`, `prepaid_giveaways`, `my_boost_slots``

```python
PremiumBoostsStatus.__init__(level: int, current_level_boosts: int, boosts: int, boost_url: str, my_boost: bool = None, gift_boosts: int = None, next_level_boosts: int = None, premium_audience: 'StatsPercentValue' = None, prepaid_giveaways: 'Vector' = None, my_boost_slots: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `level` | `int` | `—` |
| `current_level_boosts` | `int` | `—` |
| `boosts` | `int` | `—` |
| `boost_url` | `str` | `—` |
| `my_boost` | `bool` | `None` |
| `gift_boosts` | `int` | `None` |
| `next_level_boosts` | `int` | `None` |
| `premium_audience` | `'StatsPercentValue'` | `None` |
| `prepaid_giveaways` | `'Vector'` | `None` |
| `my_boost_slots` | `'Vector'` | `None` |

```python
from sent.tl.types.premium import PremiumBoostsStatus
```
