{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Types: `users`

Import from `sent.tl.types.users`.

## Contents

- [UsersUserFull](#usersuserfull)
- [UsersUsers](#usersusers)
- [UsersUsersSlice](#usersusersslice)
- [UsersSavedMusicNotModified](#userssavedmusicnotmodified)
- [UsersSavedMusic](#userssavedmusic)

## UsersUserFull

**Constructor ID:** `997004590`

**Fields:** ``full_user`, `chats`, `users``

```python
UsersUserFull.__init__(full_user: 'UserFull', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `full_user` | `'UserFull'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.users import UsersUserFull
```

## UsersUsers

**Constructor ID:** `1658259128`

```python
UsersUsers.__init__(users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.users import UsersUsers
```

## UsersUsersSlice

**Constructor ID:** `828000628`

**Fields:** ``count`, `users``

```python
UsersUsersSlice.__init__(count: int, users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.users import UsersUsersSlice
```

## UsersSavedMusicNotModified

**Constructor ID:** `3817310884`

```python
UsersSavedMusicNotModified.__init__(count: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |

```python
from sent.tl.types.users import UsersSavedMusicNotModified
```

## UsersSavedMusic

**Constructor ID:** `883094167`

**Fields:** ``count`, `documents``

```python
UsersSavedMusic.__init__(count: int, documents: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `documents` | `'Vector'` | `—` |

```python
from sent.tl.types.users import UsersSavedMusic
```
