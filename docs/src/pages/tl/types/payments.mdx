{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Types: `payments`

Import from `sent.tl.types.payments`.

## Contents

- [PaymentsPaymentForm](#paymentspaymentform)
- [PaymentsPaymentFormStars](#paymentspaymentformstars)
- [PaymentsPaymentFormStarGift](#paymentspaymentformstargift)
- [PaymentsValidatedRequestedInfo](#paymentsvalidatedrequestedinfo)
- [PaymentsPaymentResult](#paymentspaymentresult)
- [PaymentsPaymentVerificationNeeded](#paymentspaymentverificationneeded)
- [PaymentsPaymentReceipt](#paymentspaymentreceipt)
- [PaymentsPaymentReceiptStars](#paymentspaymentreceiptstars)
- [PaymentsSavedInfo](#paymentssavedinfo)
- [PaymentsBankCardData](#paymentsbankcarddata)
- [PaymentsExportedInvoice](#paymentsexportedinvoice)
- [PaymentsCheckedGiftCode](#paymentscheckedgiftcode)
- [PaymentsGiveawayInfo](#paymentsgiveawayinfo)
- [PaymentsGiveawayInfoResults](#paymentsgiveawayinforesults)
- [PaymentsStarsStatus](#paymentsstarsstatus)
- [PaymentsStarsRevenueStats](#paymentsstarsrevenuestats)
- [PaymentsStarsRevenueWithdrawalUrl](#paymentsstarsrevenuewithdrawalurl)
- [PaymentsStarsRevenueAdsAccountUrl](#paymentsstarsrevenueadsaccounturl)
- [PaymentsStarGiftsNotModified](#paymentsstargiftsnotmodified)
- [PaymentsStarGifts](#paymentsstargifts)
- [PaymentsConnectedStarRefBots](#paymentsconnectedstarrefbots)
- [PaymentsSuggestedStarRefBots](#paymentssuggestedstarrefbots)
- [PaymentsStarGiftUpgradePreview](#paymentsstargiftupgradepreview)
- [PaymentsUniqueStarGift](#paymentsuniquestargift)
- [PaymentsSavedStarGifts](#paymentssavedstargifts)
- [PaymentsStarGiftWithdrawalUrl](#paymentsstargiftwithdrawalurl)
- [PaymentsResaleStarGifts](#paymentsresalestargifts)
- [PaymentsStarGiftCollectionsNotModified](#paymentsstargiftcollectionsnotmodified)
- [PaymentsStarGiftCollections](#paymentsstargiftcollections)
- [PaymentsUniqueStarGiftValueInfo](#paymentsuniquestargiftvalueinfo)
- [PaymentsCheckCanSendGiftResultOk](#paymentscheckcansendgiftresultok)
- [PaymentsCheckCanSendGiftResultFail](#paymentscheckcansendgiftresultfail)
- [PaymentsStarGiftAuctionState](#paymentsstargiftauctionstate)
- [PaymentsStarGiftAuctionAcquiredGifts](#paymentsstargiftauctionacquiredgifts)
- [PaymentsStarGiftActiveAuctionsNotModified](#paymentsstargiftactiveauctionsnotmodified)
- [PaymentsStarGiftActiveAuctions](#paymentsstargiftactiveauctions)
- [PaymentsStarGiftUpgradeAttributes](#paymentsstargiftupgradeattributes)

## PaymentsPaymentForm

**Constructor ID:** `2684716881`

**Fields:** ``can_save_credentials`, `password_missing`, `form_id`, `bot_id`, `title`, `description`, `photo`, `invoice`, `provider_id`, `url`, `native_provider`, `native_params`, `additional_methods`, `saved_info`, `saved_credentials`, `users``

```python
PaymentsPaymentForm.__init__(form_id: int, bot_id: int, title: str, description: str, invoice: 'Invoice', provider_id: int, url: str, users: 'Vector', can_save_credentials: bool = None, password_missing: bool = None, photo: 'WebDocument' = None, native_provider: str = None, native_params: 'DataJSON' = None, additional_methods: 'Vector' = None, saved_info: 'PaymentRequestedInfo' = None, saved_credentials: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `form_id` | `int` | `—` |
| `bot_id` | `int` | `—` |
| `title` | `str` | `—` |
| `description` | `str` | `—` |
| `invoice` | `'Invoice'` | `—` |
| `provider_id` | `int` | `—` |
| `url` | `str` | `—` |
| `users` | `'Vector'` | `—` |
| `can_save_credentials` | `bool` | `None` |
| `password_missing` | `bool` | `None` |
| `photo` | `'WebDocument'` | `None` |
| `native_provider` | `str` | `None` |
| `native_params` | `'DataJSON'` | `None` |
| `additional_methods` | `'Vector'` | `None` |
| `saved_info` | `'PaymentRequestedInfo'` | `None` |
| `saved_credentials` | `'Vector'` | `None` |

```python
from sent.tl.types.payments import PaymentsPaymentForm
```

## PaymentsPaymentFormStars

**Constructor ID:** `2079764828`

**Fields:** ``form_id`, `bot_id`, `title`, `description`, `photo`, `invoice`, `users``

```python
PaymentsPaymentFormStars.__init__(form_id: int, bot_id: int, title: str, description: str, invoice: 'Invoice', users: 'Vector', photo: 'WebDocument' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `form_id` | `int` | `—` |
| `bot_id` | `int` | `—` |
| `title` | `str` | `—` |
| `description` | `str` | `—` |
| `invoice` | `'Invoice'` | `—` |
| `users` | `'Vector'` | `—` |
| `photo` | `'WebDocument'` | `None` |

```python
from sent.tl.types.payments import PaymentsPaymentFormStars
```

## PaymentsPaymentFormStarGift

**Constructor ID:** `3022376929`

**Fields:** ``form_id`, `invoice``

```python
PaymentsPaymentFormStarGift.__init__(form_id: int, invoice: 'Invoice')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `form_id` | `int` | `—` |
| `invoice` | `'Invoice'` | `—` |

```python
from sent.tl.types.payments import PaymentsPaymentFormStarGift
```

## PaymentsValidatedRequestedInfo

**Constructor ID:** `3510966403`

**Fields:** ``id`, `shipping_options``

```python
PaymentsValidatedRequestedInfo.__init__(id: str = None, shipping_options: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `str` | `None` |
| `shipping_options` | `'Vector'` | `None` |

```python
from sent.tl.types.payments import PaymentsValidatedRequestedInfo
```

## PaymentsPaymentResult

**Constructor ID:** `1314881805`

```python
PaymentsPaymentResult.__init__(updates: 'Updates')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `updates` | `'Updates'` | `—` |

```python
from sent.tl.types.payments import PaymentsPaymentResult
```

## PaymentsPaymentVerificationNeeded

**Constructor ID:** `3628142905`

```python
PaymentsPaymentVerificationNeeded.__init__(url: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `url` | `str` | `—` |

```python
from sent.tl.types.payments import PaymentsPaymentVerificationNeeded
```

## PaymentsPaymentReceipt

**Constructor ID:** `1891958275`

**Fields:** ``date`, `bot_id`, `provider_id`, `title`, `description`, `photo`, `invoice`, `info`, `shipping`, `tip_amount`, `currency`, `total_amount`, `credentials_title`, `users``

```python
PaymentsPaymentReceipt.__init__(date: int, bot_id: int, provider_id: int, title: str, description: str, invoice: 'Invoice', currency: str, total_amount: int, credentials_title: str, users: 'Vector', photo: 'WebDocument' = None, info: 'PaymentRequestedInfo' = None, shipping: 'ShippingOption' = None, tip_amount: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `date` | `int` | `—` |
| `bot_id` | `int` | `—` |
| `provider_id` | `int` | `—` |
| `title` | `str` | `—` |
| `description` | `str` | `—` |
| `invoice` | `'Invoice'` | `—` |
| `currency` | `str` | `—` |
| `total_amount` | `int` | `—` |
| `credentials_title` | `str` | `—` |
| `users` | `'Vector'` | `—` |
| `photo` | `'WebDocument'` | `None` |
| `info` | `'PaymentRequestedInfo'` | `None` |
| `shipping` | `'ShippingOption'` | `None` |
| `tip_amount` | `int` | `None` |

```python
from sent.tl.types.payments import PaymentsPaymentReceipt
```

## PaymentsPaymentReceiptStars

**Constructor ID:** `3669751866`

**Fields:** ``date`, `bot_id`, `title`, `description`, `photo`, `invoice`, `currency`, `total_amount`, `transaction_id`, `users``

```python
PaymentsPaymentReceiptStars.__init__(date: int, bot_id: int, title: str, description: str, invoice: 'Invoice', currency: str, total_amount: int, transaction_id: str, users: 'Vector', photo: 'WebDocument' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `date` | `int` | `—` |
| `bot_id` | `int` | `—` |
| `title` | `str` | `—` |
| `description` | `str` | `—` |
| `invoice` | `'Invoice'` | `—` |
| `currency` | `str` | `—` |
| `total_amount` | `int` | `—` |
| `transaction_id` | `str` | `—` |
| `users` | `'Vector'` | `—` |
| `photo` | `'WebDocument'` | `None` |

```python
from sent.tl.types.payments import PaymentsPaymentReceiptStars
```

## PaymentsSavedInfo

**Constructor ID:** `4220511292`

**Fields:** ``has_saved_credentials`, `saved_info``

```python
PaymentsSavedInfo.__init__(has_saved_credentials: bool = None, saved_info: 'PaymentRequestedInfo' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `has_saved_credentials` | `bool` | `None` |
| `saved_info` | `'PaymentRequestedInfo'` | `None` |

```python
from sent.tl.types.payments import PaymentsSavedInfo
```

## PaymentsBankCardData

**Constructor ID:** `1042605427`

**Fields:** ``title`, `open_urls``

```python
PaymentsBankCardData.__init__(title: str, open_urls: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `title` | `str` | `—` |
| `open_urls` | `'Vector'` | `—` |

```python
from sent.tl.types.payments import PaymentsBankCardData
```

## PaymentsExportedInvoice

**Constructor ID:** `2932919257`

```python
PaymentsExportedInvoice.__init__(url: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `url` | `str` | `—` |

```python
from sent.tl.types.payments import PaymentsExportedInvoice
```

## PaymentsCheckedGiftCode

**Constructor ID:** `3952623503`

**Fields:** ``via_giveaway`, `from_id`, `giveaway_msg_id`, `to_id`, `date`, `days`, `used_date`, `chats`, `users``

```python
PaymentsCheckedGiftCode.__init__(date: int, days: int, chats: 'Vector', users: 'Vector', via_giveaway: bool = None, from_id: 'Peer' = None, giveaway_msg_id: int = None, to_id: int = None, used_date: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `date` | `int` | `—` |
| `days` | `int` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `via_giveaway` | `bool` | `None` |
| `from_id` | `'Peer'` | `None` |
| `giveaway_msg_id` | `int` | `None` |
| `to_id` | `int` | `None` |
| `used_date` | `int` | `None` |

```python
from sent.tl.types.payments import PaymentsCheckedGiftCode
```

## PaymentsGiveawayInfo

**Constructor ID:** `1130879648`

**Fields:** ``participating`, `preparing_results`, `start_date`, `joined_too_early_date`, `admin_disallowed_chat_id`, `disallowed_country``

```python
PaymentsGiveawayInfo.__init__(start_date: int, participating: bool = None, preparing_results: bool = None, joined_too_early_date: int = None, admin_disallowed_chat_id: int = None, disallowed_country: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `start_date` | `int` | `—` |
| `participating` | `bool` | `None` |
| `preparing_results` | `bool` | `None` |
| `joined_too_early_date` | `int` | `None` |
| `admin_disallowed_chat_id` | `int` | `None` |
| `disallowed_country` | `str` | `None` |

```python
from sent.tl.types.payments import PaymentsGiveawayInfo
```

## PaymentsGiveawayInfoResults

**Constructor ID:** `3782600303`

**Fields:** ``winner`, `refunded`, `start_date`, `gift_code_slug`, `stars_prize`, `finish_date`, `winners_count`, `activated_count``

```python
PaymentsGiveawayInfoResults.__init__(start_date: int, finish_date: int, winners_count: int, winner: bool = None, refunded: bool = None, gift_code_slug: str = None, stars_prize: int = None, activated_count: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `start_date` | `int` | `—` |
| `finish_date` | `int` | `—` |
| `winners_count` | `int` | `—` |
| `winner` | `bool` | `None` |
| `refunded` | `bool` | `None` |
| `gift_code_slug` | `str` | `None` |
| `stars_prize` | `int` | `None` |
| `activated_count` | `int` | `None` |

```python
from sent.tl.types.payments import PaymentsGiveawayInfoResults
```

## PaymentsStarsStatus

**Constructor ID:** `1822222573`

**Fields:** ``balance`, `subscriptions`, `subscriptions_next_offset`, `subscriptions_missing_balance`, `history`, `next_offset`, `chats`, `users``

```python
PaymentsStarsStatus.__init__(balance: 'StarsAmount', chats: 'Vector', users: 'Vector', subscriptions: 'Vector' = None, subscriptions_next_offset: str = None, subscriptions_missing_balance: int = None, history: 'Vector' = None, next_offset: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `balance` | `'StarsAmount'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `subscriptions` | `'Vector'` | `None` |
| `subscriptions_next_offset` | `str` | `None` |
| `subscriptions_missing_balance` | `int` | `None` |
| `history` | `'Vector'` | `None` |
| `next_offset` | `str` | `None` |

```python
from sent.tl.types.payments import PaymentsStarsStatus
```

## PaymentsStarsRevenueStats

**Constructor ID:** `1814066038`

**Fields:** ``top_hours_graph`, `revenue_graph`, `status`, `usd_rate``

```python
PaymentsStarsRevenueStats.__init__(revenue_graph: 'StatsGraph', status: 'StarsRevenueStatus', usd_rate: float, top_hours_graph: 'StatsGraph' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `revenue_graph` | `'StatsGraph'` | `—` |
| `status` | `'StarsRevenueStatus'` | `—` |
| `usd_rate` | `float` | `—` |
| `top_hours_graph` | `'StatsGraph'` | `None` |

```python
from sent.tl.types.payments import PaymentsStarsRevenueStats
```

## PaymentsStarsRevenueWithdrawalUrl

**Constructor ID:** `497778871`

```python
PaymentsStarsRevenueWithdrawalUrl.__init__(url: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `url` | `str` | `—` |

```python
from sent.tl.types.payments import PaymentsStarsRevenueWithdrawalUrl
```

## PaymentsStarsRevenueAdsAccountUrl

**Constructor ID:** `961445665`

```python
PaymentsStarsRevenueAdsAccountUrl.__init__(url: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `url` | `str` | `—` |

```python
from sent.tl.types.payments import PaymentsStarsRevenueAdsAccountUrl
```

## PaymentsStarGiftsNotModified

**Constructor ID:** `2743640936`

```python
PaymentsStarGiftsNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.payments import PaymentsStarGiftsNotModified
```

## PaymentsStarGifts

**Constructor ID:** `785918357`

**Fields:** ``hash`, `gifts`, `chats`, `users``

```python
PaymentsStarGifts.__init__(hash: int, gifts: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `gifts` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.payments import PaymentsStarGifts
```

## PaymentsConnectedStarRefBots

**Constructor ID:** `2564155933`

**Fields:** ``count`, `connected_bots`, `users``

```python
PaymentsConnectedStarRefBots.__init__(count: int, connected_bots: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `connected_bots` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.payments import PaymentsConnectedStarRefBots
```

## PaymentsSuggestedStarRefBots

**Constructor ID:** `3033913433`

**Fields:** ``count`, `suggested_bots`, `users`, `next_offset``

```python
PaymentsSuggestedStarRefBots.__init__(count: int, suggested_bots: 'Vector', users: 'Vector', next_offset: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `suggested_bots` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `next_offset` | `str` | `None` |

```python
from sent.tl.types.payments import PaymentsSuggestedStarRefBots
```

## PaymentsStarGiftUpgradePreview

**Constructor ID:** `1038213101`

**Fields:** ``sample_attributes`, `prices`, `next_prices``

```python
PaymentsStarGiftUpgradePreview.__init__(sample_attributes: 'Vector', prices: 'Vector', next_prices: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `sample_attributes` | `'Vector'` | `—` |
| `prices` | `'Vector'` | `—` |
| `next_prices` | `'Vector'` | `—` |

```python
from sent.tl.types.payments import PaymentsStarGiftUpgradePreview
```

## PaymentsUniqueStarGift

**Constructor ID:** `1097619176`

**Fields:** ``gift`, `chats`, `users``

```python
PaymentsUniqueStarGift.__init__(gift: 'StarGift', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `gift` | `'StarGift'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.payments import PaymentsUniqueStarGift
```

## PaymentsSavedStarGifts

**Constructor ID:** `2515765681`

**Fields:** ``count`, `chat_notifications_enabled`, `gifts`, `next_offset`, `chats`, `users``

```python
PaymentsSavedStarGifts.__init__(count: int, gifts: 'Vector', chats: 'Vector', users: 'Vector', chat_notifications_enabled: bool = None, next_offset: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `gifts` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `chat_notifications_enabled` | `bool` | `None` |
| `next_offset` | `str` | `None` |

```python
from sent.tl.types.payments import PaymentsSavedStarGifts
```

## PaymentsStarGiftWithdrawalUrl

**Constructor ID:** `2225748636`

```python
PaymentsStarGiftWithdrawalUrl.__init__(url: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `url` | `str` | `—` |

```python
from sent.tl.types.payments import PaymentsStarGiftWithdrawalUrl
```

## PaymentsResaleStarGifts

**Constructor ID:** `2491028191`

**Fields:** ``count`, `gifts`, `next_offset`, `attributes`, `attributes_hash`, `chats`, `counters`, `users``

```python
PaymentsResaleStarGifts.__init__(count: int, gifts: 'Vector', chats: 'Vector', users: 'Vector', next_offset: str = None, attributes: 'Vector' = None, attributes_hash: int = None, counters: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `gifts` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `next_offset` | `str` | `None` |
| `attributes` | `'Vector'` | `None` |
| `attributes_hash` | `int` | `None` |
| `counters` | `'Vector'` | `None` |

```python
from sent.tl.types.payments import PaymentsResaleStarGifts
```

## PaymentsStarGiftCollectionsNotModified

**Constructor ID:** `2696564503`

```python
PaymentsStarGiftCollectionsNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.payments import PaymentsStarGiftCollectionsNotModified
```

## PaymentsStarGiftCollections

**Constructor ID:** `2317955827`

```python
PaymentsStarGiftCollections.__init__(collections: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `collections` | `'Vector'` | `—` |

```python
from sent.tl.types.payments import PaymentsStarGiftCollections
```

## PaymentsUniqueStarGiftValueInfo

**Constructor ID:** `1362093126`

**Fields:** ``last_sale_on_fragment`, `value_is_average`, `currency`, `value`, `initial_sale_date`, `initial_sale_stars`, `initial_sale_price`, `last_sale_date`, `last_sale_price`, `floor_price`, `average_price`, `listed_count`, `fragment_listed_count`, `fragment_listed_url``

```python
PaymentsUniqueStarGiftValueInfo.__init__(currency: str, value: int, initial_sale_date: int, initial_sale_stars: int, initial_sale_price: int, last_sale_on_fragment: bool = None, value_is_average: bool = None, last_sale_date: int = None, last_sale_price: int = None, floor_price: int = None, average_price: int = None, listed_count: int = None, fragment_listed_count: int = None, fragment_listed_url: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `currency` | `str` | `—` |
| `value` | `int` | `—` |
| `initial_sale_date` | `int` | `—` |
| `initial_sale_stars` | `int` | `—` |
| `initial_sale_price` | `int` | `—` |
| `last_sale_on_fragment` | `bool` | `None` |
| `value_is_average` | `bool` | `None` |
| `last_sale_date` | `int` | `None` |
| `last_sale_price` | `int` | `None` |
| `floor_price` | `int` | `None` |
| `average_price` | `int` | `None` |
| `listed_count` | `int` | `None` |
| `fragment_listed_count` | `int` | `None` |
| `fragment_listed_url` | `str` | `None` |

```python
from sent.tl.types.payments import PaymentsUniqueStarGiftValueInfo
```

## PaymentsCheckCanSendGiftResultOk

**Constructor ID:** `927967149`

```python
PaymentsCheckCanSendGiftResultOk.__init__()
```

_No parameters._

```python
from sent.tl.types.payments import PaymentsCheckCanSendGiftResultOk
```

## PaymentsCheckCanSendGiftResultFail

**Constructor ID:** `3588588148`

```python
PaymentsCheckCanSendGiftResultFail.__init__(reason: 'TextWithEntities')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `reason` | `'TextWithEntities'` | `—` |

```python
from sent.tl.types.payments import PaymentsCheckCanSendGiftResultFail
```

## PaymentsStarGiftAuctionState

**Constructor ID:** `1798960364`

**Fields:** ``gift`, `state`, `user_state`, `timeout`, `users`, `chats``

```python
PaymentsStarGiftAuctionState.__init__(gift: 'StarGift', state: 'StarGiftAuctionState', user_state: 'StarGiftAuctionUserState', timeout: int, users: 'Vector', chats: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `gift` | `'StarGift'` | `—` |
| `state` | `'StarGiftAuctionState'` | `—` |
| `user_state` | `'StarGiftAuctionUserState'` | `—` |
| `timeout` | `int` | `—` |
| `users` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |

```python
from sent.tl.types.payments import PaymentsStarGiftAuctionState
```

## PaymentsStarGiftAuctionAcquiredGifts

**Constructor ID:** `2103169520`

**Fields:** ``gifts`, `users`, `chats``

```python
PaymentsStarGiftAuctionAcquiredGifts.__init__(gifts: 'Vector', users: 'Vector', chats: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `gifts` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |

```python
from sent.tl.types.payments import PaymentsStarGiftAuctionAcquiredGifts
```

## PaymentsStarGiftActiveAuctionsNotModified

**Constructor ID:** `3677608656`

```python
PaymentsStarGiftActiveAuctionsNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.payments import PaymentsStarGiftActiveAuctionsNotModified
```

## PaymentsStarGiftActiveAuctions

**Constructor ID:** `2935401404`

**Fields:** ``auctions`, `users`, `chats``

```python
PaymentsStarGiftActiveAuctions.__init__(auctions: 'Vector', users: 'Vector', chats: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `auctions` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |

```python
from sent.tl.types.payments import PaymentsStarGiftActiveAuctions
```

## PaymentsStarGiftUpgradeAttributes

**Constructor ID:** `1187439471`

```python
PaymentsStarGiftUpgradeAttributes.__init__(attributes: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `attributes` | `'Vector'` | `—` |

```python
from sent.tl.types.payments import PaymentsStarGiftUpgradeAttributes
```
