{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Types: `chatlists`

Import from `sent.tl.types.chatlists`.

## Contents

- [ChatlistsExportedChatlistInvite](#chatlistsexportedchatlistinvite)
- [ChatlistsExportedInvites](#chatlistsexportedinvites)
- [ChatlistsChatlistInviteAlready](#chatlistschatlistinvitealready)
- [ChatlistsChatlistInvite](#chatlistschatlistinvite)
- [ChatlistsChatlistUpdates](#chatlistschatlistupdates)

## ChatlistsExportedChatlistInvite

**Constructor ID:** `283567014`

**Fields:** ``filter`, `invite``

```python
ChatlistsExportedChatlistInvite.__init__(filter: 'DialogFilter', invite: 'ExportedChatlistInvite')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `filter` | `'DialogFilter'` | `—` |
| `invite` | `'ExportedChatlistInvite'` | `—` |

```python
from sent.tl.types.chatlists import ChatlistsExportedChatlistInvite
```

## ChatlistsExportedInvites

**Constructor ID:** `279670215`

**Fields:** ``invites`, `chats`, `users``

```python
ChatlistsExportedInvites.__init__(invites: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `invites` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.chatlists import ChatlistsExportedInvites
```

## ChatlistsChatlistInviteAlready

**Constructor ID:** `4203214425`

**Fields:** ``filter_id`, `missing_peers`, `already_peers`, `chats`, `users``

```python
ChatlistsChatlistInviteAlready.__init__(filter_id: int, missing_peers: 'Vector', already_peers: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `filter_id` | `int` | `—` |
| `missing_peers` | `'Vector'` | `—` |
| `already_peers` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.chatlists import ChatlistsChatlistInviteAlready
```

## ChatlistsChatlistInvite

**Constructor ID:** `4044279343`

**Fields:** ``title_noanimate`, `title`, `emoticon`, `peers`, `chats`, `users``

```python
ChatlistsChatlistInvite.__init__(title: 'TextWithEntities', peers: 'Vector', chats: 'Vector', users: 'Vector', title_noanimate: bool = None, emoticon: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `title` | `'TextWithEntities'` | `—` |
| `peers` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `title_noanimate` | `bool` | `None` |
| `emoticon` | `str` | `None` |

```python
from sent.tl.types.chatlists import ChatlistsChatlistInvite
```

## ChatlistsChatlistUpdates

**Constructor ID:** `2478671757`

**Fields:** ``missing_peers`, `chats`, `users``

```python
ChatlistsChatlistUpdates.__init__(missing_peers: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `missing_peers` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.chatlists import ChatlistsChatlistUpdates
```
