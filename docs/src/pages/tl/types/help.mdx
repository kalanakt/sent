{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Types: `help`

Import from `sent.tl.types.help`.

## Contents

- [HelpConfigSimple](#helpconfigsimple)
- [HelpAppUpdate](#helpappupdate)
- [HelpNoAppUpdate](#helpnoappupdate)
- [HelpInviteText](#helpinvitetext)
- [HelpSupport](#helpsupport)
- [HelpTermsOfService](#helptermsofservice)
- [HelpRecentMeUrls](#helprecentmeurls)
- [HelpTermsOfServiceUpdateEmpty](#helptermsofserviceupdateempty)
- [HelpTermsOfServiceUpdate](#helptermsofserviceupdate)
- [HelpDeepLinkInfoEmpty](#helpdeeplinkinfoempty)
- [HelpDeepLinkInfo](#helpdeeplinkinfo)
- [HelpPassportConfigNotModified](#helppassportconfignotmodified)
- [HelpPassportConfig](#helppassportconfig)
- [HelpSupportName](#helpsupportname)
- [HelpUserInfoEmpty](#helpuserinfoempty)
- [HelpUserInfo](#helpuserinfo)
- [HelpPromoDataEmpty](#helppromodataempty)
- [HelpPromoData](#helppromodata)
- [HelpCountryCode](#helpcountrycode)
- [HelpCountry](#helpcountry)
- [HelpCountriesListNotModified](#helpcountrieslistnotmodified)
- [HelpCountriesList](#helpcountrieslist)
- [HelpPremiumPromo](#helppremiumpromo)
- [HelpAppConfigNotModified](#helpappconfignotmodified)
- [HelpAppConfig](#helpappconfig)
- [HelpPeerColorSet](#helppeercolorset)
- [HelpPeerColorProfileSet](#helppeercolorprofileset)
- [HelpPeerColorOption](#helppeercoloroption)
- [HelpPeerColorsNotModified](#helppeercolorsnotmodified)
- [HelpPeerColors](#helppeercolors)
- [HelpTimezonesListNotModified](#helptimezoneslistnotmodified)
- [HelpTimezonesList](#helptimezoneslist)

## HelpConfigSimple

**Constructor ID:** `1515793004`

**Fields:** ``date`, `expires`, `rules``

```python
HelpConfigSimple.__init__(date: int, expires: int, rules: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `date` | `int` | `—` |
| `expires` | `int` | `—` |
| `rules` | `'Vector'` | `—` |

```python
from sent.tl.types.help import HelpConfigSimple
```

## HelpAppUpdate

**Constructor ID:** `3434860080`

**Fields:** ``can_not_skip`, `id`, `version`, `text`, `entities`, `document`, `url`, `sticker``

```python
HelpAppUpdate.__init__(id: int, version: str, text: str, entities: 'Vector', can_not_skip: bool = None, document: 'Document' = None, url: str = None, sticker: 'Document' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `int` | `—` |
| `version` | `str` | `—` |
| `text` | `str` | `—` |
| `entities` | `'Vector'` | `—` |
| `can_not_skip` | `bool` | `None` |
| `document` | `'Document'` | `None` |
| `url` | `str` | `None` |
| `sticker` | `'Document'` | `None` |

```python
from sent.tl.types.help import HelpAppUpdate
```

## HelpNoAppUpdate

**Constructor ID:** `3294258486`

```python
HelpNoAppUpdate.__init__()
```

_No parameters._

```python
from sent.tl.types.help import HelpNoAppUpdate
```

## HelpInviteText

**Constructor ID:** `415997816`

```python
HelpInviteText.__init__(message: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `message` | `str` | `—` |

```python
from sent.tl.types.help import HelpInviteText
```

## HelpSupport

**Constructor ID:** `398898678`

**Fields:** ``phone_number`, `user``

```python
HelpSupport.__init__(phone_number: str, user: 'User')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `phone_number` | `str` | `—` |
| `user` | `'User'` | `—` |

```python
from sent.tl.types.help import HelpSupport
```

## HelpTermsOfService

**Constructor ID:** `2013922064`

**Fields:** ``popup`, `id`, `text`, `entities`, `min_age_confirm``

```python
HelpTermsOfService.__init__(id: 'DataJSON', text: str, entities: 'Vector', popup: bool = None, min_age_confirm: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `'DataJSON'` | `—` |
| `text` | `str` | `—` |
| `entities` | `'Vector'` | `—` |
| `popup` | `bool` | `None` |
| `min_age_confirm` | `int` | `None` |

```python
from sent.tl.types.help import HelpTermsOfService
```

## HelpRecentMeUrls

**Constructor ID:** `235081943`

**Fields:** ``urls`, `chats`, `users``

```python
HelpRecentMeUrls.__init__(urls: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `urls` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.help import HelpRecentMeUrls
```

## HelpTermsOfServiceUpdateEmpty

**Constructor ID:** `3811614591`

```python
HelpTermsOfServiceUpdateEmpty.__init__(expires: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `expires` | `int` | `—` |

```python
from sent.tl.types.help import HelpTermsOfServiceUpdateEmpty
```

## HelpTermsOfServiceUpdate

**Constructor ID:** `686618977`

**Fields:** ``expires`, `terms_of_service``

```python
HelpTermsOfServiceUpdate.__init__(expires: int, terms_of_service: 'HelpTermsOfService')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `expires` | `int` | `—` |
| `terms_of_service` | `'HelpTermsOfService'` | `—` |

```python
from sent.tl.types.help import HelpTermsOfServiceUpdate
```

## HelpDeepLinkInfoEmpty

**Constructor ID:** `1722786150`

```python
HelpDeepLinkInfoEmpty.__init__()
```

_No parameters._

```python
from sent.tl.types.help import HelpDeepLinkInfoEmpty
```

## HelpDeepLinkInfo

**Constructor ID:** `1783556146`

**Fields:** ``update_app`, `message`, `entities``

```python
HelpDeepLinkInfo.__init__(message: str, update_app: bool = None, entities: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `message` | `str` | `—` |
| `update_app` | `bool` | `None` |
| `entities` | `'Vector'` | `None` |

```python
from sent.tl.types.help import HelpDeepLinkInfo
```

## HelpPassportConfigNotModified

**Constructor ID:** `3216634967`

```python
HelpPassportConfigNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.help import HelpPassportConfigNotModified
```

## HelpPassportConfig

**Constructor ID:** `2694370991`

**Fields:** ``hash`, `countries_langs``

```python
HelpPassportConfig.__init__(hash: int, countries_langs: 'DataJSON')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `countries_langs` | `'DataJSON'` | `—` |

```python
from sent.tl.types.help import HelpPassportConfig
```

## HelpSupportName

**Constructor ID:** `2349199817`

```python
HelpSupportName.__init__(name: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `name` | `str` | `—` |

```python
from sent.tl.types.help import HelpSupportName
```

## HelpUserInfoEmpty

**Constructor ID:** `4088278765`

```python
HelpUserInfoEmpty.__init__()
```

_No parameters._

```python
from sent.tl.types.help import HelpUserInfoEmpty
```

## HelpUserInfo

**Constructor ID:** `32192344`

**Fields:** ``message`, `entities`, `author`, `date``

```python
HelpUserInfo.__init__(message: str, entities: 'Vector', author: str, date: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `message` | `str` | `—` |
| `entities` | `'Vector'` | `—` |
| `author` | `str` | `—` |
| `date` | `int` | `—` |

```python
from sent.tl.types.help import HelpUserInfo
```

## HelpPromoDataEmpty

**Constructor ID:** `2566302837`

```python
HelpPromoDataEmpty.__init__(expires: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `expires` | `int` | `—` |

```python
from sent.tl.types.help import HelpPromoDataEmpty
```

## HelpPromoData

**Constructor ID:** `145021050`

**Fields:** ``proxy`, `expires`, `peer`, `psa_type`, `psa_message`, `pending_suggestions`, `dismissed_suggestions`, `custom_pending_suggestion`, `chats`, `users``

```python
HelpPromoData.__init__(expires: int, pending_suggestions: 'Vector', dismissed_suggestions: 'Vector', chats: 'Vector', users: 'Vector', proxy: bool = None, peer: 'Peer' = None, psa_type: str = None, psa_message: str = None, custom_pending_suggestion: 'PendingSuggestion' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `expires` | `int` | `—` |
| `pending_suggestions` | `'Vector'` | `—` |
| `dismissed_suggestions` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `proxy` | `bool` | `None` |
| `peer` | `'Peer'` | `None` |
| `psa_type` | `str` | `None` |
| `psa_message` | `str` | `None` |
| `custom_pending_suggestion` | `'PendingSuggestion'` | `None` |

```python
from sent.tl.types.help import HelpPromoData
```

## HelpCountryCode

**Constructor ID:** `1107543535`

**Fields:** ``country_code`, `prefixes`, `patterns``

```python
HelpCountryCode.__init__(country_code: str, prefixes: 'Vector' = None, patterns: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `country_code` | `str` | `—` |
| `prefixes` | `'Vector'` | `None` |
| `patterns` | `'Vector'` | `None` |

```python
from sent.tl.types.help import HelpCountryCode
```

## HelpCountry

**Constructor ID:** `3280440867`

**Fields:** ``hidden`, `iso2`, `default_name`, `name`, `country_codes``

```python
HelpCountry.__init__(iso2: str, default_name: str, country_codes: 'Vector', hidden: bool = None, name: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `iso2` | `str` | `—` |
| `default_name` | `str` | `—` |
| `country_codes` | `'Vector'` | `—` |
| `hidden` | `bool` | `None` |
| `name` | `str` | `None` |

```python
from sent.tl.types.help import HelpCountry
```

## HelpCountriesListNotModified

**Constructor ID:** `2479628082`

```python
HelpCountriesListNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.help import HelpCountriesListNotModified
```

## HelpCountriesList

**Constructor ID:** `2278585758`

**Fields:** ``countries`, `hash``

```python
HelpCountriesList.__init__(countries: 'Vector', hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `countries` | `'Vector'` | `—` |
| `hash` | `int` | `—` |

```python
from sent.tl.types.help import HelpCountriesList
```

## HelpPremiumPromo

**Constructor ID:** `1395946908`

**Fields:** ``status_text`, `status_entities`, `video_sections`, `videos`, `period_options`, `users``

```python
HelpPremiumPromo.__init__(status_text: str, status_entities: 'Vector', video_sections: 'Vector', videos: 'Vector', period_options: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `status_text` | `str` | `—` |
| `status_entities` | `'Vector'` | `—` |
| `video_sections` | `'Vector'` | `—` |
| `videos` | `'Vector'` | `—` |
| `period_options` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.help import HelpPremiumPromo
```

## HelpAppConfigNotModified

**Constructor ID:** `2094949405`

```python
HelpAppConfigNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.help import HelpAppConfigNotModified
```

## HelpAppConfig

**Constructor ID:** `3709368366`

**Fields:** ``hash`, `config``

```python
HelpAppConfig.__init__(hash: int, config: 'JSONValue')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `config` | `'JSONValue'` | `—` |

```python
from sent.tl.types.help import HelpAppConfig
```

## HelpPeerColorSet

**Constructor ID:** `639736408`

```python
HelpPeerColorSet.__init__(colors: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `colors` | `'Vector'` | `—` |

```python
from sent.tl.types.help import HelpPeerColorSet
```

## HelpPeerColorProfileSet

**Constructor ID:** `1987928555`

**Fields:** ``palette_colors`, `bg_colors`, `story_colors``

```python
HelpPeerColorProfileSet.__init__(palette_colors: 'Vector', bg_colors: 'Vector', story_colors: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `palette_colors` | `'Vector'` | `—` |
| `bg_colors` | `'Vector'` | `—` |
| `story_colors` | `'Vector'` | `—` |

```python
from sent.tl.types.help import HelpPeerColorProfileSet
```

## HelpPeerColorOption

**Constructor ID:** `2917953214`

**Fields:** ``hidden`, `color_id`, `colors`, `dark_colors`, `channel_min_level`, `group_min_level``

```python
HelpPeerColorOption.__init__(color_id: int, hidden: bool = None, colors: 'HelpPeerColorSet' = None, dark_colors: 'HelpPeerColorSet' = None, channel_min_level: int = None, group_min_level: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `color_id` | `int` | `—` |
| `hidden` | `bool` | `None` |
| `colors` | `'HelpPeerColorSet'` | `None` |
| `dark_colors` | `'HelpPeerColorSet'` | `None` |
| `channel_min_level` | `int` | `None` |
| `group_min_level` | `int` | `None` |

```python
from sent.tl.types.help import HelpPeerColorOption
```

## HelpPeerColorsNotModified

**Constructor ID:** `732034510`

```python
HelpPeerColorsNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.help import HelpPeerColorsNotModified
```

## HelpPeerColors

**Constructor ID:** `16313608`

**Fields:** ``hash`, `colors``

```python
HelpPeerColors.__init__(hash: int, colors: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `colors` | `'Vector'` | `—` |

```python
from sent.tl.types.help import HelpPeerColors
```

## HelpTimezonesListNotModified

**Constructor ID:** `2533820620`

```python
HelpTimezonesListNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.help import HelpTimezonesListNotModified
```

## HelpTimezonesList

**Constructor ID:** `2071260529`

**Fields:** ``timezones`, `hash``

```python
HelpTimezonesList.__init__(timezones: 'Vector', hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `timezones` | `'Vector'` | `—` |
| `hash` | `int` | `—` |

```python
from sent.tl.types.help import HelpTimezonesList
```
