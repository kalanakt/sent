{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Types: `channels`

Import from `sent.tl.types.channels`.

## Contents

- [ChannelsChannelParticipants](#channelschannelparticipants)
- [ChannelsChannelParticipantsNotModified](#channelschannelparticipantsnotmodified)
- [ChannelsChannelParticipant](#channelschannelparticipant)
- [ChannelsAdminLogResults](#channelsadminlogresults)
- [ChannelsSendAsPeers](#channelssendaspeers)
- [ChannelsSponsoredMessageReportResultChooseOption](#channelssponsoredmessagereportresultchooseoption)
- [ChannelsSponsoredMessageReportResultAdsHidden](#channelssponsoredmessagereportresultadshidden)
- [ChannelsSponsoredMessageReportResultReported](#channelssponsoredmessagereportresultreported)

## ChannelsChannelParticipants

**Constructor ID:** `2595290799`

**Fields:** ``count`, `participants`, `chats`, `users``

```python
ChannelsChannelParticipants.__init__(count: int, participants: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `participants` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.channels import ChannelsChannelParticipants
```

## ChannelsChannelParticipantsNotModified

**Constructor ID:** `4028055529`

```python
ChannelsChannelParticipantsNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.channels import ChannelsChannelParticipantsNotModified
```

## ChannelsChannelParticipant

**Constructor ID:** `3753378583`

**Fields:** ``participant`, `chats`, `users``

```python
ChannelsChannelParticipant.__init__(participant: 'ChannelParticipant', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `participant` | `'ChannelParticipant'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.channels import ChannelsChannelParticipant
```

## ChannelsAdminLogResults

**Constructor ID:** `3985307469`

**Fields:** ``events`, `chats`, `users``

```python
ChannelsAdminLogResults.__init__(events: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `events` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.channels import ChannelsAdminLogResults
```

## ChannelsSendAsPeers

**Constructor ID:** `4103516358`

**Fields:** ``peers`, `chats`, `users``

```python
ChannelsSendAsPeers.__init__(peers: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peers` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.channels import ChannelsSendAsPeers
```

## ChannelsSponsoredMessageReportResultChooseOption

**Constructor ID:** `2221907522`

**Fields:** ``title`, `options``

```python
ChannelsSponsoredMessageReportResultChooseOption.__init__(title: str, options: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `title` | `str` | `—` |
| `options` | `'Vector'` | `—` |

```python
from sent.tl.types.channels import ChannelsSponsoredMessageReportResultChooseOption
```

## ChannelsSponsoredMessageReportResultAdsHidden

**Constructor ID:** `1044107055`

```python
ChannelsSponsoredMessageReportResultAdsHidden.__init__()
```

_No parameters._

```python
from sent.tl.types.channels import ChannelsSponsoredMessageReportResultAdsHidden
```

## ChannelsSponsoredMessageReportResultReported

**Constructor ID:** `2910423113`

```python
ChannelsSponsoredMessageReportResultReported.__init__()
```

_No parameters._

```python
from sent.tl.types.channels import ChannelsSponsoredMessageReportResultReported
```
