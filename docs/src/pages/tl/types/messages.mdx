{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Types: `messages`

Import from `sent.tl.types.messages`.

## Contents

- [MessagesDialogs](#messagesdialogs)
- [MessagesDialogsSlice](#messagesdialogsslice)
- [MessagesDialogsNotModified](#messagesdialogsnotmodified)
- [MessagesMessages](#messagesmessages)
- [MessagesMessagesSlice](#messagesmessagesslice)
- [MessagesChannelMessages](#messageschannelmessages)
- [MessagesMessagesNotModified](#messagesmessagesnotmodified)
- [MessagesChats](#messageschats)
- [MessagesChatsSlice](#messageschatsslice)
- [MessagesChatFull](#messageschatfull)
- [MessagesAffectedHistory](#messagesaffectedhistory)
- [MessagesDhConfigNotModified](#messagesdhconfignotmodified)
- [MessagesDhConfig](#messagesdhconfig)
- [MessagesSentEncryptedMessage](#messagessentencryptedmessage)
- [MessagesSentEncryptedFile](#messagessentencryptedfile)
- [MessagesStickersNotModified](#messagesstickersnotmodified)
- [MessagesStickers](#messagesstickers)
- [MessagesAllStickersNotModified](#messagesallstickersnotmodified)
- [MessagesAllStickers](#messagesallstickers)
- [MessagesAffectedMessages](#messagesaffectedmessages)
- [MessagesStickerSet](#messagesstickerset)
- [MessagesStickerSetNotModified](#messagesstickersetnotmodified)
- [MessagesSavedGifsNotModified](#messagessavedgifsnotmodified)
- [MessagesSavedGifs](#messagessavedgifs)
- [MessagesBotResults](#messagesbotresults)
- [MessagesBotCallbackAnswer](#messagesbotcallbackanswer)
- [MessagesMessageEditData](#messagesmessageeditdata)
- [MessagesPeerDialogs](#messagespeerdialogs)
- [MessagesFeaturedStickersNotModified](#messagesfeaturedstickersnotmodified)
- [MessagesFeaturedStickers](#messagesfeaturedstickers)
- [MessagesRecentStickersNotModified](#messagesrecentstickersnotmodified)
- [MessagesRecentStickers](#messagesrecentstickers)
- [MessagesArchivedStickers](#messagesarchivedstickers)
- [MessagesStickerSetInstallResultSuccess](#messagesstickersetinstallresultsuccess)
- [MessagesStickerSetInstallResultArchive](#messagesstickersetinstallresultarchive)
- [MessagesHighScores](#messageshighscores)
- [MessagesFavedStickersNotModified](#messagesfavedstickersnotmodified)
- [MessagesFavedStickers](#messagesfavedstickers)
- [MessagesFoundStickerSetsNotModified](#messagesfoundstickersetsnotmodified)
- [MessagesFoundStickerSets](#messagesfoundstickersets)
- [MessagesSearchCounter](#messagessearchcounter)
- [MessagesInactiveChats](#messagesinactivechats)
- [MessagesVotesList](#messagesvoteslist)
- [MessagesMessageViews](#messagesmessageviews)
- [MessagesDiscussionMessage](#messagesdiscussionmessage)
- [MessagesHistoryImport](#messageshistoryimport)
- [MessagesHistoryImportParsed](#messageshistoryimportparsed)
- [MessagesAffectedFoundMessages](#messagesaffectedfoundmessages)
- [MessagesExportedChatInvites](#messagesexportedchatinvites)
- [MessagesExportedChatInvite](#messagesexportedchatinvite)
- [MessagesExportedChatInviteReplaced](#messagesexportedchatinvitereplaced)
- [MessagesChatInviteImporters](#messageschatinviteimporters)
- [MessagesChatAdminsWithInvites](#messageschatadminswithinvites)
- [MessagesCheckedHistoryImportPeer](#messagescheckedhistoryimportpeer)
- [MessagesSponsoredMessages](#messagessponsoredmessages)
- [MessagesSponsoredMessagesEmpty](#messagessponsoredmessagesempty)
- [MessagesSearchResultsCalendar](#messagessearchresultscalendar)
- [MessagesSearchResultsPositions](#messagessearchresultspositions)
- [MessagesPeerSettings](#messagespeersettings)
- [MessagesMessageReactionsList](#messagesmessagereactionslist)
- [MessagesAvailableReactionsNotModified](#messagesavailablereactionsnotmodified)
- [MessagesAvailableReactions](#messagesavailablereactions)
- [MessagesTranscribedAudio](#messagestranscribedaudio)
- [MessagesReactionsNotModified](#messagesreactionsnotmodified)
- [MessagesReactions](#messagesreactions)
- [MessagesForumTopics](#messagesforumtopics)
- [MessagesEmojiGroupsNotModified](#messagesemojigroupsnotmodified)
- [MessagesEmojiGroups](#messagesemojigroups)
- [MessagesTranslateResult](#messagestranslateresult)
- [MessagesBotApp](#messagesbotapp)
- [MessagesWebPage](#messageswebpage)
- [MessagesSavedDialogs](#messagessaveddialogs)
- [MessagesSavedDialogsSlice](#messagessaveddialogsslice)
- [MessagesSavedDialogsNotModified](#messagessaveddialogsnotmodified)
- [MessagesSavedReactionTagsNotModified](#messagessavedreactiontagsnotmodified)
- [MessagesSavedReactionTags](#messagessavedreactiontags)
- [MessagesQuickReplies](#messagesquickreplies)
- [MessagesQuickRepliesNotModified](#messagesquickrepliesnotmodified)
- [MessagesDialogFilters](#messagesdialogfilters)
- [MessagesMyStickers](#messagesmystickers)
- [MessagesInvitedUsers](#messagesinvitedusers)
- [MessagesAvailableEffectsNotModified](#messagesavailableeffectsnotmodified)
- [MessagesAvailableEffects](#messagesavailableeffects)
- [MessagesBotPreparedInlineMessage](#messagesbotpreparedinlinemessage)
- [MessagesPreparedInlineMessage](#messagespreparedinlinemessage)
- [MessagesFoundStickersNotModified](#messagesfoundstickersnotmodified)
- [MessagesFoundStickers](#messagesfoundstickers)
- [MessagesWebPagePreview](#messageswebpagepreview)
- [MessagesEmojiGameOutcome](#messagesemojigameoutcome)
- [MessagesEmojiGameUnavailable](#messagesemojigameunavailable)
- [MessagesEmojiGameDiceInfo](#messagesemojigamediceinfo)
- [MessagesComposedMessageWithAI](#messagescomposedmessagewithai)

## MessagesDialogs

**Constructor ID:** `364538944`

**Fields:** ``dialogs`, `messages`, `chats`, `users``

```python
MessagesDialogs.__init__(dialogs: 'Vector', messages: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `dialogs` | `'Vector'` | `—` |
| `messages` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesDialogs
```

## MessagesDialogsSlice

**Constructor ID:** `1910543603`

**Fields:** ``count`, `dialogs`, `messages`, `chats`, `users``

```python
MessagesDialogsSlice.__init__(count: int, dialogs: 'Vector', messages: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `dialogs` | `'Vector'` | `—` |
| `messages` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesDialogsSlice
```

## MessagesDialogsNotModified

**Constructor ID:** `4041467286`

```python
MessagesDialogsNotModified.__init__(count: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |

```python
from sent.tl.types.messages import MessagesDialogsNotModified
```

## MessagesMessages

**Constructor ID:** `494135274`

**Fields:** ``messages`, `topics`, `chats`, `users``

```python
MessagesMessages.__init__(messages: 'Vector', topics: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `messages` | `'Vector'` | `—` |
| `topics` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesMessages
```

## MessagesMessagesSlice

**Constructor ID:** `1595959062`

**Fields:** ``inexact`, `count`, `next_rate`, `offset_id_offset`, `search_flood`, `messages`, `topics`, `chats`, `users``

```python
MessagesMessagesSlice.__init__(count: int, messages: 'Vector', topics: 'Vector', chats: 'Vector', users: 'Vector', inexact: bool = None, next_rate: int = None, offset_id_offset: int = None, search_flood: 'SearchPostsFlood' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `messages` | `'Vector'` | `—` |
| `topics` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `inexact` | `bool` | `None` |
| `next_rate` | `int` | `None` |
| `offset_id_offset` | `int` | `None` |
| `search_flood` | `'SearchPostsFlood'` | `None` |

```python
from sent.tl.types.messages import MessagesMessagesSlice
```

## MessagesChannelMessages

**Constructor ID:** `3346446926`

**Fields:** ``inexact`, `pts`, `count`, `offset_id_offset`, `messages`, `topics`, `chats`, `users``

```python
MessagesChannelMessages.__init__(pts: int, count: int, messages: 'Vector', topics: 'Vector', chats: 'Vector', users: 'Vector', inexact: bool = None, offset_id_offset: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `pts` | `int` | `—` |
| `count` | `int` | `—` |
| `messages` | `'Vector'` | `—` |
| `topics` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `inexact` | `bool` | `None` |
| `offset_id_offset` | `int` | `None` |

```python
from sent.tl.types.messages import MessagesChannelMessages
```

## MessagesMessagesNotModified

**Constructor ID:** `1951620897`

```python
MessagesMessagesNotModified.__init__(count: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |

```python
from sent.tl.types.messages import MessagesMessagesNotModified
```

## MessagesChats

**Constructor ID:** `1694474197`

```python
MessagesChats.__init__(chats: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `chats` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesChats
```

## MessagesChatsSlice

**Constructor ID:** `2631405892`

**Fields:** ``count`, `chats``

```python
MessagesChatsSlice.__init__(count: int, chats: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `chats` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesChatsSlice
```

## MessagesChatFull

**Constructor ID:** `3856126364`

**Fields:** ``full_chat`, `chats`, `users``

```python
MessagesChatFull.__init__(full_chat: 'ChatFull', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `full_chat` | `'ChatFull'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesChatFull
```

## MessagesAffectedHistory

**Constructor ID:** `3025955281`

**Fields:** ``pts`, `pts_count`, `offset``

```python
MessagesAffectedHistory.__init__(pts: int, pts_count: int, offset: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `pts` | `int` | `—` |
| `pts_count` | `int` | `—` |
| `offset` | `int` | `—` |

```python
from sent.tl.types.messages import MessagesAffectedHistory
```

## MessagesDhConfigNotModified

**Constructor ID:** `3236054581`

```python
MessagesDhConfigNotModified.__init__(random: bytes)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `random` | `bytes` | `—` |

```python
from sent.tl.types.messages import MessagesDhConfigNotModified
```

## MessagesDhConfig

**Constructor ID:** `740433629`

**Fields:** ``g`, `p`, `version`, `random``

```python
MessagesDhConfig.__init__(g: int, p: bytes, version: int, random: bytes)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `g` | `int` | `—` |
| `p` | `bytes` | `—` |
| `version` | `int` | `—` |
| `random` | `bytes` | `—` |

```python
from sent.tl.types.messages import MessagesDhConfig
```

## MessagesSentEncryptedMessage

**Constructor ID:** `1443858741`

```python
MessagesSentEncryptedMessage.__init__(date: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `date` | `int` | `—` |

```python
from sent.tl.types.messages import MessagesSentEncryptedMessage
```

## MessagesSentEncryptedFile

**Constructor ID:** `2492727090`

**Fields:** ``date`, `file``

```python
MessagesSentEncryptedFile.__init__(date: int, file: 'EncryptedFile')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `date` | `int` | `—` |
| `file` | `'EncryptedFile'` | `—` |

```python
from sent.tl.types.messages import MessagesSentEncryptedFile
```

## MessagesStickersNotModified

**Constructor ID:** `4050950690`

```python
MessagesStickersNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.messages import MessagesStickersNotModified
```

## MessagesStickers

**Constructor ID:** `816245886`

**Fields:** ``hash`, `stickers``

```python
MessagesStickers.__init__(hash: int, stickers: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `stickers` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesStickers
```

## MessagesAllStickersNotModified

**Constructor ID:** `3898999491`

```python
MessagesAllStickersNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.messages import MessagesAllStickersNotModified
```

## MessagesAllStickers

**Constructor ID:** `3451637435`

**Fields:** ``hash`, `sets``

```python
MessagesAllStickers.__init__(hash: int, sets: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `sets` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesAllStickers
```

## MessagesAffectedMessages

**Constructor ID:** `2228326789`

**Fields:** ``pts`, `pts_count``

```python
MessagesAffectedMessages.__init__(pts: int, pts_count: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `pts` | `int` | `—` |
| `pts_count` | `int` | `—` |

```python
from sent.tl.types.messages import MessagesAffectedMessages
```

## MessagesStickerSet

**Constructor ID:** `1846886166`

**Fields:** ``set`, `packs`, `keywords`, `documents``

```python
MessagesStickerSet.__init__(set: 'StickerSet', packs: 'Vector', keywords: 'Vector', documents: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `set` | `'StickerSet'` | `—` |
| `packs` | `'Vector'` | `—` |
| `keywords` | `'Vector'` | `—` |
| `documents` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesStickerSet
```

## MessagesStickerSetNotModified

**Constructor ID:** `3556320491`

```python
MessagesStickerSetNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.messages import MessagesStickerSetNotModified
```

## MessagesSavedGifsNotModified

**Constructor ID:** `3892468898`

```python
MessagesSavedGifsNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.messages import MessagesSavedGifsNotModified
```

## MessagesSavedGifs

**Constructor ID:** `2225089037`

**Fields:** ``hash`, `gifs``

```python
MessagesSavedGifs.__init__(hash: int, gifs: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `gifs` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesSavedGifs
```

## MessagesBotResults

**Constructor ID:** `3760321270`

**Fields:** ``gallery`, `query_id`, `next_offset`, `switch_pm`, `switch_webview`, `results`, `cache_time`, `users``

```python
MessagesBotResults.__init__(query_id: int, results: 'Vector', cache_time: int, users: 'Vector', gallery: bool = None, next_offset: str = None, switch_pm: 'InlineBotSwitchPM' = None, switch_webview: 'InlineBotWebView' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `query_id` | `int` | `—` |
| `results` | `'Vector'` | `—` |
| `cache_time` | `int` | `—` |
| `users` | `'Vector'` | `—` |
| `gallery` | `bool` | `None` |
| `next_offset` | `str` | `None` |
| `switch_pm` | `'InlineBotSwitchPM'` | `None` |
| `switch_webview` | `'InlineBotWebView'` | `None` |

```python
from sent.tl.types.messages import MessagesBotResults
```

## MessagesBotCallbackAnswer

**Constructor ID:** `911761060`

**Fields:** ``alert`, `has_url`, `native_ui`, `message`, `url`, `cache_time``

```python
MessagesBotCallbackAnswer.__init__(cache_time: int, alert: bool = None, has_url: bool = None, native_ui: bool = None, message: str = None, url: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `cache_time` | `int` | `—` |
| `alert` | `bool` | `None` |
| `has_url` | `bool` | `None` |
| `native_ui` | `bool` | `None` |
| `message` | `str` | `None` |
| `url` | `str` | `None` |

```python
from sent.tl.types.messages import MessagesBotCallbackAnswer
```

## MessagesMessageEditData

**Constructor ID:** `649453030`

```python
MessagesMessageEditData.__init__(caption: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `caption` | `bool` | `None` |

```python
from sent.tl.types.messages import MessagesMessageEditData
```

## MessagesPeerDialogs

**Constructor ID:** `863093588`

**Fields:** ``dialogs`, `messages`, `chats`, `users`, `state``

```python
MessagesPeerDialogs.__init__(dialogs: 'Vector', messages: 'Vector', chats: 'Vector', users: 'Vector', state: 'UpdatesState')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `dialogs` | `'Vector'` | `—` |
| `messages` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `state` | `'UpdatesState'` | `—` |

```python
from sent.tl.types.messages import MessagesPeerDialogs
```

## MessagesFeaturedStickersNotModified

**Constructor ID:** `3336309862`

```python
MessagesFeaturedStickersNotModified.__init__(count: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |

```python
from sent.tl.types.messages import MessagesFeaturedStickersNotModified
```

## MessagesFeaturedStickers

**Constructor ID:** `3191351558`

**Fields:** ``premium`, `hash`, `count`, `sets`, `unread``

```python
MessagesFeaturedStickers.__init__(hash: int, count: int, sets: 'Vector', unread: 'Vector', premium: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `count` | `int` | `—` |
| `sets` | `'Vector'` | `—` |
| `unread` | `'Vector'` | `—` |
| `premium` | `bool` | `None` |

```python
from sent.tl.types.messages import MessagesFeaturedStickers
```

## MessagesRecentStickersNotModified

**Constructor ID:** `186120336`

```python
MessagesRecentStickersNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.messages import MessagesRecentStickersNotModified
```

## MessagesRecentStickers

**Constructor ID:** `2295561302`

**Fields:** ``hash`, `packs`, `stickers`, `dates``

```python
MessagesRecentStickers.__init__(hash: int, packs: 'Vector', stickers: 'Vector', dates: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `packs` | `'Vector'` | `—` |
| `stickers` | `'Vector'` | `—` |
| `dates` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesRecentStickers
```

## MessagesArchivedStickers

**Constructor ID:** `1338747336`

**Fields:** ``count`, `sets``

```python
MessagesArchivedStickers.__init__(count: int, sets: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `sets` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesArchivedStickers
```

## MessagesStickerSetInstallResultSuccess

**Constructor ID:** `946083368`

```python
MessagesStickerSetInstallResultSuccess.__init__()
```

_No parameters._

```python
from sent.tl.types.messages import MessagesStickerSetInstallResultSuccess
```

## MessagesStickerSetInstallResultArchive

**Constructor ID:** `904138920`

```python
MessagesStickerSetInstallResultArchive.__init__(sets: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `sets` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesStickerSetInstallResultArchive
```

## MessagesHighScores

**Constructor ID:** `2587622809`

**Fields:** ``scores`, `users``

```python
MessagesHighScores.__init__(scores: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `scores` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesHighScores
```

## MessagesFavedStickersNotModified

**Constructor ID:** `2660214483`

```python
MessagesFavedStickersNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.messages import MessagesFavedStickersNotModified
```

## MessagesFavedStickers

**Constructor ID:** `750063767`

**Fields:** ``hash`, `packs`, `stickers``

```python
MessagesFavedStickers.__init__(hash: int, packs: 'Vector', stickers: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `packs` | `'Vector'` | `—` |
| `stickers` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesFavedStickers
```

## MessagesFoundStickerSetsNotModified

**Constructor ID:** `223655517`

```python
MessagesFoundStickerSetsNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.messages import MessagesFoundStickerSetsNotModified
```

## MessagesFoundStickerSets

**Constructor ID:** `2331024850`

**Fields:** ``hash`, `sets``

```python
MessagesFoundStickerSets.__init__(hash: int, sets: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `sets` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesFoundStickerSets
```

## MessagesSearchCounter

**Constructor ID:** `3896830975`

**Fields:** ``inexact`, `filter`, `count``

```python
MessagesSearchCounter.__init__(filter: 'MessagesFilter', count: int, inexact: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `filter` | `'MessagesFilter'` | `—` |
| `count` | `int` | `—` |
| `inexact` | `bool` | `None` |

```python
from sent.tl.types.messages import MessagesSearchCounter
```

## MessagesInactiveChats

**Constructor ID:** `2837970629`

**Fields:** ``dates`, `chats`, `users``

```python
MessagesInactiveChats.__init__(dates: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `dates` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesInactiveChats
```

## MessagesVotesList

**Constructor ID:** `1218005070`

**Fields:** ``count`, `votes`, `chats`, `users`, `next_offset``

```python
MessagesVotesList.__init__(count: int, votes: 'Vector', chats: 'Vector', users: 'Vector', next_offset: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `votes` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `next_offset` | `str` | `None` |

```python
from sent.tl.types.messages import MessagesVotesList
```

## MessagesMessageViews

**Constructor ID:** `3066361155`

**Fields:** ``views`, `chats`, `users``

```python
MessagesMessageViews.__init__(views: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `views` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesMessageViews
```

## MessagesDiscussionMessage

**Constructor ID:** `2788431746`

**Fields:** ``messages`, `max_id`, `read_inbox_max_id`, `read_outbox_max_id`, `unread_count`, `chats`, `users``

```python
MessagesDiscussionMessage.__init__(messages: 'Vector', unread_count: int, chats: 'Vector', users: 'Vector', max_id: int = None, read_inbox_max_id: int = None, read_outbox_max_id: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `messages` | `'Vector'` | `—` |
| `unread_count` | `int` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `max_id` | `int` | `None` |
| `read_inbox_max_id` | `int` | `None` |
| `read_outbox_max_id` | `int` | `None` |

```python
from sent.tl.types.messages import MessagesDiscussionMessage
```

## MessagesHistoryImport

**Constructor ID:** `375566091`

```python
MessagesHistoryImport.__init__(id: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `int` | `—` |

```python
from sent.tl.types.messages import MessagesHistoryImport
```

## MessagesHistoryImportParsed

**Constructor ID:** `1578088377`

**Fields:** ``pm`, `group`, `title``

```python
MessagesHistoryImportParsed.__init__(pm: bool = None, group: bool = None, title: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `pm` | `bool` | `None` |
| `group` | `bool` | `None` |
| `title` | `str` | `None` |

```python
from sent.tl.types.messages import MessagesHistoryImportParsed
```

## MessagesAffectedFoundMessages

**Constructor ID:** `4019011180`

**Fields:** ``pts`, `pts_count`, `offset`, `messages``

```python
MessagesAffectedFoundMessages.__init__(pts: int, pts_count: int, offset: int, messages: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `pts` | `int` | `—` |
| `pts_count` | `int` | `—` |
| `offset` | `int` | `—` |
| `messages` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesAffectedFoundMessages
```

## MessagesExportedChatInvites

**Constructor ID:** `3183881676`

**Fields:** ``count`, `invites`, `users``

```python
MessagesExportedChatInvites.__init__(count: int, invites: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `invites` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesExportedChatInvites
```

## MessagesExportedChatInvite

**Constructor ID:** `410107472`

**Fields:** ``invite`, `users``

```python
MessagesExportedChatInvite.__init__(invite: 'ExportedChatInvite', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `invite` | `'ExportedChatInvite'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesExportedChatInvite
```

## MessagesExportedChatInviteReplaced

**Constructor ID:** `572915951`

**Fields:** ``invite`, `new_invite`, `users``

```python
MessagesExportedChatInviteReplaced.__init__(invite: 'ExportedChatInvite', new_invite: 'ExportedChatInvite', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `invite` | `'ExportedChatInvite'` | `—` |
| `new_invite` | `'ExportedChatInvite'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesExportedChatInviteReplaced
```

## MessagesChatInviteImporters

**Constructor ID:** `2176233482`

**Fields:** ``count`, `importers`, `users``

```python
MessagesChatInviteImporters.__init__(count: int, importers: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `importers` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesChatInviteImporters
```

## MessagesChatAdminsWithInvites

**Constructor ID:** `3063640791`

**Fields:** ``admins`, `users``

```python
MessagesChatAdminsWithInvites.__init__(admins: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `admins` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesChatAdminsWithInvites
```

## MessagesCheckedHistoryImportPeer

**Constructor ID:** `2723014423`

```python
MessagesCheckedHistoryImportPeer.__init__(confirm_text: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `confirm_text` | `str` | `—` |

```python
from sent.tl.types.messages import MessagesCheckedHistoryImportPeer
```

## MessagesSponsoredMessages

**Constructor ID:** `4292502893`

**Fields:** ``posts_between`, `start_delay`, `between_delay`, `messages`, `chats`, `users``

```python
MessagesSponsoredMessages.__init__(messages: 'Vector', chats: 'Vector', users: 'Vector', posts_between: int = None, start_delay: int = None, between_delay: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `messages` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `posts_between` | `int` | `None` |
| `start_delay` | `int` | `None` |
| `between_delay` | `int` | `None` |

```python
from sent.tl.types.messages import MessagesSponsoredMessages
```

## MessagesSponsoredMessagesEmpty

**Constructor ID:** `406407439`

```python
MessagesSponsoredMessagesEmpty.__init__()
```

_No parameters._

```python
from sent.tl.types.messages import MessagesSponsoredMessagesEmpty
```

## MessagesSearchResultsCalendar

**Constructor ID:** `343859772`

**Fields:** ``inexact`, `count`, `min_date`, `min_msg_id`, `offset_id_offset`, `periods`, `messages`, `chats`, `users``

```python
MessagesSearchResultsCalendar.__init__(count: int, min_date: int, min_msg_id: int, periods: 'Vector', messages: 'Vector', chats: 'Vector', users: 'Vector', inexact: bool = None, offset_id_offset: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `min_date` | `int` | `—` |
| `min_msg_id` | `int` | `—` |
| `periods` | `'Vector'` | `—` |
| `messages` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `inexact` | `bool` | `None` |
| `offset_id_offset` | `int` | `None` |

```python
from sent.tl.types.messages import MessagesSearchResultsCalendar
```

## MessagesSearchResultsPositions

**Constructor ID:** `1404185519`

**Fields:** ``count`, `positions``

```python
MessagesSearchResultsPositions.__init__(count: int, positions: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `positions` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesSearchResultsPositions
```

## MessagesPeerSettings

**Constructor ID:** `1753266509`

**Fields:** ``settings`, `chats`, `users``

```python
MessagesPeerSettings.__init__(settings: 'PeerSettings', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `settings` | `'PeerSettings'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesPeerSettings
```

## MessagesMessageReactionsList

**Constructor ID:** `834488621`

**Fields:** ``count`, `reactions`, `chats`, `users`, `next_offset``

```python
MessagesMessageReactionsList.__init__(count: int, reactions: 'Vector', chats: 'Vector', users: 'Vector', next_offset: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `reactions` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `next_offset` | `str` | `None` |

```python
from sent.tl.types.messages import MessagesMessageReactionsList
```

## MessagesAvailableReactionsNotModified

**Constructor ID:** `2668042583`

```python
MessagesAvailableReactionsNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.messages import MessagesAvailableReactionsNotModified
```

## MessagesAvailableReactions

**Constructor ID:** `1989032621`

**Fields:** ``hash`, `reactions``

```python
MessagesAvailableReactions.__init__(hash: int, reactions: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `reactions` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesAvailableReactions
```

## MessagesTranscribedAudio

**Constructor ID:** `3485063511`

**Fields:** ``pending`, `transcription_id`, `text`, `trial_remains_num`, `trial_remains_until_date``

```python
MessagesTranscribedAudio.__init__(transcription_id: int, text: str, pending: bool = None, trial_remains_num: int = None, trial_remains_until_date: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `transcription_id` | `int` | `—` |
| `text` | `str` | `—` |
| `pending` | `bool` | `None` |
| `trial_remains_num` | `int` | `None` |
| `trial_remains_until_date` | `int` | `None` |

```python
from sent.tl.types.messages import MessagesTranscribedAudio
```

## MessagesReactionsNotModified

**Constructor ID:** `2960120799`

```python
MessagesReactionsNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.messages import MessagesReactionsNotModified
```

## MessagesReactions

**Constructor ID:** `3942512406`

**Fields:** ``hash`, `reactions``

```python
MessagesReactions.__init__(hash: int, reactions: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `reactions` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesReactions
```

## MessagesForumTopics

**Constructor ID:** `913709011`

**Fields:** ``order_by_create_date`, `count`, `topics`, `messages`, `chats`, `users`, `pts``

```python
MessagesForumTopics.__init__(count: int, topics: 'Vector', messages: 'Vector', chats: 'Vector', users: 'Vector', pts: int, order_by_create_date: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `topics` | `'Vector'` | `—` |
| `messages` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `pts` | `int` | `—` |
| `order_by_create_date` | `bool` | `None` |

```python
from sent.tl.types.messages import MessagesForumTopics
```

## MessagesEmojiGroupsNotModified

**Constructor ID:** `1874111879`

```python
MessagesEmojiGroupsNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.messages import MessagesEmojiGroupsNotModified
```

## MessagesEmojiGroups

**Constructor ID:** `2283780427`

**Fields:** ``hash`, `groups``

```python
MessagesEmojiGroups.__init__(hash: int, groups: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `groups` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesEmojiGroups
```

## MessagesTranslateResult

**Constructor ID:** `870003448`

```python
MessagesTranslateResult.__init__(result: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `result` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesTranslateResult
```

## MessagesBotApp

**Constructor ID:** `3947933173`

**Fields:** ``inactive`, `request_write_access`, `has_settings`, `app``

```python
MessagesBotApp.__init__(app: 'BotApp', inactive: bool = None, request_write_access: bool = None, has_settings: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `app` | `'BotApp'` | `—` |
| `inactive` | `bool` | `None` |
| `request_write_access` | `bool` | `None` |
| `has_settings` | `bool` | `None` |

```python
from sent.tl.types.messages import MessagesBotApp
```

## MessagesWebPage

**Constructor ID:** `4250800829`

**Fields:** ``webpage`, `chats`, `users``

```python
MessagesWebPage.__init__(webpage: 'WebPage', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `webpage` | `'WebPage'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesWebPage
```

## MessagesSavedDialogs

**Constructor ID:** `4164608545`

**Fields:** ``dialogs`, `messages`, `chats`, `users``

```python
MessagesSavedDialogs.__init__(dialogs: 'Vector', messages: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `dialogs` | `'Vector'` | `—` |
| `messages` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesSavedDialogs
```

## MessagesSavedDialogsSlice

**Constructor ID:** `1153080793`

**Fields:** ``count`, `dialogs`, `messages`, `chats`, `users``

```python
MessagesSavedDialogsSlice.__init__(count: int, dialogs: 'Vector', messages: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `dialogs` | `'Vector'` | `—` |
| `messages` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesSavedDialogsSlice
```

## MessagesSavedDialogsNotModified

**Constructor ID:** `3223285736`

```python
MessagesSavedDialogsNotModified.__init__(count: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |

```python
from sent.tl.types.messages import MessagesSavedDialogsNotModified
```

## MessagesSavedReactionTagsNotModified

**Constructor ID:** `2291882479`

```python
MessagesSavedReactionTagsNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.messages import MessagesSavedReactionTagsNotModified
```

## MessagesSavedReactionTags

**Constructor ID:** `844731658`

**Fields:** ``tags`, `hash``

```python
MessagesSavedReactionTags.__init__(tags: 'Vector', hash: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `tags` | `'Vector'` | `—` |
| `hash` | `int` | `—` |

```python
from sent.tl.types.messages import MessagesSavedReactionTags
```

## MessagesQuickReplies

**Constructor ID:** `3331155605`

**Fields:** ``quick_replies`, `messages`, `chats`, `users``

```python
MessagesQuickReplies.__init__(quick_replies: 'Vector', messages: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `quick_replies` | `'Vector'` | `—` |
| `messages` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesQuickReplies
```

## MessagesQuickRepliesNotModified

**Constructor ID:** `1603398491`

```python
MessagesQuickRepliesNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.messages import MessagesQuickRepliesNotModified
```

## MessagesDialogFilters

**Constructor ID:** `718878489`

**Fields:** ``tags_enabled`, `filters``

```python
MessagesDialogFilters.__init__(filters: 'Vector', tags_enabled: bool = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `filters` | `'Vector'` | `—` |
| `tags_enabled` | `bool` | `None` |

```python
from sent.tl.types.messages import MessagesDialogFilters
```

## MessagesMyStickers

**Constructor ID:** `4211040925`

**Fields:** ``count`, `sets``

```python
MessagesMyStickers.__init__(count: int, sets: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `sets` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesMyStickers
```

## MessagesInvitedUsers

**Constructor ID:** `2136862630`

**Fields:** ``updates`, `missing_invitees``

```python
MessagesInvitedUsers.__init__(updates: 'Updates', missing_invitees: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `updates` | `'Updates'` | `—` |
| `missing_invitees` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesInvitedUsers
```

## MessagesAvailableEffectsNotModified

**Constructor ID:** `3522009691`

```python
MessagesAvailableEffectsNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.messages import MessagesAvailableEffectsNotModified
```

## MessagesAvailableEffects

**Constructor ID:** `3185271150`

**Fields:** ``hash`, `effects`, `documents``

```python
MessagesAvailableEffects.__init__(hash: int, effects: 'Vector', documents: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `effects` | `'Vector'` | `—` |
| `documents` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesAvailableEffects
```

## MessagesBotPreparedInlineMessage

**Constructor ID:** `2395931921`

**Fields:** ``id`, `expire_date``

```python
MessagesBotPreparedInlineMessage.__init__(id: str, expire_date: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `id` | `str` | `—` |
| `expire_date` | `int` | `—` |

```python
from sent.tl.types.messages import MessagesBotPreparedInlineMessage
```

## MessagesPreparedInlineMessage

**Constructor ID:** `4283920525`

**Fields:** ``query_id`, `result`, `peer_types`, `cache_time`, `users``

```python
MessagesPreparedInlineMessage.__init__(query_id: int, result: 'BotInlineResult', peer_types: 'Vector', cache_time: int, users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `query_id` | `int` | `—` |
| `result` | `'BotInlineResult'` | `—` |
| `peer_types` | `'Vector'` | `—` |
| `cache_time` | `int` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesPreparedInlineMessage
```

## MessagesFoundStickersNotModified

**Constructor ID:** `1611711796`

```python
MessagesFoundStickersNotModified.__init__(next_offset: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `next_offset` | `int` | `None` |

```python
from sent.tl.types.messages import MessagesFoundStickersNotModified
```

## MessagesFoundStickers

**Constructor ID:** `2194268816`

**Fields:** ``next_offset`, `hash`, `stickers``

```python
MessagesFoundStickers.__init__(hash: int, stickers: 'Vector', next_offset: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `hash` | `int` | `—` |
| `stickers` | `'Vector'` | `—` |
| `next_offset` | `int` | `None` |

```python
from sent.tl.types.messages import MessagesFoundStickers
```

## MessagesWebPagePreview

**Constructor ID:** `2358937772`

**Fields:** ``media`, `chats`, `users``

```python
MessagesWebPagePreview.__init__(media: 'MessageMedia', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `media` | `'MessageMedia'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.messages import MessagesWebPagePreview
```

## MessagesEmojiGameOutcome

**Constructor ID:** `3660240455`

**Fields:** ``seed`, `stake_ton_amount`, `ton_amount``

```python
MessagesEmojiGameOutcome.__init__(seed: bytes, stake_ton_amount: int, ton_amount: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `seed` | `bytes` | `—` |
| `stake_ton_amount` | `int` | `—` |
| `ton_amount` | `int` | `—` |

```python
from sent.tl.types.messages import MessagesEmojiGameOutcome
```

## MessagesEmojiGameUnavailable

**Constructor ID:** `1508266805`

```python
MessagesEmojiGameUnavailable.__init__()
```

_No parameters._

```python
from sent.tl.types.messages import MessagesEmojiGameUnavailable
```

## MessagesEmojiGameDiceInfo

**Constructor ID:** `1155883043`

**Fields:** ``game_hash`, `prev_stake`, `current_streak`, `params`, `plays_left``

```python
MessagesEmojiGameDiceInfo.__init__(game_hash: str, prev_stake: int, current_streak: int, params: 'Vector', plays_left: int = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `game_hash` | `str` | `—` |
| `prev_stake` | `int` | `—` |
| `current_streak` | `int` | `—` |
| `params` | `'Vector'` | `—` |
| `plays_left` | `int` | `None` |

```python
from sent.tl.types.messages import MessagesEmojiGameDiceInfo
```

## MessagesComposedMessageWithAI

**Constructor ID:** `2430053882`

**Fields:** ``result_text`, `diff_text``

```python
MessagesComposedMessageWithAI.__init__(result_text: 'TextWithEntities', diff_text: 'TextWithEntities' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `result_text` | `'TextWithEntities'` | `—` |
| `diff_text` | `'TextWithEntities'` | `None` |

```python
from sent.tl.types.messages import MessagesComposedMessageWithAI
```
