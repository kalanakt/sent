{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Types: `bots`

Import from `sent.tl.types.bots`.

## Contents

- [BotsBotInfo](#botsbotinfo)
- [BotsPopularAppBots](#botspopularappbots)
- [BotsPreviewInfo](#botspreviewinfo)
- [BotsExportedBotToken](#botsexportedbottoken)
- [BotsRequestedButton](#botsrequestedbutton)
- [BotsAccessSettings](#botsaccesssettings)

## BotsBotInfo

**Constructor ID:** `3903288752`

**Fields:** ``name`, `about`, `description``

```python
BotsBotInfo.__init__(name: str, about: str, description: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `name` | `str` | `—` |
| `about` | `str` | `—` |
| `description` | `str` | `—` |

```python
from sent.tl.types.bots import BotsBotInfo
```

## BotsPopularAppBots

**Constructor ID:** `428978491`

**Fields:** ``next_offset`, `users``

```python
BotsPopularAppBots.__init__(users: 'Vector', next_offset: str = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `users` | `'Vector'` | `—` |
| `next_offset` | `str` | `None` |

```python
from sent.tl.types.bots import BotsPopularAppBots
```

## BotsPreviewInfo

**Constructor ID:** `212278628`

**Fields:** ``media`, `lang_codes``

```python
BotsPreviewInfo.__init__(media: 'Vector', lang_codes: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `media` | `'Vector'` | `—` |
| `lang_codes` | `'Vector'` | `—` |

```python
from sent.tl.types.bots import BotsPreviewInfo
```

## BotsExportedBotToken

**Constructor ID:** `1012971041`

```python
BotsExportedBotToken.__init__(token: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `token` | `str` | `—` |

```python
from sent.tl.types.bots import BotsExportedBotToken
```

## BotsRequestedButton

**Constructor ID:** `4047224023`

```python
BotsRequestedButton.__init__(webapp_req_id: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `webapp_req_id` | `str` | `—` |

```python
from sent.tl.types.bots import BotsRequestedButton
```

## BotsAccessSettings

**Constructor ID:** `3709845395`

**Fields:** ``restricted`, `add_users``

```python
BotsAccessSettings.__init__(restricted: bool = None, add_users: 'Vector' = None)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `restricted` | `bool` | `None` |
| `add_users` | `'Vector'` | `None` |

```python
from sent.tl.types.bots import BotsAccessSettings
```
