{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Types: `contacts`

Import from `sent.tl.types.contacts`.

## Contents

- [ContactsContactsNotModified](#contactscontactsnotmodified)
- [ContactsContacts](#contactscontacts)
- [ContactsImportedContacts](#contactsimportedcontacts)
- [ContactsBlocked](#contactsblocked)
- [ContactsBlockedSlice](#contactsblockedslice)
- [ContactsFound](#contactsfound)
- [ContactsResolvedPeer](#contactsresolvedpeer)
- [ContactsTopPeersNotModified](#contactstoppeersnotmodified)
- [ContactsTopPeers](#contactstoppeers)
- [ContactsTopPeersDisabled](#contactstoppeersdisabled)
- [ContactsContactBirthdays](#contactscontactbirthdays)
- [ContactsSponsoredPeersEmpty](#contactssponsoredpeersempty)
- [ContactsSponsoredPeers](#contactssponsoredpeers)

## ContactsContactsNotModified

**Constructor ID:** `3075189202`

```python
ContactsContactsNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.contacts import ContactsContactsNotModified
```

## ContactsContacts

**Constructor ID:** `3941105218`

**Fields:** ``contacts`, `saved_count`, `users``

```python
ContactsContacts.__init__(contacts: 'Vector', saved_count: int, users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `contacts` | `'Vector'` | `—` |
| `saved_count` | `int` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.contacts import ContactsContacts
```

## ContactsImportedContacts

**Constructor ID:** `2010127419`

**Fields:** ``imported`, `popular_invites`, `retry_contacts`, `users``

```python
ContactsImportedContacts.__init__(imported: 'Vector', popular_invites: 'Vector', retry_contacts: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `imported` | `'Vector'` | `—` |
| `popular_invites` | `'Vector'` | `—` |
| `retry_contacts` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.contacts import ContactsImportedContacts
```

## ContactsBlocked

**Constructor ID:** `182326673`

**Fields:** ``blocked`, `chats`, `users``

```python
ContactsBlocked.__init__(blocked: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `blocked` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.contacts import ContactsBlocked
```

## ContactsBlockedSlice

**Constructor ID:** `3781575060`

**Fields:** ``count`, `blocked`, `chats`, `users``

```python
ContactsBlockedSlice.__init__(count: int, blocked: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `blocked` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.contacts import ContactsBlockedSlice
```

## ContactsFound

**Constructor ID:** `3004386717`

**Fields:** ``my_results`, `results`, `chats`, `users``

```python
ContactsFound.__init__(my_results: 'Vector', results: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `my_results` | `'Vector'` | `—` |
| `results` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.contacts import ContactsFound
```

## ContactsResolvedPeer

**Constructor ID:** `2131196633`

**Fields:** ``peer`, `chats`, `users``

```python
ContactsResolvedPeer.__init__(peer: 'Peer', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peer` | `'Peer'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.contacts import ContactsResolvedPeer
```

## ContactsTopPeersNotModified

**Constructor ID:** `3727060725`

```python
ContactsTopPeersNotModified.__init__()
```

_No parameters._

```python
from sent.tl.types.contacts import ContactsTopPeersNotModified
```

## ContactsTopPeers

**Constructor ID:** `1891070632`

**Fields:** ``categories`, `chats`, `users``

```python
ContactsTopPeers.__init__(categories: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `categories` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.contacts import ContactsTopPeers
```

## ContactsTopPeersDisabled

**Constructor ID:** `3039597469`

```python
ContactsTopPeersDisabled.__init__()
```

_No parameters._

```python
from sent.tl.types.contacts import ContactsTopPeersDisabled
```

## ContactsContactBirthdays

**Constructor ID:** `290452237`

**Fields:** ``contacts`, `users``

```python
ContactsContactBirthdays.__init__(contacts: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `contacts` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.contacts import ContactsContactBirthdays
```

## ContactsSponsoredPeersEmpty

**Constructor ID:** `3929191601`

```python
ContactsSponsoredPeersEmpty.__init__()
```

_No parameters._

```python
from sent.tl.types.contacts import ContactsSponsoredPeersEmpty
```

## ContactsSponsoredPeers

**Constructor ID:** `3942852740`

**Fields:** ``peers`, `chats`, `users``

```python
ContactsSponsoredPeers.__init__(peers: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peers` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.contacts import ContactsSponsoredPeers
```
