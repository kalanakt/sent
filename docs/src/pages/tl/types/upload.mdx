{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Types: `upload`

Import from `sent.tl.types.upload`.

## Contents

- [UploadFile](#uploadfile)
- [UploadFileCdnRedirect](#uploadfilecdnredirect)
- [UploadWebFile](#uploadwebfile)
- [UploadCdnFileReuploadNeeded](#uploadcdnfilereuploadneeded)
- [UploadCdnFile](#uploadcdnfile)

## UploadFile

**Constructor ID:** `157948117`

**Fields:** ``type_`, `mtime`, `bytes``

```python
UploadFile.__init__(type_: 'StorageFileType', mtime: int, bytes: bytes)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `type_` | `'StorageFileType'` | `—` |
| `mtime` | `int` | `—` |
| `bytes` | `bytes` | `—` |

```python
from sent.tl.types.upload import UploadFile
```

## UploadFileCdnRedirect

**Constructor ID:** `4052539972`

**Fields:** ``dc_id`, `file_token`, `encryption_key`, `encryption_iv`, `file_hashes``

```python
UploadFileCdnRedirect.__init__(dc_id: int, file_token: bytes, encryption_key: bytes, encryption_iv: bytes, file_hashes: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `dc_id` | `int` | `—` |
| `file_token` | `bytes` | `—` |
| `encryption_key` | `bytes` | `—` |
| `encryption_iv` | `bytes` | `—` |
| `file_hashes` | `'Vector'` | `—` |

```python
from sent.tl.types.upload import UploadFileCdnRedirect
```

## UploadWebFile

**Constructor ID:** `568808380`

**Fields:** ``size`, `mime_type`, `file_type`, `mtime`, `bytes``

```python
UploadWebFile.__init__(size: int, mime_type: str, file_type: 'StorageFileType', mtime: int, bytes: bytes)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `size` | `int` | `—` |
| `mime_type` | `str` | `—` |
| `file_type` | `'StorageFileType'` | `—` |
| `mtime` | `int` | `—` |
| `bytes` | `bytes` | `—` |

```python
from sent.tl.types.upload import UploadWebFile
```

## UploadCdnFileReuploadNeeded

**Constructor ID:** `4004045934`

```python
UploadCdnFileReuploadNeeded.__init__(request_token: bytes)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `request_token` | `bytes` | `—` |

```python
from sent.tl.types.upload import UploadCdnFileReuploadNeeded
```

## UploadCdnFile

**Constructor ID:** `2845821519`

```python
UploadCdnFile.__init__(bytes: bytes)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `bytes` | `bytes` | `—` |

```python
from sent.tl.types.upload import UploadCdnFile
```
