{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# TL Types: `phone`

Import from `sent.tl.types.phone`.

## Contents

- [PhonePhoneCall](#phonephonecall)
- [PhoneGroupCall](#phonegroupcall)
- [PhoneGroupParticipants](#phonegroupparticipants)
- [PhoneJoinAsPeers](#phonejoinaspeers)
- [PhoneExportedGroupCallInvite](#phoneexportedgroupcallinvite)
- [PhoneGroupCallStreamChannels](#phonegroupcallstreamchannels)
- [PhoneGroupCallStreamRtmpUrl](#phonegroupcallstreamrtmpurl)
- [PhoneGroupCallStars](#phonegroupcallstars)

## PhonePhoneCall

**Constructor ID:** `3968000320`

**Fields:** ``phone_call`, `users``

```python
PhonePhoneCall.__init__(phone_call: 'PhoneCall', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `phone_call` | `'PhoneCall'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.phone import PhonePhoneCall
```

## PhoneGroupCall

**Constructor ID:** `2658302637`

**Fields:** ``call`, `participants`, `participants_next_offset`, `chats`, `users``

```python
PhoneGroupCall.__init__(call: 'GroupCall', participants: 'Vector', participants_next_offset: str, chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `call` | `'GroupCall'` | `—` |
| `participants` | `'Vector'` | `—` |
| `participants_next_offset` | `str` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.phone import PhoneGroupCall
```

## PhoneGroupParticipants

**Constructor ID:** `4101460406`

**Fields:** ``count`, `participants`, `next_offset`, `chats`, `users`, `version``

```python
PhoneGroupParticipants.__init__(count: int, participants: 'Vector', next_offset: str, chats: 'Vector', users: 'Vector', version: int)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `count` | `int` | `—` |
| `participants` | `'Vector'` | `—` |
| `next_offset` | `str` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |
| `version` | `int` | `—` |

```python
from sent.tl.types.phone import PhoneGroupParticipants
```

## PhoneJoinAsPeers

**Constructor ID:** `2951045695`

**Fields:** ``peers`, `chats`, `users``

```python
PhoneJoinAsPeers.__init__(peers: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `peers` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.phone import PhoneJoinAsPeers
```

## PhoneExportedGroupCallInvite

**Constructor ID:** `541839704`

```python
PhoneExportedGroupCallInvite.__init__(link: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `link` | `str` | `—` |

```python
from sent.tl.types.phone import PhoneExportedGroupCallInvite
```

## PhoneGroupCallStreamChannels

**Constructor ID:** `3504636594`

```python
PhoneGroupCallStreamChannels.__init__(channels: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `channels` | `'Vector'` | `—` |

```python
from sent.tl.types.phone import PhoneGroupCallStreamChannels
```

## PhoneGroupCallStreamRtmpUrl

**Constructor ID:** `767505458`

**Fields:** ``url`, `key``

```python
PhoneGroupCallStreamRtmpUrl.__init__(url: str, key: str)
```

| Parameter | Type | Default |
|-----------|------|---------|
| `url` | `str` | `—` |
| `key` | `str` | `—` |

```python
from sent.tl.types.phone import PhoneGroupCallStreamRtmpUrl
```

## PhoneGroupCallStars

**Constructor ID:** `2635971878`

**Fields:** ``total_stars`, `top_donors`, `chats`, `users``

```python
PhoneGroupCallStars.__init__(total_stars: int, top_donors: 'Vector', chats: 'Vector', users: 'Vector')
```

| Parameter | Type | Default |
|-----------|------|---------|
| `total_stars` | `int` | `—` |
| `top_donors` | `'Vector'` | `—` |
| `chats` | `'Vector'` | `—` |
| `users` | `'Vector'` | `—` |

```python
from sent.tl.types.phone import PhoneGroupCallStars
```
