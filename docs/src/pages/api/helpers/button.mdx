{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# Button

Build inline and reply keyboards.

### `inline`

```python
inline(text: str, data: bytes = None, url: str = None)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `text` | `str` | `—` |
| `data` | `bytes` | `None` |
| `url` | `str` | `None` |

### `text`

```python
text(text: str)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `text` | `str` | `—` |

### `request_phone`

```python
request_phone(text: str)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `text` | `str` | `—` |

### `request_location`

```python
request_location(text: str)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `text` | `str` | `—` |

### `build`

```python
build(rows: List[List], inline: bool = False)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `rows` | `List[List]` | `—` |
| `inline` | `bool` | `False` |

## Example

```python
from sent.client.buttons import Button

buttons = Button.build([
    [Button.inline('Yes', data=b'yes'), Button.inline('No', data=b'no')],
])
await client.send_message(chat, 'Confirm?', buttons=buttons)
```