{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# Conversation

Wait for replies in a chat.

Simple conversation helper for waiting on replies.

### `__aenter__`

```python
async __aenter__()
```

No description available.

**Parameters**

_No parameters._

**Example**

```python
async with client.conversation("username") as conv:
    await conv.send_message("What's your name?")
    response = await conv.get_response()
```

### `__aexit__`

```python
async __aexit__(exc_type, exc_val, exc_tb)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `exc_type` | `—` | `—` |
| `exc_val` | `—` | `—` |
| `exc_tb` | `—` | `—` |

### `get_response`

```python
async get_response(timeout: Optional[float] = None)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `timeout` | `Optional[float]` | `None` |

### `send_message`

```python
async send_message(text: str, **kwargs)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `text` | `str` | `—` |
| `**kwargs` | `—` | `—` |
