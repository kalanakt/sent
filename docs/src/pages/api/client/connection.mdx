{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# Connection

### `__init__`

```python
__init__(session: Union[str, Session], api_id: int, api_hash: str, connection: str = 'abridged', use_ipv6: bool = False, proxy = None, timeout: int = 10, request_retries: int = 5, entity_cache_limit: int = 5000, flood_sleep_threshold: int = 60, raise_last_call_error: bool = False, device_model: str = None, system_version: str = None, app_version: str = None, lang_code: str = 'en', system_lang_code: str = 'en')
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `session` | `Union[str, Session]` | `—` |
| `api_id` | `int` | `—` |
| `api_hash` | `str` | `—` |
| `connection` | `str` | `'abridged'` |
| `use_ipv6` | `bool` | `False` |
| `proxy` | `—` | `None` |
| `timeout` | `int` | `10` |
| `request_retries` | `int` | `5` |
| `entity_cache_limit` | `int` | `5000` |
| `flood_sleep_threshold` | `int` | `60` |
| `raise_last_call_error` | `bool` | `False` |
| `device_model` | `str` | `None` |
| `system_version` | `str` | `None` |
| `app_version` | `str` | `None` |
| `lang_code` | `str` | `'en'` |
| `system_lang_code` | `str` | `'en'` |

### `connect`

```python
async connect() -> None
```

Connect to Telegram, authenticate if needed, and start the sender.

**Parameters**

_No parameters._

**Returns:** `None`

**Example**

```python
await client.connect()
```

### `disconnect`

```python
async disconnect() -> None
```

No description available.

**Parameters**

_No parameters._

**Returns:** `None`

### `is_connected`

```python
is_connected() -> bool
```

No description available.

**Parameters**

_No parameters._

**Returns:** `bool`

### `__call__`

```python
async __call__(request: TLObject) -> Any
```

Invoke an RPC method.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `request` | `TLObject` | `—` |

**Returns:** `Any`

### `__aenter__`

```python
async __aenter__()
```

No description available.

**Parameters**

_No parameters._

### `__aexit__`

```python
async __aexit__(exc_type, exc_val, exc_tb)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `exc_type` | `—` | `—` |
| `exc_val` | `—` | `—` |
| `exc_tb` | `—` | `—` |

### `__init__`

```python
__init__(*args, **kwargs)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `*args` | `—` | `—` |
| `**kwargs` | `—` | `—` |

### `start`

```python
async start(*args, **kwargs)
```

Connect and authenticate (interactive, bot token, or programmatic).

Calls ``connect()`` then delegates to ``AuthMethods.start`` for login.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `*args` | `—` | `—` |
| `**kwargs` | `—` | `—` |

**Example**

```python
await client.start()
await client.start(bot_token=BOT_TOKEN)
await client.start(phone=PHONE, code=CODE)
```
