{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# Updates & Events

### `on`

```python
on(event: Callable)
```

Register an event handler via decorator.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `event` | `Callable` | `—` |

**Example**

```python
@client.on(events.NewMessage)
async def handler(event):
    await event.reply("Hi!")
```

### `add_event_handler`

```python
add_event_handler(callback, event: Callable)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `callback` | `—` | `—` |
| `event` | `Callable` | `—` |

### `remove_event_handler`

```python
remove_event_handler(callback, event: Callable = None)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `callback` | `—` | `—` |
| `event` | `Callable` | `None` |

### `run_until_disconnected`

```python
async run_until_disconnected()
```

Process updates until disconnected.

**Parameters**

_No parameters._

### `catch_up`

```python
async catch_up()
```

Fetch missed updates.

**Parameters**

_No parameters._
