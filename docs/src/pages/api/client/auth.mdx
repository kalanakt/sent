{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# Authentication

### `send_code_request`

```python
async send_code_request(phone: str, force_sms: bool = False, _retry_count: int = 0)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `phone` | `str` | `—` |
| `force_sms` | `bool` | `False` |
| `_retry_count` | `int` | `0` |

**Underlying TL:** [`AuthSendCode`](/tl/functions/auth#authsendcode)

### `sign_in`

```python
async sign_in(phone: str = None, code = None, password: str = None, bot_token: str = None, phone_code_hash: str = None)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `phone` | `str` | `None` |
| `code` | `—` | `None` |
| `password` | `str` | `None` |
| `bot_token` | `str` | `None` |
| `phone_code_hash` | `str` | `None` |

**Underlying TL:** [`AuthSignIn`](/tl/functions/auth#authsignin)

### `edit_2fa`

```python
async edit_2fa(current_password: str = None, new_password: str = None, hint: str = '', email: str = None)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `current_password` | `str` | `None` |
| `new_password` | `str` | `None` |
| `hint` | `str` | `''` |
| `email` | `str` | `None` |

### `qr_login`

```python
async qr_login(timeout: float = 30.0)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `timeout` | `float` | `30.0` |

### `takeout`

```python
async takeout(contacts: bool = True, message_users: bool = True)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `contacts` | `bool` | `True` |
| `message_users` | `bool` | `True` |

### `sign_up`

```python
async sign_up(phone: str, code, first_name: str, last_name: str = '')
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `phone` | `str` | `—` |
| `code` | `—` | `—` |
| `first_name` | `str` | `—` |
| `last_name` | `str` | `''` |

### `log_out`

```python
async log_out()
```

No description available.

**Parameters**

_No parameters._

### `start`

```python
async start(phone = None, password = None, bot_token: str = None, code_callback = None, first_name: str = 'User', last_name: str = '', max_attempts: int = 3)
```

Interactive or programmatic start.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `phone` | `—` | `None` |
| `password` | `—` | `None` |
| `bot_token` | `str` | `None` |
| `code_callback` | `—` | `None` |
| `first_name` | `str` | `'User'` |
| `last_name` | `str` | `''` |
| `max_attempts` | `int` | `3` |

**Example**

```python
await client.start()
await client.start(bot_token=BOT_TOKEN)
await client.start(phone=PHONE, code=CODE)
```

### `is_user_authorized`

```python
async is_user_authorized() -> bool
```

No description available.

**Parameters**

_No parameters._

**Returns:** `bool`
