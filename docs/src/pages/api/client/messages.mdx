{/* Auto-generated by docs/scripts/generate_docs.py. Do not edit manually. */}

# Messages

### `send_message`

```python
async send_message(entity, message: str, reply_to: int = None, parse_mode: str = None, link_preview: bool = True, file = None, thumb = None, force_document: bool = False, buttons = None, silent: bool = None, schedule = None, comment_to: int = None)
```

Send a text message to a user, chat, or channel.

Args:
    entity: Username, ID, or TL peer to send to.
    message: Text content of the message.
    reply_to: Message ID to reply to.
    parse_mode: Markdown parse mode for formatting.
    link_preview: Whether to show link previews.
    file: Optional file path or bytes to attach.
    buttons: Inline or reply keyboard markup.
    silent: Send without notification sound.
    schedule: Unix timestamp to schedule the message.
    comment_to: Post ID to comment on in a channel discussion.

Returns:
    The sent Message object.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `entity` | `—` | `—` |
| `message` | `str` | `—` |
| `reply_to` | `int` | `None` |
| `parse_mode` | `str` | `None` |
| `link_preview` | `bool` | `True` |
| `file` | `—` | `None` |
| `thumb` | `—` | `None` |
| `force_document` | `bool` | `False` |
| `buttons` | `—` | `None` |
| `silent` | `bool` | `None` |
| `schedule` | `—` | `None` |
| `comment_to` | `int` | `None` |

**Example**

```python
await client.send_message("username", "Hello!")
```

**Underlying TL:** [`MessagesSendMessage`](/tl/functions/messages#messagessendmessage)

### `edit_message`

```python
async edit_message(entity, message: int, text: str = None, buttons = None, parse_mode: str = None)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `entity` | `—` | `—` |
| `message` | `int` | `—` |
| `text` | `str` | `None` |
| `buttons` | `—` | `None` |
| `parse_mode` | `str` | `None` |

**Underlying TL:** [`MessagesEditMessage`](/tl/functions/messages#messageseditmessage)

### `delete_messages`

```python
async delete_messages(entity, message_ids: List[int], revoke: bool = True)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `entity` | `—` | `—` |
| `message_ids` | `List[int]` | `—` |
| `revoke` | `bool` | `True` |

**Underlying TL:** [`MessagesDeleteMessages`](/tl/functions/messages#messagesdeletemessages)

### `forward_messages`

```python
async forward_messages(entity, messages, from_peer = None, silent: bool = None, schedule = None)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `entity` | `—` | `—` |
| `messages` | `—` | `—` |
| `from_peer` | `—` | `None` |
| `silent` | `bool` | `None` |
| `schedule` | `—` | `None` |

### `get_messages`

```python
async get_messages(entity, ids = None, limit: int = None, offset_date = None, offset_id: int = 0, max_id: int = 0, min_id: int = 0, search: str = None, from_user = None, reverse: bool = False)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `entity` | `—` | `—` |
| `ids` | `—` | `None` |
| `limit` | `int` | `None` |
| `offset_date` | `—` | `None` |
| `offset_id` | `int` | `0` |
| `max_id` | `int` | `0` |
| `min_id` | `int` | `0` |
| `search` | `str` | `None` |
| `from_user` | `—` | `None` |
| `reverse` | `bool` | `False` |

### `iter_messages`

```python
async iter_messages(entity, **kwargs)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `entity` | `—` | `—` |
| `**kwargs` | `—` | `—` |

**Example**

```python
async for message in client.iter_messages("channel"):
    print(message.text)
```

### `pin_message`

```python
async pin_message(entity, message, notify: bool = False)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `entity` | `—` | `—` |
| `message` | `—` | `—` |
| `notify` | `bool` | `False` |

### `unpin_message`

```python
async unpin_message(entity, message = None)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `entity` | `—` | `—` |
| `message` | `—` | `None` |

### `send_poll`

```python
async send_poll(entity, question: str, options: List[str], multiple_choice: bool = False, quiz: bool = False, correct_answer: int = None)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `entity` | `—` | `—` |
| `question` | `str` | `—` |
| `options` | `List[str]` | `—` |
| `multiple_choice` | `bool` | `False` |
| `quiz` | `bool` | `False` |
| `correct_answer` | `int` | `None` |

### `send_reaction`

```python
async send_reaction(entity, message, reaction)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `entity` | `—` | `—` |
| `message` | `—` | `—` |
| `reaction` | `—` | `—` |

### `set_typing`

```python
async set_typing(entity, action = None)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `entity` | `—` | `—` |
| `action` | `—` | `None` |

### `send_read_acknowledge`

```python
async send_read_acknowledge(entity, max_id: int = 0)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `entity` | `—` | `—` |
| `max_id` | `int` | `0` |

### `conversation`

```python
conversation(entity, timeout: float = 60.0)
```

No description available.

**Parameters**

| Parameter | Type | Default |
|-----------|------|---------|
| `entity` | `—` | `—` |
| `timeout` | `float` | `60.0` |

**Example**

```python
async with client.conversation("username") as conv:
    await conv.send_message("What's your name?")
    response = await conv.get_response()
```
